# README

Open `00. INDEX.md` first. Replace generic runtime, model format, path, secret, and endpoint placeholders with approved values for your cluster. Create storage connections through the OpenShift AI dashboard or use the exact schema supported by your installed release.
