# 09. Dashboard pod failure

> Corporate L3 deep-dive for engineers with senior OpenShift, Kubernetes, Linux, networking, security, and operations experience.
> Baseline used for terminology: OpenShift Container Platform 4.19 and Red Hat OpenShift AI Self-Managed 3.5.
> Verify the exact supported product version, update channel, architecture, and dependency matrix before changing a real cluster.
> Back up custom resources and collect evidence before deleting or recreating OLM or OpenShift AI objects.

## Learning outcomes
- Explain the complete catalog-to-dashboard control flow.
- Diagnose symptoms using conditions, events, logs, ownership, and generation.
- Create a safe break-fix lab from scratch.
- Apply supported remediation with validation and rollback.
- Convert the runbook into GitOps gates, alerts, SLOs, and incident procedures.

## Contents
- Executive overview
- Architecture
- Symptoms and impact
- Prerequisites
- Evidence collection
- Diagnostic workflow
- Root-cause catalogue
- Remediation
- Project from scratch
- Failure injection labs
- Automation and GitOps
- Monitoring and SLOs
- Security
- Upgrade and rollback
- Incident runbook
- Extended L3 drills
- Interview questions
- Command reference
- Glossary
- Official references

## 1. Executive overview
`Dashboard pod failure` must be diagnosed as a control-loop failure, not merely as a pod failure.
The normal chain is catalog content → Subscription → resolver → InstallPlan → CSV → Operator deployment → DSCI/DSC → operands → service/route/OAuth/user access.
A late symptom can originate much earlier in this chain. The senior engineer locates the earliest broken boundary and verifies ownership before remediation.
- Typical symptom: dashboard CrashLoopBackOff.
- Typical symptom: oauth proxy not ready.
- Typical symptom: probe failure.
- Typical symptom: frontend 500.
- Typical symptom: deployment unavailable.

### Senior principles
1. Identify desired state, observed state, and responsible controller.
2. Use status reasons and messages before assumptions.
3. Running does not imply Ready or Available.
4. CSV Succeeded does not prove every operand is healthy.
5. Do not delete CRDs as a troubleshooting experiment.
6. Test with the same identity, namespace, proxy, DNS, and CA context.
7. Preserve timestamps and correlate events across controllers.
8. Make one reversible change per iteration.
9. Define success and rollback before change.
10. Use Red Hat documentation matching the installed release.

## 2. Architecture
### Primary resources
- `Deployment/rhods-dashboard` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `Pod` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `Service` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `ConfigMap` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `Secret` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `Route` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
- `OAuthClient` — inspect spec, status, conditions, ownerReferences, generation, observedGeneration, events, and controller logs.
### Relevant namespaces
- `redhat-ods-applications`
- `redhat-ods-operator`
### Control flow
1. Catalog serves package, channel, bundle, CSV, CRD, related-image, and dependency metadata.
2. Subscription requests package, source, channel, and approval policy.
3. Resolver calculates a compatible Operator set.
4. InstallPlan records ordered resource steps.
5. Approval policy allows or blocks execution.
6. CSV evaluates requirements and deploys the Operator.
7. OpenShift AI Operator reconciles DSCI and DSC.
8. Component controllers create operands.
9. Shared infrastructure determines image, network, certificate, storage, and route readiness.
10. User access validates the complete path.

## 3. Symptoms and impact
### Symptom pattern 1: dashboard CrashLoopBackOff
- First evidence: previous logs.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 2: oauth proxy not ready
- First evidence: probe result.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 3: probe failure
- First evidence: certificate chain.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 4: frontend 500
- First evidence: InstallPlan step.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 5: deployment unavailable
- First evidence: SubjectAccessReview.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 6: dashboard CrashLoopBackOff
- First evidence: resourceVersion.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 7: oauth proxy not ready
- First evidence: events.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 8: probe failure
- First evidence: rollout status.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 9: frontend 500
- First evidence: service endpoints.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 10: deployment unavailable
- First evidence: image pull event.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 11: dashboard CrashLoopBackOff
- First evidence: CSV requirementStatus.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 12: oauth proxy not ready
- First evidence: ownerReferences.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 13: probe failure
- First evidence: audit event.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 14: frontend 500
- First evidence: current logs.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 15: deployment unavailable
- First evidence: termination reason.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 16: dashboard CrashLoopBackOff
- First evidence: route admission.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 17: oauth proxy not ready
- First evidence: resolver message.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 18: probe failure
- First evidence: API discovery.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 19: frontend 500
- First evidence: observedGeneration.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 20: deployment unavailable
- First evidence: status conditions.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 21: dashboard CrashLoopBackOff
- First evidence: previous logs.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 22: oauth proxy not ready
- First evidence: probe result.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 23: probe failure
- First evidence: certificate chain.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 24: frontend 500
- First evidence: InstallPlan step.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 25: deployment unavailable
- First evidence: SubjectAccessReview.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 26: dashboard CrashLoopBackOff
- First evidence: resourceVersion.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 27: oauth proxy not ready
- First evidence: events.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 28: probe failure
- First evidence: rollout status.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 29: frontend 500
- First evidence: service endpoints.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 30: deployment unavailable
- First evidence: image pull event.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 31: dashboard CrashLoopBackOff
- First evidence: CSV requirementStatus.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 32: oauth proxy not ready
- First evidence: ownerReferences.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 33: probe failure
- First evidence: audit event.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 34: frontend 500
- First evidence: current logs.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 35: deployment unavailable
- First evidence: termination reason.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 36: dashboard CrashLoopBackOff
- First evidence: route admission.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 37: oauth proxy not ready
- First evidence: resolver message.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 38: probe failure
- First evidence: API discovery.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 39: frontend 500
- First evidence: observedGeneration.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Symptom pattern 40: deployment unavailable
- First evidence: status conditions.
- Determine scope: cluster, namespace, node, user, release, or specific component.
- Record first occurrence, last known healthy time, and last change.
- Classify as install, steady-state reconcile, upgrade, or access-path failure.
- Capture current and previous logs before restart.

### Severity model
| Severity | Impact | Response |
|---|---|---|
| SEV-1 | Platform-wide outage or production traffic risk | Incident command, freeze changes, immediate evidence and support escalation |
| SEV-2 | Installation/upgrade blocked or major component unavailable | Platform lead diagnosis and cross-team response |
| SEV-3 | Single component or namespace with workaround | Standard incident workflow |
| SEV-4 | Nonproduction, cosmetic, or documentation issue | Planned correction |

## 4. Prerequisites
- Approved nonproduction cluster
- cluster-admin for installation labs
- compatible oc CLI
- jq and optional yq
- skopeo or podman
- openssl
- Git repository
- change and rollback plan
### Lab repository
```bash
mkdir -p rhoai-install-l3/{00-context,01-baseline,02-evidence,03-manifests,04-tests,05-remediation,06-validation,07-rollback,08-report}
cd rhoai-install-l3
git init
printf 'secrets/\n*.kubeconfig\n*.token\n' > .gitignore
```
### Safety controls
1. Export YAML before mutation.
2. Redact tokens, passwords, dockerconfigjson, and private keys.
3. Use lab namespaces for failure injection.
4. Never force-remove finalizers without ownership analysis.
5. Never perform unsupported downgrade.
6. Capture version and channel in every record.
7. Prepare rollback before apply.
8. Do not hand-edit Operator-owned resources as a permanent fix.

## 5. Evidence collection
### 5.1 Identity
```bash
oc whoami && oc whoami --show-server
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.2 Versions
```bash
oc version
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.3 Cluster version
```bash
oc get clusterversion version -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.4 Cluster Operators
```bash
oc get clusteroperators
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.5 Nodes
```bash
oc get nodes -o wide
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.6 Events
```bash
oc get events -A --sort-by=.lastTimestamp
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.7 Subscriptions
```bash
oc get subscriptions.operators.coreos.com -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.8 InstallPlans
```bash
oc get installplans.operators.coreos.com -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.9 CSVs
```bash
oc get clusterserviceversions.operators.coreos.com -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.10 OperatorGroups
```bash
oc get operatorgroups.operators.coreos.com -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.11 CatalogSources
```bash
oc get catalogsources.operators.coreos.com -n openshift-marketplace -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.12 Catalog pods
```bash
oc get pods -n openshift-marketplace -o wide
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.13 OLM pods
```bash
oc get pods -n openshift-operator-lifecycle-manager -o wide
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.14 Operator pods
```bash
oc get pods -n redhat-ods-operator -o wide
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.15 Application pods
```bash
oc get pods -n redhat-ods-applications -o wide
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.16 DSC
```bash
oc get datascienceclusters.datasciencecluster.opendatahub.io -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.17 DSCI
```bash
oc get dscinitializations.dscinitialization.opendatahub.io -A -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.18 CRDs
```bash
oc get crd | grep -Ei 'opendatahub|datascience|rhods|kserve|trustyai|ray'
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.19 Routes
```bash
oc get routes -A
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.20 Proxy
```bash
oc get proxy cluster -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### 5.21 Image mirrors
```bash
oc get imagedigestmirrorsets,imagetagmirrorsets -o yaml
```
- Save output with a timestamp.
- Correlate object identity, namespace, reason, message, and age.
- Redact secrets before sharing.

### Reusable evidence script
```bash
#!/usr/bin/env bash
set -euo pipefail
OUT="${1:-evidence-$(date +%Y%m%d-%H%M%S)}"
mkdir -p "$OUT"
oc version > "$OUT/oc-version.txt" 2>&1 || true
oc get clusterversion version -o yaml > "$OUT/clusterversion.yaml" 2>&1 || true
oc get clusteroperators > "$OUT/clusteroperators.txt" 2>&1 || true
oc get subscriptions.operators.coreos.com -A -o yaml > "$OUT/subscriptions.yaml" 2>&1 || true
oc get installplans.operators.coreos.com -A -o yaml > "$OUT/installplans.yaml" 2>&1 || true
oc get clusterserviceversions.operators.coreos.com -A -o yaml > "$OUT/csvs.yaml" 2>&1 || true
oc get catalogsources.operators.coreos.com -n openshift-marketplace -o yaml > "$OUT/catalogsources.yaml" 2>&1 || true
oc get pods -A -o wide > "$OUT/pods.txt" 2>&1 || true
oc get events -A --sort-by=.lastTimestamp > "$OUT/events.txt" 2>&1 || true
oc get datasciencecluster -A -o yaml > "$OUT/dsc.yaml" 2>&1 || true
oc get dscinitialization -A -o yaml > "$OUT/dsci.yaml" 2>&1 || true
echo "Evidence saved to $OUT
```

## 6. Diagnostic workflow
### Establish boundary
1. Verify API and cluster health. Evidence: current logs. Practice: test with the exact service account.
2. Find earliest failed resource. Evidence: rollout status. Practice: confirm the intended channel.
3. Identify install versus upgrade context. Evidence: probe result. Practice: avoid premature deletion.
4. Check last change. Evidence: route admission. Practice: watch reconciliation.

### Validate OLM inputs
1. Package visibility. Evidence: current logs. Practice: test with the exact service account.
2. Catalog health. Evidence: rollout status. Practice: confirm the intended channel.
3. Subscription source and channel. Evidence: probe result. Practice: avoid premature deletion.
4. Approval policy. Evidence: route admission. Practice: watch reconciliation.

### Validate OLM execution
1. InstallPlan steps. Evidence: current logs. Practice: test with the exact service account.
2. Bundle unpack. Evidence: rollout status. Practice: confirm the intended channel.
3. CSV requirements. Evidence: probe result. Practice: avoid premature deletion.
4. OLM logs. Evidence: route admission. Practice: watch reconciliation.

### Validate Operator runtime
1. Deployment rollout. Evidence: current logs. Practice: test with the exact service account.
2. Current and previous logs. Evidence: rollout status. Practice: confirm the intended channel.
3. Probes and resources. Evidence: probe result. Practice: avoid premature deletion.
4. Service-account authorization. Evidence: route admission. Practice: watch reconciliation.

### Validate DSCI and DSC
1. Live schema. Evidence: current logs. Practice: test with the exact service account.
2. observedGeneration. Evidence: rollout status. Practice: confirm the intended channel.
3. conditions. Evidence: probe result. Practice: avoid premature deletion.
4. component dependency. Evidence: route admission. Practice: watch reconciliation.

### Validate infrastructure
1. registry. Evidence: current logs. Practice: test with the exact service account.
2. proxy. Evidence: rollout status. Practice: confirm the intended channel.
3. CA. Evidence: probe result. Practice: avoid premature deletion.
4. DNS. Evidence: route admission. Practice: watch reconciliation.
5. ingress. Evidence: image pull event. Practice: collect before mutation.
6. storage. Evidence: InstallPlan step. Practice: test with the exact service account.
7. scheduling. Evidence: API discovery. Practice: confirm the intended channel.
8. network policy. Evidence: ownerReferences. Practice: avoid premature deletion.

### Prove recovery
1. one change. Evidence: current logs. Practice: test with the exact service account.
2. watch reconciliation. Evidence: rollout status. Practice: confirm the intended channel.
3. validate generated resources. Evidence: probe result. Practice: avoid premature deletion.
4. test user path. Evidence: route admission. Practice: watch reconciliation.
5. document prevention. Evidence: image pull event. Practice: collect before mutation.

### Decision tree
1. **Package visible?** No: catalog or mirror path. Yes: Subscription.
2. **InstallPlan present?** No: resolver conditions. Yes: approval and phase.
3. **InstallPlan Complete?** No: failed/pending step. Yes: CSV.
4. **CSV Succeeded?** No: requirementStatus/deployment. Yes: Operator/operands.
5. **Operator Available?** No: scheduling/pull/admission/probes/logs. Yes: DSCI/DSC.
6. **DSCI and DSC current?** No: reconcile issue. Yes: component conditions.
7. **Operands ready?** No: component path. Yes: route/OAuth/user test.

## 7. Root-cause catalogue
### 7.1 Invalid Configmap
- Hypothesis: invalid ConfigMap can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.2 Missing Secret
- Hypothesis: missing Secret can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.3 Oauth Mismatch
- Hypothesis: OAuth mismatch can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.4 Certificate Mount Failure
- Hypothesis: certificate mount failure can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.5 Resource Pressure
- Hypothesis: resource pressure can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.6 Image Pull Failure
- Hypothesis: image pull failure can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### 7.7 Backend Api Unreachable
- Hypothesis: backend API unreachable can produce `Dashboard pod failure`.
- API availability: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- authorization: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- admission: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- OLM resolution: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- catalog health: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- image transport: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- certificate trust: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- proxy routing: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- DNS: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- service discovery: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- storage: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- scheduling: determine whether the cause changes desired state, blocks reconciliation, or breaks a dependency.
- Evidence: inspect status conditions; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect events; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect current logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect previous logs; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect rollout status; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect termination reason; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect probe result; require matching ownership, timestamp, namespace, and active generation.
- Evidence: inspect service endpoints; require matching ownership, timestamp, namespace, and active generation.
- Confirmation: reproduce with a read-only or isolated test.
- False-positive guard: ensure the message is current, not historical.
- Escalation package: YAML, events, logs, version, channel, and exact timestamps.

### Cross-cutting matrix
- Matrix 1: evaluate **missing Secret** through **admission** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 2: evaluate **OAuth mismatch** through **catalog health** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 3: evaluate **certificate mount failure** through **certificate trust** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 4: evaluate **resource pressure** through **DNS** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 5: evaluate **image pull failure** through **storage** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 6: evaluate **backend API unreachable** through **resource pressure** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 7: evaluate **invalid ConfigMap** through **version compatibility** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 8: evaluate **missing Secret** through **namespace ownership** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 9: evaluate **OAuth mismatch** through **rollout** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 10: evaluate **certificate mount failure** through **API availability** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 11: evaluate **resource pressure** through **admission** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 12: evaluate **image pull failure** through **catalog health** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 13: evaluate **backend API unreachable** through **certificate trust** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 14: evaluate **invalid ConfigMap** through **DNS** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 15: evaluate **missing Secret** through **storage** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 16: evaluate **OAuth mismatch** through **resource pressure** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 17: evaluate **certificate mount failure** through **version compatibility** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 18: evaluate **resource pressure** through **namespace ownership** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 19: evaluate **image pull failure** through **rollout** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 20: evaluate **backend API unreachable** through **API availability** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 21: evaluate **invalid ConfigMap** through **admission** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 22: evaluate **missing Secret** through **catalog health** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 23: evaluate **OAuth mismatch** through **certificate trust** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 24: evaluate **certificate mount failure** through **DNS** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 25: evaluate **resource pressure** through **storage** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 26: evaluate **image pull failure** through **resource pressure** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 27: evaluate **backend API unreachable** through **version compatibility** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 28: evaluate **invalid ConfigMap** through **namespace ownership** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 29: evaluate **missing Secret** through **rollout** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 30: evaluate **OAuth mismatch** through **API availability** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 31: evaluate **certificate mount failure** through **admission** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 32: evaluate **resource pressure** through **catalog health** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 33: evaluate **image pull failure** through **certificate trust** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 34: evaluate **backend API unreachable** through **DNS** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 35: evaluate **invalid ConfigMap** through **storage** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 36: evaluate **missing Secret** through **resource pressure** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 37: evaluate **OAuth mismatch** through **version compatibility** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 38: evaluate **certificate mount failure** through **namespace ownership** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 39: evaluate **resource pressure** through **rollout** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 40: evaluate **image pull failure** through **API availability** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 41: evaluate **backend API unreachable** through **admission** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 42: evaluate **invalid ConfigMap** through **catalog health** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 43: evaluate **missing Secret** through **certificate trust** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 44: evaluate **OAuth mismatch** through **DNS** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 45: evaluate **certificate mount failure** through **storage** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 46: evaluate **resource pressure** through **resource pressure** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 47: evaluate **image pull failure** through **version compatibility** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 48: evaluate **backend API unreachable** through **namespace ownership** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 49: evaluate **invalid ConfigMap** through **rollout** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 50: evaluate **missing Secret** through **API availability** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 51: evaluate **OAuth mismatch** through **admission** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 52: evaluate **certificate mount failure** through **catalog health** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 53: evaluate **resource pressure** through **certificate trust** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 54: evaluate **image pull failure** through **DNS** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 55: evaluate **backend API unreachable** through **storage** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 56: evaluate **invalid ConfigMap** through **resource pressure** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 57: evaluate **missing Secret** through **version compatibility** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 58: evaluate **OAuth mismatch** through **namespace ownership** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 59: evaluate **certificate mount failure** through **rollout** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 60: evaluate **resource pressure** through **API availability** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 61: evaluate **image pull failure** through **admission** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 62: evaluate **backend API unreachable** through **catalog health** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 63: evaluate **invalid ConfigMap** through **certificate trust** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 64: evaluate **missing Secret** through **DNS** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 65: evaluate **OAuth mismatch** through **storage** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 66: evaluate **certificate mount failure** through **resource pressure** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 67: evaluate **resource pressure** through **version compatibility** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 68: evaluate **image pull failure** through **namespace ownership** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 69: evaluate **backend API unreachable** through **rollout** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 70: evaluate **invalid ConfigMap** through **API availability** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 71: evaluate **missing Secret** through **admission** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 72: evaluate **OAuth mismatch** through **catalog health** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 73: evaluate **certificate mount failure** through **certificate trust** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 74: evaluate **resource pressure** through **DNS** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 75: evaluate **image pull failure** through **storage** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 76: evaluate **backend API unreachable** through **resource pressure** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 77: evaluate **invalid ConfigMap** through **version compatibility** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 78: evaluate **missing Secret** through **namespace ownership** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 79: evaluate **OAuth mismatch** through **rollout** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 80: evaluate **certificate mount failure** through **API availability** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 81: evaluate **resource pressure** through **admission** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 82: evaluate **image pull failure** through **catalog health** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 83: evaluate **backend API unreachable** through **certificate trust** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 84: evaluate **invalid ConfigMap** through **DNS** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 85: evaluate **missing Secret** through **storage** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 86: evaluate **OAuth mismatch** through **resource pressure** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 87: evaluate **certificate mount failure** through **version compatibility** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 88: evaluate **resource pressure** through **namespace ownership** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 89: evaluate **image pull failure** through **rollout** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 90: evaluate **backend API unreachable** through **API availability** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 91: evaluate **invalid ConfigMap** through **admission** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 92: evaluate **missing Secret** through **catalog health** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 93: evaluate **OAuth mismatch** through **certificate trust** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 94: evaluate **certificate mount failure** through **DNS** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 95: evaluate **resource pressure** through **storage** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 96: evaluate **image pull failure** through **resource pressure** using **status conditions**; reject if ownership or chronology does not match.
- Matrix 97: evaluate **backend API unreachable** through **version compatibility** using **termination reason**; reject if ownership or chronology does not match.
- Matrix 98: evaluate **invalid ConfigMap** through **namespace ownership** using **image pull event**; reject if ownership or chronology does not match.
- Matrix 99: evaluate **missing Secret** through **rollout** using **SubjectAccessReview**; reject if ownership or chronology does not match.
- Matrix 100: evaluate **OAuth mismatch** through **API availability** using **status conditions**; reject if ownership or chronology does not match.

## 8. Remediation
### 8.1 Separate container logs
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### 8.2 Inspect probes and mounts
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### 8.3 Restore generated secret via reconciliation
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### 8.4 Verify oauth redirects
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### 8.5 Correct resources
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### 8.6 Test backend dns and tls
1. State evidence-backed hypothesis.
1. Export affected resources.
1. Confirm supportability for installed versions.
1. Apply smallest reversible change.
1. Watch conditions, events, rollout, and logs.
1. Validate control and user paths.
1. Rollback on regression.
1. Record root cause and preventive control.

### Guarded OLM refresh
Use only after confirming image accessibility and following the matching Red Hat recovery procedure.
```bash
oc get subscription -n <operator-namespace> <name> -o yaml > subscription.backup.yaml
oc get csv -n <operator-namespace> -o yaml > csv.backup.yaml
oc get installplan -n <operator-namespace> -o yaml > installplans.backup.yaml
# Review ownership and impact before deleting any OLM resource.
```
### Validation checklist
- [ ] Validation 1: events proves healthy convergence for `Pod`.
- [ ] Validation 2: current logs proves healthy convergence for `Service`.
- [ ] Validation 3: previous logs proves healthy convergence for `ConfigMap`.
- [ ] Validation 4: rollout status proves healthy convergence for `Secret`.
- [ ] Validation 5: termination reason proves healthy convergence for `Route`.
- [ ] Validation 6: probe result proves healthy convergence for `OAuthClient`.
- [ ] Validation 7: service endpoints proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 8: route admission proves healthy convergence for `Pod`.
- [ ] Validation 9: certificate chain proves healthy convergence for `Service`.
- [ ] Validation 10: image pull event proves healthy convergence for `ConfigMap`.
- [ ] Validation 11: resolver message proves healthy convergence for `Secret`.
- [ ] Validation 12: InstallPlan step proves healthy convergence for `Route`.
- [ ] Validation 13: CSV requirementStatus proves healthy convergence for `OAuthClient`.
- [ ] Validation 14: API discovery proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 15: SubjectAccessReview proves healthy convergence for `Pod`.
- [ ] Validation 16: ownerReferences proves healthy convergence for `Service`.
- [ ] Validation 17: observedGeneration proves healthy convergence for `ConfigMap`.
- [ ] Validation 18: resourceVersion proves healthy convergence for `Secret`.
- [ ] Validation 19: audit event proves healthy convergence for `Route`.
- [ ] Validation 20: status conditions proves healthy convergence for `OAuthClient`.
- [ ] Validation 21: events proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 22: current logs proves healthy convergence for `Pod`.
- [ ] Validation 23: previous logs proves healthy convergence for `Service`.
- [ ] Validation 24: rollout status proves healthy convergence for `ConfigMap`.
- [ ] Validation 25: termination reason proves healthy convergence for `Secret`.
- [ ] Validation 26: probe result proves healthy convergence for `Route`.
- [ ] Validation 27: service endpoints proves healthy convergence for `OAuthClient`.
- [ ] Validation 28: route admission proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 29: certificate chain proves healthy convergence for `Pod`.
- [ ] Validation 30: image pull event proves healthy convergence for `Service`.
- [ ] Validation 31: resolver message proves healthy convergence for `ConfigMap`.
- [ ] Validation 32: InstallPlan step proves healthy convergence for `Secret`.
- [ ] Validation 33: CSV requirementStatus proves healthy convergence for `Route`.
- [ ] Validation 34: API discovery proves healthy convergence for `OAuthClient`.
- [ ] Validation 35: SubjectAccessReview proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 36: ownerReferences proves healthy convergence for `Pod`.
- [ ] Validation 37: observedGeneration proves healthy convergence for `Service`.
- [ ] Validation 38: resourceVersion proves healthy convergence for `ConfigMap`.
- [ ] Validation 39: audit event proves healthy convergence for `Secret`.
- [ ] Validation 40: status conditions proves healthy convergence for `Route`.
- [ ] Validation 41: events proves healthy convergence for `OAuthClient`.
- [ ] Validation 42: current logs proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 43: previous logs proves healthy convergence for `Pod`.
- [ ] Validation 44: rollout status proves healthy convergence for `Service`.
- [ ] Validation 45: termination reason proves healthy convergence for `ConfigMap`.
- [ ] Validation 46: probe result proves healthy convergence for `Secret`.
- [ ] Validation 47: service endpoints proves healthy convergence for `Route`.
- [ ] Validation 48: route admission proves healthy convergence for `OAuthClient`.
- [ ] Validation 49: certificate chain proves healthy convergence for `Deployment/rhods-dashboard`.
- [ ] Validation 50: image pull event proves healthy convergence for `Pod`.

## 9. Project from scratch
Project: `rhoai-l3-09-dashboard-pod-failure`
### Step 1 — Baseline
```bash
mkdir -p 01-baseline
oc version > 01-baseline/version.txt
oc get clusterversion version -o yaml > 01-baseline/clusterversion.yaml
oc get clusteroperators > 01-baseline/clusteroperators.txt
oc get storageclass > 01-baseline/storageclasses.txt
oc get proxy cluster -o yaml > 01-baseline/proxy.yaml
```
### Step 2 — Catalog
```bash
mkdir -p 02-catalog
oc get catalogsources -n openshift-marketplace -o yaml > 02-catalog/catalogsources.yaml
oc get packagemanifest -n openshift-marketplace | grep -Ei 'openshift.*ai|rhods' || true
```
### Step 3 — Reviewed manifests
```bash
mkdir -p 03-manifests
cat > 03-manifests/namespace.yaml <<'EOF'
apiVersion: v1
kind: Namespace
metadata:
  name: redhat-ods-operator
EOF
cat > 03-manifests/operatorgroup.yaml <<'EOF'
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: redhat-ods-operator
  namespace: redhat-ods-operator
spec: {}
EOF
cat > 03-manifests/subscription.yaml <<'EOF'
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhods-operator
  namespace: redhat-ods-operator
spec:
  name: rhods-operator
  channel: <verified-supported-channel>
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  installPlanApproval: Manual
EOF
oc apply --server-side --dry-run=server -f 03-manifests/
```
### Step 4 — Apply and watch
```bash
oc apply -f 03-manifests/namespace.yaml
oc apply -f 03-manifests/operatorgroup.yaml
oc apply -f 03-manifests/subscription.yaml
oc get subscription,installplan,csv -n redhat-ods-operator -w
```
### Step 5 — Review and approve
```bash
PLAN="$(oc get installplan -n redhat-ods-operator -o jsonpath='{.items[-1:].metadata.name}')"
oc get installplan -n redhat-ods-operator "$PLAN" -o yaml
oc patch installplan -n redhat-ods-operator "$PLAN" --type merge -p '{"spec":{"approved":true}}'
```
### Step 6 — Verify Operator
```bash
oc get csv -n redhat-ods-operator
oc get deployment,pod -n redhat-ods-operator -o wide
oc logs -n redhat-ods-operator deployment/rhods-operator --all-containers --tail=200
```
### Step 7 — Validate live APIs
```bash
oc api-resources | grep -Ei 'DataScienceCluster|DSCInitialization'
oc explain dscinitialization.spec
oc explain datasciencecluster.spec
# Validate release-matched DSCI and DSC manifests using --dry-run=server.
```
### Topic-specific checks
- Inspect `Deployment/rhods-dashboard`.
- Inspect `Pod`.
- Inspect `Service`.
- Inspect `ConfigMap`.
- Inspect `Secret`.
- Inspect `Route`.
- Inspect `OAuthClient`.
- Test hypothesis: invalid ConfigMap.
- Test hypothesis: missing Secret.
- Test hypothesis: OAuth mismatch.
- Test hypothesis: certificate mount failure.
- Test hypothesis: resource pressure.
- Test hypothesis: image pull failure.
- Test hypothesis: backend API unreachable.
### Success criteria
- [ ] Catalog healthy
- [ ] Subscription resolved
- [ ] InstallPlan Complete
- [ ] CSV Succeeded
- [ ] Operator Available
- [ ] DSCI and DSC current and Ready
- [ ] Enabled operands Ready
- [ ] rh-ai route and authentication healthy
- [ ] Evidence archived without secrets

## 10. Failure injection labs
### 10.1 Manual approval
- Inject: Leave a lab InstallPlan unapproved.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Approve after review.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.2 Invalid channel
- Inject: Use a nonexistent lab channel.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Restore PackageManifest channel.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.3 Missing image credential
- Inject: Do not link lab pull Secret.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Link narrow Secret.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.4 Untrusted CA
- Inject: Use lab HTTPS CA.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Inject approved CA.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.5 Zero endpoints
- Inject: Scale test backend to zero.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Restore replicas.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.6 RBAC denial
- Inject: Use read-only service account.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Add narrow RoleBinding.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.7 Resource pressure
- Inject: Set unrealistic lab limits.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Restore evidence-based resources.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.8 Obsolete field
- Inject: Use server-side dry-run.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Update to live schema.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.9 NO_PROXY omission
- Inject: Route internal test through proxy.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Add internal domain/CIDR.
- Repeat the original transaction.
- Document prevention and rollback.

### 10.10 Mirror omission
- Inject: Omit a lab image.
- Observe events, conditions, logs, and chronology.
- Identify the owning controller and first failed dependency.
- Recover: Mirror missing digest.
- Repeat the original transaction.
- Document prevention and rollback.

## 11. Automation and GitOps
- Review channel changes through pull requests.
- Use Manual approval where permission review is required.
- Server-side dry-run manifests.
- Policy-check namespace, OperatorGroup, Subscription, proxy, and CA references.
- Never store Secret values in Git.
- Export DSCI and DSC before change.
- Run post-change health gates.
- Fail pipelines on unresolved conditions.
### Health gate
```bash
#!/usr/bin/env bash
set -euo pipefail
NS=${OPERATOR_NAMESPACE:-redhat-ods-operator}
oc get subscription -n "$NS" rhods-operator -o json
oc get installplan -n "$NS"
oc get csv -n "$NS"
oc wait --for=condition=Available deployment/rhods-operator -n "$NS" --timeout=10m
oc get dscinitialization -A -o yaml
oc get datasciencecluster -A -o yaml
oc get pods -n redhat-ods-applications
```
- Policy 1: require events evidence for a change affecting `Pod`.
- Policy 2: require current logs evidence for a change affecting `Service`.
- Policy 3: require previous logs evidence for a change affecting `ConfigMap`.
- Policy 4: require rollout status evidence for a change affecting `Secret`.
- Policy 5: require termination reason evidence for a change affecting `Route`.
- Policy 6: require probe result evidence for a change affecting `OAuthClient`.
- Policy 7: require service endpoints evidence for a change affecting `Deployment/rhods-dashboard`.
- Policy 8: require route admission evidence for a change affecting `Pod`.
- Policy 9: require certificate chain evidence for a change affecting `Service`.
- Policy 10: require image pull event evidence for a change affecting `ConfigMap`.
- Policy 11: require resolver message evidence for a change affecting `Secret`.
- Policy 12: require InstallPlan step evidence for a change affecting `Route`.
- Policy 13: require CSV requirementStatus evidence for a change affecting `OAuthClient`.
- Policy 14: require API discovery evidence for a change affecting `Deployment/rhods-dashboard`.
- Policy 15: require SubjectAccessReview evidence for a change affecting `Pod`.
- Policy 16: require ownerReferences evidence for a change affecting `Service`.
- Policy 17: require observedGeneration evidence for a change affecting `ConfigMap`.
- Policy 18: require resourceVersion evidence for a change affecting `Secret`.
- Policy 19: require audit event evidence for a change affecting `Route`.
- Policy 20: require status conditions evidence for a change affecting `OAuthClient`.
- Policy 21: require events evidence for a change affecting `Deployment/rhods-dashboard`.
- Policy 22: require current logs evidence for a change affecting `Pod`.
- Policy 23: require previous logs evidence for a change affecting `Service`.
- Policy 24: require rollout status evidence for a change affecting `ConfigMap`.
- Policy 25: require termination reason evidence for a change affecting `Secret`.
- Policy 26: require probe result evidence for a change affecting `Route`.
- Policy 27: require service endpoints evidence for a change affecting `OAuthClient`.
- Policy 28: require route admission evidence for a change affecting `Deployment/rhods-dashboard`.
- Policy 29: require certificate chain evidence for a change affecting `Pod`.
- Policy 30: require image pull event evidence for a change affecting `Service`.
- Policy 31: require resolver message evidence for a change affecting `ConfigMap`.
- Policy 32: require InstallPlan step evidence for a change affecting `Secret`.
- Policy 33: require CSV requirementStatus evidence for a change affecting `Route`.
- Policy 34: require API discovery evidence for a change affecting `OAuthClient`.
- Policy 35: require SubjectAccessReview evidence for a change affecting `Deployment/rhods-dashboard`.
- Policy 36: require ownerReferences evidence for a change affecting `Pod`.
- Policy 37: require observedGeneration evidence for a change affecting `Service`.
- Policy 38: require resourceVersion evidence for a change affecting `ConfigMap`.
- Policy 39: require audit event evidence for a change affecting `Secret`.
- Policy 40: require status conditions evidence for a change affecting `Route`.

## 12. Monitoring and SLOs
- Monitor CatalogSource state.
- Monitor Subscription conditions.
- Monitor InstallPlan phase and age.
- Monitor CSV phase and reason.
- Monitor Operator unavailable replicas.
- Monitor pod restart rate.
- Monitor DSCI Ready.
- Monitor DSC component conditions.
- Monitor image pull failures.
- Monitor x509/proxy errors.
- Monitor route probe and TLS expiry.
- Monitor admission errors.
- Monitor quota exhaustion.
- Monitor node pressure.
| SLI | Target | Measurement |
|---|---|---|
| Operator availability | 99.9% | at least one available rhods-operator replica |
| Reconciliation latency | 95% under 10 minutes | supported change reaches stable ready conditions |
| Dashboard availability | 99.9% | authenticated synthetic request |
| Upgrade success | 100% planned | every validation gate passes |
- Alert runbook field 1: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 2: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 3: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 4: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 5: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 6: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 7: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 8: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 9: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 10: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 11: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 12: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 13: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 14: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 15: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 16: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 17: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 18: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 19: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 20: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 21: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 22: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 23: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 24: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 25: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 26: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 27: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 28: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 29: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 30: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 31: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 32: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 33: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 34: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 35: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 36: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 37: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 38: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 39: object, namespace, phase/reason, age, cluster version, channel, and read-only command.
- Alert runbook field 40: object, namespace, phase/reason, age, cluster version, channel, and read-only command.

## 13. Security
- Review CSV permissions before approval.
- Restrict InstallPlan approval rights.
- Protect pull secrets and proxy credentials.
- Redact evidence.
- Verify CA provenance and expiry.
- Never permanently bypass TLS verification.
- Audit manual changes.
- Validate mirror access control.
- Separate admin and reviewer duties.
- Time-bound break-glass access.
- Check admission policy impact.
- [ ] Evidence control 1: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 2: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 3: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 4: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 5: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 6: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 7: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 8: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 9: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 10: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 11: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 12: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 13: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 14: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 15: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 16: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 17: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 18: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 19: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 20: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 21: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 22: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 23: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 24: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 25: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 26: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 27: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 28: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 29: archive contains no token, password, dockerconfigjson content, or private key.
- [ ] Evidence control 30: archive contains no token, password, dockerconfigjson content, or private key.

## 14. Upgrade and rollback
1. Confirm supported upgrade path.
2. Review channel lifecycle.
3. Back up DSCI, DSC, and component CRs.
4. Mirror target and related images.
5. Validate dependent Operators.
6. Review exact InstallPlan.
7. Keep rolling-update capacity.
8. Watch CSV replacement and CRD conversion.
9. Do not use unsupported downgrade.
10. Engage Red Hat before reversing migrated state.
11. Validate dashboard and components after upgrade.
- `trigger`:
- `impact`:
- `last reversible point`:
- `schema changes`:
- `persistent migrations`:
- `supported reversal`:
- `support case`:
- `approver`:
- `post-rollback validation`:

## 15. Incident runbook
### 15.1 Detect
- Focus: `Dashboard pod failure`.
- Evidence: previous logs.
- Quality gate: validate against the live CRD.
- Record timestamp, owner, decision, and next check.

### 15.2 Declare
- Focus: `Dashboard pod failure`.
- Evidence: probe result.
- Quality gate: record rollback.
- Record timestamp, owner, decision, and next check.

### 15.3 Stabilize
- Focus: `Dashboard pod failure`.
- Evidence: certificate chain.
- Quality gate: collect before mutation.
- Record timestamp, owner, decision, and next check.

### 15.4 Collect
- Focus: `Dashboard pod failure`.
- Evidence: InstallPlan step.
- Quality gate: validate against the live CRD.
- Record timestamp, owner, decision, and next check.

### 15.5 Bound
- Focus: `Dashboard pod failure`.
- Evidence: SubjectAccessReview.
- Quality gate: record rollback.
- Record timestamp, owner, decision, and next check.

### 15.6 Hypothesize
- Focus: `Dashboard pod failure`.
- Evidence: resourceVersion.
- Quality gate: collect before mutation.
- Record timestamp, owner, decision, and next check.

### 15.7 Test
- Focus: `Dashboard pod failure`.
- Evidence: events.
- Quality gate: validate against the live CRD.
- Record timestamp, owner, decision, and next check.

### 15.8 Remediate
- Focus: `Dashboard pod failure`.
- Evidence: rollout status.
- Quality gate: record rollback.
- Record timestamp, owner, decision, and next check.

### 15.9 Validate
- Focus: `Dashboard pod failure`.
- Evidence: service endpoints.
- Quality gate: collect before mutation.
- Record timestamp, owner, decision, and next check.

### 15.10 Monitor
- Focus: `Dashboard pod failure`.
- Evidence: image pull event.
- Quality gate: validate against the live CRD.
- Record timestamp, owner, decision, and next check.

### 15.11 Communicate
- Focus: `Dashboard pod failure`.
- Evidence: CSV requirementStatus.
- Quality gate: record rollback.
- Record timestamp, owner, decision, and next check.

### 15.12 Close
- Focus: `Dashboard pod failure`.
- Evidence: ownerReferences.
- Quality gate: collect before mutation.
- Record timestamp, owner, decision, and next check.

## 16. Extended L3 drills
### Drill 1: OLM resolution on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 2: certificate trust on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 3: service discovery on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 4: resource pressure on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 5: dependency readiness on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 6: rollout on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 7: authorization on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 8: catalog health on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 9: proxy routing on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 10: storage on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 11: schema validation on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 12: namespace ownership on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 13: auditability on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 14: admission on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 15: image transport on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 16: DNS on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 17: scheduling on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 18: version compatibility on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 19: webhook health on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 20: API availability on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 21: OLM resolution on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 22: certificate trust on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 23: service discovery on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 24: resource pressure on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 25: dependency readiness on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 26: rollout on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 27: authorization on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 28: catalog health on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 29: proxy routing on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 30: storage on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 31: schema validation on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 32: namespace ownership on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 33: auditability on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 34: admission on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 35: image transport on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 36: DNS on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 37: scheduling on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 38: version compatibility on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 39: webhook health on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 40: API availability on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 41: OLM resolution on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 42: certificate trust on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 43: service discovery on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 44: resource pressure on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 45: dependency readiness on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 46: rollout on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 47: authorization on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 48: catalog health on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 49: proxy routing on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 50: storage on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 51: schema validation on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 52: namespace ownership on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 53: auditability on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 54: admission on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 55: image transport on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 56: DNS on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 57: scheduling on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 58: version compatibility on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 59: webhook health on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 60: API availability on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 61: OLM resolution on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 62: certificate trust on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 63: service discovery on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 64: resource pressure on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 65: dependency readiness on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 66: rollout on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 67: authorization on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 68: catalog health on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 69: proxy routing on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 70: storage on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 71: schema validation on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 72: namespace ownership on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 73: auditability on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 74: admission on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 75: image transport on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 76: DNS on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 77: scheduling on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 78: version compatibility on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 79: webhook health on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 80: API availability on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 81: OLM resolution on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 82: certificate trust on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 83: service discovery on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 84: resource pressure on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 85: dependency readiness on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 86: rollout on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 87: authorization on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 88: catalog health on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 89: proxy routing on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 90: storage on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 91: schema validation on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 92: namespace ownership on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 93: auditability on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 94: admission on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 95: image transport on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 96: DNS on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 97: scheduling on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 98: version compatibility on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 99: webhook health on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 100: API availability on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 101: OLM resolution on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 102: certificate trust on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 103: service discovery on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 104: resource pressure on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 105: dependency readiness on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 106: rollout on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 107: authorization on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 108: catalog health on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 109: proxy routing on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 110: storage on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 111: schema validation on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 112: namespace ownership on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 113: auditability on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 114: admission on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 115: image transport on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 116: DNS on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 117: scheduling on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 118: version compatibility on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 119: webhook health on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 120: API availability on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 121: OLM resolution on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 122: certificate trust on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 123: service discovery on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 124: resource pressure on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 125: dependency readiness on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 126: rollout on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 127: authorization on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 128: catalog health on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 129: proxy routing on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 130: storage on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 131: schema validation on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 132: namespace ownership on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 133: auditability on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 134: admission on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 135: image transport on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 136: DNS on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 137: scheduling on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 138: version compatibility on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 139: webhook health on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 140: API availability on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 141: OLM resolution on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 142: certificate trust on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 143: service discovery on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 144: resource pressure on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 145: dependency readiness on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 146: rollout on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 147: authorization on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 148: catalog health on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 149: proxy routing on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 150: storage on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 151: schema validation on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 152: namespace ownership on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 153: auditability on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 154: admission on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 155: image transport on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 156: DNS on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 157: scheduling on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 158: version compatibility on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 159: webhook health on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 160: API availability on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 161: OLM resolution on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 162: certificate trust on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 163: service discovery on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 164: resource pressure on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 165: dependency readiness on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 166: rollout on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 167: authorization on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 168: catalog health on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 169: proxy routing on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 170: storage on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 171: schema validation on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 172: namespace ownership on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 173: auditability on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 174: admission on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 175: image transport on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 176: DNS on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 177: scheduling on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 178: version compatibility on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 179: webhook health on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 180: API availability on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 181: OLM resolution on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 182: certificate trust on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 183: service discovery on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 184: resource pressure on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 185: dependency readiness on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 186: rollout on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 187: authorization on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 188: catalog health on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 189: proxy routing on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 190: storage on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 191: schema validation on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 192: namespace ownership on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 193: auditability on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 194: admission on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 195: image transport on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 196: DNS on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 197: scheduling on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 198: version compatibility on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 199: webhook health on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 200: API availability on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 201: OLM resolution on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 202: certificate trust on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 203: service discovery on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 204: resource pressure on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 205: dependency readiness on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 206: rollout on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 207: authorization on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 208: catalog health on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: compare desired and observed state.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 209: proxy routing on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: use the same CA bundle.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 210: storage on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: collect before mutation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 211: schema validation on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: test from the cluster network.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 212: namespace ownership on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: capture must-gather for escalation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 213: auditability on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: confirm the intended channel.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 214: admission on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: validate user and control paths.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 215: image transport on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: validate against the live CRD.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 216: DNS on Route
- Scenario: `Dashboard pod failure` with plausible cause `image pull failure`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: watch reconciliation.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 217: scheduling on OAuthClient
- Scenario: `Dashboard pod failure` with plausible cause `invalid ConfigMap`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: test backend DNS and TLS; apply after backup and supportability review.
- Senior practice: use server-side dry-run.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 218: version compatibility on Deployment/rhods-dashboard
- Scenario: `Dashboard pod failure` with plausible cause `OAuth mismatch`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: correct resources; apply after backup and supportability review.
- Senior practice: change one item at a time.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 219: webhook health on Pod
- Scenario: `Dashboard pod failure` with plausible cause `resource pressure`.
- Evidence: inspect SubjectAccessReview and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: verify OAuth redirects; apply after backup and supportability review.
- Senior practice: test with the exact service account.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 220: API availability on Service
- Scenario: `Dashboard pod failure` with plausible cause `backend API unreachable`.
- Evidence: inspect status conditions and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: restore generated Secret via reconciliation; apply after backup and supportability review.
- Senior practice: record rollback.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 221: OLM resolution on ConfigMap
- Scenario: `Dashboard pod failure` with plausible cause `missing Secret`.
- Evidence: inspect termination reason and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: inspect probes and mounts; apply after backup and supportability review.
- Senior practice: isolate the first failing dependency.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

### Drill 222: certificate trust on Secret
- Scenario: `Dashboard pod failure` with plausible cause `certificate mount failure`.
- Evidence: inspect image pull event and verify active generation, owner, namespace, and timestamp.
- Read-only test: choose `oc get`, `oc describe`, `oc logs`, `oc auth can-i`, `openssl`, or `skopeo` using the failing identity and network context.
- Decision: accept the hypothesis only when dependency ordering and chronology align.
- Candidate remediation: separate container logs; apply after backup and supportability review.
- Senior practice: avoid premature deletion.
- Validation: prove controller convergence and the original user transaction.
- Prevention: add alert, policy, preflight, or runbook control.

## 17. Interview questions
### Question 1
Difference among Subscription, InstallPlan, CSV, and deployment?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 2
How locate earliest failed control loop?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 3
Why can CSV Succeeded coexist with unhealthy operands?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 4
Manual versus Automatic approval?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 5
How prove controller observed latest spec?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 6
How solve x509 without disabling TLS?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 7
Authentication versus reachability?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 8
What changes in disconnected installation?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 9
Why is CRD deletion dangerous?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 10
How use oc auth can-i for service account?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 11
Custom namespace timing?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 12
Pre-upgrade backups?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 13
When refresh failing Subscription?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 14
How diagnose CSV Pending?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 15
How validate route 503?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 16
OwnerReferences impact?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 17
Why current and previous logs?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 18
How automate GitOps health gates?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 19
What goes in a support case?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 20
How lead an incident for Dashboard pod failure?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 21
Difference among Subscription, InstallPlan, CSV, and deployment?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 22
How locate earliest failed control loop?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 23
Why can CSV Succeeded coexist with unhealthy operands?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 24
Manual versus Automatic approval?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 25
How prove controller observed latest spec?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 26
How solve x509 without disabling TLS?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 27
Authentication versus reachability?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 28
What changes in disconnected installation?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 29
Why is CRD deletion dangerous?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 30
How use oc auth can-i for service account?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 31
Custom namespace timing?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 32
Pre-upgrade backups?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 33
When refresh failing Subscription?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 34
How diagnose CSV Pending?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 35
How validate route 503?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 36
OwnerReferences impact?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 37
Why current and previous logs?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 38
How automate GitOps health gates?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 39
What goes in a support case?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 40
How lead an incident for Dashboard pod failure?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 41
Difference among Subscription, InstallPlan, CSV, and deployment?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 42
How locate earliest failed control loop?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 43
Why can CSV Succeeded coexist with unhealthy operands?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 44
Manual versus Automatic approval?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 45
How prove controller observed latest spec?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 46
How solve x509 without disabling TLS?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 47
Authentication versus reachability?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 48
What changes in disconnected installation?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 49
Why is CRD deletion dangerous?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 50
How use oc auth can-i for service account?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 51
Custom namespace timing?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 52
Pre-upgrade backups?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 53
When refresh failing Subscription?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 54
How diagnose CSV Pending?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 55
How validate route 503?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 56
OwnerReferences impact?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 57
Why current and previous logs?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 58
How automate GitOps health gates?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 59
What goes in a support case?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 60
How lead an incident for Dashboard pod failure?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 61
Difference among Subscription, InstallPlan, CSV, and deployment?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 62
How locate earliest failed control loop?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 63
Why can CSV Succeeded coexist with unhealthy operands?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 64
Manual versus Automatic approval?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 65
How prove controller observed latest spec?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 66
How solve x509 without disabling TLS?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 67
Authentication versus reachability?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 68
What changes in disconnected installation?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 69
Why is CRD deletion dangerous?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 70
How use oc auth can-i for service account?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 71
Custom namespace timing?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 72
Pre-upgrade backups?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 73
When refresh failing Subscription?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 74
How diagnose CSV Pending?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 75
How validate route 503?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 76
OwnerReferences impact?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 77
Why current and previous logs?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 78
How automate GitOps health gates?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 79
What goes in a support case?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 80
How lead an incident for Dashboard pod failure?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 81
Difference among Subscription, InstallPlan, CSV, and deployment?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 82
How locate earliest failed control loop?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 83
Why can CSV Succeeded coexist with unhealthy operands?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 84
Manual versus Automatic approval?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 85
How prove controller observed latest spec?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 86
How solve x509 without disabling TLS?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 87
Authentication versus reachability?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 88
What changes in disconnected installation?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 89
Why is CRD deletion dangerous?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 90
How use oc auth can-i for service account?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 91
Custom namespace timing?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 92
Pre-upgrade backups?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 93
When refresh failing Subscription?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 94
How diagnose CSV Pending?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 95
How validate route 503?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 96
OwnerReferences impact?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 97
Why current and previous logs?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 98
How automate GitOps health gates?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 99
What goes in a support case?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

### Question 100
How lead an incident for Dashboard pod failure?
- Expected answer: desired state, observed state, controller, evidence, safe remediation, validation, rollback, and prevention.

## 18. Command reference
### 18.1 Identity
```bash
oc whoami && oc whoami --show-server
```

### 18.2 Versions
```bash
oc version
```

### 18.3 Cluster version
```bash
oc get clusterversion version -o yaml
```

### 18.4 Cluster Operators
```bash
oc get clusteroperators
```

### 18.5 Nodes
```bash
oc get nodes -o wide
```

### 18.6 Events
```bash
oc get events -A --sort-by=.lastTimestamp
```

### 18.7 Subscriptions
```bash
oc get subscriptions.operators.coreos.com -A -o yaml
```

### 18.8 InstallPlans
```bash
oc get installplans.operators.coreos.com -A -o yaml
```

### 18.9 CSVs
```bash
oc get clusterserviceversions.operators.coreos.com -A -o yaml
```

### 18.10 OperatorGroups
```bash
oc get operatorgroups.operators.coreos.com -A -o yaml
```

### 18.11 CatalogSources
```bash
oc get catalogsources.operators.coreos.com -n openshift-marketplace -o yaml
```

### 18.12 Catalog pods
```bash
oc get pods -n openshift-marketplace -o wide
```

### 18.13 OLM pods
```bash
oc get pods -n openshift-operator-lifecycle-manager -o wide
```

### 18.14 Operator pods
```bash
oc get pods -n redhat-ods-operator -o wide
```

### 18.15 Application pods
```bash
oc get pods -n redhat-ods-applications -o wide
```

### 18.16 DSC
```bash
oc get datascienceclusters.datasciencecluster.opendatahub.io -A -o yaml
```

### 18.17 DSCI
```bash
oc get dscinitializations.dscinitialization.opendatahub.io -A -o yaml
```

### 18.18 CRDs
```bash
oc get crd | grep -Ei 'opendatahub|datascience|rhods|kserve|trustyai|ray'
```

### 18.19 Routes
```bash
oc get routes -A
```

### 18.20 Proxy
```bash
oc get proxy cluster -o yaml
```

### 18.21 Image mirrors
```bash
oc get imagedigestmirrorsets,imagetagmirrorsets -o yaml
```

### JSON and jq patterns
```bash
oc get subscription -A -o json | jq -r '.items[] | [.metadata.namespace,.metadata.name,.status.currentCSV,.status.installedCSV] | @tsv'
oc get csv -A -o json | jq -r '.items[] | [.metadata.namespace,.metadata.name,.status.phase,.status.reason] | @tsv'
oc get installplan -A -o json | jq -r '.items[] | [.metadata.namespace,.metadata.name,.spec.approved,.status.phase] | @tsv'
```
### TLS
```bash
HOST=<route-host>
openssl s_client -connect ${HOST}:443 -servername ${HOST} -showcerts </dev/null
curl -vkI https://${HOST}/
# Final verification must use trusted CA validation, not -k.
```
### Registry
```bash
skopeo inspect --authfile <authfile> docker://<registry>/<repository>@sha256:<digest>
podman login <registry>
podman pull <registry>/<repository>@sha256:<digest>
```

## 19. Glossary
- **OLM** — Operator Lifecycle Manager.
- **Subscription** — Requested package, channel, source, and approval policy.
- **InstallPlan** — Resolved ordered resource steps.
- **CSV** — Operator metadata, APIs, permissions, deployment, and status.
- **CatalogSource** — Operator catalog metadata source.
- **OperatorGroup** — OLM installation scope.
- **DSCI** — Cluster-wide OpenShift AI initialization.
- **DSC** — Desired component state.
- **Operand** — Workload managed by an Operator.
- **CRD** — Kubernetes API extension.
- **Observed generation** — Spec generation processed by controller.
- **IDMS** — Digest mirror policy.
- **ITMS** — Tag mirror policy.
- **SLO** — Service-level objective.
- **RTO** — Recovery time objective.

## 20. Official references
Use the documentation matching the exact installed product version.
- [OpenShift AI 3.5 installing and uninstalling](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/installing_and_uninstalling_openshift_ai_self-managed/index)
- [OpenShift AI 3.5 disconnected installation](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/installing_and_uninstalling_openshift_ai_self-managed_in_a_disconnected_environment)
- [OpenShift Container Platform 4.19 Understanding Operators](https://docs.redhat.com/en/documentation/openshift_container_platform/4.19/html/operators/understanding-operators)
- [OpenShift Container Platform 4.19 Operator administrator tasks](https://docs.redhat.com/en/documentation/openshift_container_platform/4.19/html/operators/administrator-tasks)
- [OpenShift Container Platform 4.19 troubleshooting](https://docs.redhat.com/en/documentation/openshift_container_platform/4.19/html/support/troubleshooting)

## Completion checklist
- [ ] Completion 1: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 2: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 3: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 4: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 5: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 6: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 7: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 8: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 9: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 10: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 11: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 12: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 13: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 14: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 15: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 16: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 17: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 18: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 19: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 20: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 21: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 22: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 23: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 24: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 25: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 26: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 27: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 28: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 29: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 30: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 31: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 32: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 33: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 34: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 35: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 36: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 37: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 38: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 39: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.
- [ ] Completion 40: evidence, cause, remediation, validation, rollback, monitoring, security, and knowledge transfer are complete.

---
Generated guide: 09. Dashboard pod failure
Baseline date: 2026-07-21
Red Hat documentation and support guidance take precedence for version-specific recovery actions.
