# 12. Pipeline deployment automation

> Corporate L3 / senior platform engineering guide for Red Hat OpenShift AI GitOps and automation.
> Version scope: examples are aligned to OpenShift AI 3.4/3.5-era documentation, OpenShift GitOps 1.20, and OpenShift Pipelines 1.14-era guidance. Always inspect the CRDs and supported channels installed on the target cluster before production use.

## Document outcomes
- Explain Pipeline deployment automation from architecture through day-2 operations.
- Create an enterprise project for Pipeline deployment automation from an empty Git repository.
- Apply security, governance, observability, backup, and disaster-recovery controls.
- Diagnose controller, API, storage, networking, image, permission, and rollout failures.
- Design reusable assets suitable for development, test, production, and disconnected environments.

## 1. Executive explanation
Automate compilation, validation, upload, versioning, scheduling, and operational checks for OpenShift AI Kubeflow Pipelines workflows.

At senior level, Pipeline deployment automation is not treated as a single command or YAML file. It is treated as a controlled reconciliation system whose inputs are Git revisions, signed artifacts, policy decisions, cluster capabilities, and environment-specific configuration.

The desired state must be reviewable, reproducible, and recoverable. The live state must be observable. Every automated action needs an owner, an evidence trail, a failure boundary, and a tested rollback method.

OpenShift AI adds specialized APIs and operational dependencies to normal Kubernetes delivery: operators, DataScienceCluster components, object storage, workbench images, pipeline servers, KServe runtimes, model artifacts, accelerators, and model endpoint tests.

A strong design separates platform configuration from tenant workloads, separates build from deployment, promotes immutable digests rather than mutable tags, and uses GitOps controllers for ongoing convergence rather than allowing CI systems unrestricted production credentials.

## 2. Scope and responsibility model
### Platform Administrator
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through Kubernetes events.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### GitOps Platform Engineer
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### ML Engineer
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Data Scientist
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through Kubernetes events.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### SRE
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Security Engineer
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through Prometheus metrics.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Release Manager
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through controller logs.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Auditor
- Owns or reviews the parts of Pipeline deployment automation that match the persona boundary.
- Produces evidence through controller logs.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

## 3. Core resource map
### 3.1. `KFP Python DSL`
- Purpose: `KFP Python DSL` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.2. `IR YAML`
- Purpose: `IR YAML` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.3. `pipeline server`
- Purpose: `pipeline server` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.4. `pipeline version`
- Purpose: `pipeline version` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.5. `experiment`
- Purpose: `experiment` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.6. `run`
- Purpose: `run` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.7. `recurring run`
- Purpose: `recurring run` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.8. `artifact bucket`
- Purpose: `artifact bucket` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.9. `database`
- Purpose: `database` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.10. `service account`
- Purpose: `service account` is part of the control or workload surface for Pipeline deployment automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

## 4. Reference architecture
1. Developer or platform engineer creates a change on a feature branch.
2. CI validates syntax, schemas, policy, signatures, and artifact identity.
3. A pull request records review, approval, test evidence, and change intent.
4. The protected branch becomes the desired-state source of truth.
5. Argo CD or another approved reconciler renders Helm or Kustomize content.
6. The Kubernetes and OpenShift APIs perform admission and persist desired resources.
7. Operators and workload controllers reconcile dependent resources.
8. Health gates inspect status conditions, events, logs, metrics, and endpoint behavior.
9. Promotion changes only immutable references and environment-specific approved settings.
10. Rollback reselects a known-good Git revision, image digest, model version, or release revision.

### Control-plane flow
```text
Engineer -> Git branch -> CI validation -> Pull request -> Protected branch
Protected branch -> Argo CD -> OpenShift API -> Operators/controllers -> Workloads
Workloads -> Metrics/logs/events/tests -> Release gate -> Promotion or rollback
```

## 5. Project from scratch
1. Define the success criteria for the component authoring phase.
2. Identify APIs, controllers, external services, and credentials required for component authoring.
3. Document the failure boundary and rollback action for component authoring.
4. Create a least-privilege service account or human role for component authoring.
5. Add schema, policy, and security validation for component authoring.
6. Add a health gate based on status, events, logs, metrics, and functional tests for component authoring.
7. Record release evidence and ownership for component authoring.
8. Test idempotency by rerunning the automation for component authoring.
9. Define the success criteria for the pipeline compilation phase.
10. Identify APIs, controllers, external services, and credentials required for pipeline compilation.
11. Document the failure boundary and rollback action for pipeline compilation.
12. Create a least-privilege service account or human role for pipeline compilation.
13. Add schema, policy, and security validation for pipeline compilation.
14. Add a health gate based on status, events, logs, metrics, and functional tests for pipeline compilation.
15. Record release evidence and ownership for pipeline compilation.
16. Test idempotency by rerunning the automation for pipeline compilation.
17. Define the success criteria for the static validation phase.
18. Identify APIs, controllers, external services, and credentials required for static validation.
19. Document the failure boundary and rollback action for static validation.
20. Create a least-privilege service account or human role for static validation.
21. Add schema, policy, and security validation for static validation.
22. Add a health gate based on status, events, logs, metrics, and functional tests for static validation.
23. Record release evidence and ownership for static validation.
24. Test idempotency by rerunning the automation for static validation.
25. Define the success criteria for the artifact publication phase.
26. Identify APIs, controllers, external services, and credentials required for artifact publication.
27. Document the failure boundary and rollback action for artifact publication.
28. Create a least-privilege service account or human role for artifact publication.
29. Add schema, policy, and security validation for artifact publication.
30. Add a health gate based on status, events, logs, metrics, and functional tests for artifact publication.
31. Record release evidence and ownership for artifact publication.
32. Test idempotency by rerunning the automation for artifact publication.
33. Define the success criteria for the pipeline upload phase.
34. Identify APIs, controllers, external services, and credentials required for pipeline upload.
35. Document the failure boundary and rollback action for pipeline upload.
36. Create a least-privilege service account or human role for pipeline upload.
37. Add schema, policy, and security validation for pipeline upload.
38. Add a health gate based on status, events, logs, metrics, and functional tests for pipeline upload.
39. Record release evidence and ownership for pipeline upload.
40. Test idempotency by rerunning the automation for pipeline upload.
41. Define the success criteria for the run creation phase.
42. Identify APIs, controllers, external services, and credentials required for run creation.
43. Document the failure boundary and rollback action for run creation.
44. Create a least-privilege service account or human role for run creation.
45. Add schema, policy, and security validation for run creation.
46. Add a health gate based on status, events, logs, metrics, and functional tests for run creation.
47. Record release evidence and ownership for run creation.
48. Test idempotency by rerunning the automation for run creation.
49. Define the success criteria for the run observation phase.
50. Identify APIs, controllers, external services, and credentials required for run observation.
51. Document the failure boundary and rollback action for run observation.
52. Create a least-privilege service account or human role for run observation.
53. Add schema, policy, and security validation for run observation.
54. Add a health gate based on status, events, logs, metrics, and functional tests for run observation.
55. Record release evidence and ownership for run observation.
56. Test idempotency by rerunning the automation for run observation.
57. Define the success criteria for the version retirement phase.
58. Identify APIs, controllers, external services, and credentials required for version retirement.
59. Document the failure boundary and rollback action for version retirement.
60. Create a least-privilege service account or human role for version retirement.
61. Add schema, policy, and security validation for version retirement.
62. Add a health gate based on status, events, logs, metrics, and functional tests for version retirement.
63. Record release evidence and ownership for version retirement.
64. Test idempotency by rerunning the automation for version retirement.

## 6. Recommended repository structure
```text
rhoai-automation/
├── README.md
├── docs/
│   ├── architecture.md
│   ├── security-model.md
│   ├── operations-runbook.md
│   └── rollback-runbook.md
├── platform/
│   ├── base/
│   │   ├── operators/
│   │   ├── datasciencecluster/
│   │   ├── policies/
│   │   └── monitoring/
│   └── overlays/
│       ├── dev/
│       ├── test/
│       └── prod/
├── workloads/
│   ├── workbenches/
│   ├── pipelines/
│   ├── runtimes/
│   └── models/
├── ci/
│   ├── tasks/
│   ├── pipelines/
│   ├── policy/
│   └── tests/
├── ansible/
│   ├── roles/
│   ├── playbooks/
│   └── execution-environment.yml
└── evidence/
    └── README.md
```

## 7. Representative implementation
The following example is intentionally generic. Replace placeholders, confirm installed CRD schemas, and use only image digests and channels supported for your environment.

```python
from kfp import compiler, dsl

@dsl.component(base_image="registry.access.redhat.com/ubi9/python-311@sha256:REPLACE_ME")
def validate_model(model_uri: str) -> str:
    import hashlib
    # Download using an approved SDK and verify the expected checksum.
    return hashlib.sha256(model_uri.encode()).hexdigest()

@dsl.pipeline(name="validate-model-release")
def pipeline(model_uri: str):
    validate_model(model_uri=model_uri)

compiler.Compiler().compile(
    pipeline_func=pipeline,
    package_path="validate-model-release.yaml",
)
```

## 8. Command workflow
### 8.1. Verification using `kfp`
```bash
oc whoami
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.2. Verification using `oc`
```bash
oc version
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.3. Verification using `curl`
```bash
oc api-resources | sort
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.4. Verification using `tkn`
```bash
oc get crd | grep -E "opendatahub|datascience|kserve|tekton|argoproj"
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.5. Verification using `git`
```bash
oc auth can-i --list -n <namespace>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.6. Verification using `jq`
```bash
oc apply --dry-run=server -f <manifest>.yaml
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.7. Verification using `python`
```bash
oc diff -f <manifest>.yaml
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.8. Verification using `kfp`
```bash
oc apply -f <manifest>.yaml
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.9. Verification using `oc`
```bash
oc get events -n <namespace> --sort-by=.lastTimestamp
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.10. Verification using `curl`
```bash
oc get pods -n <namespace> -o wide
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.11. Verification using `tkn`
```bash
oc describe <kind> <name> -n <namespace>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.12. Verification using `git`
```bash
oc logs <pod> -n <namespace> --all-containers --prefix
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.13. Verification using `jq`
```bash
argocd app get <application> --refresh
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.14. Verification using `python`
```bash
argocd app diff <application>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.15. Verification using `kfp`
```bash
argocd app sync <application>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.16. Verification using `oc`
```bash
argocd app wait <application> --sync --health --timeout 600
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.17. Verification using `curl`
```bash
tkn pipelinerun list -n <namespace>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.18. Verification using `tkn`
```bash
tkn pipelinerun logs -f -L <run> -n <namespace>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.19. Verification using `git`
```bash
git log --show-signature -1
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.20. Verification using `jq`
```bash
git diff <known-good>..<candidate>
```
- Expected result: the command provides evidence for Pipeline deployment automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

## 9. Security and compliance controls
### 9.1. Kfp sdk pinning
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `KFP SDK pinning` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.2. Component image digest
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `component image digest` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.3. Ir schema validation
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `IR schema validation` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.4. Artifact naming convention
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `artifact naming convention` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.5. Pipeline version immutability
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `pipeline version immutability` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.6. Service account least privilege
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `service account least privilege` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.7. S3 tls verification
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `S3 TLS verification` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.8. External database for production
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `external database for production` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.9. Run timeout
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `run timeout` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.10. Recurring-run change control
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `recurring-run change control` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.11. Artifact retention
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `artifact retention` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.12. Failure notification
- Threat addressed: uncontrolled or unverifiable behavior in Pipeline deployment automation.
- Implementation: encode `failure notification` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

## 10. Observability and SRE
### Availability
- Signal: Controller replicas available and API endpoints reachable.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Reconciliation
- Signal: Desired resources converge without repeated errors or excessive queue time.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Drift
- Signal: Applications remain Synced or approved differences are explicitly documented.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Latency
- Signal: Build, sync, rollout, model load, and inference gates stay within defined objectives.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Errors
- Signal: Pipeline failures, sync errors, admission denials, image failures, and inference failures are classified.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Saturation
- Signal: CPU, memory, GPU, PVC, API rate limits, controller queues, and registry capacity have headroom.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Security
- Signal: Signature failures, policy violations, denied access, and unexpected secret reads generate evidence.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Business outcome
- Signal: The released model version and endpoint behavior match the approved change record.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

## 11. Troubleshooting runbook
### 11.1. Compile error
- Symptom: compile error.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `kfp` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.2. Ir incompatible
- Symptom: IR incompatible.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.3. Pipeline server unavailable
- Symptom: pipeline server unavailable.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `curl` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.4. S3 authentication failed
- Symptom: S3 authentication failed.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `tkn` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.5. Database unavailable
- Symptom: database unavailable.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `git` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.6. Component image pull failed
- Symptom: component image pull failed.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `jq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.7. Run stuck pending
- Symptom: run stuck pending.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `python` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.8. Artifact missing
- Symptom: artifact missing.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `kfp` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.9. Recurring run duplicates
- Symptom: recurring run duplicates.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.10. Cleanup deletes active artifact
- Symptom: cleanup deletes active artifact.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `curl` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

## 12. Production readiness checklist
- [ ] PR-01: Supported OpenShift, OpenShift AI, GitOps, Pipelines, and dependency versions are documented.
- [ ] PR-02: All required CRDs are established before custom resources are synchronized.
- [ ] PR-03: Repositories have protected branches, required reviews, and signed change policy.
- [ ] PR-04: Argo CD AppProjects restrict sources, destinations, and resource kinds.
- [ ] PR-05: CI service accounts cannot make unrestricted production changes.
- [ ] PR-06: Images and models are referenced by immutable digest or version and retained for rollback.
- [ ] PR-07: Secrets are referenced from an approved secret system and rotation has been tested.
- [ ] PR-08: RBAC has been tested with positive and negative `can-i` cases.
- [ ] PR-09: NetworkPolicy has default deny plus tested DNS, registry, Git, S3, database, monitoring, and API paths.
- [ ] PR-10: ResourceQuota and LimitRange align with hardware profiles and capacity.
- [ ] PR-11: Status and functional health gates are both implemented.
- [ ] PR-12: Metrics, logs, events, audit data, and release evidence have retention owners.
- [ ] PR-13: Backup and restore include Git, cluster resources, external databases, object storage, registry metadata, and encryption keys where applicable.
- [ ] PR-14: Rollback is rehearsed and has a maximum decision time.
- [ ] PR-15: Disconnected registry and catalog dependencies are mirrored when required.
- [ ] PR-16: Support boundaries and Technology Preview features are identified before use.

## 13. Senior interview and design questions
### Question 1: Why is Pipeline deployment automation a reconciliation problem rather than only a scripting problem?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 2: Where should build end and deployment begin?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 3: How do you prevent a CI service account from becoming a permanent production administrator?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 4: How do you order an Operator, its CRDs, and its custom resources?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 5: How do you distinguish health from successful apply?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 6: How do you promote an image or model without rebuilding it?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 7: How do you handle secrets in GitOps without treating base64 as encryption?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 8: How do you test RBAC and NetworkPolicy negatively?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 9: What evidence is required to approve a model for production?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 10: When is rollback unsafe because of schema or state migration?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 11: How do you manage manual emergency changes without accepting permanent drift?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 12: How do you validate the examples after a product upgrade?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 13: How do you design for disconnected operation?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 14: How do you measure reconciliation latency and deployment lead time?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 15: How would you separate platform, tenant, and model repositories?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

## 14. Hands-on lab sequence
### Lab 1: Create the repository and branch protections.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 2: Bootstrap a non-production Argo CD project with restricted destinations.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 3: Render and server-side dry-run all manifests.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 4: Deploy a minimal namespace, RBAC, quota, and default-deny policy.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 5: Install or validate required operators using an approval gate.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 6: Apply the OpenShift AI platform resource and wait for reconciliation.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 7: Build or register a digest-pinned workbench or runtime image.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 8: Deploy a pipeline or model workload and capture evidence.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 9: Introduce a controlled failure and troubleshoot from status to root cause.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 10: Promote the immutable release to a second environment.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 11: Execute the documented rollback.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 12: Produce an operations report with risks and follow-up actions.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

## 15. Enterprise scenario drills
### Drill 001: Component Authoring in the developer sandbox
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 002: Component Authoring in the disconnected site
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 003: Component Authoring in the production
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 004: Component Authoring in the test
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 005: Component Authoring in the integration
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 006: Component Authoring in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 007: Component Authoring in the disaster-recovery
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 008: Component Authoring in the pre-production
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 009: Component Authoring in the integration
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 010: Component Authoring in the multi-cluster fleet
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 011: Component Authoring in the disaster-recovery
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 012: Component Authoring in the pre-production
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 013: Component Authoring in the test
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 014: Component Authoring in the developer sandbox
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 015: Component Authoring in the disconnected site
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 016: Component Authoring in the production
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 017: Component Authoring in the test
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 018: Component Authoring in the developer sandbox
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 019: Component Authoring in the disconnected site
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 020: Component Authoring in the production
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 021: Component Authoring in the pre-production
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 022: Component Authoring in the integration
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 023: Component Authoring in the multi-cluster fleet
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 024: Component Authoring in the disaster-recovery
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 025: Component Authoring in the pre-production
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 026: Component Authoring in the integration
- Situation: the security engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 027: Component Authoring in the multi-cluster fleet
- Situation: the release manager is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 028: Component Authoring in the disaster-recovery
- Situation: the auditor is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 029: Component Authoring in the production
- Situation: the platform administrator is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 030: Component Authoring in the test
- Situation: the GitOps platform engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 031: Component Authoring in the developer sandbox
- Situation: the ML engineer is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 032: Component Authoring in the disconnected site
- Situation: the data scientist is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 033: Component Authoring in the production
- Situation: the SRE is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 034: Component Authoring in the test
- Situation: the security engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 035: Component Authoring in the developer sandbox
- Situation: the release manager is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 036: Component Authoring in the disconnected site
- Situation: the auditor is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 037: Component Authoring in the disaster-recovery
- Situation: the platform administrator is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 038: Component Authoring in the pre-production
- Situation: the GitOps platform engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 039: Component Authoring in the integration
- Situation: the ML engineer is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 040: Component Authoring in the multi-cluster fleet
- Situation: the data scientist is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 041: Component Authoring in the disaster-recovery
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 042: Component Authoring in the pre-production
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 043: Component Authoring in the integration
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 044: Component Authoring in the multi-cluster fleet
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 045: Component Authoring in the disconnected site
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 046: Component Authoring in the production
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 047: Component Authoring in the test
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 048: Component Authoring in the developer sandbox
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 049: Component Authoring in the disconnected site
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 050: Component Authoring in the production
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 051: Component Authoring in the test
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 052: Component Authoring in the developer sandbox
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 053: Component Authoring in the multi-cluster fleet
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 054: Component Authoring in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 055: Component Authoring in the pre-production
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 056: Component Authoring in the integration
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 057: Component Authoring in the multi-cluster fleet
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 058: Component Authoring in the disaster-recovery
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 059: Component Authoring in the pre-production
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 060: Component Authoring in the integration
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 061: Component Authoring in the developer sandbox
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 062: Component Authoring in the disconnected site
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 063: Component Authoring in the production
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 064: Component Authoring in the test
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 065: Component Authoring in the developer sandbox
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 066: Component Authoring in the disconnected site
- Situation: the security engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 067: Component Authoring in the production
- Situation: the release manager is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 068: Component Authoring in the test
- Situation: the auditor is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 069: Component Authoring in the integration
- Situation: the platform administrator is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 070: Component Authoring in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 071: Component Authoring in the disaster-recovery
- Situation: the ML engineer is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 072: Component Authoring in the pre-production
- Situation: the data scientist is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 073: Component Authoring in the integration
- Situation: the SRE is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 074: Component Authoring in the multi-cluster fleet
- Situation: the security engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 075: Component Authoring in the disaster-recovery
- Situation: the release manager is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 076: Component Authoring in the pre-production
- Situation: the auditor is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 077: Component Authoring in the test
- Situation: the platform administrator is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 078: Component Authoring in the developer sandbox
- Situation: the GitOps platform engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 079: Component Authoring in the disconnected site
- Situation: the ML engineer is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 080: Component Authoring in the production
- Situation: the data scientist is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 081: Component Authoring in the test
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 082: Component Authoring in the developer sandbox
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 083: Component Authoring in the disconnected site
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 084: Component Authoring in the production
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 085: Component Authoring in the pre-production
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 086: Component Authoring in the integration
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 087: Component Authoring in the multi-cluster fleet
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 088: Component Authoring in the disaster-recovery
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 089: Component Authoring in the pre-production
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that S3 TLS verification is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 090: Component Authoring in the integration
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 091: Component Authoring in the multi-cluster fleet
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 092: Component Authoring in the disaster-recovery
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 093: Component Authoring in the production
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 094: Component Authoring in the test
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 095: Component Authoring in the developer sandbox
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 096: Component Authoring in the disconnected site
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 097: Component Authoring in the production
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 098: Component Authoring in the test
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 099: Component Authoring in the developer sandbox
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 100: Component Authoring in the disconnected site
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 101: Component Authoring in the disaster-recovery
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 102: Component Authoring in the pre-production
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that external database for production is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 103: Component Authoring in the integration
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 104: Component Authoring in the multi-cluster fleet
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 105: Component Authoring in the disaster-recovery
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 106: Component Authoring in the pre-production
- Situation: the security engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 107: Component Authoring in the integration
- Situation: the release manager is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 108: Component Authoring in the multi-cluster fleet
- Situation: the auditor is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 109: Component Authoring in the disconnected site
- Situation: the platform administrator is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 110: Component Authoring in the production
- Situation: the GitOps platform engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 111: Component Authoring in the test
- Situation: the ML engineer is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 112: Component Authoring in the developer sandbox
- Situation: the data scientist is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 113: Component Authoring in the disconnected site
- Situation: the SRE is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 114: Component Authoring in the production
- Situation: the security engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 115: Component Authoring in the test
- Situation: the release manager is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that run timeout is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 116: Component Authoring in the developer sandbox
- Situation: the auditor is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 117: Component Authoring in the multi-cluster fleet
- Situation: the platform administrator is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 118: Component Authoring in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 119: Component Authoring in the pre-production
- Situation: the ML engineer is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 120: Component Authoring in the integration
- Situation: the data scientist is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 121: Component Authoring in the multi-cluster fleet
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 122: Component Authoring in the disaster-recovery
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 123: Component Authoring in the pre-production
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 124: Component Authoring in the integration
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 125: Component Authoring in the developer sandbox
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 126: Component Authoring in the disconnected site
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 127: Component Authoring in the production
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 128: Component Authoring in the test
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that recurring-run change control is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 129: Component Authoring in the developer sandbox
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 130: Component Authoring in the disconnected site
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 131: Component Authoring in the production
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 132: Component Authoring in the test
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 133: Component Authoring in the integration
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 134: Component Authoring in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 135: Component Authoring in the disaster-recovery
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 136: Component Authoring in the pre-production
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 137: Component Authoring in the integration
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 138: Component Authoring in the multi-cluster fleet
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 139: Component Authoring in the disaster-recovery
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 140: Component Authoring in the pre-production
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 141: Component Authoring in the test
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that artifact retention is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 142: Component Authoring in the developer sandbox
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 143: Component Authoring in the disconnected site
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 144: Component Authoring in the production
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 145: Component Authoring in the test
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 146: Component Authoring in the developer sandbox
- Situation: the security engineer is preparing `service account` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 147: Component Authoring in the disconnected site
- Situation: the release manager is preparing `database` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 148: Component Authoring in the production
- Situation: the auditor is preparing `artifact bucket` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 149: Component Authoring in the pre-production
- Situation: the platform administrator is preparing `recurring run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 150: Component Authoring in the integration
- Situation: the GitOps platform engineer is preparing `run` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 151: Component Authoring in the multi-cluster fleet
- Situation: the ML engineer is preparing `experiment` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 152: Component Authoring in the disaster-recovery
- Situation: the data scientist is preparing `pipeline version` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 153: Component Authoring in the pre-production
- Situation: the SRE is preparing `pipeline server` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 154: Component Authoring in the integration
- Situation: the security engineer is preparing `IR YAML` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 155: Component Authoring in the multi-cluster fleet
- Situation: the release manager is preparing `KFP Python DSL` changes for Pipeline deployment automation during component authoring.
- Control objective: prove that failure notification is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 156: Pipeline Compilation in the disaster-recovery
- Situation: the auditor is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 157: Pipeline Compilation in the production
- Situation: the platform administrator is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 158: Pipeline Compilation in the test
- Situation: the GitOps platform engineer is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 159: Pipeline Compilation in the developer sandbox
- Situation: the ML engineer is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 160: Pipeline Compilation in the disconnected site
- Situation: the data scientist is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 161: Pipeline Compilation in the production
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 162: Pipeline Compilation in the test
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 163: Pipeline Compilation in the developer sandbox
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 164: Pipeline Compilation in the disconnected site
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 165: Pipeline Compilation in the disaster-recovery
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 166: Pipeline Compilation in the pre-production
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 167: Pipeline Compilation in the integration
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 168: Pipeline Compilation in the multi-cluster fleet
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that KFP SDK pinning is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 169: Pipeline Compilation in the disaster-recovery
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 170: Pipeline Compilation in the pre-production
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 171: Pipeline Compilation in the integration
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 172: Pipeline Compilation in the multi-cluster fleet
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 173: Pipeline Compilation in the disconnected site
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 174: Pipeline Compilation in the production
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 175: Pipeline Compilation in the test
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 176: Pipeline Compilation in the developer sandbox
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 177: Pipeline Compilation in the disconnected site
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 178: Pipeline Compilation in the production
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 179: Pipeline Compilation in the test
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 180: Pipeline Compilation in the developer sandbox
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 181: Pipeline Compilation in the multi-cluster fleet
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that component image digest is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 182: Pipeline Compilation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 183: Pipeline Compilation in the pre-production
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 184: Pipeline Compilation in the integration
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 185: Pipeline Compilation in the multi-cluster fleet
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 186: Pipeline Compilation in the disaster-recovery
- Situation: the security engineer is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 187: Pipeline Compilation in the pre-production
- Situation: the release manager is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 188: Pipeline Compilation in the integration
- Situation: the auditor is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 189: Pipeline Compilation in the developer sandbox
- Situation: the platform administrator is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 190: Pipeline Compilation in the disconnected site
- Situation: the GitOps platform engineer is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 191: Pipeline Compilation in the production
- Situation: the ML engineer is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 192: Pipeline Compilation in the test
- Situation: the data scientist is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 193: Pipeline Compilation in the developer sandbox
- Situation: the SRE is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 194: Pipeline Compilation in the disconnected site
- Situation: the security engineer is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that IR schema validation is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 195: Pipeline Compilation in the production
- Situation: the release manager is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 196: Pipeline Compilation in the test
- Situation: the auditor is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 197: Pipeline Compilation in the integration
- Situation: the platform administrator is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 198: Pipeline Compilation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 199: Pipeline Compilation in the disaster-recovery
- Situation: the ML engineer is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 200: Pipeline Compilation in the pre-production
- Situation: the data scientist is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 201: Pipeline Compilation in the integration
- Situation: the SRE is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 202: Pipeline Compilation in the multi-cluster fleet
- Situation: the security engineer is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 203: Pipeline Compilation in the disaster-recovery
- Situation: the release manager is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 204: Pipeline Compilation in the pre-production
- Situation: the auditor is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 205: Pipeline Compilation in the test
- Situation: the platform administrator is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 206: Pipeline Compilation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 207: Pipeline Compilation in the disconnected site
- Situation: the ML engineer is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that artifact naming convention is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 208: Pipeline Compilation in the production
- Situation: the data scientist is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 209: Pipeline Compilation in the test
- Situation: the SRE is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 210: Pipeline Compilation in the developer sandbox
- Situation: the security engineer is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 211: Pipeline Compilation in the disconnected site
- Situation: the release manager is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 212: Pipeline Compilation in the production
- Situation: the auditor is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 213: Pipeline Compilation in the pre-production
- Situation: the platform administrator is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 214: Pipeline Compilation in the integration
- Situation: the GitOps platform engineer is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 215: Pipeline Compilation in the multi-cluster fleet
- Situation: the ML engineer is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: component image pull failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 216: Pipeline Compilation in the disaster-recovery
- Situation: the data scientist is preparing `service account` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: run stuck pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `service account`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 217: Pipeline Compilation in the pre-production
- Situation: the SRE is preparing `database` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: artifact missing.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `database`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 218: Pipeline Compilation in the integration
- Situation: the security engineer is preparing `artifact bucket` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `artifact bucket`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 219: Pipeline Compilation in the multi-cluster fleet
- Situation: the release manager is preparing `recurring run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: recurring run duplicates.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `recurring run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 220: Pipeline Compilation in the disaster-recovery
- Situation: the auditor is preparing `run` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that pipeline version immutability is enforced before the change can progress.
- Injected fault: cleanup deletes active artifact.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `python` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `run`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 221: Pipeline Compilation in the production
- Situation: the platform administrator is preparing `experiment` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: compile error.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kfp` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `experiment`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 222: Pipeline Compilation in the test
- Situation: the GitOps platform engineer is preparing `pipeline version` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: IR incompatible.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `pipeline version`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 223: Pipeline Compilation in the developer sandbox
- Situation: the ML engineer is preparing `pipeline server` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `curl` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `pipeline server`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 224: Pipeline Compilation in the disconnected site
- Situation: the data scientist is preparing `IR YAML` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: pipeline server unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `IR YAML`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 225: Pipeline Compilation in the production
- Situation: the SRE is preparing `KFP Python DSL` changes for Pipeline deployment automation during pipeline compilation.
- Control objective: prove that service account least privilege is enforced before the change can progress.
- Injected fault: S3 authentication failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `KFP Python DSL`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

## 16. Operational knowledge cards
### Card 001: `run`
- Design question: who owns `run` and where is its source of truth?
- Guardrail: service account least privilege.
- Failure to recognize: cleanup deletes active artifact.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 002: `database`
- Design question: who owns `database` and where is its source of truth?
- Guardrail: artifact retention.
- Failure to recognize: run stuck pending.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 003: `IR YAML`
- Design question: who owns `IR YAML` and where is its source of truth?
- Guardrail: artifact naming convention.
- Failure to recognize: S3 authentication failed.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 004: `experiment`
- Design question: who owns `experiment` and where is its source of truth?
- Guardrail: run timeout.
- Failure to recognize: compile error.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 005: `artifact bucket`
- Design question: who owns `artifact bucket` and where is its source of truth?
- Guardrail: component image digest.
- Failure to recognize: artifact missing.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

## 17. Official references
- [Red Hat OpenShift AI Self-Managed 3.4 - Installing and uninstalling](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Configuring model serving](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/configuring_your_model-serving_platform/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_on_projects/)
- [Red Hat OpenShift AI Self-Managed 3.4 - AI pipelines](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_with_ai_pipelines/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Creating a workbench](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/creating_a_workbench/)
- [Red Hat OpenShift GitOps 1.20 documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/1.20/)
- [Red Hat OpenShift Pipelines documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/)
- [Argo CD documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Tekton documentation](https://tekton.dev/docs/)
- [Helm documentation](https://helm.sh/docs/)
- [Kubernetes Kustomize documentation](https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/)
- [Kubernetes RBAC documentation](https://kubernetes.io/docs/reference/access-authn-authz/rbac/)
- [Kubernetes NetworkPolicy documentation](https://kubernetes.io/docs/concepts/services-networking/network-policies/)
- [Kubernetes ResourceQuota documentation](https://kubernetes.io/docs/concepts/policy/resource-quotas/)
- [Ansible kubernetes.core collection](https://docs.ansible.com/projects/ansible/latest/collections/kubernetes/core/)

## 18. Final L3 summary
Pipeline deployment automation is production-ready only when the full lifecycle is declarative, reviewable, least-privilege, observable, and recoverable. The strongest implementation does not merely automate the happy path; it creates evidence, stops unsafe promotion, detects drift, and provides a rehearsed route back to a known-good state.

> End of guide.
- Final review prompt 001: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 002: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 003: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 004: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 005: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 006: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 007: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 008: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 009: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 010: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 011: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 012: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 013: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 014: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
