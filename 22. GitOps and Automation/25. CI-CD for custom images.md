# 25. CI/CD for custom images

> Corporate L3 / senior platform engineering guide for Red Hat OpenShift AI GitOps and automation.
> Version scope: examples are aligned to OpenShift AI 3.4/3.5-era documentation, OpenShift GitOps 1.20, and OpenShift Pipelines 1.14-era guidance. Always inspect the CRDs and supported channels installed on the target cluster before production use.

## Document outcomes
- Explain CI/CD for custom images from architecture through day-2 operations.
- Create an enterprise project for CI/CD for custom images from an empty Git repository.
- Apply security, governance, observability, backup, and disaster-recovery controls.
- Diagnose controller, API, storage, networking, image, permission, and rollout failures.
- Design reusable assets suitable for development, test, production, and disconnected environments.

## 1. Executive explanation
Build a secure software supply chain for OpenShift AI workbench and model-serving images, including tests, vulnerability scans, SBOMs, signatures, registry publication, and GitOps promotion.

At senior level, CI/CD for custom images is not treated as a single command or YAML file. It is treated as a controlled reconciliation system whose inputs are Git revisions, signed artifacts, policy decisions, cluster capabilities, and environment-specific configuration.

The desired state must be reviewable, reproducible, and recoverable. The live state must be observable. Every automated action needs an owner, an evidence trail, a failure boundary, and a tested rollback method.

OpenShift AI adds specialized APIs and operational dependencies to normal Kubernetes delivery: operators, DataScienceCluster components, object storage, workbench images, pipeline servers, KServe runtimes, model artifacts, accelerators, and model endpoint tests.

A strong design separates platform configuration from tenant workloads, separates build from deployment, promotes immutable digests rather than mutable tags, and uses GitOps controllers for ongoing convergence rather than allowing CI systems unrestricted production credentials.

## 2. Scope and responsibility model
### Platform Administrator
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### GitOps Platform Engineer
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through Argo CD Application status.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### ML Engineer
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Data Scientist
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### SRE
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Security Engineer
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through Kubernetes events.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Release Manager
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Auditor
- Owns or reviews the parts of CI/CD for custom images that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

## 3. Core resource map
### 3.1. `Containerfile`
- Purpose: `Containerfile` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.2. `Tekton Pipeline`
- Purpose: `Tekton Pipeline` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.3. `Buildah task`
- Purpose: `Buildah task` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.4. `ImageStream`
- Purpose: `ImageStream` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.5. `OCI registry`
- Purpose: `OCI registry` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.6. `SBOM`
- Purpose: `SBOM` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.7. `provenance`
- Purpose: `provenance` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.8. `signature`
- Purpose: `signature` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.9. `vulnerability report`
- Purpose: `vulnerability report` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.10. `promotion manifest`
- Purpose: `promotion manifest` is part of the control or workload surface for CI/CD for custom images.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

## 4. Reference architecture
1. Developer or platform engineer creates a change on a feature branch.
2. CI validates syntax, schemas, policy, signatures, and artifact identity.
3. A pull request records review, approval, test evidence, and change intent.
4. The protected branch becomes the desired-state source of truth.
5. Argo CD or another approved reconciler renders Helm or Kustomize content.
6. The Kubernetes and OpenShift APIs perform admission and persist desired resources.
7. Operators and workload controllers reconcile dependent resources.
8. Health gates inspect status conditions, events, logs, metrics, and endpoint behavior.
9. Promotion changes only immutable references and environment-specific approved settings.
10. Rollback reselects a known-good Git revision, image digest, model version, or release revision.

### Control-plane flow
```text
Engineer -> Git branch -> CI validation -> Pull request -> Protected branch
Protected branch -> Argo CD -> OpenShift API -> Operators/controllers -> Workloads
Workloads -> Metrics/logs/events/tests -> Release gate -> Promotion or rollback
```

## 5. Project from scratch
1. Define the success criteria for the source checkout phase.
2. Identify APIs, controllers, external services, and credentials required for source checkout.
3. Document the failure boundary and rollback action for source checkout.
4. Create a least-privilege service account or human role for source checkout.
5. Add schema, policy, and security validation for source checkout.
6. Add a health gate based on status, events, logs, metrics, and functional tests for source checkout.
7. Record release evidence and ownership for source checkout.
8. Test idempotency by rerunning the automation for source checkout.
9. Define the success criteria for the dependency resolution phase.
10. Identify APIs, controllers, external services, and credentials required for dependency resolution.
11. Document the failure boundary and rollback action for dependency resolution.
12. Create a least-privilege service account or human role for dependency resolution.
13. Add schema, policy, and security validation for dependency resolution.
14. Add a health gate based on status, events, logs, metrics, and functional tests for dependency resolution.
15. Record release evidence and ownership for dependency resolution.
16. Test idempotency by rerunning the automation for dependency resolution.
17. Define the success criteria for the container build phase.
18. Identify APIs, controllers, external services, and credentials required for container build.
19. Document the failure boundary and rollback action for container build.
20. Create a least-privilege service account or human role for container build.
21. Add schema, policy, and security validation for container build.
22. Add a health gate based on status, events, logs, metrics, and functional tests for container build.
23. Record release evidence and ownership for container build.
24. Test idempotency by rerunning the automation for container build.
25. Define the success criteria for the unit test phase.
26. Identify APIs, controllers, external services, and credentials required for unit test.
27. Document the failure boundary and rollback action for unit test.
28. Create a least-privilege service account or human role for unit test.
29. Add schema, policy, and security validation for unit test.
30. Add a health gate based on status, events, logs, metrics, and functional tests for unit test.
31. Record release evidence and ownership for unit test.
32. Test idempotency by rerunning the automation for unit test.
33. Define the success criteria for the image test phase.
34. Identify APIs, controllers, external services, and credentials required for image test.
35. Document the failure boundary and rollback action for image test.
36. Create a least-privilege service account or human role for image test.
37. Add schema, policy, and security validation for image test.
38. Add a health gate based on status, events, logs, metrics, and functional tests for image test.
39. Record release evidence and ownership for image test.
40. Test idempotency by rerunning the automation for image test.
41. Define the success criteria for the scan phase.
42. Identify APIs, controllers, external services, and credentials required for scan.
43. Document the failure boundary and rollback action for scan.
44. Create a least-privilege service account or human role for scan.
45. Add schema, policy, and security validation for scan.
46. Add a health gate based on status, events, logs, metrics, and functional tests for scan.
47. Record release evidence and ownership for scan.
48. Test idempotency by rerunning the automation for scan.
49. Define the success criteria for the SBOM and provenance phase.
50. Identify APIs, controllers, external services, and credentials required for SBOM and provenance.
51. Document the failure boundary and rollback action for SBOM and provenance.
52. Create a least-privilege service account or human role for SBOM and provenance.
53. Add schema, policy, and security validation for SBOM and provenance.
54. Add a health gate based on status, events, logs, metrics, and functional tests for SBOM and provenance.
55. Record release evidence and ownership for SBOM and provenance.
56. Test idempotency by rerunning the automation for SBOM and provenance.
57. Define the success criteria for the sign and publish phase.
58. Identify APIs, controllers, external services, and credentials required for sign and publish.
59. Document the failure boundary and rollback action for sign and publish.
60. Create a least-privilege service account or human role for sign and publish.
61. Add schema, policy, and security validation for sign and publish.
62. Add a health gate based on status, events, logs, metrics, and functional tests for sign and publish.
63. Record release evidence and ownership for sign and publish.
64. Test idempotency by rerunning the automation for sign and publish.

## 6. Recommended repository structure
```text
rhoai-automation/
├── README.md
├── docs/
│   ├── architecture.md
│   ├── security-model.md
│   ├── operations-runbook.md
│   └── rollback-runbook.md
├── platform/
│   ├── base/
│   │   ├── operators/
│   │   ├── datasciencecluster/
│   │   ├── policies/
│   │   └── monitoring/
│   └── overlays/
│       ├── dev/
│       ├── test/
│       └── prod/
├── workloads/
│   ├── workbenches/
│   ├── pipelines/
│   ├── runtimes/
│   └── models/
├── ci/
│   ├── tasks/
│   ├── pipelines/
│   ├── policy/
│   └── tests/
├── ansible/
│   ├── roles/
│   ├── playbooks/
│   └── execution-environment.yml
└── evidence/
    └── README.md
```

## 7. Representative implementation
The following example is intentionally generic. Replace placeholders, confirm installed CRD schemas, and use only image digests and channels supported for your environment.

```yaml
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: build-sign-promote-ai-image
spec:
  params:
  - name: IMAGE
  - name: GIT_REVISION
  workspaces:
  - name: source
  tasks:
  - name: build
    taskRef:
      resolver: cluster
      params:
      - {name: kind, value: task}
      - {name: name, value: buildah}
      - {name: namespace, value: openshift-pipelines}
  - name: scan
    runAfter: [build]
    taskRef:
      name: scan-image
  - name: sign
    runAfter: [scan]
    taskRef:
      name: sign-image
  finally:
  - name: publish-evidence
    taskRef:
      name: publish-evidence
```

## 8. Command workflow
### 8.1. Verification using `buildah`
```bash
oc whoami
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.2. Verification using `podman`
```bash
oc version
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.3. Verification using `skopeo`
```bash
oc api-resources | sort
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.4. Verification using `cosign`
```bash
oc get crd | grep -E "opendatahub|datascience|kserve|tekton|argoproj"
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.5. Verification using `syft`
```bash
oc auth can-i --list -n <namespace>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.6. Verification using `grype`
```bash
oc apply --dry-run=server -f <manifest>.yaml
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.7. Verification using `trivy`
```bash
oc diff -f <manifest>.yaml
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.8. Verification using `oc`
```bash
oc apply -f <manifest>.yaml
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.9. Verification using `git`
```bash
oc get events -n <namespace> --sort-by=.lastTimestamp
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.10. Verification using `tkn`
```bash
oc get pods -n <namespace> -o wide
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.11. Verification using `buildah`
```bash
oc describe <kind> <name> -n <namespace>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.12. Verification using `podman`
```bash
oc logs <pod> -n <namespace> --all-containers --prefix
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.13. Verification using `skopeo`
```bash
argocd app get <application> --refresh
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.14. Verification using `cosign`
```bash
argocd app diff <application>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.15. Verification using `syft`
```bash
argocd app sync <application>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.16. Verification using `grype`
```bash
argocd app wait <application> --sync --health --timeout 600
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.17. Verification using `trivy`
```bash
tkn pipelinerun list -n <namespace>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.18. Verification using `oc`
```bash
tkn pipelinerun logs -f -L <run> -n <namespace>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.19. Verification using `git`
```bash
git log --show-signature -1
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.20. Verification using `tkn`
```bash
git diff <known-good>..<candidate>
```
- Expected result: the command provides evidence for CI/CD for custom images without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

## 9. Security and compliance controls
### 9.1. Trusted base digest
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `trusted base digest` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.2. Lock dependencies
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `lock dependencies` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.3. Rootless build where possible
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `rootless build where possible` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.4. Reproducible build arguments
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `reproducible build arguments` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.5. No embedded secrets
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `no embedded secrets` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.6. Critical cve policy
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `critical CVE policy` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.7. Sbom
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `SBOM` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.8. Slsa-style provenance
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `SLSA-style provenance` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.9. Keyless or managed signing
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `keyless or managed signing` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.10. Registry retention
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `registry retention` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.11. Digest promotion
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `digest promotion` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.12. Runtime smoke test
- Threat addressed: uncontrolled or unverifiable behavior in CI/CD for custom images.
- Implementation: encode `runtime smoke test` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

## 10. Observability and SRE
### Availability
- Signal: Controller replicas available and API endpoints reachable.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Reconciliation
- Signal: Desired resources converge without repeated errors or excessive queue time.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Drift
- Signal: Applications remain Synced or approved differences are explicitly documented.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Latency
- Signal: Build, sync, rollout, model load, and inference gates stay within defined objectives.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Errors
- Signal: Pipeline failures, sync errors, admission denials, image failures, and inference failures are classified.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Saturation
- Signal: CPU, memory, GPU, PVC, API rate limits, controller queues, and registry capacity have headroom.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Security
- Signal: Signature failures, policy violations, denied access, and unexpected secret reads generate evidence.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Business outcome
- Signal: The released model version and endpoint behavior match the approved change record.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

## 11. Troubleshooting runbook
### 11.1. Base digest unavailable
- Symptom: base digest unavailable.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `buildah` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.2. Dependency supply chain compromised
- Symptom: dependency supply chain compromised.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `podman` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.3. Secret copied into layer
- Symptom: secret copied into layer.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `skopeo` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.4. Build cache stale
- Symptom: build cache stale.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `cosign` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.5. Architecture wrong
- Symptom: architecture wrong.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `syft` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.6. Scan database unavailable
- Symptom: scan database unavailable.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `grype` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.7. False-positive waiver untracked
- Symptom: false-positive waiver untracked.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `trivy` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.8. Signature identity mismatch
- Symptom: signature identity mismatch.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.9. Imagestream import delayed
- Symptom: ImageStream import delayed.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `git` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.10. Promotion uses mutable tag
- Symptom: promotion uses mutable tag.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `tkn` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

## 12. Production readiness checklist
- [ ] PR-01: Supported OpenShift, OpenShift AI, GitOps, Pipelines, and dependency versions are documented.
- [ ] PR-02: All required CRDs are established before custom resources are synchronized.
- [ ] PR-03: Repositories have protected branches, required reviews, and signed change policy.
- [ ] PR-04: Argo CD AppProjects restrict sources, destinations, and resource kinds.
- [ ] PR-05: CI service accounts cannot make unrestricted production changes.
- [ ] PR-06: Images and models are referenced by immutable digest or version and retained for rollback.
- [ ] PR-07: Secrets are referenced from an approved secret system and rotation has been tested.
- [ ] PR-08: RBAC has been tested with positive and negative `can-i` cases.
- [ ] PR-09: NetworkPolicy has default deny plus tested DNS, registry, Git, S3, database, monitoring, and API paths.
- [ ] PR-10: ResourceQuota and LimitRange align with hardware profiles and capacity.
- [ ] PR-11: Status and functional health gates are both implemented.
- [ ] PR-12: Metrics, logs, events, audit data, and release evidence have retention owners.
- [ ] PR-13: Backup and restore include Git, cluster resources, external databases, object storage, registry metadata, and encryption keys where applicable.
- [ ] PR-14: Rollback is rehearsed and has a maximum decision time.
- [ ] PR-15: Disconnected registry and catalog dependencies are mirrored when required.
- [ ] PR-16: Support boundaries and Technology Preview features are identified before use.

## 13. Senior interview and design questions
### Question 1: Why is CI/CD for custom images a reconciliation problem rather than only a scripting problem?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 2: Where should build end and deployment begin?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 3: How do you prevent a CI service account from becoming a permanent production administrator?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 4: How do you order an Operator, its CRDs, and its custom resources?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 5: How do you distinguish health from successful apply?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 6: How do you promote an image or model without rebuilding it?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 7: How do you handle secrets in GitOps without treating base64 as encryption?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 8: How do you test RBAC and NetworkPolicy negatively?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 9: What evidence is required to approve a model for production?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 10: When is rollback unsafe because of schema or state migration?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 11: How do you manage manual emergency changes without accepting permanent drift?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 12: How do you validate the examples after a product upgrade?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 13: How do you design for disconnected operation?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 14: How do you measure reconciliation latency and deployment lead time?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 15: How would you separate platform, tenant, and model repositories?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

## 14. Hands-on lab sequence
### Lab 1: Create the repository and branch protections.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 2: Bootstrap a non-production Argo CD project with restricted destinations.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 3: Render and server-side dry-run all manifests.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 4: Deploy a minimal namespace, RBAC, quota, and default-deny policy.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 5: Install or validate required operators using an approval gate.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 6: Apply the OpenShift AI platform resource and wait for reconciliation.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 7: Build or register a digest-pinned workbench or runtime image.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 8: Deploy a pipeline or model workload and capture evidence.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 9: Introduce a controlled failure and troubleshoot from status to root cause.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 10: Promote the immutable release to a second environment.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 11: Execute the documented rollback.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 12: Produce an operations report with risks and follow-up actions.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

## 15. Enterprise scenario drills
### Drill 001: Source Checkout in the test
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 002: Source Checkout in the test
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 003: Source Checkout in the integration
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 004: Source Checkout in the integration
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 005: Source Checkout in the integration
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 006: Source Checkout in the integration
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 007: Source Checkout in the developer sandbox
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 008: Source Checkout in the developer sandbox
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 009: Source Checkout in the developer sandbox
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 010: Source Checkout in the developer sandbox
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 011: Source Checkout in the multi-cluster fleet
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 012: Source Checkout in the multi-cluster fleet
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 013: Source Checkout in the multi-cluster fleet
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 014: Source Checkout in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 015: Source Checkout in the disconnected site
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 016: Source Checkout in the disconnected site
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 017: Source Checkout in the disconnected site
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 018: Source Checkout in the disconnected site
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 019: Source Checkout in the disaster-recovery
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 020: Source Checkout in the disaster-recovery
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 021: Source Checkout in the disaster-recovery
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 022: Source Checkout in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 023: Source Checkout in the production
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 024: Source Checkout in the production
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 025: Source Checkout in the production
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 026: Source Checkout in the production
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 027: Source Checkout in the pre-production
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 028: Source Checkout in the pre-production
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 029: Source Checkout in the pre-production
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 030: Source Checkout in the pre-production
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 031: Source Checkout in the test
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 032: Source Checkout in the test
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 033: Source Checkout in the test
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 034: Source Checkout in the test
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 035: Source Checkout in the integration
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 036: Source Checkout in the integration
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 037: Source Checkout in the integration
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 038: Source Checkout in the integration
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 039: Source Checkout in the developer sandbox
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 040: Source Checkout in the developer sandbox
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 041: Source Checkout in the developer sandbox
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 042: Source Checkout in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 043: Source Checkout in the multi-cluster fleet
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 044: Source Checkout in the multi-cluster fleet
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 045: Source Checkout in the multi-cluster fleet
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 046: Source Checkout in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 047: Source Checkout in the disconnected site
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 048: Source Checkout in the disconnected site
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 049: Source Checkout in the disconnected site
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 050: Source Checkout in the disconnected site
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 051: Source Checkout in the disaster-recovery
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 052: Source Checkout in the disaster-recovery
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 053: Source Checkout in the disaster-recovery
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 054: Source Checkout in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 055: Source Checkout in the production
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 056: Source Checkout in the production
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 057: Source Checkout in the production
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 058: Source Checkout in the production
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 059: Source Checkout in the pre-production
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 060: Source Checkout in the pre-production
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 061: Source Checkout in the pre-production
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 062: Source Checkout in the pre-production
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 063: Source Checkout in the test
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 064: Source Checkout in the test
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 065: Source Checkout in the test
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 066: Source Checkout in the test
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 067: Source Checkout in the integration
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 068: Source Checkout in the integration
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 069: Source Checkout in the integration
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 070: Source Checkout in the integration
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 071: Source Checkout in the developer sandbox
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 072: Source Checkout in the developer sandbox
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 073: Source Checkout in the developer sandbox
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 074: Source Checkout in the developer sandbox
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 075: Source Checkout in the multi-cluster fleet
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 076: Source Checkout in the multi-cluster fleet
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 077: Source Checkout in the multi-cluster fleet
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 078: Source Checkout in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 079: Source Checkout in the disconnected site
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 080: Source Checkout in the disconnected site
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 081: Source Checkout in the disconnected site
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 082: Source Checkout in the disconnected site
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 083: Source Checkout in the disaster-recovery
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 084: Source Checkout in the disaster-recovery
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 085: Source Checkout in the disaster-recovery
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 086: Source Checkout in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 087: Source Checkout in the production
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 088: Source Checkout in the production
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 089: Source Checkout in the production
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 090: Source Checkout in the production
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 091: Source Checkout in the pre-production
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 092: Source Checkout in the pre-production
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 093: Source Checkout in the pre-production
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 094: Source Checkout in the pre-production
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 095: Source Checkout in the test
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 096: Source Checkout in the test
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 097: Source Checkout in the test
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 098: Source Checkout in the test
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 099: Source Checkout in the integration
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 100: Source Checkout in the integration
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 101: Source Checkout in the integration
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 102: Source Checkout in the integration
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 103: Source Checkout in the developer sandbox
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 104: Source Checkout in the developer sandbox
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 105: Source Checkout in the developer sandbox
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 106: Source Checkout in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 107: Source Checkout in the multi-cluster fleet
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 108: Source Checkout in the multi-cluster fleet
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 109: Source Checkout in the multi-cluster fleet
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 110: Source Checkout in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 111: Source Checkout in the disconnected site
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 112: Source Checkout in the disconnected site
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 113: Source Checkout in the disconnected site
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 114: Source Checkout in the disconnected site
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 115: Source Checkout in the disaster-recovery
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 116: Source Checkout in the disaster-recovery
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 117: Source Checkout in the disaster-recovery
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 118: Source Checkout in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 119: Source Checkout in the production
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 120: Source Checkout in the production
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during source checkout.
- Control objective: prove that runtime smoke test is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 121: Dependency Resolution in the production
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 122: Dependency Resolution in the production
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 123: Dependency Resolution in the pre-production
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 124: Dependency Resolution in the pre-production
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 125: Dependency Resolution in the pre-production
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 126: Dependency Resolution in the pre-production
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 127: Dependency Resolution in the test
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 128: Dependency Resolution in the test
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 129: Dependency Resolution in the test
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 130: Dependency Resolution in the test
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that trusted base digest is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 131: Dependency Resolution in the integration
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 132: Dependency Resolution in the integration
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 133: Dependency Resolution in the integration
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 134: Dependency Resolution in the integration
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 135: Dependency Resolution in the developer sandbox
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 136: Dependency Resolution in the developer sandbox
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 137: Dependency Resolution in the developer sandbox
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 138: Dependency Resolution in the developer sandbox
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 139: Dependency Resolution in the multi-cluster fleet
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 140: Dependency Resolution in the multi-cluster fleet
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 141: Dependency Resolution in the multi-cluster fleet
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that lock dependencies is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 142: Dependency Resolution in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 143: Dependency Resolution in the disconnected site
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 144: Dependency Resolution in the disconnected site
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 145: Dependency Resolution in the disconnected site
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 146: Dependency Resolution in the disconnected site
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 147: Dependency Resolution in the disaster-recovery
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 148: Dependency Resolution in the disaster-recovery
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 149: Dependency Resolution in the disaster-recovery
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 150: Dependency Resolution in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 151: Dependency Resolution in the production
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that rootless build where possible is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 152: Dependency Resolution in the production
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 153: Dependency Resolution in the production
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 154: Dependency Resolution in the production
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 155: Dependency Resolution in the pre-production
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 156: Dependency Resolution in the pre-production
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 157: Dependency Resolution in the pre-production
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 158: Dependency Resolution in the pre-production
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 159: Dependency Resolution in the test
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 160: Dependency Resolution in the test
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 161: Dependency Resolution in the test
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that reproducible build arguments is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 162: Dependency Resolution in the test
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 163: Dependency Resolution in the integration
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 164: Dependency Resolution in the integration
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 165: Dependency Resolution in the integration
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 166: Dependency Resolution in the integration
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 167: Dependency Resolution in the developer sandbox
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 168: Dependency Resolution in the developer sandbox
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 169: Dependency Resolution in the developer sandbox
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 170: Dependency Resolution in the developer sandbox
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 171: Dependency Resolution in the multi-cluster fleet
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 172: Dependency Resolution in the multi-cluster fleet
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that no embedded secrets is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 173: Dependency Resolution in the multi-cluster fleet
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 174: Dependency Resolution in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 175: Dependency Resolution in the disconnected site
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 176: Dependency Resolution in the disconnected site
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 177: Dependency Resolution in the disconnected site
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 178: Dependency Resolution in the disconnected site
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 179: Dependency Resolution in the disaster-recovery
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 180: Dependency Resolution in the disaster-recovery
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 181: Dependency Resolution in the disaster-recovery
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 182: Dependency Resolution in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that critical CVE policy is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 183: Dependency Resolution in the production
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 184: Dependency Resolution in the production
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 185: Dependency Resolution in the production
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 186: Dependency Resolution in the production
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 187: Dependency Resolution in the pre-production
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 188: Dependency Resolution in the pre-production
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 189: Dependency Resolution in the pre-production
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 190: Dependency Resolution in the pre-production
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 191: Dependency Resolution in the test
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 192: Dependency Resolution in the test
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SBOM is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 193: Dependency Resolution in the test
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 194: Dependency Resolution in the test
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 195: Dependency Resolution in the integration
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 196: Dependency Resolution in the integration
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 197: Dependency Resolution in the integration
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 198: Dependency Resolution in the integration
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 199: Dependency Resolution in the developer sandbox
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 200: Dependency Resolution in the developer sandbox
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 201: Dependency Resolution in the developer sandbox
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 202: Dependency Resolution in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 203: Dependency Resolution in the multi-cluster fleet
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that SLSA-style provenance is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 204: Dependency Resolution in the multi-cluster fleet
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 205: Dependency Resolution in the multi-cluster fleet
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 206: Dependency Resolution in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 207: Dependency Resolution in the disconnected site
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 208: Dependency Resolution in the disconnected site
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 209: Dependency Resolution in the disconnected site
- Situation: the data scientist is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 210: Dependency Resolution in the disconnected site
- Situation: the GitOps platform engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 211: Dependency Resolution in the disaster-recovery
- Situation: the auditor is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 212: Dependency Resolution in the disaster-recovery
- Situation: the security engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 213: Dependency Resolution in the disaster-recovery
- Situation: the data scientist is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that keyless or managed signing is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 214: Dependency Resolution in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 215: Dependency Resolution in the production
- Situation: the auditor is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 216: Dependency Resolution in the production
- Situation: the security engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 217: Dependency Resolution in the production
- Situation: the data scientist is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 218: Dependency Resolution in the production
- Situation: the GitOps platform engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 219: Dependency Resolution in the pre-production
- Situation: the auditor is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: scan database unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `syft` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 220: Dependency Resolution in the pre-production
- Situation: the security engineer is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: false-positive waiver untracked.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 221: Dependency Resolution in the pre-production
- Situation: the data scientist is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: signature identity mismatch.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `buildah` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 222: Dependency Resolution in the pre-production
- Situation: the GitOps platform engineer is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: ImageStream import delayed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `cosign` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 223: Dependency Resolution in the test
- Situation: the auditor is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that registry retention is enforced before the change can progress.
- Injected fault: promotion uses mutable tag.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `trivy` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 224: Dependency Resolution in the test
- Situation: the security engineer is preparing `provenance` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: base digest unavailable.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `provenance`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 225: Dependency Resolution in the test
- Situation: the data scientist is preparing `vulnerability report` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: dependency supply chain compromised.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `skopeo` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `vulnerability report`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 226: Dependency Resolution in the test
- Situation: the GitOps platform engineer is preparing `Containerfile` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: secret copied into layer.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `grype` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Containerfile`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 227: Dependency Resolution in the integration
- Situation: the auditor is preparing `Buildah task` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: build cache stale.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Buildah task`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 228: Dependency Resolution in the integration
- Situation: the security engineer is preparing `OCI registry` changes for CI/CD for custom images during dependency resolution.
- Control objective: prove that digest promotion is enforced before the change can progress.
- Injected fault: architecture wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `podman` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `OCI registry`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

## 16. Operational knowledge cards
### Card 001: `vulnerability report`
- Design question: who owns `vulnerability report` and where is its source of truth?
- Guardrail: SBOM.
- Failure to recognize: secret copied into layer.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 002: `Tekton Pipeline`
- Design question: who owns `Tekton Pipeline` and where is its source of truth?
- Guardrail: runtime smoke test.
- Failure to recognize: promotion uses mutable tag.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 003: `OCI registry`
- Design question: who owns `OCI registry` and where is its source of truth?
- Guardrail: no embedded secrets.
- Failure to recognize: false-positive waiver untracked.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 004: `signature`
- Design question: who owns `signature` and where is its source of truth?
- Guardrail: registry retention.
- Failure to recognize: build cache stale.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 005: `Containerfile`
- Design question: who owns `Containerfile` and where is its source of truth?
- Guardrail: rootless build where possible.
- Failure to recognize: base digest unavailable.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

## 17. Official references
- [Red Hat OpenShift AI Self-Managed 3.4 - Installing and uninstalling](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Configuring model serving](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/configuring_your_model-serving_platform/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_on_projects/)
- [Red Hat OpenShift AI Self-Managed 3.4 - AI pipelines](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_with_ai_pipelines/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Creating a workbench](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/creating_a_workbench/)
- [Red Hat OpenShift GitOps 1.20 documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/1.20/)
- [Red Hat OpenShift Pipelines documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/)
- [Argo CD documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Tekton documentation](https://tekton.dev/docs/)
- [Helm documentation](https://helm.sh/docs/)
- [Kubernetes Kustomize documentation](https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/)
- [Kubernetes RBAC documentation](https://kubernetes.io/docs/reference/access-authn-authz/rbac/)
- [Kubernetes NetworkPolicy documentation](https://kubernetes.io/docs/concepts/services-networking/network-policies/)
- [Kubernetes ResourceQuota documentation](https://kubernetes.io/docs/concepts/policy/resource-quotas/)
- [Ansible kubernetes.core collection](https://docs.ansible.com/projects/ansible/latest/collections/kubernetes/core/)

## 18. Final L3 summary
CI/CD for custom images is production-ready only when the full lifecycle is declarative, reviewable, least-privilege, observable, and recoverable. The strongest implementation does not merely automate the happy path; it creates evidence, stops unsafe promotion, detects drift, and provides a rehearsed route back to a known-good state.

> End of guide.
- Final review prompt 001: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 002: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 003: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 004: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 005: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 006: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 007: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 008: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 009: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 010: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 011: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 012: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 013: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 014: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
