# 09. Operator installation automation

> Corporate L3 / senior platform engineering guide for Red Hat OpenShift AI GitOps and automation.
> Version scope: examples are aligned to OpenShift AI 3.4/3.5-era documentation, OpenShift GitOps 1.20, and OpenShift Pipelines 1.14-era guidance. Always inspect the CRDs and supported channels installed on the target cluster before production use.

## Document outcomes
- Explain Operator installation automation from architecture through day-2 operations.
- Create an enterprise project for Operator installation automation from an empty Git repository.
- Apply security, governance, observability, backup, and disaster-recovery controls.
- Diagnose controller, API, storage, networking, image, permission, and rollout failures.
- Design reusable assets suitable for development, test, production, and disconnected environments.

## 1. Executive explanation
Automate OperatorGroup, Subscription, InstallPlan, CSV, and CRD readiness for OpenShift AI and its dependencies using GitOps and controlled pipeline gates.

At senior level, Operator installation automation is not treated as a single command or YAML file. It is treated as a controlled reconciliation system whose inputs are Git revisions, signed artifacts, policy decisions, cluster capabilities, and environment-specific configuration.

The desired state must be reviewable, reproducible, and recoverable. The live state must be observable. Every automated action needs an owner, an evidence trail, a failure boundary, and a tested rollback method.

OpenShift AI adds specialized APIs and operational dependencies to normal Kubernetes delivery: operators, DataScienceCluster components, object storage, workbench images, pipeline servers, KServe runtimes, model artifacts, accelerators, and model endpoint tests.

A strong design separates platform configuration from tenant workloads, separates build from deployment, promotes immutable digests rather than mutable tags, and uses GitOps controllers for ongoing convergence rather than allowing CI systems unrestricted production credentials.

## 2. Scope and responsibility model
### Platform Administrator
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### GitOps Platform Engineer
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through Argo CD Application status.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### ML Engineer
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Data Scientist
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through model registry metadata.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### SRE
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Security Engineer
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through Kubernetes events.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Release Manager
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Auditor
- Owns or reviews the parts of Operator installation automation that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

## 3. Core resource map
### 3.1. `Namespace`
- Purpose: `Namespace` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.2. `OperatorGroup`
- Purpose: `OperatorGroup` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.3. `Subscription`
- Purpose: `Subscription` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.4. `CatalogSource`
- Purpose: `CatalogSource` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.5. `InstallPlan`
- Purpose: `InstallPlan` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.6. `ClusterServiceVersion`
- Purpose: `ClusterServiceVersion` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.7. `CustomResourceDefinition`
- Purpose: `CustomResourceDefinition` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.8. `PackageManifest`
- Purpose: `PackageManifest` is part of the control or workload surface for Operator installation automation.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

## 4. Reference architecture
1. Developer or platform engineer creates a change on a feature branch.
2. CI validates syntax, schemas, policy, signatures, and artifact identity.
3. A pull request records review, approval, test evidence, and change intent.
4. The protected branch becomes the desired-state source of truth.
5. Argo CD or another approved reconciler renders Helm or Kustomize content.
6. The Kubernetes and OpenShift APIs perform admission and persist desired resources.
7. Operators and workload controllers reconcile dependent resources.
8. Health gates inspect status conditions, events, logs, metrics, and endpoint behavior.
9. Promotion changes only immutable references and environment-specific approved settings.
10. Rollback reselects a known-good Git revision, image digest, model version, or release revision.

### Control-plane flow
```text
Engineer -> Git branch -> CI validation -> Pull request -> Protected branch
Protected branch -> Argo CD -> OpenShift API -> Operators/controllers -> Workloads
Workloads -> Metrics/logs/events/tests -> Release gate -> Promotion or rollback
```

## 5. Project from scratch
1. Define the success criteria for the catalog validation phase.
2. Identify APIs, controllers, external services, and credentials required for catalog validation.
3. Document the failure boundary and rollback action for catalog validation.
4. Create a least-privilege service account or human role for catalog validation.
5. Add schema, policy, and security validation for catalog validation.
6. Add a health gate based on status, events, logs, metrics, and functional tests for catalog validation.
7. Record release evidence and ownership for catalog validation.
8. Test idempotency by rerunning the automation for catalog validation.
9. Define the success criteria for the namespace creation phase.
10. Identify APIs, controllers, external services, and credentials required for namespace creation.
11. Document the failure boundary and rollback action for namespace creation.
12. Create a least-privilege service account or human role for namespace creation.
13. Add schema, policy, and security validation for namespace creation.
14. Add a health gate based on status, events, logs, metrics, and functional tests for namespace creation.
15. Record release evidence and ownership for namespace creation.
16. Test idempotency by rerunning the automation for namespace creation.
17. Define the success criteria for the OperatorGroup creation phase.
18. Identify APIs, controllers, external services, and credentials required for OperatorGroup creation.
19. Document the failure boundary and rollback action for OperatorGroup creation.
20. Create a least-privilege service account or human role for OperatorGroup creation.
21. Add schema, policy, and security validation for OperatorGroup creation.
22. Add a health gate based on status, events, logs, metrics, and functional tests for OperatorGroup creation.
23. Record release evidence and ownership for OperatorGroup creation.
24. Test idempotency by rerunning the automation for OperatorGroup creation.
25. Define the success criteria for the Subscription creation phase.
26. Identify APIs, controllers, external services, and credentials required for Subscription creation.
27. Document the failure boundary and rollback action for Subscription creation.
28. Create a least-privilege service account or human role for Subscription creation.
29. Add schema, policy, and security validation for Subscription creation.
30. Add a health gate based on status, events, logs, metrics, and functional tests for Subscription creation.
31. Record release evidence and ownership for Subscription creation.
32. Test idempotency by rerunning the automation for Subscription creation.
33. Define the success criteria for the InstallPlan approval phase.
34. Identify APIs, controllers, external services, and credentials required for InstallPlan approval.
35. Document the failure boundary and rollback action for InstallPlan approval.
36. Create a least-privilege service account or human role for InstallPlan approval.
37. Add schema, policy, and security validation for InstallPlan approval.
38. Add a health gate based on status, events, logs, metrics, and functional tests for InstallPlan approval.
39. Record release evidence and ownership for InstallPlan approval.
40. Test idempotency by rerunning the automation for InstallPlan approval.
41. Define the success criteria for the CSV readiness phase.
42. Identify APIs, controllers, external services, and credentials required for CSV readiness.
43. Document the failure boundary and rollback action for CSV readiness.
44. Create a least-privilege service account or human role for CSV readiness.
45. Add schema, policy, and security validation for CSV readiness.
46. Add a health gate based on status, events, logs, metrics, and functional tests for CSV readiness.
47. Record release evidence and ownership for CSV readiness.
48. Test idempotency by rerunning the automation for CSV readiness.
49. Define the success criteria for the CRD readiness phase.
50. Identify APIs, controllers, external services, and credentials required for CRD readiness.
51. Document the failure boundary and rollback action for CRD readiness.
52. Create a least-privilege service account or human role for CRD readiness.
53. Add schema, policy, and security validation for CRD readiness.
54. Add a health gate based on status, events, logs, metrics, and functional tests for CRD readiness.
55. Record release evidence and ownership for CRD readiness.
56. Test idempotency by rerunning the automation for CRD readiness.
57. Define the success criteria for the operand creation phase.
58. Identify APIs, controllers, external services, and credentials required for operand creation.
59. Document the failure boundary and rollback action for operand creation.
60. Create a least-privilege service account or human role for operand creation.
61. Add schema, policy, and security validation for operand creation.
62. Add a health gate based on status, events, logs, metrics, and functional tests for operand creation.
63. Record release evidence and ownership for operand creation.
64. Test idempotency by rerunning the automation for operand creation.

## 6. Recommended repository structure
```text
rhoai-automation/
├── README.md
├── docs/
│   ├── architecture.md
│   ├── security-model.md
│   ├── operations-runbook.md
│   └── rollback-runbook.md
├── platform/
│   ├── base/
│   │   ├── operators/
│   │   ├── datasciencecluster/
│   │   ├── policies/
│   │   └── monitoring/
│   └── overlays/
│       ├── dev/
│       ├── test/
│       └── prod/
├── workloads/
│   ├── workbenches/
│   ├── pipelines/
│   ├── runtimes/
│   └── models/
├── ci/
│   ├── tasks/
│   ├── pipelines/
│   ├── policy/
│   └── tests/
├── ansible/
│   ├── roles/
│   ├── playbooks/
│   └── execution-environment.yml
└── evidence/
    └── README.md
```

## 7. Representative implementation
The following example is intentionally generic. Replace placeholders, confirm installed CRD schemas, and use only image digests and channels supported for your environment.

```yaml
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: redhat-ods-operator
  namespace: redhat-ods-operator
spec:
  targetNamespaces:
  - redhat-ods-operator
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhods-operator
  namespace: redhat-ods-operator
spec:
  channel: <supported-channel>
  installPlanApproval: Manual
  name: rhods-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
```

## 8. Command workflow
### 8.1. Verification using `jq`
```bash
oc whoami
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.2. Verification using `yq`
```bash
oc version
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.3. Verification using `argocd`
```bash
oc api-resources | sort
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.4. Verification using `tkn`
```bash
oc get crd | grep -E "opendatahub|datascience|kserve|tekton|argoproj"
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.5. Verification using `ansible-playbook`
```bash
oc auth can-i --list -n <namespace>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.6. Verification using `oc`
```bash
oc apply --dry-run=server -f <manifest>.yaml
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.7. Verification using `jq`
```bash
oc diff -f <manifest>.yaml
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.8. Verification using `yq`
```bash
oc apply -f <manifest>.yaml
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.9. Verification using `argocd`
```bash
oc get events -n <namespace> --sort-by=.lastTimestamp
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.10. Verification using `tkn`
```bash
oc get pods -n <namespace> -o wide
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.11. Verification using `ansible-playbook`
```bash
oc describe <kind> <name> -n <namespace>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.12. Verification using `oc`
```bash
oc logs <pod> -n <namespace> --all-containers --prefix
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.13. Verification using `jq`
```bash
argocd app get <application> --refresh
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.14. Verification using `yq`
```bash
argocd app diff <application>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.15. Verification using `argocd`
```bash
argocd app sync <application>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.16. Verification using `tkn`
```bash
argocd app wait <application> --sync --health --timeout 600
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.17. Verification using `ansible-playbook`
```bash
tkn pipelinerun list -n <namespace>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.18. Verification using `oc`
```bash
tkn pipelinerun logs -f -L <run> -n <namespace>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.19. Verification using `jq`
```bash
git log --show-signature -1
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.20. Verification using `yq`
```bash
git diff <known-good>..<candidate>
```
- Expected result: the command provides evidence for Operator installation automation without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

## 9. Security and compliance controls
### 9.1. Channel compatibility
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `channel compatibility` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.2. Source pinning
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `source pinning` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.3. Manual approval for production
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `manual approval for production` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.4. Startingcsv only when justified
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `startingCSV only when justified` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.5. Crd establishment gate
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `CRD establishment gate` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.6. Csv phase gate
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `CSV phase gate` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.7. Operator dependency ordering
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `operator dependency ordering` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.8. Namespace scope review
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `namespace scope review` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.9. Upgrade window
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `upgrade window` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.10. Rollback plan
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `rollback plan` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.11. Catalog health check
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `catalog health check` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.12. Disconnected mirror verification
- Threat addressed: uncontrolled or unverifiable behavior in Operator installation automation.
- Implementation: encode `disconnected mirror verification` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

## 10. Observability and SRE
### Availability
- Signal: Controller replicas available and API endpoints reachable.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Reconciliation
- Signal: Desired resources converge without repeated errors or excessive queue time.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Drift
- Signal: Applications remain Synced or approved differences are explicitly documented.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Latency
- Signal: Build, sync, rollout, model load, and inference gates stay within defined objectives.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Errors
- Signal: Pipeline failures, sync errors, admission denials, image failures, and inference failures are classified.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Saturation
- Signal: CPU, memory, GPU, PVC, API rate limits, controller queues, and registry capacity have headroom.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Security
- Signal: Signature failures, policy violations, denied access, and unexpected secret reads generate evidence.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Business outcome
- Signal: The released model version and endpoint behavior match the approved change record.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

## 11. Troubleshooting runbook
### 11.1. Catalogsource unhealthy
- Symptom: CatalogSource unhealthy.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `jq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.2. Package not found
- Symptom: package not found.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `yq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.3. Channel not found
- Symptom: channel not found.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `argocd` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.4. Installplan requires approval
- Symptom: InstallPlan requires approval.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `tkn` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.5. Csv pending
- Symptom: CSV pending.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `ansible-playbook` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.6. Csv failed
- Symptom: CSV failed.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.7. Crd absent
- Symptom: CRD absent.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `jq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.8. Dependency resolution failed
- Symptom: dependency resolution failed.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `yq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.9. Image mirror incomplete
- Symptom: image mirror incomplete.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `argocd` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.10. Operator namespace terminating
- Symptom: operator namespace terminating.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `tkn` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

## 12. Production readiness checklist
- [ ] PR-01: Supported OpenShift, OpenShift AI, GitOps, Pipelines, and dependency versions are documented.
- [ ] PR-02: All required CRDs are established before custom resources are synchronized.
- [ ] PR-03: Repositories have protected branches, required reviews, and signed change policy.
- [ ] PR-04: Argo CD AppProjects restrict sources, destinations, and resource kinds.
- [ ] PR-05: CI service accounts cannot make unrestricted production changes.
- [ ] PR-06: Images and models are referenced by immutable digest or version and retained for rollback.
- [ ] PR-07: Secrets are referenced from an approved secret system and rotation has been tested.
- [ ] PR-08: RBAC has been tested with positive and negative `can-i` cases.
- [ ] PR-09: NetworkPolicy has default deny plus tested DNS, registry, Git, S3, database, monitoring, and API paths.
- [ ] PR-10: ResourceQuota and LimitRange align with hardware profiles and capacity.
- [ ] PR-11: Status and functional health gates are both implemented.
- [ ] PR-12: Metrics, logs, events, audit data, and release evidence have retention owners.
- [ ] PR-13: Backup and restore include Git, cluster resources, external databases, object storage, registry metadata, and encryption keys where applicable.
- [ ] PR-14: Rollback is rehearsed and has a maximum decision time.
- [ ] PR-15: Disconnected registry and catalog dependencies are mirrored when required.
- [ ] PR-16: Support boundaries and Technology Preview features are identified before use.

## 13. Senior interview and design questions
### Question 1: Why is Operator installation automation a reconciliation problem rather than only a scripting problem?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 2: Where should build end and deployment begin?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 3: How do you prevent a CI service account from becoming a permanent production administrator?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 4: How do you order an Operator, its CRDs, and its custom resources?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 5: How do you distinguish health from successful apply?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 6: How do you promote an image or model without rebuilding it?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 7: How do you handle secrets in GitOps without treating base64 as encryption?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 8: How do you test RBAC and NetworkPolicy negatively?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 9: What evidence is required to approve a model for production?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 10: When is rollback unsafe because of schema or state migration?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 11: How do you manage manual emergency changes without accepting permanent drift?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 12: How do you validate the examples after a product upgrade?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 13: How do you design for disconnected operation?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 14: How do you measure reconciliation latency and deployment lead time?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 15: How would you separate platform, tenant, and model repositories?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

## 14. Hands-on lab sequence
### Lab 1: Create the repository and branch protections.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 2: Bootstrap a non-production Argo CD project with restricted destinations.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 3: Render and server-side dry-run all manifests.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 4: Deploy a minimal namespace, RBAC, quota, and default-deny policy.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 5: Install or validate required operators using an approval gate.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 6: Apply the OpenShift AI platform resource and wait for reconciliation.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 7: Build or register a digest-pinned workbench or runtime image.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 8: Deploy a pipeline or model workload and capture evidence.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 9: Introduce a controlled failure and troubleshoot from status to root cause.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 10: Promote the immutable release to a second environment.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 11: Execute the documented rollback.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 12: Produce an operations report with risks and follow-up actions.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

## 15. Enterprise scenario drills
### Drill 001: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 002: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 003: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 004: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 005: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 006: Catalog Validation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 007: Catalog Validation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 008: Catalog Validation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 009: Catalog Validation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 010: Catalog Validation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 011: Catalog Validation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 012: Catalog Validation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 013: Catalog Validation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 014: Catalog Validation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 015: Catalog Validation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 016: Catalog Validation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 017: Catalog Validation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 018: Catalog Validation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 019: Catalog Validation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 020: Catalog Validation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 021: Catalog Validation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 022: Catalog Validation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 023: Catalog Validation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 024: Catalog Validation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 025: Catalog Validation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 026: Catalog Validation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 027: Catalog Validation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 028: Catalog Validation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 029: Catalog Validation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 030: Catalog Validation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 031: Catalog Validation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 032: Catalog Validation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 033: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 034: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 035: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 036: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 037: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 038: Catalog Validation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 039: Catalog Validation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 040: Catalog Validation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 041: Catalog Validation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 042: Catalog Validation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 043: Catalog Validation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 044: Catalog Validation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 045: Catalog Validation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 046: Catalog Validation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 047: Catalog Validation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 048: Catalog Validation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 049: Catalog Validation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 050: Catalog Validation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 051: Catalog Validation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 052: Catalog Validation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 053: Catalog Validation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 054: Catalog Validation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 055: Catalog Validation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 056: Catalog Validation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 057: Catalog Validation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 058: Catalog Validation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 059: Catalog Validation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 060: Catalog Validation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 061: Catalog Validation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 062: Catalog Validation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 063: Catalog Validation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 064: Catalog Validation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 065: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 066: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 067: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 068: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 069: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 070: Catalog Validation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 071: Catalog Validation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 072: Catalog Validation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 073: Catalog Validation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 074: Catalog Validation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 075: Catalog Validation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 076: Catalog Validation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 077: Catalog Validation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 078: Catalog Validation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 079: Catalog Validation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 080: Catalog Validation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 081: Catalog Validation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 082: Catalog Validation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 083: Catalog Validation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 084: Catalog Validation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 085: Catalog Validation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 086: Catalog Validation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 087: Catalog Validation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 088: Catalog Validation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 089: Catalog Validation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 090: Catalog Validation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 091: Catalog Validation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 092: Catalog Validation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 093: Catalog Validation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 094: Catalog Validation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 095: Catalog Validation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 096: Catalog Validation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that operator dependency ordering is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 097: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 098: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 099: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 100: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 101: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 102: Catalog Validation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 103: Catalog Validation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 104: Catalog Validation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 105: Catalog Validation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 106: Catalog Validation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 107: Catalog Validation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 108: Catalog Validation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 109: Catalog Validation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 110: Catalog Validation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that namespace scope review is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 111: Catalog Validation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 112: Catalog Validation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 113: Catalog Validation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 114: Catalog Validation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 115: Catalog Validation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 116: Catalog Validation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 117: Catalog Validation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 118: Catalog Validation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 119: Catalog Validation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 120: Catalog Validation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 121: Catalog Validation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 122: Catalog Validation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 123: Catalog Validation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 124: Catalog Validation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that upgrade window is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 125: Catalog Validation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 126: Catalog Validation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 127: Catalog Validation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 128: Catalog Validation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 129: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 130: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 131: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 132: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 133: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 134: Catalog Validation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 135: Catalog Validation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 136: Catalog Validation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 137: Catalog Validation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that rollback plan is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 138: Catalog Validation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 139: Catalog Validation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 140: Catalog Validation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 141: Catalog Validation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 142: Catalog Validation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 143: Catalog Validation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 144: Catalog Validation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 145: Catalog Validation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 146: Catalog Validation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 147: Catalog Validation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 148: Catalog Validation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 149: Catalog Validation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 150: Catalog Validation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 151: Catalog Validation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that catalog health check is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 152: Catalog Validation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 153: Catalog Validation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 154: Catalog Validation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 155: Catalog Validation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 156: Catalog Validation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 157: Catalog Validation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 158: Catalog Validation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 159: Catalog Validation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 160: Catalog Validation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 161: Catalog Validation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 162: Catalog Validation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 163: Catalog Validation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 164: Catalog Validation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 165: Catalog Validation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during catalog validation.
- Control objective: prove that disconnected mirror verification is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 166: Namespace Creation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 167: Namespace Creation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 168: Namespace Creation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 169: Namespace Creation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 170: Namespace Creation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 171: Namespace Creation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 172: Namespace Creation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 173: Namespace Creation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 174: Namespace Creation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 175: Namespace Creation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 176: Namespace Creation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 177: Namespace Creation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 178: Namespace Creation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 179: Namespace Creation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that channel compatibility is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 180: Namespace Creation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 181: Namespace Creation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 182: Namespace Creation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 183: Namespace Creation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 184: Namespace Creation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 185: Namespace Creation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 186: Namespace Creation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 187: Namespace Creation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 188: Namespace Creation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 189: Namespace Creation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 190: Namespace Creation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 191: Namespace Creation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 192: Namespace Creation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 193: Namespace Creation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that source pinning is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 194: Namespace Creation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 195: Namespace Creation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 196: Namespace Creation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 197: Namespace Creation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 198: Namespace Creation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 199: Namespace Creation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 200: Namespace Creation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 201: Namespace Creation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 202: Namespace Creation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 203: Namespace Creation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 204: Namespace Creation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 205: Namespace Creation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 206: Namespace Creation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 207: Namespace Creation in the production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that manual approval for production is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 208: Namespace Creation in the test
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 209: Namespace Creation in the developer sandbox
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 210: Namespace Creation in the disconnected site
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 211: Namespace Creation in the pre-production
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 212: Namespace Creation in the integration
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 213: Namespace Creation in the multi-cluster fleet
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 214: Namespace Creation in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 215: Namespace Creation in the test
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 216: Namespace Creation in the developer sandbox
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 217: Namespace Creation in the disconnected site
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 218: Namespace Creation in the production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 219: Namespace Creation in the integration
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 220: Namespace Creation in the multi-cluster fleet
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 221: Namespace Creation in the disaster-recovery
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that startingCSV only when justified is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 222: Namespace Creation in the pre-production
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 223: Namespace Creation in the developer sandbox
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 224: Namespace Creation in the disconnected site
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 225: Namespace Creation in the production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: channel not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 226: Namespace Creation in the test
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 227: Namespace Creation in the multi-cluster fleet
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: InstallPlan requires approval.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 228: Namespace Creation in the disaster-recovery
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CSV pending.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 229: Namespace Creation in the pre-production
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CSV failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 230: Namespace Creation in the integration
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 231: Namespace Creation in the disconnected site
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: CRD absent.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 232: Namespace Creation in the production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: dependency resolution failed.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 233: Namespace Creation in the test
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: image mirror incomplete.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 234: Namespace Creation in the developer sandbox
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `yq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 235: Namespace Creation in the disaster-recovery
- Situation: the auditor is preparing `Namespace` changes for Operator installation automation during namespace creation.
- Control objective: prove that CRD establishment gate is enforced before the change can progress.
- Injected fault: operator namespace terminating.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `Namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 236: Namespace Creation in the pre-production
- Situation: the security engineer is preparing `CustomResourceDefinition` changes for Operator installation automation during namespace creation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: CatalogSource unhealthy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `CustomResourceDefinition`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 237: Namespace Creation in the integration
- Situation: the data scientist is preparing `InstallPlan` changes for Operator installation automation during namespace creation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `ansible-playbook` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `InstallPlan`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 238: Namespace Creation in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `Subscription` changes for Operator installation automation during namespace creation.
- Control objective: prove that CSV phase gate is enforced before the change can progress.
- Injected fault: package not found.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `Subscription`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

## 16. Operational knowledge cards
### Card 001: `InstallPlan`
- Design question: who owns `InstallPlan` and where is its source of truth?
- Guardrail: manual approval for production.
- Failure to recognize: CRD absent.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 002: `PackageManifest`
- Design question: who owns `PackageManifest` and where is its source of truth?
- Guardrail: namespace scope review.
- Failure to recognize: InstallPlan requires approval.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 003: `Subscription`
- Design question: who owns `Subscription` and where is its source of truth?
- Guardrail: channel compatibility.
- Failure to recognize: CatalogSource unhealthy.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 004: `ClusterServiceVersion`
- Design question: who owns `ClusterServiceVersion` and where is its source of truth?
- Guardrail: CSV phase gate.
- Failure to recognize: dependency resolution failed.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 005: `Namespace`
- Design question: who owns `Namespace` and where is its source of truth?
- Guardrail: catalog health check.
- Failure to recognize: CSV pending.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 006: `CatalogSource`
- Design question: who owns `CatalogSource` and where is its source of truth?
- Guardrail: startingCSV only when justified.
- Failure to recognize: package not found.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

## 17. Official references
- [Red Hat OpenShift AI Self-Managed 3.4 - Installing and uninstalling](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Configuring model serving](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/configuring_your_model-serving_platform/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_on_projects/)
- [Red Hat OpenShift AI Self-Managed 3.4 - AI pipelines](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_with_ai_pipelines/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Creating a workbench](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/creating_a_workbench/)
- [Red Hat OpenShift GitOps 1.20 documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/1.20/)
- [Red Hat OpenShift Pipelines documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/)
- [Argo CD documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Tekton documentation](https://tekton.dev/docs/)
- [Helm documentation](https://helm.sh/docs/)
- [Kubernetes Kustomize documentation](https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/)
- [Kubernetes RBAC documentation](https://kubernetes.io/docs/reference/access-authn-authz/rbac/)
- [Kubernetes NetworkPolicy documentation](https://kubernetes.io/docs/concepts/services-networking/network-policies/)
- [Kubernetes ResourceQuota documentation](https://kubernetes.io/docs/concepts/policy/resource-quotas/)
- [Ansible kubernetes.core collection](https://docs.ansible.com/projects/ansible/latest/collections/kubernetes/core/)

## 18. Final L3 summary
Operator installation automation is production-ready only when the full lifecycle is declarative, reviewable, least-privilege, observable, and recoverable. The strongest implementation does not merely automate the happy path; it creates evidence, stops unsafe promotion, detects drift, and provides a rehearsed route back to a known-good state.

> End of guide.
- Final review prompt 001: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 002: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 003: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 004: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 005: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 006: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 007: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 008: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 009: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 010: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 011: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
