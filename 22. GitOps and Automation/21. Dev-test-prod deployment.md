# 21. Dev/test/prod deployment

> Corporate L3 / senior platform engineering guide for Red Hat OpenShift AI GitOps and automation.
> Version scope: examples are aligned to OpenShift AI 3.4/3.5-era documentation, OpenShift GitOps 1.20, and OpenShift Pipelines 1.14-era guidance. Always inspect the CRDs and supported channels installed on the target cluster before production use.

## Document outcomes
- Explain Dev/test/prod deployment from architecture through day-2 operations.
- Create an enterprise project for Dev/test/prod deployment from an empty Git repository.
- Apply security, governance, observability, backup, and disaster-recovery controls.
- Diagnose controller, API, storage, networking, image, permission, and rollout failures.
- Design reusable assets suitable for development, test, production, and disconnected environments.

## 1. Executive explanation
Implement a complete three-environment OpenShift AI delivery architecture with isolated namespaces or clusters, controlled promotion, policy parity, and production safeguards.

At senior level, Dev/test/prod deployment is not treated as a single command or YAML file. It is treated as a controlled reconciliation system whose inputs are Git revisions, signed artifacts, policy decisions, cluster capabilities, and environment-specific configuration.

The desired state must be reviewable, reproducible, and recoverable. The live state must be observable. Every automated action needs an owner, an evidence trail, a failure boundary, and a tested rollback method.

OpenShift AI adds specialized APIs and operational dependencies to normal Kubernetes delivery: operators, DataScienceCluster components, object storage, workbench images, pipeline servers, KServe runtimes, model artifacts, accelerators, and model endpoint tests.

A strong design separates platform configuration from tenant workloads, separates build from deployment, promotes immutable digests rather than mutable tags, and uses GitOps controllers for ongoing convergence rather than allowing CI systems unrestricted production credentials.

## 2. Scope and responsibility model
### Platform Administrator
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through controller logs.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### GitOps Platform Engineer
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through Prometheus metrics.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### ML Engineer
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Data Scientist
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through controller logs.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### SRE
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through Git commit and pull request.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Security Engineer
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through signed image digest.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Release Manager
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

### Auditor
- Owns or reviews the parts of Dev/test/prod deployment that match the persona boundary.
- Produces evidence through admission audit record.
- Escalates changes that cross namespace, cluster, identity, data-classification, or production-risk boundaries.
- Must be able to explain both the desired state and the controller behavior that creates the live state.

## 3. Core resource map
### 3.1. `development cluster or namespace`
- Purpose: `development cluster or namespace` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.2. `test cluster or namespace`
- Purpose: `test cluster or namespace` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.3. `production cluster`
- Purpose: `production cluster` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.4. `ApplicationSet`
- Purpose: `ApplicationSet` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.5. `AppProject`
- Purpose: `AppProject` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.6. `overlay`
- Purpose: `overlay` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.7. `promotion repository`
- Purpose: `promotion repository` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.8. `secret store`
- Purpose: `secret store` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.9. `policy bundle`
- Purpose: `policy bundle` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

### 3.10. `SLO dashboard`
- Purpose: `SLO dashboard` is part of the control or workload surface for Dev/test/prod deployment.
- Ownership: define whether the platform team, ML team, CI service account, or GitOps controller owns this resource.
- Source of truth: store the declarative fields in the correct repository and exclude controller-populated status fields.
- Validation: confirm API availability, namespace, admission policy, RBAC, and status conditions before dependent resources are applied.
- Production rule: avoid manual edits; when emergency edits are necessary, reconcile them back into Git or explicitly revert them.

## 4. Reference architecture
1. Developer or platform engineer creates a change on a feature branch.
2. CI validates syntax, schemas, policy, signatures, and artifact identity.
3. A pull request records review, approval, test evidence, and change intent.
4. The protected branch becomes the desired-state source of truth.
5. Argo CD or another approved reconciler renders Helm or Kustomize content.
6. The Kubernetes and OpenShift APIs perform admission and persist desired resources.
7. Operators and workload controllers reconcile dependent resources.
8. Health gates inspect status conditions, events, logs, metrics, and endpoint behavior.
9. Promotion changes only immutable references and environment-specific approved settings.
10. Rollback reselects a known-good Git revision, image digest, model version, or release revision.

### Control-plane flow
```text
Engineer -> Git branch -> CI validation -> Pull request -> Protected branch
Protected branch -> Argo CD -> OpenShift API -> Operators/controllers -> Workloads
Workloads -> Metrics/logs/events/tests -> Release gate -> Promotion or rollback
```

## 5. Project from scratch
1. Define the success criteria for the development iteration phase.
2. Identify APIs, controllers, external services, and credentials required for development iteration.
3. Document the failure boundary and rollback action for development iteration.
4. Create a least-privilege service account or human role for development iteration.
5. Add schema, policy, and security validation for development iteration.
6. Add a health gate based on status, events, logs, metrics, and functional tests for development iteration.
7. Record release evidence and ownership for development iteration.
8. Test idempotency by rerunning the automation for development iteration.
9. Define the success criteria for the continuous integration phase.
10. Identify APIs, controllers, external services, and credentials required for continuous integration.
11. Document the failure boundary and rollback action for continuous integration.
12. Create a least-privilege service account or human role for continuous integration.
13. Add schema, policy, and security validation for continuous integration.
14. Add a health gate based on status, events, logs, metrics, and functional tests for continuous integration.
15. Record release evidence and ownership for continuous integration.
16. Test idempotency by rerunning the automation for continuous integration.
17. Define the success criteria for the test deployment phase.
18. Identify APIs, controllers, external services, and credentials required for test deployment.
19. Document the failure boundary and rollback action for test deployment.
20. Create a least-privilege service account or human role for test deployment.
21. Add schema, policy, and security validation for test deployment.
22. Add a health gate based on status, events, logs, metrics, and functional tests for test deployment.
23. Record release evidence and ownership for test deployment.
24. Test idempotency by rerunning the automation for test deployment.
25. Define the success criteria for the integration validation phase.
26. Identify APIs, controllers, external services, and credentials required for integration validation.
27. Document the failure boundary and rollback action for integration validation.
28. Create a least-privilege service account or human role for integration validation.
29. Add schema, policy, and security validation for integration validation.
30. Add a health gate based on status, events, logs, metrics, and functional tests for integration validation.
31. Record release evidence and ownership for integration validation.
32. Test idempotency by rerunning the automation for integration validation.
33. Define the success criteria for the production change approval phase.
34. Identify APIs, controllers, external services, and credentials required for production change approval.
35. Document the failure boundary and rollback action for production change approval.
36. Create a least-privilege service account or human role for production change approval.
37. Add schema, policy, and security validation for production change approval.
38. Add a health gate based on status, events, logs, metrics, and functional tests for production change approval.
39. Record release evidence and ownership for production change approval.
40. Test idempotency by rerunning the automation for production change approval.
41. Define the success criteria for the production deployment phase.
42. Identify APIs, controllers, external services, and credentials required for production deployment.
43. Document the failure boundary and rollback action for production deployment.
44. Create a least-privilege service account or human role for production deployment.
45. Add schema, policy, and security validation for production deployment.
46. Add a health gate based on status, events, logs, metrics, and functional tests for production deployment.
47. Record release evidence and ownership for production deployment.
48. Test idempotency by rerunning the automation for production deployment.
49. Define the success criteria for the verification phase.
50. Identify APIs, controllers, external services, and credentials required for verification.
51. Document the failure boundary and rollback action for verification.
52. Create a least-privilege service account or human role for verification.
53. Add schema, policy, and security validation for verification.
54. Add a health gate based on status, events, logs, metrics, and functional tests for verification.
55. Record release evidence and ownership for verification.
56. Test idempotency by rerunning the automation for verification.
57. Define the success criteria for the feedback phase.
58. Identify APIs, controllers, external services, and credentials required for feedback.
59. Document the failure boundary and rollback action for feedback.
60. Create a least-privilege service account or human role for feedback.
61. Add schema, policy, and security validation for feedback.
62. Add a health gate based on status, events, logs, metrics, and functional tests for feedback.
63. Record release evidence and ownership for feedback.
64. Test idempotency by rerunning the automation for feedback.

## 6. Recommended repository structure
```text
rhoai-automation/
├── README.md
├── docs/
│   ├── architecture.md
│   ├── security-model.md
│   ├── operations-runbook.md
│   └── rollback-runbook.md
├── platform/
│   ├── base/
│   │   ├── operators/
│   │   ├── datasciencecluster/
│   │   ├── policies/
│   │   └── monitoring/
│   └── overlays/
│       ├── dev/
│       ├── test/
│       └── prod/
├── workloads/
│   ├── workbenches/
│   ├── pipelines/
│   ├── runtimes/
│   └── models/
├── ci/
│   ├── tasks/
│   ├── pipelines/
│   ├── policy/
│   └── tests/
├── ansible/
│   ├── roles/
│   ├── playbooks/
│   └── execution-environment.yml
└── evidence/
    └── README.md
```

## 7. Representative implementation
The following example is intentionally generic. Replace placeholders, confirm installed CRD schemas, and use only image digests and channels supported for your environment.

```yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: model-environments
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - env: dev
        namespace: ml-dev
        revision: develop
      - env: test
        namespace: ml-test
        revision: release
      - env: prod
        namespace: ml-prod
        revision: main
  template:
    metadata:
      name: 'fraud-model-{{env}}'
    spec:
      project: rhoai-workloads
      source:
        repoURL: https://git.example.com/ml/fraud-model-gitops.git
        targetRevision: '{{revision}}'
        path: 'overlays/{{env}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
```

## 8. Command workflow
### 8.1. Verification using `oc`
```bash
oc whoami
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.2. Verification using `git`
```bash
oc version
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.3. Verification using `kustomize`
```bash
oc api-resources | sort
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.4. Verification using `helm`
```bash
oc get crd | grep -E "opendatahub|datascience|kserve|tekton|argoproj"
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.5. Verification using `tkn`
```bash
oc auth can-i --list -n <namespace>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.6. Verification using `jq`
```bash
oc apply --dry-run=server -f <manifest>.yaml
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.7. Verification using `argocd`
```bash
oc diff -f <manifest>.yaml
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.8. Verification using `oc`
```bash
oc apply -f <manifest>.yaml
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.9. Verification using `git`
```bash
oc get events -n <namespace> --sort-by=.lastTimestamp
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.10. Verification using `kustomize`
```bash
oc get pods -n <namespace> -o wide
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.11. Verification using `helm`
```bash
oc describe <kind> <name> -n <namespace>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.12. Verification using `tkn`
```bash
oc logs <pod> -n <namespace> --all-containers --prefix
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.13. Verification using `jq`
```bash
argocd app get <application> --refresh
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.14. Verification using `argocd`
```bash
argocd app diff <application>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.15. Verification using `oc`
```bash
argocd app sync <application>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.16. Verification using `git`
```bash
argocd app wait <application> --sync --health --timeout 600
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.17. Verification using `kustomize`
```bash
tkn pipelinerun list -n <namespace>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.18. Verification using `helm`
```bash
tkn pipelinerun logs -f -L <run> -n <namespace>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.19. Verification using `tkn`
```bash
git log --show-signature -1
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

### 8.20. Verification using `jq`
```bash
git diff <known-good>..<candidate>
```
- Expected result: the command provides evidence for Dev/test/prod deployment without changing unrelated resources.
- Failure handling: capture the output, correlate it with controller events and logs, and stop promotion until the result is understood.

## 9. Security and compliance controls
### 9.1. Environment isolation
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `environment isolation` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.2. Cluster version compatibility
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `cluster version compatibility` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.3. Shared base plus overlays
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `shared base plus overlays` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.4. Production manual gate
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `production manual gate` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.5. Test data controls
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `test data controls` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.6. Production secret separation
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `production secret separation` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.7. Capacity reservation
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `capacity reservation` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.8. Release freeze
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `release freeze` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.9. Change audit
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `change audit` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.10. Rollback rehearsal
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `rollback rehearsal` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.11. Slo verification
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `SLO verification` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

### 9.12. Dr replication
- Threat addressed: uncontrolled or unverifiable behavior in Dev/test/prod deployment.
- Implementation: encode `DR replication` in Git, policy, pipeline checks, controller configuration, or access controls.
- Evidence: retain the pull request, automated test output, controller status, and audit event.
- Negative test: deliberately violate the rule in a non-production namespace and confirm admission or pipeline enforcement.
- Review frequency: after product upgrade, controller configuration change, credential rotation, and at least during scheduled access reviews.

## 10. Observability and SRE
### Availability
- Signal: Controller replicas available and API endpoints reachable.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Reconciliation
- Signal: Desired resources converge without repeated errors or excessive queue time.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Drift
- Signal: Applications remain Synced or approved differences are explicitly documented.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Latency
- Signal: Build, sync, rollout, model load, and inference gates stay within defined objectives.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Errors
- Signal: Pipeline failures, sync errors, admission denials, image failures, and inference failures are classified.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Saturation
- Signal: CPU, memory, GPU, PVC, API rate limits, controller queues, and registry capacity have headroom.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Security
- Signal: Signature failures, policy violations, denied access, and unexpected secret reads generate evidence.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

### Business outcome
- Signal: The released model version and endpoint behavior match the approved change record.
- Query design: aggregate by cluster, namespace, application, model, runtime, version, and environment.
- Alert design: page only for user-impacting or release-blocking conditions; ticket persistent hygiene issues.
- Dashboard design: show desired revision, live revision, artifact digest, health, error budget, and recent changes together.

## 11. Troubleshooting runbook
### 11.1. Dev-only api used
- Symptom: dev-only API used.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.2. Test lacks production policy
- Symptom: test lacks production policy.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `git` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.3. Production quota too low
- Symptom: production quota too low.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `kustomize` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.4. Different storage class behavior
- Symptom: different storage class behavior.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `helm` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.5. Secret names differ
- Symptom: secret names differ.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `tkn` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.6. Model route hostname collision
- Symptom: model route hostname collision.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `jq` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.7. Git target revision wrong
- Symptom: Git target revision wrong.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `argocd` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.8. Manual hotfix creates drift
- Symptom: manual hotfix creates drift.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `oc` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.9. Test dataset leaks pii
- Symptom: test dataset leaks PII.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `git` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

### 11.10. Dr environment not updated
- Symptom: DR environment not updated.
- First checks: inspect the owning resource, status conditions, recent events, controller logs, and the exact Git revision.
- Isolation: determine whether the failure is manifest generation, admission, reconciliation, scheduling, networking, storage, credentials, or application behavior.
- Evidence commands: `oc describe`, `oc get events`, `oc logs`, plus the relevant `kustomize` command.
- Corrective action: fix the smallest authoritative input and let the responsible controller reconcile.
- Rollback decision: rollback when the production risk exceeds the forward-fix time and the known-good artifact is available.
- Prevention: add a preflight test that reproduces this failure before the next promotion.

## 12. Production readiness checklist
- [ ] PR-01: Supported OpenShift, OpenShift AI, GitOps, Pipelines, and dependency versions are documented.
- [ ] PR-02: All required CRDs are established before custom resources are synchronized.
- [ ] PR-03: Repositories have protected branches, required reviews, and signed change policy.
- [ ] PR-04: Argo CD AppProjects restrict sources, destinations, and resource kinds.
- [ ] PR-05: CI service accounts cannot make unrestricted production changes.
- [ ] PR-06: Images and models are referenced by immutable digest or version and retained for rollback.
- [ ] PR-07: Secrets are referenced from an approved secret system and rotation has been tested.
- [ ] PR-08: RBAC has been tested with positive and negative `can-i` cases.
- [ ] PR-09: NetworkPolicy has default deny plus tested DNS, registry, Git, S3, database, monitoring, and API paths.
- [ ] PR-10: ResourceQuota and LimitRange align with hardware profiles and capacity.
- [ ] PR-11: Status and functional health gates are both implemented.
- [ ] PR-12: Metrics, logs, events, audit data, and release evidence have retention owners.
- [ ] PR-13: Backup and restore include Git, cluster resources, external databases, object storage, registry metadata, and encryption keys where applicable.
- [ ] PR-14: Rollback is rehearsed and has a maximum decision time.
- [ ] PR-15: Disconnected registry and catalog dependencies are mirrored when required.
- [ ] PR-16: Support boundaries and Technology Preview features are identified before use.

## 13. Senior interview and design questions
### Question 1: Why is Dev/test/prod deployment a reconciliation problem rather than only a scripting problem?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 2: Where should build end and deployment begin?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 3: How do you prevent a CI service account from becoming a permanent production administrator?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 4: How do you order an Operator, its CRDs, and its custom resources?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 5: How do you distinguish health from successful apply?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 6: How do you promote an image or model without rebuilding it?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 7: How do you handle secrets in GitOps without treating base64 as encryption?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 8: How do you test RBAC and NetworkPolicy negatively?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 9: What evidence is required to approve a model for production?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 10: When is rollback unsafe because of schema or state migration?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 11: How do you manage manual emergency changes without accepting permanent drift?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 12: How do you validate the examples after a product upgrade?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 13: How do you design for disconnected operation?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 14: How do you measure reconciliation latency and deployment lead time?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

### Question 15: How would you separate platform, tenant, and model repositories?
A strong answer should name the authoritative source, controller, permissions, dependencies, evidence, failure boundary, and rollback method.

## 14. Hands-on lab sequence
### Lab 1: Create the repository and branch protections.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 2: Bootstrap a non-production Argo CD project with restricted destinations.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 3: Render and server-side dry-run all manifests.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 4: Deploy a minimal namespace, RBAC, quota, and default-deny policy.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 5: Install or validate required operators using an approval gate.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 6: Apply the OpenShift AI platform resource and wait for reconciliation.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 7: Build or register a digest-pinned workbench or runtime image.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 8: Deploy a pipeline or model workload and capture evidence.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 9: Introduce a controlled failure and troubleshoot from status to root cause.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 10: Promote the immutable release to a second environment.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 11: Execute the documented rollback.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

### Lab 12: Produce an operations report with risks and follow-up actions.
- Deliverable: Git commit, pull request, rendered manifest, test output, controller evidence, and rollback note.
- Acceptance: rerunning the workflow is idempotent and no plaintext secret appears in Git or logs.
- L3 extension: repeat with a second cluster or disconnected registry and compare behavior.

## 15. Enterprise scenario drills
### Drill 001: Development Iteration in the production
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 002: Development Iteration in the production
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 003: Development Iteration in the pre-production
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 004: Development Iteration in the test
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 005: Development Iteration in the integration
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 006: Development Iteration in the integration
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 007: Development Iteration in the developer sandbox
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 008: Development Iteration in the multi-cluster fleet
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 009: Development Iteration in the disconnected site
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 010: Development Iteration in the disconnected site
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 011: Development Iteration in the disaster-recovery
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 012: Development Iteration in the production
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 013: Development Iteration in the pre-production
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 014: Development Iteration in the pre-production
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 015: Development Iteration in the test
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 016: Development Iteration in the integration
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 017: Development Iteration in the developer sandbox
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 018: Development Iteration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 019: Development Iteration in the multi-cluster fleet
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 020: Development Iteration in the disconnected site
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 021: Development Iteration in the disaster-recovery
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 022: Development Iteration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 023: Development Iteration in the production
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 024: Development Iteration in the pre-production
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 025: Development Iteration in the test
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 026: Development Iteration in the test
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 027: Development Iteration in the integration
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 028: Development Iteration in the developer sandbox
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 029: Development Iteration in the multi-cluster fleet
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 030: Development Iteration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 031: Development Iteration in the disconnected site
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 032: Development Iteration in the disaster-recovery
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 033: Development Iteration in the production
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 034: Development Iteration in the production
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 035: Development Iteration in the pre-production
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 036: Development Iteration in the test
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 037: Development Iteration in the integration
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 038: Development Iteration in the integration
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 039: Development Iteration in the developer sandbox
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 040: Development Iteration in the multi-cluster fleet
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 041: Development Iteration in the disconnected site
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 042: Development Iteration in the disconnected site
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 043: Development Iteration in the disaster-recovery
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 044: Development Iteration in the production
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 045: Development Iteration in the pre-production
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 046: Development Iteration in the pre-production
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 047: Development Iteration in the test
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 048: Development Iteration in the integration
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 049: Development Iteration in the developer sandbox
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 050: Development Iteration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 051: Development Iteration in the multi-cluster fleet
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 052: Development Iteration in the disconnected site
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 053: Development Iteration in the disaster-recovery
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 054: Development Iteration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 055: Development Iteration in the production
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 056: Development Iteration in the pre-production
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 057: Development Iteration in the test
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 058: Development Iteration in the test
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 059: Development Iteration in the integration
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 060: Development Iteration in the developer sandbox
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 061: Development Iteration in the multi-cluster fleet
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 062: Development Iteration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 063: Development Iteration in the disconnected site
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 064: Development Iteration in the disaster-recovery
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 065: Development Iteration in the production
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 066: Development Iteration in the production
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 067: Development Iteration in the pre-production
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 068: Development Iteration in the test
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 069: Development Iteration in the integration
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 070: Development Iteration in the integration
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 071: Development Iteration in the developer sandbox
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 072: Development Iteration in the multi-cluster fleet
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 073: Development Iteration in the disconnected site
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 074: Development Iteration in the disconnected site
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 075: Development Iteration in the disaster-recovery
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 076: Development Iteration in the production
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 077: Development Iteration in the pre-production
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 078: Development Iteration in the pre-production
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 079: Development Iteration in the test
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 080: Development Iteration in the integration
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 081: Development Iteration in the developer sandbox
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 082: Development Iteration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 083: Development Iteration in the multi-cluster fleet
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 084: Development Iteration in the disconnected site
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 085: Development Iteration in the disaster-recovery
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 086: Development Iteration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 087: Development Iteration in the production
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 088: Development Iteration in the pre-production
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 089: Development Iteration in the test
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 090: Development Iteration in the test
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 091: Development Iteration in the integration
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 092: Development Iteration in the developer sandbox
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 093: Development Iteration in the multi-cluster fleet
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 094: Development Iteration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 095: Development Iteration in the disconnected site
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 096: Development Iteration in the disaster-recovery
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 097: Development Iteration in the production
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 098: Development Iteration in the production
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 099: Development Iteration in the pre-production
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 100: Development Iteration in the test
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 101: Development Iteration in the integration
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 102: Development Iteration in the integration
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 103: Development Iteration in the developer sandbox
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 104: Development Iteration in the multi-cluster fleet
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 105: Development Iteration in the disconnected site
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 106: Development Iteration in the disconnected site
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 107: Development Iteration in the disaster-recovery
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 108: Development Iteration in the production
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 109: Development Iteration in the pre-production
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 110: Development Iteration in the pre-production
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 111: Development Iteration in the test
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 112: Development Iteration in the integration
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 113: Development Iteration in the developer sandbox
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 114: Development Iteration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 115: Development Iteration in the multi-cluster fleet
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 116: Development Iteration in the disconnected site
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 117: Development Iteration in the disaster-recovery
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 118: Development Iteration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that SLO verification is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 119: Development Iteration in the production
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 120: Development Iteration in the pre-production
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 121: Development Iteration in the test
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 122: Development Iteration in the test
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 123: Development Iteration in the integration
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 124: Development Iteration in the developer sandbox
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 125: Development Iteration in the multi-cluster fleet
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 126: Development Iteration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 127: Development Iteration in the disconnected site
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 128: Development Iteration in the disaster-recovery
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 129: Development Iteration in the production
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during development iteration.
- Control objective: prove that DR replication is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 130: Continuous Integration in the production
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 131: Continuous Integration in the pre-production
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 132: Continuous Integration in the test
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 133: Continuous Integration in the integration
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 134: Continuous Integration in the integration
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 135: Continuous Integration in the developer sandbox
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 136: Continuous Integration in the multi-cluster fleet
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 137: Continuous Integration in the disconnected site
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 138: Continuous Integration in the disconnected site
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 139: Continuous Integration in the disaster-recovery
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 140: Continuous Integration in the production
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that environment isolation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 141: Continuous Integration in the pre-production
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 142: Continuous Integration in the pre-production
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 143: Continuous Integration in the test
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 144: Continuous Integration in the integration
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 145: Continuous Integration in the developer sandbox
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 146: Continuous Integration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 147: Continuous Integration in the multi-cluster fleet
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 148: Continuous Integration in the disconnected site
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 149: Continuous Integration in the disaster-recovery
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 150: Continuous Integration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 151: Continuous Integration in the production
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that cluster version compatibility is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 152: Continuous Integration in the pre-production
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 153: Continuous Integration in the test
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 154: Continuous Integration in the test
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 155: Continuous Integration in the integration
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 156: Continuous Integration in the developer sandbox
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 157: Continuous Integration in the multi-cluster fleet
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 158: Continuous Integration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 159: Continuous Integration in the disconnected site
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 160: Continuous Integration in the disaster-recovery
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 161: Continuous Integration in the production
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 162: Continuous Integration in the production
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that shared base plus overlays is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 163: Continuous Integration in the pre-production
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 164: Continuous Integration in the test
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 165: Continuous Integration in the integration
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 166: Continuous Integration in the integration
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 167: Continuous Integration in the developer sandbox
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 168: Continuous Integration in the multi-cluster fleet
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 169: Continuous Integration in the disconnected site
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 170: Continuous Integration in the disconnected site
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 171: Continuous Integration in the disaster-recovery
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 172: Continuous Integration in the production
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 173: Continuous Integration in the pre-production
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production manual gate is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 174: Continuous Integration in the pre-production
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 175: Continuous Integration in the test
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 176: Continuous Integration in the integration
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 177: Continuous Integration in the developer sandbox
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 178: Continuous Integration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 179: Continuous Integration in the multi-cluster fleet
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 180: Continuous Integration in the disconnected site
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 181: Continuous Integration in the disaster-recovery
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 182: Continuous Integration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 183: Continuous Integration in the production
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 184: Continuous Integration in the pre-production
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that test data controls is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 185: Continuous Integration in the test
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 186: Continuous Integration in the test
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 187: Continuous Integration in the integration
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 188: Continuous Integration in the developer sandbox
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 189: Continuous Integration in the multi-cluster fleet
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 190: Continuous Integration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 191: Continuous Integration in the disconnected site
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 192: Continuous Integration in the disaster-recovery
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 193: Continuous Integration in the production
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 194: Continuous Integration in the production
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 195: Continuous Integration in the pre-production
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that production secret separation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 196: Continuous Integration in the test
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 197: Continuous Integration in the integration
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 198: Continuous Integration in the integration
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 199: Continuous Integration in the developer sandbox
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 200: Continuous Integration in the multi-cluster fleet
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 201: Continuous Integration in the disconnected site
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 202: Continuous Integration in the disconnected site
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 203: Continuous Integration in the disaster-recovery
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 204: Continuous Integration in the production
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 205: Continuous Integration in the pre-production
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 206: Continuous Integration in the pre-production
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that capacity reservation is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 207: Continuous Integration in the test
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 208: Continuous Integration in the integration
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 209: Continuous Integration in the developer sandbox
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 210: Continuous Integration in the developer sandbox
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 211: Continuous Integration in the multi-cluster fleet
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 212: Continuous Integration in the disconnected site
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 213: Continuous Integration in the disaster-recovery
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 214: Continuous Integration in the disaster-recovery
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 215: Continuous Integration in the production
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 216: Continuous Integration in the pre-production
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 217: Continuous Integration in the test
- Situation: the auditor is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that release freeze is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 218: Continuous Integration in the test
- Situation: the GitOps platform engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 219: Continuous Integration in the integration
- Situation: the data scientist is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 220: Continuous Integration in the developer sandbox
- Situation: the security engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 221: Continuous Integration in the multi-cluster fleet
- Situation: the auditor is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 222: Continuous Integration in the multi-cluster fleet
- Situation: the GitOps platform engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 223: Continuous Integration in the disconnected site
- Situation: the data scientist is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 224: Continuous Integration in the disaster-recovery
- Situation: the security engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 225: Continuous Integration in the production
- Situation: the auditor is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 226: Continuous Integration in the production
- Situation: the GitOps platform engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: manual hotfix creates drift.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 227: Continuous Integration in the pre-production
- Situation: the data scientist is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: test dataset leaks PII.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 228: Continuous Integration in the test
- Situation: the security engineer is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that change audit is enforced before the change can progress.
- Injected fault: DR environment not updated.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 229: Continuous Integration in the integration
- Situation: the auditor is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: dev-only API used.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture admission audit record and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 230: Continuous Integration in the integration
- Situation: the GitOps platform engineer is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: test lacks production policy.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `git` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture model registry metadata and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 231: Continuous Integration in the developer sandbox
- Situation: the data scientist is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `tkn` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Kubernetes events and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 232: Continuous Integration in the multi-cluster fleet
- Situation: the security engineer is preparing `development cluster or namespace` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: production quota too low.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `oc` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Prometheus metrics and run a functional test appropriate to `development cluster or namespace`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 233: Continuous Integration in the disconnected site
- Situation: the auditor is preparing `policy bundle` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: different storage class behavior.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `helm` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Git commit and pull request and run a functional test appropriate to `policy bundle`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 234: Continuous Integration in the disconnected site
- Situation: the GitOps platform engineer is preparing `promotion repository` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: secret names differ.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `argocd` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture controller logs and run a functional test appropriate to `promotion repository`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 235: Continuous Integration in the disaster-recovery
- Situation: the data scientist is preparing `AppProject` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: model route hostname collision.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `kustomize` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture signed image digest and run a functional test appropriate to `AppProject`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

### Drill 236: Continuous Integration in the production
- Situation: the security engineer is preparing `production cluster` changes for Dev/test/prod deployment during continuous integration.
- Control objective: prove that rollback rehearsal is enforced before the change can progress.
- Injected fault: Git target revision wrong.
- Required investigation: compare desired revision, rendered manifest, admitted object, status conditions, events, and controller logs.
- Required command: use `jq` plus `oc` to collect repeatable evidence rather than relying only on the web console.
- Security decision: confirm the actor has only the verbs and scope needed for this phase.
- Recovery decision: choose forward fix, Git revert, artifact rollback, or environment isolation and justify the blast radius.
- Verification: capture Argo CD Application status and run a functional test appropriate to `production cluster`.
- Prevention: add a CI, policy, admission, health, or monitoring check that detects this condition earlier.
- L3 discussion: explain how the answer changes for production, multi-cluster, and disconnected operation.

## 16. Operational knowledge cards
### Card 001: `AppProject`
- Design question: who owns `AppProject` and where is its source of truth?
- Guardrail: shared base plus overlays.
- Failure to recognize: test dataset leaks PII.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 002: `secret store`
- Design question: who owns `secret store` and where is its source of truth?
- Guardrail: release freeze.
- Failure to recognize: model route hostname collision.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 003: `development cluster or namespace`
- Design question: who owns `development cluster or namespace` and where is its source of truth?
- Guardrail: environment isolation.
- Failure to recognize: production quota too low.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 004: `ApplicationSet`
- Design question: who owns `ApplicationSet` and where is its source of truth?
- Guardrail: production secret separation.
- Failure to recognize: DR environment not updated.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 005: `promotion repository`
- Design question: who owns `promotion repository` and where is its source of truth?
- Guardrail: SLO verification.
- Failure to recognize: Git target revision wrong.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

### Card 006: `SLO dashboard`
- Design question: who owns `SLO dashboard` and where is its source of truth?
- Guardrail: production manual gate.
- Failure to recognize: different storage class behavior.
- Evidence: desired revision, object generation, status condition, event, log, metric, and functional test.
- Recovery: change the authoritative input, reconcile, verify, and retain the previous known-good state.

## 17. Official references
- [Red Hat OpenShift AI Self-Managed 3.4 - Installing and uninstalling](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Configuring model serving](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/configuring_your_model-serving_platform/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_on_projects/)
- [Red Hat OpenShift AI Self-Managed 3.4 - AI pipelines](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/working_with_ai_pipelines/)
- [Red Hat OpenShift AI Self-Managed 3.4 - Creating a workbench](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/creating_a_workbench/)
- [Red Hat OpenShift GitOps 1.20 documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/1.20/)
- [Red Hat OpenShift Pipelines documentation](https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/)
- [Argo CD documentation](https://argo-cd.readthedocs.io/en/stable/)
- [Tekton documentation](https://tekton.dev/docs/)
- [Helm documentation](https://helm.sh/docs/)
- [Kubernetes Kustomize documentation](https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/)
- [Kubernetes RBAC documentation](https://kubernetes.io/docs/reference/access-authn-authz/rbac/)
- [Kubernetes NetworkPolicy documentation](https://kubernetes.io/docs/concepts/services-networking/network-policies/)
- [Kubernetes ResourceQuota documentation](https://kubernetes.io/docs/concepts/policy/resource-quotas/)
- [Ansible kubernetes.core collection](https://docs.ansible.com/projects/ansible/latest/collections/kubernetes/core/)

## 18. Final L3 summary
Dev/test/prod deployment is production-ready only when the full lifecycle is declarative, reviewable, least-privilege, observable, and recoverable. The strongest implementation does not merely automate the happy path; it creates evidence, stops unsafe promotion, detects drift, and provides a rehearsed route back to a known-good state.

> End of guide.
- Final review prompt 001: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 002: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 003: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 004: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 005: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 006: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 007: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 008: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 009: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 010: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
- Final review prompt 011: verify one assumption in this guide against the CRD schema and supported product documentation installed on your cluster.
