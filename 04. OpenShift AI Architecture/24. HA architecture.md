# 24. HA architecture
**Course:** OpenShift AI Architecture — Corporate L3
**Audience:** Senior OpenShift/Kubernetes platform engineers, MLOps engineers, SREs, security engineers, and architects with approximately 10+ years of infrastructure experience
**Production baseline used:** Red Hat OpenShift AI Self-Managed 3.4.2 GA, checked 2026-07-21
**Preview policy:** OpenShift AI 3.5 Early Access concepts are not treated as production-supported design.
**Scope:** Self-managed OpenShift AI on OpenShift Container Platform; adapt cloud-provider, storage, network, and security details to the supported configuration matrix.
> This is an architecture and build workbook, not a replacement for the Red Hat supported-configuration matrix, release notes, or a vendor support case. Validate every CRD field against the exact installed release by using `oc explain` and server-side dry run.
## 1. Executive summary
HA architecture concerns high-availability design for OpenShift AI management services and user workloads, including replica strategy, disruption budgets, topology, storage, network, and dependency resilience.
At Corporate L3 level, the goal is not merely to make the feature work. The platform must be supportable, observable, secure, repeatable, capacity-aware, upgradeable, and recoverable.
The engineer must understand which objects are declarative control-plane inputs, which pods execute user data-plane workloads, which dependencies hold state, and which team owns each failure domain.
A production design should be delivered through version control, peer review, policy checks, server-side validation, staged rollout, measurable service objectives, and a tested rollback or recovery procedure.
### 1.1 Learning outcomes
- Explain HA architecture to architects, administrators, data scientists, security teams, and incident responders.
- Map dashboard actions to Kubernetes/OpenShift custom resources and controller reconciliation.
- Build a lab implementation from an empty project using CLI-first, auditable steps.
- Apply RBAC, quotas, network isolation, storage protection, TLS trust, and secret controls.
- Define service-level indicators, dashboards, alerts, and evidence for incident response.
- Troubleshoot failures from user symptom to API object, event, controller log, pod, network path, and dependency.
- Design upgrade, high-availability, and disaster-recovery controls without relying on unsupported assumptions.
- Convert the lab into a GitOps-managed enterprise pattern with validation and change approval.
## 2. Supported-version and lifecycle guardrails
- Use OpenShift AI 3.4.2 GA as the production baseline for this guide.
- Treat OpenShift AI 3.5 Early Access as evaluation-only until Red Hat publishes a supported GA release and supported-configuration matrix.
- Review deprecations before every upgrade. In the 3.4 generation, legacy ModelMesh, KServe Serverless deployment mode, older TrustyAI APIs, older model-registry APIs, and some legacy dashboard configuration paths require migration planning.
- Prefer KServe RawDeployment patterns where supported by the product documentation and organization requirements.
- Do not install unrelated ISV applications into OpenShift AI product namespaces.
- Do not copy upstream manifests blindly into a supported OpenShift AI installation. Product operators own many operands and may reconcile or overwrite them.
- Pin approved images by digest where the support model permits, but never override operator-managed images outside documented procedures.
- Record the OpenShift version, OpenShift AI version, operator channel, CSV, CRD versions, GPU stack, storage stack, and known issues in every architecture decision record.
- Run pre-upgrade validation in a representative non-production cluster with the same topology, identity, storage, proxy, certificate, and accelerator constraints.
- Keep rollback expectations realistic: Kubernetes API migrations, databases, and metadata schemas may not support simple binary rollback.
## 3. Architecture context
OpenShift AI is operator-managed and integrates service-layer capabilities such as the dashboard, workbenches, AI pipelines, model serving, monitoring, and related AI services. The OpenShift AI Operator is the management layer that deploys and maintains components and sub-operators.
Common platform namespaces include `redhat-ods-operator` for the operator and `redhat-ods-applications` for the dashboard and related managed components. User projects hold tenant workloads and project-scoped resources.
### 3.1 Primary resources
- **Deployments/StatefulSets:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **PodDisruptionBudget:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **topology spread:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **anti-affinity:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **multi-AZ StorageClass:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **Routes/Services:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
- **backups:** identify its owner, API group/version, reconciliation controller, namespace scope, status conditions, finalizers, and upgrade behavior.
### 3.2 Relevant namespaces
- `platform namespaces`
- `tenant namespaces`
- `storage namespaces`
### 3.3 Dependencies
- OpenShift HA
- OpenShift AI Operator
- ODF/object storage/database
- monitoring
- load balancing
### 3.4 Logical data flows
1. replicated control service -> API.
2. multi-replica inference -> service.
3. stateful dependency -> quorum/durable storage.
### 3.5 Control-plane versus data-plane interpretation
The control plane accepts desired state, validates it, stores it in the Kubernetes API, and reconciles subordinate objects. The data plane executes notebooks, pipeline tasks, training jobs, model servers, storage transfers, and inference requests.
A healthy API object does not prove a healthy data path. Validate both reconciliation status and an end-to-end transaction using realistic credentials, artifacts, network paths, and resource requests.
## 4. Component ownership and RACI
| Capability | Responsible | Accountable | Consulted | Informed |
|---|---|---|---|---|
| OpenShift cluster health | OpenShift platform team | SRE | security/network/storage teams | AI product teams |
| HA architecture | AI platform team | AI platform architect | data science/MLOps/SRE | service owners |
| Identity and groups | IAM team | security owner | AI platform team | users |
| Network and DNS | network/platform team | network owner | security/SRE | AI users |
| Persistent and object storage | storage team | storage owner | AI platform/MLOps | users |
| Model and dataset governance | model owner/MLOps | business risk owner | security/legal/data governance | platform team |
| Monitoring and incident response | SRE | service owner | platform/MLOps/vendor support | stakeholders |
| Backup and restore | platform/storage/database owners | DR owner | security/application owners | users |
## 5. Discovery before implementation
1. Capture cluster and operator versions.
2. Confirm the exact supported OpenShift/OpenShift AI combination.
3. List installed CRDs and API resources.
4. Identify operator-managed namespaces and operands.
5. Confirm cluster-wide proxy, trusted CA, ingress certificate, and DNS behavior.
6. List storage classes, volume modes, expansion support, snapshot classes, and reclaim policies.
7. Identify S3 endpoints, authentication model, encryption, versioning, lifecycle, backup, and egress rules.
8. Map node roles, zones, taints, labels, CPU architecture, GPU models, MIG/time-slicing policy, and capacity.
9. Verify user-workload monitoring and log collection.
10. Export current RBAC and group membership relevant to OpenShift AI.
11. Check quotas, limit ranges, network policies, admission policies, and image policies.
12. Record known issues from the release notes that match enabled components.
```bash
oc version
oc get clusterversion
oc get csv,subscription,installplan -A
oc get datasciencecluster -o yaml
oc get dscinitialization -o yaml
oc get crd | grep -iE 'opendatahub|datascience|kubeflow|kserve|trusty|modelregistry'
oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'
oc get pods -n redhat-ods-operator -o wide
oc get pods -n redhat-ods-applications -o wide
oc get storageclass,volumesnapshotclass
oc get nodes -L topology.kubernetes.io/zone,node-role.kubernetes.io/worker
```
## 6. Detailed architecture
### 6.1 Operator ownership
Operator ownership: distinguish supported configuration surfaces from generated operands.
### 6.2 Namespace boundaries
Namespace boundaries: keep tenant workloads in user projects and product operands in product namespaces.
### 6.3 API-driven lifecycle
API-driven lifecycle: desired state is represented in CRs; controller conditions and events are primary evidence.
### 6.4 State classification
State classification: identify which state lives in etcd, PVCs, object storage, databases, image registries, Git, or external systems.
### 6.5 Identity propagation
Identity propagation: browser user, OAuth token, service account, object-store credential, registry credential, and model endpoint identity are separate security contexts.
### 6.6 Network path decomposition
Network path decomposition: client, router, service, endpoint, pod, sidecar, upstream storage/database, and external API must each be tested.
### 6.7 Resource isolation
Resource isolation: CPU, memory, ephemeral storage, PVC capacity, GPU, network bandwidth, and API rate limits require explicit governance.
### 6.8 Observability by contract
Observability by contract: every production component needs availability, latency, error, saturation, and dependency indicators.
### 6.9 Failure-domain awareness
Failure-domain awareness: replicas must be placed across nodes/zones and must not share hidden single points of failure.
### 6.10 Change safety
Change safety: configurations must be rendered, validated, diffed, approved, rolled out, observed, and reversible.
## 7. From-scratch implementation project
**Project objective:** deploy and validate a governed lab implementation of **HA architecture**, then convert it into a reusable corporate pattern.
### 7.1 Phase 0 — Define success criteria
1. Write the user journey and API transaction that proves the capability works.
2. Define availability, latency, throughput, capacity, security, and recovery acceptance criteria.
3. Name the owning team, escalation path, maintenance window, and support entitlement.
4. Document which features are GA, Technology Preview, Developer Preview, deprecated, or unsupported.
### 7.2 Phase 1 — Prepare the cluster
1. Validate cluster operators and node readiness.
2. Verify time synchronization, DNS, ingress, proxy, trusted CA, image registry, and storage.
3. Confirm OpenShift AI operator and DataScienceCluster reconciliation.
4. Capture a pre-change must-gather or targeted support snapshot for regulated environments.
### 7.3 Phase 2 — Create the project boundary
1. Create the project through GitOps or an approved project-request workflow.
2. Apply ownership labels, annotations, quota, limits, and default-deny network policy.
3. Bind groups rather than individual users whenever possible.
4. Create service accounts with token automount disabled unless an API token is required.
### 7.4 Phase 3 — Configure dependencies
1. Create storage, object-store connections, registry pull credentials, and CA bundles through approved secret delivery.
2. Permit only required DNS, object-store, registry, database, monitoring, and API egress.
3. Validate dependency health independently before creating the AI resource.
4. Record endpoint, certificate chain, authentication method, timeout, retry, and ownership.
### 7.5 Phase 4 — Create the OpenShift AI resource
1. Use the dashboard once in a lab to learn the supported resource shape.
2. Export the resource, strip generated metadata/status, replace credentials, and commit it to Git.
3. Run schema validation and `oc apply --dry-run=server` against the target cluster.
4. Apply through the controlled delivery pipeline and observe controller events and conditions.
### 7.6 Phase 5 — Validate the data path
1. Run a functional transaction using a non-admin identity.
2. Verify storage reads/writes, model or notebook startup, pipeline execution, or inference as applicable.
3. Measure cold start, steady-state latency, throughput, errors, and resource consumption.
4. Test a denied path to prove isolation and least privilege.
### 7.7 Phase 6 — Add production controls
1. Add alert rules, dashboards, log queries, SLO recording rules, and runbook links.
2. Add PDB/topology/anti-affinity only after confirming the operator-supported customization point.
3. Add backup, retention, secret rotation, image promotion, vulnerability scanning, and policy checks.
4. Run node drain, dependency interruption, credential rotation, and rollback tests.
### 7.8 Phase 7 — Handover
1. Publish architecture diagram, dependency map, RACI, SLO, dashboards, alerts, runbooks, and recovery procedure.
2. Train service desk and L2 teams on symptom classification and evidence collection.
3. Define monthly capacity review, quarterly access review, and scheduled recovery exercises.
4. Record technical debt, unsupported exceptions, and migration dates for deprecated features.
## 8. CLI implementation runbook
### 8.1 Authenticate and select the administrative context
```bash
oc whoami && oc project
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.2 Check cluster operator health
```bash
oc get clusteroperators
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.3 Check OpenShift AI subscriptions and CSVs
```bash
oc get subscription,csv,installplan -A | grep -iE 'rhods|opendatahub|nvidia|nfd'
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.4 Inspect the DataScienceCluster
```bash
oc get datasciencecluster -o yaml
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.5 Inspect OpenShift AI pods
```bash
oc get pods -n redhat-ods-operator -o wide && oc get pods -n redhat-ods-applications -o wide
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.6 Discover relevant APIs
```bash
oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.7 Create the lab project
```bash
oc new-project ai-ha-architecture-lab
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.8 Apply guardrails
```bash
oc apply -f 00-namespace-guardrails/
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.9 Validate authorization
```bash
oc auth can-i --list -n <project>
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.10 Validate storage
```bash
oc get storageclass && oc get pvc -n <project>
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.11 Validate network policy
```bash
oc get networkpolicy -n <project> -o yaml
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.12 Watch events during reconciliation
```bash
oc get events -n <project> --watch
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.13 Inspect resource status
```bash
oc get <kind> <name> -n <project> -o yaml
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.14 Inspect generated pods and services
```bash
oc get pod,svc,route,endpoints -n <project> -o wide
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.15 Inspect controller logs
```bash
oc logs -n redhat-ods-applications deploy/<controller> --since=30m
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.16 Check resource utilization
```bash
oc adm top pods -n <project> && oc adm top nodes
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.17 Run server-side validation
```bash
oc apply --dry-run=server -f manifests/
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
### 8.18 Record final evidence
```bash
oc get all,route,pvc,networkpolicy,resourcequota,limitrange -n <project> -o yaml > evidence.yaml
```
**Expected result:** the command returns usable evidence for HA architecture; investigate warnings, empty selections, denied requests, or non-ready conditions before continuing.
**Audit evidence:** save sanitized output with change ticket, cluster ID, namespace, timestamp, and engineer identity.
## 9. Security architecture
1. Use enterprise identity-provider groups and periodic access recertification.
2. Separate platform administrator, project administrator, editor, operator, observer, and auditor duties.
3. Avoid broad `cluster-admin`, wildcard resources, wildcard verbs, and direct bindings to personal accounts.
4. Disable service-account token automount unless the workload calls the Kubernetes API.
5. Keep object-store, database, registry, Git, and endpoint credentials separate and independently rotatable.
6. Use external secret management or an approved secret-injection process.
7. Encrypt traffic, validate the complete certificate chain, and document private CA distribution.
8. Apply default-deny network policy and open only verified flows.
9. Restrict image registries and require vulnerability scanning, signing/attestation, and immutable promotion.
10. Use SCC/Pod Security controls appropriate to the runtime; do not grant privileged access merely to bypass an error.
11. Log authentication, authorization, resource changes, secret access patterns, and model promotion decisions.
12. Classify datasets, features, prompts, outputs, model artifacts, and telemetry for privacy and retention.
13. Protect model endpoints against unauthorized access, abuse, excessive tokens/requests, and data exfiltration.
14. Test negative cases: unauthorized user, wrong group, expired credential, blocked egress, untrusted CA, and denied namespace access.
15. Document every security exception with owner, risk, compensating control, expiry, and removal plan.
## 10. Capacity and performance engineering
| Dimension | L3 engineering guidance |
|---|---|
| CPU | requests drive scheduling; limits can create throttling; observe throttled seconds and run-queue pressure. |
| Memory | model load, notebook kernels, pipeline tasks, caches, sidecars, and JVM/Python overhead require headroom. |
| GPU | model size, precision, KV cache, batch size, concurrency, MIG profile, and sharing mode determine capacity. |
| Storage capacity | datasets, checkpoints, model artifacts, caches, logs, metadata, and temporary files grow differently. |
| Storage performance | measure throughput, IOPS, latency, metadata operations, and object-store multipart behavior. |
| Network | model downloads, distributed training, artifact movement, inference payloads, and telemetry can saturate links. |
| API/control plane | large numbers of CRs, events, pipeline tasks, pods, and reconciles can stress API and controllers. |
| Cold start | image pull, scheduling, volume attach, model download, compilation, and readiness dominate startup. |
| Concurrency | test representative payloads, arrival patterns, timeouts, retries, and downstream dependencies. |
| Cost | tag ownership, environment, model, GPU class, storage tier, and lifecycle to support showback/chargeback. |
### 10.1 Capacity worksheet
- Capacity record 01: capture `CPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 02: capture `Memory` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 03: capture `GPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 04: capture `Storage capacity` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 05: capture `Storage performance` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 06: capture `Network` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 07: capture `API/control plane` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 08: capture `Cold start` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 09: capture `Concurrency` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 10: capture `Cost` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 11: capture `CPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 12: capture `Memory` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 13: capture `GPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 14: capture `Storage capacity` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 15: capture `Storage performance` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 16: capture `Network` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 17: capture `API/control plane` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 18: capture `Cold start` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 19: capture `Concurrency` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 20: capture `Cost` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 21: capture `CPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 22: capture `Memory` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 23: capture `GPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 24: capture `Storage capacity` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 25: capture `Storage performance` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 26: capture `Network` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 27: capture `API/control plane` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 28: capture `Cold start` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 29: capture `Concurrency` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 30: capture `Cost` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 31: capture `CPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 32: capture `Memory` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 33: capture `GPU` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 34: capture `Storage capacity` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 35: capture `Storage performance` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 36: capture `Network` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 37: capture `API/control plane` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 38: capture `Cold start` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 39: capture `Concurrency` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
- Capacity record 40: capture `Cost` baseline, peak, P95, P99, growth rate, headroom, hard limit, alert threshold, owner, and expansion lead time for HA architecture.
## 11. Observability and SLO design
| SLI | Measurement approach |
|---|---|
| Availability | successful user/API transactions divided by valid attempts |
| Latency | P50/P95/P99 end-to-end transaction duration |
| Errors | failed reconciles, HTTP errors, task failures, model-load failures, and dependency errors |
| Saturation | CPU, memory, GPU, storage, network, queue, API, and controller workqueue pressure |
| Freshness | age of model, metadata, pipeline result, monitoring data, or backup |
| Recovery | time to restore service and maximum data loss during exercises |
- Alert 01: component or endpoint unavailable; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 02: controller reconcile errors above baseline; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 03: resource remains non-ready beyond the defined startup objective; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 04: pod restart or crash-loop rate; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 05: persistent volume nearly full or object-store failures; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 06: database connection saturation or replication lag; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 07: GPU Xid errors, unavailable devices, or DCGM exporter failure; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 08: certificate expiry and failed TLS handshakes; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 09: authentication or authorization failure spike; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 10: pipeline task failure rate; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 11: model inference error or latency budget breach; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 12: TrustyAI data/metric freshness breach; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 13: backup failure or stale recovery point; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 14: quota saturation or pending pods; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
- Alert 15: high cardinality or telemetry ingestion overload; include cluster, namespace, resource, owner, severity, first diagnostic command, and runbook URL.
## 12. Baseline project guardrail manifests

### 12.1 Namespace and ownership metadata
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: ai-ha-architecture-lab
  labels:
    platform.example.com/workload: openshift-ai
    platform.example.com/topic: ha-architecture
    platform.example.com/environment: lab
    platform.example.com/owner: ml-platform
```
### 12.2 ResourceQuota
```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: ai-project-quota
spec:
  hard:
    requests.cpu: "16"
    requests.memory: 64Gi
    limits.cpu: "32"
    limits.memory: 128Gi
    requests.storage: 500Gi
    persistentvolumeclaims: "20"
    pods: "100"
    services: "30"
    secrets: "100"
    configmaps: "100"
```
### 12.3 LimitRange
```yaml
apiVersion: v1
kind: LimitRange
metadata:
  name: ai-default-limits
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 250m
      memory: 512Mi
    default:
      cpu: "2"
      memory: 4Gi
```
### 12.4 Default-deny NetworkPolicy
```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
```
### 12.5 Permit DNS egress
```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-dns
spec:
  podSelector: {}
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: openshift-dns
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
```
### 12.6 Project service account
```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-workload
automountServiceAccountToken: false
```
### 12.7 Read-only observer Role
```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ai-observer
  labels:
    opendatahub.io/dashboard: "true"
rules:
- apiGroups: [""]
  resources: ["pods","pods/log","services","events","configmaps","persistentvolumeclaims"]
  verbs: ["get","list","watch"]
- apiGroups: ["kubeflow.org"]
  resources: ["notebooks"]
  verbs: ["get","list","watch"]
- apiGroups: ["serving.kserve.io"]
  resources: ["inferenceservices","servingruntimes"]
  verbs: ["get","list","watch"]
```
## 13. Topic-specific implementation examples
> **Version rule:** before applying any CR, run `oc api-resources`, `oc explain`, and export a dashboard-created sample from the target cluster. CRD versions and supported fields change between OpenShift AI releases.
### 13.1 PodDisruptionBudget and topology spread pattern
```yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: inference-api
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: inference-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: inference-api
  template:
    metadata:
      labels:
        app: inference-api
    spec:
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: inference-api
      containers:
      - name: api
        image: registry.example.com/ai/inference-api@sha256:REPLACE
```
## 14. Hands-on laboratories
### Lab 01 — Inventory and dependency map
**Objective:** Discover live APIs, operands, routes, storage, network policies, and identity dependencies; produce a diagram and ownership table.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 02 — Least-privilege onboarding
**Objective:** Create project roles and group bindings, then prove editor, viewer, and unauthorized-user behavior.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 03 — Storage and certificate validation
**Objective:** Create a test PVC and S3 connection, verify CA trust, upload/download an artifact, and record performance.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 04 — Functional deployment
**Objective:** Create the minimum supported HA architecture resource and prove the primary user journey.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 05 — Negative security test
**Objective:** Block one required flow or remove one permission, observe the failure, restore the control, and capture evidence.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 06 — Resource pressure test
**Objective:** Constrain CPU/memory/storage or GPU capacity, observe scheduling/runtime behavior, and validate alerts.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 07 — Node maintenance test
**Objective:** Cordon and drain a worker node, observe disruption, measure recovery, and evaluate PDB/topology controls.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 08 — Credential rotation
**Objective:** Rotate an object-store, registry, database, or API credential without exposing it or causing unplanned downtime.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 09 — Upgrade rehearsal
**Objective:** Clone the configuration into non-production, run prechecks, upgrade, test data paths, and document migration issues.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 10 — Backup and restore
**Objective:** Back up declarative resources and state, delete the lab workload, restore in the approved order, and compare checksums.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 11 — Incident simulation
**Objective:** Inject a controlled replicas co-located condition, follow the runbook, calculate detection and recovery time, and write an RCA.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
### Lab 12 — GitOps conversion
**Objective:** Normalize manifests, add policy tests, server-side dry run, approval gates, environment overlays, and drift monitoring.
**Prerequisites:** non-production cluster access, approved namespace, CLI, Git repository, and permission to create only the scoped resources.
**Procedure:**
1. Open a change or lab record and state the hypothesis.
2. Capture pre-test cluster, component, resource, event, and metric state.
3. Apply the smallest controlled change.
4. Watch API conditions, controller logs, generated pods, events, routes/services, and dependencies.
5. Execute the user-level transaction with a non-admin identity.
6. Collect latency, error, saturation, security, and audit evidence.
7. Revert or complete the change through the same controlled mechanism.
8. Compare post-test state with the baseline.
9. Document findings, defect IDs, risk, owner, and next action.
10. Commit sanitized manifests and runbook updates.
**Success criteria:** expected resources are ready, the functional transaction passes, denied paths remain denied, metrics and logs are available, and cleanup/rollback is proven.
**Failure criteria:** an unexplained condition, missing evidence, hidden manual dependency, unsupported configuration, data loss, credential exposure, or inability to restore.
## 15. Troubleshooting methodology
Use an evidence-first chain. Do not jump directly to pod deletion, operator reinstall, SCC escalation, or credential replacement.
1. Clarify the exact user symptom, identity, namespace, resource, endpoint, payload, timestamp, and expected result.
2. Reproduce with the least-privileged identity and preserve the original error.
3. Check resource generation, conditions, events, finalizers, owner references, and observed generation.
4. Identify the controller responsible for the next state transition and inspect its recent logs.
5. Inspect generated pods, init containers, sidecars, probes, scheduling decisions, volumes, and security context.
6. Test service/endpoints/route/DNS/TLS/authentication as separate layers.
7. Test external dependencies such as object store, registry, database, Git, model source, and identity provider.
8. Correlate metrics, logs, audit events, node conditions, storage health, GPU health, and recent changes.
9. Apply the smallest reversible remediation and validate both positive and negative paths.
10. Create an RCA with trigger, contributing factors, detection gap, recovery actions, and prevention controls.
### 15.1 Known symptom families
#### Symptom: replicas co-located
- Confirm whether `replicas co-located` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `replicas co-located`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `replicas co-located`.
#### Symptom: PDB blocks drain
- Confirm whether `PDB blocks drain` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `PDB blocks drain`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `PDB blocks drain`.
#### Symptom: zone outage
- Confirm whether `zone outage` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `zone outage`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `zone outage`.
#### Symptom: database failover issue
- Confirm whether `database failover issue` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `database failover issue`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `database failover issue`.
#### Symptom: object-store outage
- Confirm whether `object-store outage` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `object-store outage`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `object-store outage`.
#### Symptom: route imbalance
- Confirm whether `route imbalance` affects one user, one project, one node, one zone, one component, or the full cluster.
- Review events and status conditions before restarting anything.
- Identify the exact controller and dependency involved in `route imbalance`.
- Compare a failing object with a known-good object from the same product version.
- Validate RBAC, NetworkPolicy, quota, storage, DNS, TLS, image access, and node placement.
- Capture logs with timestamps and correlation IDs; avoid unbounded log collection.
- Remediate through supported APIs or GitOps, then retest the user transaction.
- Add a preventive alert, policy, test, or capacity threshold for `route imbalance`.
## 16. Upgrade and change management
1. Read release notes, known issues, deprecations, support removals, and supported configurations.
2. Inventory CRD versions, custom runtimes/images, external databases, object stores, certificates, proxies, GPUs, and policy engines.
3. Back up declarative resources and stateful dependencies; verify restore, not merely backup completion.
4. Test upgrade in a representative environment and run automated pre/post functional transactions.
5. Freeze unrelated platform changes and define go/no-go criteria.
6. Monitor CSV, InstallPlan, operator logs, DataScienceCluster conditions, CRD conversion, and workload readiness.
7. Validate dashboard, project access, workbenches, pipelines, model registry, serving, TrustyAI, storage, GPU, and monitoring.
8. Review performance and cost because upgraded images or runtimes can change resource usage.
9. Document migration actions for deprecated resources and APIs.
10. Close the change only after an observation period and stakeholder validation.
## 17. HA and DR considerations
1. Classify each component as stateless, rebuildable state, durable state, or external dependency.
2. Do not assume increasing a Deployment replica count is supported; confirm operator-managed replica settings.
3. Use multi-zone node placement and storage only when the underlying infrastructure truly spans independent failure domains.
4. Protect databases and object stores with their native consistency and recovery mechanisms.
5. Back up Kubernetes resources, but understand that etcd/object YAML alone does not capture external artifacts or database state.
6. Store reproducible configuration in Git and immutable artifacts in governed registries/object stores.
7. Document RPO and RTO per service, not only for the cluster.
8. Test identity, certificates, DNS, routes, secrets, storage classes, and GPU capacity on the recovery cluster.
9. Define restoration order based on dependencies and controller reconciliation.
10. Run periodic game days and record measured recovery, data loss, manual steps, and gaps.
## 18. Corporate design-review questions
1. What exact user transaction defines success?
2. Which API object is the desired-state source of truth?
3. Which controller owns reconciliation and where are its logs?
4. Which state is stored in etcd, PVC, object storage, database, registry, or Git?
5. What breaks if the object store, database, identity provider, router, registry, or monitoring stack is unavailable?
6. Which features are GA, preview, deprecated, or unsupported in the deployed version?
7. How are identities, groups, service accounts, and external credentials separated?
8. How is tenant network, storage, secret, quota, and GPU isolation enforced?
9. What are the SLO, alert thresholds, and error-budget actions?
10. What is the capacity model and expansion lead time?
11. How is a new image, runtime, model, pipeline, or configuration promoted?
12. How are vulnerabilities, signatures, licenses, and provenance checked?
13. What is backed up, how often, where, encrypted by whom, and how is restore tested?
14. What is the upgrade path and migration plan for deprecated APIs?
15. What evidence is required to hand the service to L2 operations?
## 19. L3 scenario and incident workbook
Each drill is deliberately structured as a reusable incident, design-review, or change-exercise card.
### Drill 001 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 002 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 003 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 004 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 005 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 006 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 007 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 008 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 009 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 010 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 011 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 012 — development: replicas co-located during design
- **Situation:** a platform administrator reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 013 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 014 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 015 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 016 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 017 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 018 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 019 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 020 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 021 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 022 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 023 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 024 — development: PDB blocks drain during design
- **Situation:** a platform administrator reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 025 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 026 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 027 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 028 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 029 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 030 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 031 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 032 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 033 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 034 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 035 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 036 — development: zone outage during design
- **Situation:** a platform administrator reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 037 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 038 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 039 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 040 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 041 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 042 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 043 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 044 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 045 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 046 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 047 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 048 — development: database failover issue during design
- **Situation:** a platform administrator reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 049 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 050 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 051 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 052 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 053 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 054 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 055 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 056 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 057 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 058 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 059 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 060 — development: object-store outage during design
- **Situation:** a platform administrator reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 061 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 062 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 063 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 064 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 065 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 066 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 067 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 068 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 069 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 070 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 071 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 072 — development: route imbalance during design
- **Situation:** a platform administrator reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 073 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 074 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 075 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 076 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 077 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 078 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 079 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 080 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 081 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 082 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 083 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 084 — development: replicas co-located during design
- **Situation:** a data scientist reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 085 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 086 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 087 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 088 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 089 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 090 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 091 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 092 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 093 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 094 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 095 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 096 — development: PDB blocks drain during design
- **Situation:** a data scientist reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 097 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 098 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 099 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 100 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 101 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 102 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 103 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 104 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 105 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 106 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 107 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 108 — development: zone outage during design
- **Situation:** a data scientist reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 109 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 110 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 111 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 112 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 113 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 114 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 115 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 116 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 117 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 118 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 119 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 120 — development: database failover issue during design
- **Situation:** a data scientist reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 121 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 122 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 123 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 124 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 125 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 126 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 127 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 128 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 129 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 130 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 131 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 132 — development: object-store outage during design
- **Situation:** a data scientist reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 133 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 134 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 135 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 136 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 137 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 138 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 139 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 140 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 141 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 142 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 143 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 144 — development: route imbalance during design
- **Situation:** a data scientist reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 145 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 146 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 147 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 148 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 149 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 150 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 151 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 152 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 153 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 154 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 155 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 156 — development: replicas co-located during design
- **Situation:** a ML engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 157 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 158 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 159 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 160 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 161 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 162 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 163 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 164 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 165 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 166 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 167 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 168 — development: PDB blocks drain during design
- **Situation:** a ML engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 169 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 170 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 171 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 172 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 173 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 174 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 175 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 176 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 177 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 178 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 179 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 180 — development: zone outage during design
- **Situation:** a ML engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 181 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 182 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 183 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 184 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 185 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 186 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 187 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 188 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 189 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 190 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 191 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 192 — development: database failover issue during design
- **Situation:** a ML engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 193 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 194 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 195 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 196 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 197 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 198 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 199 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 200 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 201 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 202 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 203 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 204 — development: object-store outage during design
- **Situation:** a ML engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 205 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 206 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 207 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 208 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 209 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 210 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 211 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 212 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 213 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 214 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 215 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 216 — development: route imbalance during design
- **Situation:** a ML engineer reports `route imbalance` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `route imbalance`.
### Drill 217 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 218 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 219 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 220 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 221 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 222 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 223 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 224 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 225 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 226 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 227 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 228 — development: replicas co-located during design
- **Situation:** a MLOps engineer reports `replicas co-located` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `replicas co-located`.
### Drill 229 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 230 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 231 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 232 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 233 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 234 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 235 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 236 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 237 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 238 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 239 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 240 — development: PDB blocks drain during design
- **Situation:** a MLOps engineer reports `PDB blocks drain` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `PDB blocks drain`.
### Drill 241 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 242 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 243 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 244 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 245 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 246 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 247 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 248 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 249 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 250 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 251 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 252 — development: zone outage during design
- **Situation:** a MLOps engineer reports `zone outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `zone outage`.
### Drill 253 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 254 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 255 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 256 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 257 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 258 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 259 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 260 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 261 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc adm top nodes && oc adm top pods -A`.
- **Control review:** confirm that `audit logging` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 262 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get prometheusrule,servicemonitor,podmonitor -A`.
- **Control review:** confirm that `GitOps approval` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 263 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc logs <pod> -c <container> --since=30m`.
- **Control review:** confirm that `image policy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 264 — development: database failover issue during design
- **Situation:** a MLOps engineer reports `database failover issue` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc describe <resource> <name> -n <project>`.
- **Control review:** confirm that `backup verification` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `database failover issue`.
### Drill 265 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get events --sort-by=.lastTimestamp`.
- **Control review:** confirm that `RBAC` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 266 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `backups` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc get pods -A -o wide`.
- **Control review:** confirm that `NetworkPolicy` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 267 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Deployments/StatefulSets` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get csv,subscription,installplan -A`.
- **Control review:** confirm that `ResourceQuota` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 268 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `PodDisruptionBudget` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `ODF/object storage/database` before changing the OpenShift AI resource.
- **Evidence command:** `oc get datasciencecluster -o yaml`.
- **Control review:** confirm that `LimitRange` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 269 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `topology spread` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `monitoring` before changing the OpenShift AI resource.
- **Evidence command:** `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'`.
- **Control review:** confirm that `PodDisruptionBudget` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **low** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 270 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `anti-affinity` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `load balancing` before changing the OpenShift AI resource.
- **Evidence command:** `oc get route,svc,endpoints -A`.
- **Control review:** confirm that `topology spread` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **moderate** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 271 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `multi-AZ StorageClass` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift HA` before changing the OpenShift AI resource.
- **Evidence command:** `oc auth can-i --list -n <project>`.
- **Control review:** confirm that `TLS trust` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **high** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
### Drill 272 — development: object-store outage during design
- **Situation:** a MLOps engineer reports `object-store outage` while operating HA architecture in the development environment.
- **Primary object:** inspect `Routes/Services` desired state, generation, status conditions, events, finalizers, and ownership.
- **Dependency hypothesis:** verify `OpenShift AI Operator` before changing the OpenShift AI resource.
- **Evidence command:** `oc get networkpolicy,resourcequota,limitrange -n <project>`.
- **Control review:** confirm that `secret rotation` is working as designed and is not masking a capacity, identity, network, storage, or API problem.
- **Decision:** classify impact, blast radius, data risk, supportability, and rollback safety; provisional risk is **critical** until evidence narrows it.
- **Remediation:** make the smallest supported, reversible change through GitOps or the owning API; never delete stateful resources without a verified backup and owner approval.
- **Validation:** repeat the end-to-end user transaction, check negative authorization/isolation tests, and observe metrics for a defined period.
- **Prevention:** add or improve an alert, policy, test, capacity threshold, runbook, architecture decision, or recovery exercise for `object-store outage`.
## 20. Final production-readiness checklist
- [ ] Readiness control 001 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 002 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 003 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 004 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 005 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 006 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 007 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 008 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 009 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 010 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 011 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 012 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 013 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 014 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 015 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 016 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 017 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 018 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 019 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 020 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 021 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 022 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 023 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 024 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 025 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 026 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 027 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 028 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 029 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 030 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 031 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 032 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 033 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 034 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 035 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 036 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 037 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 038 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 039 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 040 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 041 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 042 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 043 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 044 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 045 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 046 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 047 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 048 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 049 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 050 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 051 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 052 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 053 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 054 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 055 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 056 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 057 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 058 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 059 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 060 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 061 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 062 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 063 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 064 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 065 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 066 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 067 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 068 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 069 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 070 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 071 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 072 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 073 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 074 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 075 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 076 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 077 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 078 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 079 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 080 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 081 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 082 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 083 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 084 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 085 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 086 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 087 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 088 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 089 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 090 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 091 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 092 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 093 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 094 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 095 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 096 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 097 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 098 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 099 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 100 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 101 — **support and lifecycle:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 102 — **architecture and ownership:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 103 — **identity and RBAC:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 104 — **network and DNS:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 105 — **TLS and certificates:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 106 — **secrets and credentials:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 107 — **images and supply chain:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 108 — **storage and object store:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 109 — **database and metadata:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 110 — **CPU and memory:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 111 — **GPU and accelerators:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 112 — **quotas and noisy-neighbor controls:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 113 — **monitoring and SLOs:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 114 — **logging and audit:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 115 — **backup and restore:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 116 — **high availability:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 117 — **disaster recovery:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 118 — **upgrade and rollback:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 119 — **GitOps and policy:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
- [ ] Readiness control 120 — **documentation and handover:** evidence is current, reviewed by the accountable owner, stored with the change record, and tested for HA architecture.
## 21. Knowledge-check questions
1. How would you prove whether `Deployments/StatefulSets` or `OpenShift HA` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
2. How would you prove whether `PodDisruptionBudget` or `OpenShift AI Operator` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
3. How would you prove whether `topology spread` or `ODF/object storage/database` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
4. How would you prove whether `anti-affinity` or `monitoring` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
5. How would you prove whether `multi-AZ StorageClass` or `load balancing` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
6. How would you prove whether `Routes/Services` or `OpenShift HA` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
7. How would you prove whether `backups` or `OpenShift AI Operator` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
8. How would you prove whether `Deployments/StatefulSets` or `ODF/object storage/database` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
9. How would you prove whether `PodDisruptionBudget` or `monitoring` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
10. How would you prove whether `topology spread` or `load balancing` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
11. How would you prove whether `anti-affinity` or `OpenShift HA` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
12. How would you prove whether `multi-AZ StorageClass` or `OpenShift AI Operator` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
13. How would you prove whether `Routes/Services` or `ODF/object storage/database` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
14. How would you prove whether `backups` or `monitoring` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
15. How would you prove whether `Deployments/StatefulSets` or `load balancing` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
16. How would you prove whether `PodDisruptionBudget` or `OpenShift HA` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
17. How would you prove whether `topology spread` or `OpenShift AI Operator` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
18. How would you prove whether `anti-affinity` or `ODF/object storage/database` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
19. How would you prove whether `multi-AZ StorageClass` or `monitoring` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
20. How would you prove whether `Routes/Services` or `load balancing` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
21. How would you prove whether `backups` or `OpenShift HA` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
22. How would you prove whether `Deployments/StatefulSets` or `OpenShift AI Operator` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
23. How would you prove whether `PodDisruptionBudget` or `ODF/object storage/database` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
24. How would you prove whether `topology spread` or `monitoring` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
25. How would you prove whether `anti-affinity` or `load balancing` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
26. How would you prove whether `multi-AZ StorageClass` or `OpenShift HA` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
27. How would you prove whether `Routes/Services` or `OpenShift AI Operator` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
28. How would you prove whether `backups` or `ODF/object storage/database` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
29. How would you prove whether `Deployments/StatefulSets` or `monitoring` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
30. How would you prove whether `PodDisruptionBudget` or `load balancing` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
31. How would you prove whether `topology spread` or `OpenShift HA` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
32. How would you prove whether `anti-affinity` or `OpenShift AI Operator` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
33. How would you prove whether `multi-AZ StorageClass` or `ODF/object storage/database` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
34. How would you prove whether `Routes/Services` or `monitoring` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
35. How would you prove whether `backups` or `load balancing` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
36. How would you prove whether `Deployments/StatefulSets` or `OpenShift HA` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
37. How would you prove whether `PodDisruptionBudget` or `OpenShift AI Operator` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
38. How would you prove whether `topology spread` or `ODF/object storage/database` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
39. How would you prove whether `anti-affinity` or `monitoring` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
40. How would you prove whether `multi-AZ StorageClass` or `load balancing` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
41. How would you prove whether `Routes/Services` or `OpenShift HA` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
42. How would you prove whether `backups` or `OpenShift AI Operator` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
43. How would you prove whether `Deployments/StatefulSets` or `ODF/object storage/database` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
44. How would you prove whether `PodDisruptionBudget` or `monitoring` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
45. How would you prove whether `topology spread` or `load balancing` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
46. How would you prove whether `anti-affinity` or `OpenShift HA` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
47. How would you prove whether `multi-AZ StorageClass` or `OpenShift AI Operator` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
48. How would you prove whether `Routes/Services` or `ODF/object storage/database` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
49. How would you prove whether `backups` or `monitoring` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
50. How would you prove whether `Deployments/StatefulSets` or `load balancing` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
51. How would you prove whether `PodDisruptionBudget` or `OpenShift HA` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
52. How would you prove whether `topology spread` or `OpenShift AI Operator` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
53. How would you prove whether `anti-affinity` or `ODF/object storage/database` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
54. How would you prove whether `multi-AZ StorageClass` or `monitoring` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
55. How would you prove whether `Routes/Services` or `load balancing` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
56. How would you prove whether `backups` or `OpenShift HA` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
57. How would you prove whether `Deployments/StatefulSets` or `OpenShift AI Operator` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
58. How would you prove whether `PodDisruptionBudget` or `ODF/object storage/database` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
59. How would you prove whether `topology spread` or `monitoring` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
60. How would you prove whether `anti-affinity` or `load balancing` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
61. How would you prove whether `multi-AZ StorageClass` or `OpenShift HA` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
62. How would you prove whether `Routes/Services` or `OpenShift AI Operator` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
63. How would you prove whether `backups` or `ODF/object storage/database` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
64. How would you prove whether `Deployments/StatefulSets` or `monitoring` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
65. How would you prove whether `PodDisruptionBudget` or `load balancing` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
66. How would you prove whether `topology spread` or `OpenShift HA` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
67. How would you prove whether `anti-affinity` or `OpenShift AI Operator` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
68. How would you prove whether `multi-AZ StorageClass` or `ODF/object storage/database` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
69. How would you prove whether `Routes/Services` or `monitoring` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
70. How would you prove whether `backups` or `load balancing` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
71. How would you prove whether `Deployments/StatefulSets` or `OpenShift HA` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
72. How would you prove whether `PodDisruptionBudget` or `OpenShift AI Operator` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
73. How would you prove whether `topology spread` or `ODF/object storage/database` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
74. How would you prove whether `anti-affinity` or `monitoring` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
75. How would you prove whether `multi-AZ StorageClass` or `load balancing` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
76. How would you prove whether `Routes/Services` or `OpenShift HA` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
77. How would you prove whether `backups` or `OpenShift AI Operator` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
78. How would you prove whether `Deployments/StatefulSets` or `ODF/object storage/database` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
79. How would you prove whether `PodDisruptionBudget` or `monitoring` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
80. How would you prove whether `topology spread` or `load balancing` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
81. How would you prove whether `anti-affinity` or `OpenShift HA` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
82. How would you prove whether `multi-AZ StorageClass` or `OpenShift AI Operator` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
83. How would you prove whether `Routes/Services` or `ODF/object storage/database` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
84. How would you prove whether `backups` or `monitoring` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
85. How would you prove whether `Deployments/StatefulSets` or `load balancing` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
86. How would you prove whether `PodDisruptionBudget` or `OpenShift HA` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
87. How would you prove whether `topology spread` or `OpenShift AI Operator` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
88. How would you prove whether `anti-affinity` or `ODF/object storage/database` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
89. How would you prove whether `multi-AZ StorageClass` or `monitoring` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
90. How would you prove whether `Routes/Services` or `load balancing` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
91. How would you prove whether `backups` or `OpenShift HA` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
92. How would you prove whether `Deployments/StatefulSets` or `OpenShift AI Operator` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
93. How would you prove whether `PodDisruptionBudget` or `ODF/object storage/database` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
94. How would you prove whether `topology spread` or `monitoring` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
95. How would you prove whether `anti-affinity` or `load balancing` is responsible for `object-store outage` in HA architecture, and what evidence would you collect before remediation?
96. How would you prove whether `multi-AZ StorageClass` or `OpenShift HA` is responsible for `route imbalance` in HA architecture, and what evidence would you collect before remediation?
97. How would you prove whether `Routes/Services` or `OpenShift AI Operator` is responsible for `replicas co-located` in HA architecture, and what evidence would you collect before remediation?
98. How would you prove whether `backups` or `ODF/object storage/database` is responsible for `PDB blocks drain` in HA architecture, and what evidence would you collect before remediation?
99. How would you prove whether `Deployments/StatefulSets` or `monitoring` is responsible for `zone outage` in HA architecture, and what evidence would you collect before remediation?
100. How would you prove whether `PodDisruptionBudget` or `load balancing` is responsible for `database failover issue` in HA architecture, and what evidence would you collect before remediation?
## 22. Reference commands
- Command pattern 01: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 02: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 03: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 04: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 05: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 06: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 07: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 08: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 09: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 10: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 11: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 12: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 13: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 14: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 15: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 16: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 17: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 18: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 19: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 20: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 21: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 22: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 23: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 24: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 25: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 26: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 27: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 28: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 29: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 30: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 31: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 32: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 33: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 34: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 35: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 36: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 37: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 38: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 39: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 40: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 41: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 42: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 43: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 44: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 45: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 46: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 47: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 48: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 49: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 50: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 51: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 52: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 53: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 54: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 55: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 56: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 57: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 58: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 59: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 60: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 61: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 62: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 63: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 64: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 65: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 66: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 67: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 68: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 69: `oc adm top nodes && oc adm top pods -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 70: `oc get prometheusrule,servicemonitor,podmonitor -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 71: `oc logs <pod> -c <container> --since=30m` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 72: `oc describe <resource> <name> -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 73: `oc get events --sort-by=.lastTimestamp` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 74: `oc get pods -A -o wide` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 75: `oc get csv,subscription,installplan -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 76: `oc get datasciencecluster -o yaml` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 77: `oc api-resources | grep -iE 'notebook|pipeline|inference|serving|trusty|registry'` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 78: `oc get route,svc,endpoints -A` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 79: `oc auth can-i --list -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
- Command pattern 80: `oc get networkpolicy,resourcequota,limitrange -n <project>` — save sanitized output, timestamp, cluster, namespace, and ticket reference.
## 23. Official references
- [Red Hat OpenShift AI 3.4 release notes](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/release_notes/index)
- [Architecture of OpenShift AI Self-Managed](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/architecture-of-openshift-ai-self-managed_install)
- [Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/working_on_projects/index)
- [Monitoring your AI systems](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/monitoring_your_ai_systems/index)
- [KServe introduction and architecture](https://kserve.github.io/website/docs/intro)
- [KServe ServingRuntime](https://kserve.github.io/website/docs/concepts/resources/servingruntime)
- [Kubeflow Pipelines documentation](https://www.kubeflow.org/docs/components/pipelines/)
- [NVIDIA GPU Operator](https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/latest/getting-started.html)
- [NVIDIA GPU Operator on OpenShift](https://docs.nvidia.com/datacenter/cloud-native/openshift/latest/)
## 24. Closing guidance
A senior engineer should be able to build HA architecture, explain every dependency and reconciliation boundary, prove tenant and credential isolation, measure the service, recover it, and defend the design in an architecture or incident review.
The most reliable implementation is the one that is supportable, observable, reproducible, tested under failure, and owned by named teams—not the one with the largest number of enabled features.
