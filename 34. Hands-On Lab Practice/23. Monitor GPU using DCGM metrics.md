---
title: "23. Monitor GPU using DCGM metrics"
track: "Red Hat OpenShift AI Hands-On Lab Practice"
level: "Corporate L3 / 10+ years experience"
baseline: "Red Hat OpenShift AI Self-Managed 3.4.2"
openshift_baseline: "OpenShift Container Platform 4.19+"
last_reviewed: "2026-07-21"
status: "Hands-on lab and production runbook"
---

# 23. Monitor GPU using DCGM metrics

> This chapter is written as a standalone enterprise lab, implementation guide, operational runbook,
> troubleshooting workbook, interview preparation guide, and production-hardening checklist.
> Replace every placeholder with values approved for your cluster before applying commands.

## 1. Executive objective

Scrape NVIDIA DCGM exporter metrics, validate GPU utilization and memory series, build queries, and
create actionable alerts without excessive cardinality.

**Primary resource or control:** DCGM exporter, ServiceMonitor, Prometheus metrics

**Primary namespace or scope:** `nvidia-gpu-operator and openshift-monitoring`

The lab starts from an empty or minimally prepared environment and ends only after the change is
validated functionally, operationally, securely, and from a rollback perspective. The expected standard
is that another senior engineer can reproduce monitor gpu using dcgm metrics by following this file.

## 2. Version and support baseline

- Use Red Hat OpenShift AI Self-Managed 3.4.2 as the production baseline for this lab.
- Use OpenShift Container Platform 4.19 or later, subject to the current Red Hat supported-configuration matrix.
- Treat OpenShift AI 3.5 Early Access as non-production unless Red Hat documentation states otherwise.
- Discover the API schema served by the installed cluster before applying any custom resource.
- Prefer stable or EUS channels according to the organization's lifecycle and upgrade policy.
- Pin custom images by digest and record the component versions captured during the lab.
- Do not copy manifests from an older OpenShift AI minor without server-side dry-run validation.

```bash
oc version
oc get clusterversion
oc get datasciencecluster default-dsc -o jsonpath='{.apiVersion}{"\n"}{.status.phase}{"\n"}'
oc api-resources | sort
oc get csv -A | egrep -i 'rhods|opendatahub|kserve|nvidia|nfd|gitops|oadp'
oc get storageclass
oc get infrastructure cluster -o yaml
```

## 3. Learning outcomes

1. Explain the architecture and lifecycle of DCGM exporter, ServiceMonitor, Prometheus metrics.
2. Implement monitor gpu using dcgm metrics through both dashboard-oriented and CLI/GitOps-oriented workflows.
3. Use server-side validation, events, conditions, logs, metrics, and generated resources as evidence.
4. Separate platform prerequisites from project-scoped configuration.
5. Apply least privilege, network isolation, quota, secret management, and immutable artifact controls.
6. Design acceptance tests, rollback criteria, backup requirements, and operational ownership.
7. Troubleshoot failures without changing multiple variables at the same time.
8. Defend the design in a senior corporate interview and change advisory board review.

## 4. Enterprise scenario

A regulated enterprise ML platform team must monitor gpu using dcgm metrics for the `rhoai-lab` tenant.
The change must be reproducible, reviewed through Git, compatible with restricted security controls,
observable through the OpenShift monitoring stack, and recoverable within the documented RTO. The
implementation must avoid plaintext credentials, mutable image tags, unrestricted network access, and
undocumented cluster-scoped privileges.

| Attribute | Lab value | Production decision |
|---|---|---|
| Platform owner | ML Platform Engineering | Named accountable team and escalation path |
| Tenant | rhoai-lab | One namespace per governed workload or environment |
| Environment | lab | dev, test, stage, prod separation |
| Data classification | internal | policy-driven labels and egress controls |
| Change method | CLI first, then GitOps | pull request, review, signed commit, controlled sync |
| Secrets | placeholders only | Vault, External Secrets, sealed secrets, or approved equivalent |
| Recovery | export plus OADP/S3/database backups | aligned application-consistent recovery point |
| Validation | functional, security, observability, rollback | automated release gates |

## 5. Architecture and dependency map

The focal object is DCGM exporter, ServiceMonitor, Prometheus metrics. It interacts with OpenShift APIs,
Operators, project resources, storage, identity, networking, monitoring, and external artifact systems.
Before implementation, identify which controller owns each field and which resources are generated rather
than directly managed.

```text
Enterprise identity provider
        |
OpenShift OAuth / OIDC / service accounts
        |
Red Hat OpenShift AI dashboard and APIs
        |
DCGM exporter, ServiceMonitor, Prometheus metrics
        |
Operators and controllers -> generated Deployments, StatefulSets, Pods, Services, Routes/Gateways
        |
PVC / S3-compatible object storage / OCI registry / external database
        |
Prometheus metrics, events, logs, audit trail, Git history, backup catalog
```

| Layer | Components | Why it matters |
|---|---|---|
| Identity | OAuth/OIDC, groups, service accounts | Authentication, authorization, audit identity |
| API and Operators | CRDs, OLM, controllers | Declarative reconciliation and lifecycle |
| Compute | CPU, memory, GPU, node selectors, taints | Scheduling and capacity |
| Storage | PVC, S3, OCI, registry database | Artifacts, datasets, metadata, checkpoints |
| Network | Service, Route, Gateway, DNS, NetworkPolicy | Connectivity and endpoint exposure |
| Security | SCC, RBAC, TLS, secrets, image policy | Workload and supply-chain controls |
| Observability | events, logs, metrics, alerts | Evidence and operational response |
| Recovery | Git, OADP, snapshots, S3 versioning | RPO/RTO and rollback |

## 6. Prerequisites

- [ ] P01: GPU Operator with DCGM exporter.
- [ ] P02: cluster monitoring access.
- [ ] P03: user-workload monitoring if custom scrape objects are used.
- [ ] P04: GPU workload for test traffic.
- [ ] P05: The cluster version and RHOAI version are supported together.
- [ ] P06: The engineer is logged in with the minimum role required for the planned scope.
- [ ] P07: The namespace name, owner, environment, cost center, and data classification are approved.
- [ ] P08: A dynamic StorageClass exists and capacity is available.
- [ ] P09: The object-storage endpoint, CA chain, bucket, region, and prefix policy are documented.
- [ ] P10: Proxy and no-proxy settings include cluster and object-storage endpoints where required.
- [ ] P11: Image registries are reachable and pull secrets are valid.
- [ ] P12: Time synchronization and DNS are healthy.
- [ ] P13: Quota and LimitRange allow generated sidecars and controller-created pods.
- [ ] P14: A break-glass recovery method has been approved but is not used for routine work.
- [ ] P15: A rollback artifact or Git commit exists before changing shared resources.
- [ ] P16: Monitoring and logging access is available to the implementation team.

## 7. Variables and naming standard

```bash
export LAB_TOPIC=23
export LAB_PROJECT=rhoai-lab
export RHOAI_OPERATOR_NS=redhat-ods-operator
export RHOAI_APPS_NS=redhat-ods-applications
export RHOAI_REGISTRY_NS=rhoai-model-registries
export GPU_OPERATOR_NS=nvidia-gpu-operator
export GITOPS_NS=openshift-gitops
export OADP_NS=openshift-adp
export S3_ENDPOINT=https://s3.example.com
export S3_REGION=us-east-1
export S3_BUCKET=rhoai-lab
export CHANGE_ID=CHG-000000
export OWNER=ml-platform
export ENVIRONMENT=lab
export DATA_CLASSIFICATION=internal

printf 'topic=%s project=%s change=%s\n' "$LAB_TOPIC" "$LAB_PROJECT" "$CHANGE_ID"
oc whoami
oc project "$LAB_PROJECT" 2>/dev/null || true
```

| Object type | Standard | Example |
|---|---|---|
| Namespace | `<business>-<application>-<environment>` | `ml-fraud-dev` |
| Workbench | `<team>-<purpose>` | `risk-feature-lab` |
| PVC | `<workload>-data` | `jupyterlab-data` |
| Secret | `<system>-<purpose>` | `lab-s3` |
| InferenceService | `<model>-<stage>` | `iris-candidate` |
| ServingRuntime | `<engine>-<accelerator>-<scope>` | `vllm-nvidia-shared` |
| Backup | `<namespace>-<date>-<purpose>` | `rhoai-lab-20260721-prechange` |
| Git tag | `<environment>/<release>` | `lab/2026.07.21.1` |

## 8. Pre-change evidence capture

```bash
set -o errexit -o nounset -o pipefail
mkdir -p evidence/23-monitor-gpu-using-dcgm-metrics
EVIDENCE_DIR=$PWD/evidence/23-monitor-gpu-using-dcgm-metrics

oc whoami > "$EVIDENCE_DIR/whoami.txt"
oc version > "$EVIDENCE_DIR/version.txt"
oc get clusterversion -o yaml > "$EVIDENCE_DIR/clusterversion.yaml"
oc get datasciencecluster default-dsc -o yaml > "$EVIDENCE_DIR/datasciencecluster.yaml" 2>&1 || true
oc get all -n "$LAB_PROJECT" -o wide > "$EVIDENCE_DIR/workloads.txt" 2>&1 || true
oc get events -n "$LAB_PROJECT" --sort-by=.lastTimestamp > "$EVIDENCE_DIR/events-before.txt" 2>&1 || true
oc get resourcequota,limitrange,networkpolicy -n "$LAB_PROJECT" -o yaml > "$EVIDENCE_DIR/policy-before.yaml" 2>&1 || true
oc get pvc,secret,configmap -n "$LAB_PROJECT" > "$EVIDENCE_DIR/state-before.txt" 2>&1 || true
date --iso-8601=seconds > "$EVIDENCE_DIR/captured-at.txt"
sha256sum "$EVIDENCE_DIR"/* > "$EVIDENCE_DIR/SHA256SUMS" 2>/dev/null || true
```

## 9. Detailed implementation workflow

Perform the following phases in order. A senior engineer should stop at the first failed acceptance gate,
collect evidence, and correct one causal layer before proceeding.

### Phase 1: Verify exporter pods and service

Goal: complete `Verify exporter pods and service` for monitor gpu using dcgm metrics while preserving
change traceability and controller ownership. Confirm inputs, execute the smallest declarative change,
observe conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 1.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 1 complete only after its acceptance test passes.

### Phase 2: Confirm Prometheus target health

Goal: complete `Confirm Prometheus target health` for monitor gpu using dcgm metrics while preserving
change traceability and controller ownership. Confirm inputs, execute the smallest declarative change,
observe conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 2.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 2 complete only after its acceptance test passes.

### Phase 3: Query core DCGM metrics

Goal: complete `Query core DCGM metrics` for monitor gpu using dcgm metrics while preserving change
traceability and controller ownership. Confirm inputs, execute the smallest declarative change, observe
conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 3.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 3 complete only after its acceptance test passes.

### Phase 4: Correlate pod-to-GPU attribution

Goal: complete `Correlate pod-to-GPU attribution` for monitor gpu using dcgm metrics while preserving
change traceability and controller ownership. Confirm inputs, execute the smallest declarative change,
observe conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 4.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 4 complete only after its acceptance test passes.

### Phase 5: Create dashboard panels

Goal: complete `Create dashboard panels` for monitor gpu using dcgm metrics while preserving change
traceability and controller ownership. Confirm inputs, execute the smallest declarative change, observe
conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 5.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 5 complete only after its acceptance test passes.

### Phase 6: Create warning and critical alerts

Goal: complete `Create warning and critical alerts` for monitor gpu using dcgm metrics while preserving
change traceability and controller ownership. Confirm inputs, execute the smallest declarative change,
observe conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 6.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 6 complete only after its acceptance test passes.

### Phase 7: Test alert routing

Goal: complete `Test alert routing` for monitor gpu using dcgm metrics while preserving change
traceability and controller ownership. Confirm inputs, execute the smallest declarative change, observe
conditions and generated resources, and record the result.

- [ ] Confirm the prerequisite specifically required for phase 7.
- [ ] Run a server-side dry run when the phase changes an API object.
- [ ] Save before-and-after YAML without secret values.
- [ ] Watch events and controller logs during reconciliation.
- [ ] Mark phase 7 complete only after its acceptance test passes.

## 10. Core command runbook

```bash
oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm

oc get servicemonitor -A | grep -i dcgm

oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL

oc adm top pods -n rhoai-lab
```

Command execution rules:
- Run read-only discovery commands before write commands.
- Use `--dry-run=server -o yaml` for schema and admission validation.
- Use explicit namespaces in every command.
- Never paste secret values into shell history; prefer approved secret tooling.
- Capture `oc describe`, events, and logs before deleting a failed pod.
- Use labels and owner references to map generated resources back to the controlling CR.
- Do not use `oc edit` for production changes that should be reviewed through Git.

## 11. Reference manifest or implementation artifact

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: gpu-l3-alerts
  namespace: openshift-monitoring
spec:
  groups:
    - name: gpu.rules
      rules:
        - alert: GPUHighMemoryUsage
          expr: |
            100 * DCGM_FI_DEV_FB_USED
              / (DCGM_FI_DEV_FB_USED + DCGM_FI_DEV_FB_FREE) > 95
          for: 10m
          labels:
            severity: warning
          annotations:
            summary: GPU framebuffer memory above 95 percent
        - alert: GPUXidErrors
          expr: increase(DCGM_FI_DEV_XID_ERRORS[5m]) > 0
          labels:
            severity: critical
          annotations:
            summary: NVIDIA XID error detected
```

Important: the installed CRD is the source of truth. Run `oc explain`, export a dashboard-created known-
good object when appropriate, and validate the manifest with `oc apply --dry-run=server` before
committing it.

## 12. Manifest validation and safe apply

```bash
# Syntax and schema checks
yamllint manifest.yaml 2>/dev/null || true
oc apply --dry-run=client -f manifest.yaml -o yaml
oc apply --dry-run=server -f manifest.yaml -o yaml

# Diff before apply
oc diff -f manifest.yaml || true

# Apply and record the server-accepted object
oc apply -f manifest.yaml
oc get -f manifest.yaml -o yaml > server-accepted.yaml

# Observe reconciliation
oc get events -n "$LAB_PROJECT" --sort-by=.lastTimestamp
oc get pods -n "$LAB_PROJECT" -w
```

Validation interpretation:

- Client-side validation catches basic syntax and local schema errors.
- Server-side validation catches served-version, admission, policy, and webhook errors.
- `oc diff` exposes ownership conflicts and unintended field changes.
- The server-accepted object is more valuable than the submitted file during troubleshooting.
- Reconciliation is complete only when the controlling CR condition and generated workload are healthy.

## 13. Functional acceptance criteria

- [ ] F01: DCGM exporter targets are Up.
- [ ] F02: GPU utilization changes under load.
- [ ] F03: Memory used/free values are plausible.
- [ ] F04: Alerts include node, GPU, and workload context.
- [ ] F05: No new Warning event remains unexplained.
- [ ] F06: The relevant pod or controller has stable restart counts.
- [ ] F07: The implementation survives one controlled pod recreation.
- [ ] F08: A negative test proves unauthorized or invalid usage is rejected safely.
- [ ] F09: The change is represented in Git or the approved configuration system.
- [ ] F10: The owner, runbook, monitoring, and rollback references are recorded.

## 14. Security and compliance controls

### Control S01: Least Privilege

For monitor gpu using dcgm metrics, implement least privilege as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S02: Immutable Image Digests

For monitor gpu using dcgm metrics, implement immutable image digests as an explicit release gate. Record
the policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S03: Secret Rotation

For monitor gpu using dcgm metrics, implement secret rotation as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S04: Tls Verification

For monitor gpu using dcgm metrics, implement TLS verification as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S05: Data Classification

For monitor gpu using dcgm metrics, implement data classification as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S06: Namespace Isolation

For monitor gpu using dcgm metrics, implement namespace isolation as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S07: Quota Alignment

For monitor gpu using dcgm metrics, implement quota alignment as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S08: Backup Consistency

For monitor gpu using dcgm metrics, implement backup consistency as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S09: Git Review

For monitor gpu using dcgm metrics, implement Git review as an explicit release gate. Record the policy
owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S10: Change Approval

For monitor gpu using dcgm metrics, implement change approval as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S11: Observability

For monitor gpu using dcgm metrics, implement observability as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S12: Slo Definition

For monitor gpu using dcgm metrics, implement SLO definition as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S13: Audit Logging

For monitor gpu using dcgm metrics, implement audit logging as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S14: Artifact Checksum

For monitor gpu using dcgm metrics, implement artifact checksum as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S15: Model Approval

For monitor gpu using dcgm metrics, implement model approval as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S16: Rollback Readiness

For monitor gpu using dcgm metrics, implement rollback readiness as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S17: Break-Glass Access

For monitor gpu using dcgm metrics, implement break-glass access as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S18: Dependency Inventory

For monitor gpu using dcgm metrics, implement dependency inventory as an explicit release gate. Record
the policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S19: Capacity Headroom

For monitor gpu using dcgm metrics, implement capacity headroom as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S20: Incident Evidence

For monitor gpu using dcgm metrics, implement incident evidence as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S21: Vulnerability Scanning

For monitor gpu using dcgm metrics, implement vulnerability scanning as an explicit release gate. Record
the policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S22: Signature Verification

For monitor gpu using dcgm metrics, implement signature verification as an explicit release gate. Record
the policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S23: Retention Policy

For monitor gpu using dcgm metrics, implement retention policy as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S24: Access Review

For monitor gpu using dcgm metrics, implement access review as an explicit release gate. Record the
policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

### Control S25: Disaster-Recovery Testing

For monitor gpu using dcgm metrics, implement disaster-recovery testing as an explicit release gate.
Record the policy owner, technical enforcement point, evidence query, exception process, and review date.

- Enforcement: identify the API, admission policy, RBAC binding, storage policy, or pipeline gate.
- Evidence: store a command result, signed report, dashboard link, or immutable audit record.
- Failure response: block promotion or revert to the last approved state.

## 15. Observability design

| Signal | What to observe |
|---|---|
| Availability | Ready conditions, desired/available replicas, route or gateway admission |
| Latency | controller reconciliation, startup, model load, request p50/p95/p99 |
| Errors | 4xx/5xx, pod restarts, warning events, failed conditions |
| Saturation | CPU, memory, GPU memory, storage, quota, connection pools |
| Traffic | requests, tokens, pipeline runs, artifact operations |
| Integrity | artifact checksum, registry version, Git commit, image digest |

```bash
oc get events -n "$LAB_PROJECT" --sort-by=.lastTimestamp
oc get pods -n "$LAB_PROJECT" -o wide
oc get pods -n "$LAB_PROJECT" -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{.status.phase}{"\t"}{.status.containerStatuses[*].restartCount}{"\n"}{end}'
oc adm top pods -n "$LAB_PROJECT" --containers
oc get route,gateway,httproute,service,endpoints -n "$LAB_PROJECT" 2>/dev/null || true
oc get prometheusrule,servicemonitor,podmonitor -A 2>/dev/null | grep -iE 'rhoai|kserve|gpu|trusty|pipeline' || true
```

## 16. Failure-injection tests

- [ ] FI01: Use an invalid resource name and confirm admission rejects it.
- [ ] FI02: Remove one required permission from a test service account and confirm a controlled 403.
- [ ] FI03: Use an invalid S3 key in a temporary secret and identify the exact failing component.
- [ ] FI04: Block one dependency with a temporary NetworkPolicy and observe timeout evidence.
- [ ] FI05: Set a request above quota and confirm the API or scheduler failure is explicit.
- [ ] FI06: Use a nonexistent image tag and capture ImagePullBackOff evidence.
- [ ] FI07: Use a wrong model path or artifact checksum and confirm integrity checks fail.
- [ ] FI08: Restart a generated pod and confirm the controller restores it.
- [ ] FI09: Revoke a short-lived token and confirm the endpoint rejects it.
- [ ] FI10: Revert the Git commit and confirm the previous healthy state is restored.

Run failure injection only in a lab or approved non-production window. Every test must include a stop
condition, expected failure signal, rollback command, and evidence capture.

## 17. Rollback plan

1. Remove or disable noisy rules.
2. Revert dashboards and rules through Git.
3. Do not restart drivers merely to clear alert state.
- Declare rollback if the controlling CR does not progress, the workload cannot become Ready, security controls fail, or the SLO regression exceeds the approved threshold.
- Capture evidence before rollback.
- Revert one change set rather than applying unrelated fixes.
- Verify the previous state functionally and operationally.
- Record whether data or artifacts created during the failed change require reconciliation.

```bash
# GitOps-oriented rollback
git log --oneline -10
git revert <bad-commit>
git push origin main
argocd app diff rhoai-lab
argocd app sync rhoai-lab

# Direct-manifest rollback for lab use
oc apply -f last-known-good.yaml
oc get events -n "$LAB_PROJECT" --sort-by=.lastTimestamp
oc get pods -n "$LAB_PROJECT" -w
```

## 18. Production hardening checklist

- [ ] H01: Pin Operator channels and document the upgrade path.
- [ ] H02: Pin images and model artifacts by immutable digest or version.
- [ ] H03: Use external production-grade databases where the component requires persistence.
- [ ] H04: Use S3 bucket versioning, encryption, lifecycle controls, and least-privilege prefixes.
- [ ] H05: Use enterprise CA trust and reject insecure TLS workarounds.
- [ ] H06: Use project-scoped RBAC and avoid individual cluster-admin grants.
- [ ] H07: Apply default-deny network policies with explicit dependency allowances.
- [ ] H08: Set requests, limits, quotas, and capacity alerts.
- [ ] H09: Configure PodDisruptionBudgets or replicas where supported and meaningful.
- [ ] H10: Back up API resources and all external state as one recovery design.
- [ ] H11: Test restore into an isolated namespace or cluster.
- [ ] H12: Create SLOs and alerts with runbook links.
- [ ] H13: Define image vulnerability and signature gates.
- [ ] H14: Define model approval, promotion, rollback, and retirement policy.
- [ ] H15: Review known issues for the installed RHOAI minor before rollout.

## 19. Known design risks

- Risk R01: Exporter running but not scraped.
- Risk R02: Metric names differ by DCGM version.
- Risk R03: No pod attribution labels.
- Risk R04: Alert fires on intentionally reserved memory.
- Risk R05: A dashboard workflow may generate fields that are not obvious from a minimal CR.
- Risk R06: An Operator may reconcile fields that GitOps also attempts to own.
- Risk R07: A healthy API object may still depend on external S3, database, DNS, or CA state.
- Risk R08: A rollback can restore configuration without restoring compatible data.
- Risk R09: Mutable tags and unversioned model paths make root-cause analysis unreliable.
- Risk R10: Namespace deletion can remove PVCs, secrets, service accounts, and audit context.

## 20. L3 troubleshooting decision tree

```text
Start
  |
  +-- Was the object accepted by the API server?
  |      +-- No -> schema, served API version, admission, policy, RBAC
  |      +-- Yes
  |
  +-- Does the controlling CR show Ready/Available?
  |      +-- No -> conditions, events, controller logs, prerequisites
  |      +-- Yes
  |
  +-- Were generated workloads created?
  |      +-- No -> controller ownership, labels, webhook, namespace scope
  |      +-- Yes
  |
  +-- Are pods scheduled and images pulled?
  |      +-- No -> quota, taints, affinity, storage, image auth, GPU
  |      +-- Yes
  |
  +-- Are init containers, probes, and application process healthy?
  |      +-- No -> S3, TLS, permissions, runtime arguments, model load
  |      +-- Yes
  |
  +-- Is the service reachable and authorized?
  |      +-- No -> Service/Endpoints, Route/Gateway, NetworkPolicy, authn/authz
  |      +-- Yes
  |
  +-- Does it meet correctness, latency, security, and recovery requirements?
         +-- No -> tune or redesign with evidence
         +-- Yes -> close change and retain evidence
```

## 21. Detailed troubleshooting catalog

### Case T001: API discovery during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CRD is missing or the requested API version is not served.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc api-resources; oc get crd; oc explain`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T002: Admission during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the API server rejects the manifest.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc apply --dry-run=server -f; oc get validatingwebhookconfiguration,mutatingwebhookconfiguration`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T003: OLM during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the managing Operator is unavailable or degraded.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get csv,subscription,installplan; oc describe csv`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T004: RBAC during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the caller or service account is forbidden.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T005: Quota during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, resource creation is rejected because quota is exhausted.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe resourcequota; oc get limitrange`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T006: Scheduling during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a workload remains Pending.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; oc get nodes; oc get events`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T007: Storage during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a PVC is Pending or a volume cannot mount.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T008: Object storage during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, S3 access fails or an artifact cannot be downloaded.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `curl -vk endpoint; aws s3api head-bucket; inspect secret keys`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T009: DNS during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a service or external endpoint cannot resolve.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `getent hosts; dig; oc get dns.operator/default`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T010: TLS during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, certificate verification or handshake fails.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `openssl s_client; curl -v; inspect CA bundle`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T011: NetworkPolicy during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, required traffic is silently dropped or times out.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get networkpolicy; connectivity test from the same pod identity`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T012: Image during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, an image cannot pull or cannot run under OpenShift security constraints.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; inspect image pull secret; podman inspect`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T013: Probe during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, liveness or readiness probes repeatedly fail.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; curl the probe endpoint from the pod`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T014: Runtime during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the process starts but exits because arguments or environment are wrong.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc logs --previous; inspect command, args, env`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T015: Model load during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the runtime cannot find, parse, or allocate the model.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `storage initializer logs; runtime logs; artifact checksum`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T016: Authentication during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 401.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T017: Authorization during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 403.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; inspect policy and group snapshot`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T018: Routing during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 404 or route/gateway is not admitted.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get route,gateway,httproute,service,endpoints`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T019: Availability during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 503.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T020: Performance during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, latency or throughput misses the SLO.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `collect p50/p95/p99, CPU, memory, GPU, storage, and network metrics`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T021: GPU discovery during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, nvidia.com/gpu is not allocatable.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get node labels; device-plugin logs; nvidia-smi on host`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T022: GPU memory during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CUDA out of memory occurs.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T023: Monitoring during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metrics are absent or stale.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect exporter, ServiceMonitor, target health, and relabeling`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T024: GitOps during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the resource is OutOfSync or continuously flaps.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `argocd app diff; inspect managedFields and operator ownership`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T025: Backup during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, backup or restore is PartiallyFailed.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T026: Data integrity during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metadata and artifact versions do not match.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare registry URI, object version, checksum, and Git commit`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T027: Security context during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the container is blocked by SCC or cannot write.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pod -o yaml; oc adm policy who-can use scc; id and mount permissions`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T028: Capacity during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the platform has no headroom for the change.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T029: Upgrade during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a previously valid field or workflow changes after upgrade.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare served CRD schema, release notes, and generated manifests`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T030: Human process during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the technical change is correct but ownership or approval is missing.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `review change ticket, owner labels, approval evidence, and runbook`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T031: API discovery during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CRD is missing or the requested API version is not served.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc api-resources; oc get crd; oc explain`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T032: Admission during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the API server rejects the manifest.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc apply --dry-run=server -f; oc get validatingwebhookconfiguration,mutatingwebhookconfiguration`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T033: OLM during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the managing Operator is unavailable or degraded.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get csv,subscription,installplan; oc describe csv`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T034: RBAC during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the caller or service account is forbidden.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T035: Quota during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, resource creation is rejected because quota is exhausted.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe resourcequota; oc get limitrange`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T036: Scheduling during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a workload remains Pending.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; oc get nodes; oc get events`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T037: Storage during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a PVC is Pending or a volume cannot mount.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T038: Object storage during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, S3 access fails or an artifact cannot be downloaded.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `curl -vk endpoint; aws s3api head-bucket; inspect secret keys`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T039: DNS during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a service or external endpoint cannot resolve.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `getent hosts; dig; oc get dns.operator/default`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T040: TLS during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, certificate verification or handshake fails.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `openssl s_client; curl -v; inspect CA bundle`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T041: NetworkPolicy during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, required traffic is silently dropped or times out.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get networkpolicy; connectivity test from the same pod identity`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T042: Image during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, an image cannot pull or cannot run under OpenShift security constraints.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; inspect image pull secret; podman inspect`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T043: Probe during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, liveness or readiness probes repeatedly fail.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; curl the probe endpoint from the pod`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T044: Runtime during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the process starts but exits because arguments or environment are wrong.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc logs --previous; inspect command, args, env`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T045: Model load during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the runtime cannot find, parse, or allocate the model.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `storage initializer logs; runtime logs; artifact checksum`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T046: Authentication during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 401.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T047: Authorization during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 403.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; inspect policy and group snapshot`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T048: Routing during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 404 or route/gateway is not admitted.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get route,gateway,httproute,service,endpoints`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T049: Availability during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 503.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T050: Performance during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, latency or throughput misses the SLO.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `collect p50/p95/p99, CPU, memory, GPU, storage, and network metrics`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T051: GPU discovery during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, nvidia.com/gpu is not allocatable.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get node labels; device-plugin logs; nvidia-smi on host`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T052: GPU memory during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CUDA out of memory occurs.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T053: Monitoring during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metrics are absent or stale.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect exporter, ServiceMonitor, target health, and relabeling`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T054: GitOps during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the resource is OutOfSync or continuously flaps.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `argocd app diff; inspect managedFields and operator ownership`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T055: Backup during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, backup or restore is PartiallyFailed.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T056: Data integrity during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metadata and artifact versions do not match.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare registry URI, object version, checksum, and Git commit`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T057: Security context during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the container is blocked by SCC or cannot write.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pod -o yaml; oc adm policy who-can use scc; id and mount permissions`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T058: Capacity during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the platform has no headroom for the change.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T059: Upgrade during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a previously valid field or workflow changes after upgrade.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare served CRD schema, release notes, and generated manifests`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T060: Human process during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the technical change is correct but ownership or approval is missing.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `review change ticket, owner labels, approval evidence, and runbook`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T061: API discovery during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CRD is missing or the requested API version is not served.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc api-resources; oc get crd; oc explain`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T062: Admission during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the API server rejects the manifest.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc apply --dry-run=server -f; oc get validatingwebhookconfiguration,mutatingwebhookconfiguration`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T063: OLM during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the managing Operator is unavailable or degraded.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get csv,subscription,installplan; oc describe csv`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T064: RBAC during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the caller or service account is forbidden.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T065: Quota during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, resource creation is rejected because quota is exhausted.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe resourcequota; oc get limitrange`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T066: Scheduling during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a workload remains Pending.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; oc get nodes; oc get events`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T067: Storage during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a PVC is Pending or a volume cannot mount.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T068: Object storage during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, S3 access fails or an artifact cannot be downloaded.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `curl -vk endpoint; aws s3api head-bucket; inspect secret keys`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T069: DNS during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a service or external endpoint cannot resolve.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `getent hosts; dig; oc get dns.operator/default`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T070: TLS during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, certificate verification or handshake fails.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `openssl s_client; curl -v; inspect CA bundle`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T071: NetworkPolicy during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, required traffic is silently dropped or times out.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get networkpolicy; connectivity test from the same pod identity`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T072: Image during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, an image cannot pull or cannot run under OpenShift security constraints.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; inspect image pull secret; podman inspect`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T073: Probe during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, liveness or readiness probes repeatedly fail.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; curl the probe endpoint from the pod`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T074: Runtime during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the process starts but exits because arguments or environment are wrong.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc logs --previous; inspect command, args, env`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T075: Model load during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the runtime cannot find, parse, or allocate the model.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `storage initializer logs; runtime logs; artifact checksum`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T076: Authentication during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 401.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T077: Authorization during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a request receives 403.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; inspect policy and group snapshot`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T078: Routing during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 404 or route/gateway is not admitted.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get route,gateway,httproute,service,endpoints`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T079: Availability during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the endpoint returns 503.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T080: Performance during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, latency or throughput misses the SLO.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `collect p50/p95/p99, CPU, memory, GPU, storage, and network metrics`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T081: GPU discovery during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, nvidia.com/gpu is not allocatable.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get node labels; device-plugin logs; nvidia-smi on host`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T082: GPU memory during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CUDA out of memory occurs.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T083: Monitoring during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metrics are absent or stale.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `inspect exporter, ServiceMonitor, target health, and relabeling`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T084: GitOps during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the resource is OutOfSync or continuously flaps.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `argocd app diff; inspect managedFields and operator ownership`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T085: Backup during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, backup or restore is PartiallyFailed.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T086: Data integrity during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, metadata and artifact versions do not match.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare registry URI, object version, checksum, and Git commit`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T087: Security context during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the container is blocked by SCC or cannot write.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pod -o yaml; oc adm policy who-can use scc; id and mount permissions`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T088: Capacity during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the platform has no headroom for the change.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T089: Upgrade during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a previously valid field or workflow changes after upgrade.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `compare served CRD schema, release notes, and generated manifests`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T090: Human process during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the technical change is correct but ownership or approval is missing.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `review change ticket, owner labels, approval evidence, and runbook`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T091: API discovery during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, CRD is missing or the requested API version is not served.
- **First owner:** The platform engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc api-resources; oc get crd; oc explain`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T092: Admission during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the API server rejects the manifest.
- **First owner:** The OpenShift administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc apply --dry-run=server -f; oc get validatingwebhookconfiguration,mutatingwebhookconfiguration`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T093: OLM during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the managing Operator is unavailable or degraded.
- **First owner:** The data scientist gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get csv,subscription,installplan; oc describe csv`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T094: RBAC during Query core DCGM metrics

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, the caller or service account is forbidden.
- **First owner:** The MLOps engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T095: Quota during Correlate pod-to-GPU attribution

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, resource creation is rejected because quota is exhausted.
- **First owner:** The SRE gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe resourcequota; oc get limitrange`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T096: Scheduling during Create dashboard panels

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a workload remains Pending.
- **First owner:** The security engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc describe pod; oc get nodes; oc get events`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T097: Storage during Create warning and critical alerts

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a PVC is Pending or a volume cannot mount.
- **First owner:** The storage administrator gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Topic probe:** `oc get ds,svc,pod -n nvidia-gpu-operator | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T098: Object storage during Test alert routing

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, S3 access fails or an artifact cannot be downloaded.
- **First owner:** The network engineer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `curl -vk endpoint; aws s3api head-bucket; inspect secret keys`.
- **Topic probe:** `oc get servicemonitor -A | grep -i dcgm`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T099: DNS during Verify exporter pods and service

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, a service or external endpoint cannot resolve.
- **First owner:** The model risk reviewer gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `getent hosts; dig; oc get dns.operator/default`.
- **Topic probe:** `oc -n openshift-monitoring exec prometheus-k8s-0 -c prometheus -- wget -qO- http://localhost:9090/api/v1/query?query=DCGM_FI_DEV_GPU_UTIL`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

### Case T100: TLS during Confirm Prometheus target health

- **Observed symptom:** While attempting to monitor gpu using dcgm metrics, certificate verification or handshake fails.
- **First owner:** The incident commander gathers evidence; ownership changes only after the failing layer is proven.
- **Primary probe:** `openssl s_client; curl -v; inspect CA bundle`.
- **Topic probe:** `oc adm top pods -n rhoai-lab`.
- **Expected evidence:** The condition, event, log, or metric names the failing dependency rather than only the downstream symptom.
- **Likely root-cause classes:** configuration drift, missing prerequisite, incompatible version, exhausted capacity, denied access, or unreachable dependency.
- **Minimal correction:** Fix one proven cause, keep the submitted and server-accepted YAML, and observe reconciliation.
- **Validation:** Repeat the original operation and at least one negative test.
- **Prevention:** Add a preflight check, policy gate, alert, or runbook entry that catches this class before production.

## 22. Operational drill catalog

### Drill D001: Verify exporter pods and service with least privilege

- **Scenario:** The data scientist must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce least privilege; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D002: Confirm Prometheus target health with immutable image digests

- **Scenario:** The SRE must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce immutable image digests; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D003: Query core DCGM metrics with secret rotation

- **Scenario:** The storage administrator must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce secret rotation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D004: Correlate pod-to-GPU attribution with TLS verification

- **Scenario:** The model risk reviewer must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce TLS verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D005: Create dashboard panels with data classification

- **Scenario:** The platform engineer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce data classification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D006: Create warning and critical alerts with namespace isolation

- **Scenario:** The data scientist must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce namespace isolation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D007: Test alert routing with quota alignment

- **Scenario:** The SRE must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce quota alignment; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D008: Verify exporter pods and service with backup consistency

- **Scenario:** The storage administrator must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce backup consistency; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D009: Confirm Prometheus target health with Git review

- **Scenario:** The model risk reviewer must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce Git review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D010: Query core DCGM metrics with change approval

- **Scenario:** The platform engineer must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce change approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D011: Correlate pod-to-GPU attribution with observability

- **Scenario:** The data scientist must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce observability; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D012: Create dashboard panels with SLO definition

- **Scenario:** The SRE must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce SLO definition; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D013: Create warning and critical alerts with audit logging

- **Scenario:** The storage administrator must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce audit logging; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D014: Test alert routing with artifact checksum

- **Scenario:** The model risk reviewer must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce artifact checksum; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D015: Verify exporter pods and service with model approval

- **Scenario:** The platform engineer must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce model approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D016: Confirm Prometheus target health with rollback readiness

- **Scenario:** The data scientist must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce rollback readiness; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D017: Query core DCGM metrics with break-glass access

- **Scenario:** The SRE must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce break-glass access; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D018: Correlate pod-to-GPU attribution with dependency inventory

- **Scenario:** The storage administrator must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce dependency inventory; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D019: Create dashboard panels with capacity headroom

- **Scenario:** The model risk reviewer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce capacity headroom; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D020: Create warning and critical alerts with incident evidence

- **Scenario:** The platform engineer must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce incident evidence; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D021: Test alert routing with vulnerability scanning

- **Scenario:** The data scientist must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce vulnerability scanning; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D022: Verify exporter pods and service with signature verification

- **Scenario:** The SRE must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce signature verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D023: Confirm Prometheus target health with retention policy

- **Scenario:** The storage administrator must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce retention policy; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D024: Query core DCGM metrics with access review

- **Scenario:** The model risk reviewer must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce access review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D025: Correlate pod-to-GPU attribution with disaster-recovery testing

- **Scenario:** The platform engineer must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce disaster-recovery testing; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D026: Create dashboard panels with least privilege

- **Scenario:** The data scientist must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce least privilege; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D027: Create warning and critical alerts with immutable image digests

- **Scenario:** The SRE must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce immutable image digests; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D028: Test alert routing with secret rotation

- **Scenario:** The storage administrator must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce secret rotation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D029: Verify exporter pods and service with TLS verification

- **Scenario:** The model risk reviewer must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce TLS verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D030: Confirm Prometheus target health with data classification

- **Scenario:** The platform engineer must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce data classification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D031: Query core DCGM metrics with namespace isolation

- **Scenario:** The data scientist must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce namespace isolation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D032: Correlate pod-to-GPU attribution with quota alignment

- **Scenario:** The SRE must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce quota alignment; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D033: Create dashboard panels with backup consistency

- **Scenario:** The storage administrator must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce backup consistency; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D034: Create warning and critical alerts with Git review

- **Scenario:** The model risk reviewer must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce Git review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D035: Test alert routing with change approval

- **Scenario:** The platform engineer must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce change approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D036: Verify exporter pods and service with observability

- **Scenario:** The data scientist must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce observability; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D037: Confirm Prometheus target health with SLO definition

- **Scenario:** The SRE must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce SLO definition; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D038: Query core DCGM metrics with audit logging

- **Scenario:** The storage administrator must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce audit logging; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D039: Correlate pod-to-GPU attribution with artifact checksum

- **Scenario:** The model risk reviewer must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce artifact checksum; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D040: Create dashboard panels with model approval

- **Scenario:** The platform engineer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce model approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D041: Create warning and critical alerts with rollback readiness

- **Scenario:** The data scientist must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce rollback readiness; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D042: Test alert routing with break-glass access

- **Scenario:** The SRE must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce break-glass access; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D043: Verify exporter pods and service with dependency inventory

- **Scenario:** The storage administrator must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce dependency inventory; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D044: Confirm Prometheus target health with capacity headroom

- **Scenario:** The model risk reviewer must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce capacity headroom; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D045: Query core DCGM metrics with incident evidence

- **Scenario:** The platform engineer must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce incident evidence; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D046: Correlate pod-to-GPU attribution with vulnerability scanning

- **Scenario:** The data scientist must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce vulnerability scanning; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D047: Create dashboard panels with signature verification

- **Scenario:** The SRE must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce signature verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D048: Create warning and critical alerts with retention policy

- **Scenario:** The storage administrator must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce retention policy; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D049: Test alert routing with access review

- **Scenario:** The model risk reviewer must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce access review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D050: Verify exporter pods and service with disaster-recovery testing

- **Scenario:** The platform engineer must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce disaster-recovery testing; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D051: Confirm Prometheus target health with least privilege

- **Scenario:** The data scientist must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce least privilege; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D052: Query core DCGM metrics with immutable image digests

- **Scenario:** The SRE must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce immutable image digests; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D053: Correlate pod-to-GPU attribution with secret rotation

- **Scenario:** The storage administrator must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce secret rotation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D054: Create dashboard panels with TLS verification

- **Scenario:** The model risk reviewer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce TLS verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D055: Create warning and critical alerts with data classification

- **Scenario:** The platform engineer must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce data classification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D056: Test alert routing with namespace isolation

- **Scenario:** The data scientist must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce namespace isolation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D057: Verify exporter pods and service with quota alignment

- **Scenario:** The SRE must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce quota alignment; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D058: Confirm Prometheus target health with backup consistency

- **Scenario:** The storage administrator must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce backup consistency; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D059: Query core DCGM metrics with Git review

- **Scenario:** The model risk reviewer must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce Git review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D060: Correlate pod-to-GPU attribution with change approval

- **Scenario:** The platform engineer must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce change approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D061: Create dashboard panels with observability

- **Scenario:** The data scientist must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce observability; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D062: Create warning and critical alerts with SLO definition

- **Scenario:** The SRE must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce SLO definition; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D063: Test alert routing with audit logging

- **Scenario:** The storage administrator must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce audit logging; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D064: Verify exporter pods and service with artifact checksum

- **Scenario:** The model risk reviewer must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce artifact checksum; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D065: Confirm Prometheus target health with model approval

- **Scenario:** The platform engineer must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce model approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D066: Query core DCGM metrics with rollback readiness

- **Scenario:** The data scientist must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce rollback readiness; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D067: Correlate pod-to-GPU attribution with break-glass access

- **Scenario:** The SRE must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce break-glass access; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D068: Create dashboard panels with dependency inventory

- **Scenario:** The storage administrator must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce dependency inventory; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D069: Create warning and critical alerts with capacity headroom

- **Scenario:** The model risk reviewer must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce capacity headroom; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D070: Test alert routing with incident evidence

- **Scenario:** The platform engineer must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce incident evidence; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D071: Verify exporter pods and service with vulnerability scanning

- **Scenario:** The data scientist must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce vulnerability scanning; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D072: Confirm Prometheus target health with signature verification

- **Scenario:** The SRE must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce signature verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D073: Query core DCGM metrics with retention policy

- **Scenario:** The storage administrator must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce retention policy; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D074: Correlate pod-to-GPU attribution with access review

- **Scenario:** The model risk reviewer must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce access review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D075: Create dashboard panels with disaster-recovery testing

- **Scenario:** The platform engineer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce disaster-recovery testing; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D076: Create warning and critical alerts with least privilege

- **Scenario:** The data scientist must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce least privilege; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D077: Test alert routing with immutable image digests

- **Scenario:** The SRE must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce immutable image digests; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D078: Verify exporter pods and service with secret rotation

- **Scenario:** The storage administrator must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce secret rotation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D079: Confirm Prometheus target health with TLS verification

- **Scenario:** The model risk reviewer must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce TLS verification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D080: Query core DCGM metrics with data classification

- **Scenario:** The platform engineer must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce data classification; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D081: Correlate pod-to-GPU attribution with namespace isolation

- **Scenario:** The data scientist must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce namespace isolation; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the caller or service account is forbidden.
- **Evidence command:** `oc auth can-i; oc get role,rolebinding,clusterrole,clusterrolebinding`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D082: Create dashboard panels with quota alignment

- **Scenario:** The SRE must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce quota alignment; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a PVC is Pending or a volume cannot mount.
- **Evidence command:** `oc get pvc,pv,storageclass; oc describe pvc`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D083: Create warning and critical alerts with backup consistency

- **Scenario:** The storage administrator must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce backup consistency; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** certificate verification or handshake fails.
- **Evidence command:** `openssl s_client; curl -v; inspect CA bundle`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D084: Test alert routing with Git review

- **Scenario:** The model risk reviewer must complete `Test alert routing` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce Git review; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** liveness or readiness probes repeatedly fail.
- **Evidence command:** `oc describe pod; curl the probe endpoint from the pod`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D085: Verify exporter pods and service with change approval

- **Scenario:** The platform engineer must complete `Verify exporter pods and service` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce change approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** a request receives 401.
- **Evidence command:** `inspect issuer, audience, token expiry, and Authorization header`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D086: Confirm Prometheus target health with observability

- **Scenario:** The data scientist must complete `Confirm Prometheus target health` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce observability; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the endpoint returns 503.
- **Evidence command:** `inspect Ready conditions, endpoints, probes, and upstream pods`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D087: Query core DCGM metrics with SLO definition

- **Scenario:** The SRE must complete `Query core DCGM metrics` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce SLO definition; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CUDA out of memory occurs.
- **Evidence command:** `nvidia-smi; framework allocator stats; model/batch/context sizing`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D088: Correlate pod-to-GPU attribution with audit logging

- **Scenario:** The storage administrator must complete `Correlate pod-to-GPU attribution` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce audit logging; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** backup or restore is PartiallyFailed.
- **Evidence command:** `oc describe backup/restore; inspect volume and plugin logs`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D089: Create dashboard panels with artifact checksum

- **Scenario:** The model risk reviewer must complete `Create dashboard panels` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce artifact checksum; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** the platform has no headroom for the change.
- **Evidence command:** `oc adm top nodes; quota usage; GPU and storage capacity`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

### Drill D090: Create warning and critical alerts with model approval

- **Scenario:** The platform engineer must complete `Create warning and critical alerts` as part of monitor gpu using dcgm metrics.
- **Constraint:** Enforce model approval; do not bypass it for convenience.
- **Pre-check:** Confirm version, scope, owner, dependency health, capacity, and rollback artifact.
- **Action:** Use the topic manifest or command runbook and record the exact change.
- **Observe:** Watch conditions, events, generated resources, logs, and resource consumption.
- **Injected complication:** CRD is missing or the requested API version is not served.
- **Evidence command:** `oc api-resources; oc get crd; oc explain`.
- **Acceptance:** Functional behavior, security control, observability, and recovery evidence all pass.
- **Rollback:** Reapply the last known-good state or revert the Git commit, then prove recovery.

## 23. Corporate interview questions and model answers

### Q001. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce backup consistency,
keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If a
workload remains Pending, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q002. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
model approval, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If required traffic is silently dropped or times out, I change only the proven causal layer and
retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q003. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce
signature verification, keep credentials outside Git, and define acceptance, rollback, and evidence
before the change. If a request receives 401, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q004. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce TLS
verification, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q005. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce
observability, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If metadata and artifact versions do not match, I change only the proven causal layer and retest
the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q006. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce dependency inventory, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q007. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce disaster-recovery
testing, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If a workload remains Pending, I change only the proven causal layer and retest the original operation
plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q008. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
quota alignment, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If required traffic is silently dropped or times out, I change only the proven causal layer and
retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q009. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce
artifact checksum, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If a request receives 401, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q010. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce
vulnerability scanning, keep credentials outside Git, and define acceptance, rollback, and evidence
before the change. If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest
the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q011. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce secret
rotation, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If metadata and artifact versions do not match, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q012. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce change approval, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q013. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce break-glass access,
keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If a
workload remains Pending, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q014. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
access review, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If required traffic is silently dropped or times out, I change only the proven causal layer and
retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q015. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce
namespace isolation, keep credentials outside Git, and define acceptance, rollback, and evidence before
the change. If a request receives 401, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q016. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce audit
logging, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q017. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce incident
evidence, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If metadata and artifact versions do not match, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q018. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce immutable image digests, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q019. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce Git review, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If a workload
remains Pending, I change only the proven causal layer and retest the original operation plus a negative
case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q020. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
rollback readiness, keep credentials outside Git, and define acceptance, rollback, and evidence before
the change. If required traffic is silently dropped or times out, I change only the proven causal layer
and retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q021. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce
retention policy, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If a request receives 401, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q022. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce data
classification, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q023. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce SLO
definition, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If metadata and artifact versions do not match, I change only the proven causal layer and retest
the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q024. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce capacity headroom, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q025. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce least privilege, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If a workload
remains Pending, I change only the proven causal layer and retest the original operation plus a negative
case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q026. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
backup consistency, keep credentials outside Git, and define acceptance, rollback, and evidence before
the change. If required traffic is silently dropped or times out, I change only the proven causal layer
and retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q027. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce model
approval, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If a request receives 401, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q028. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce signature
verification, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q029. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce TLS
verification, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If metadata and artifact versions do not match, I change only the proven causal layer and retest
the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q030. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce observability, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q031. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce dependency inventory,
keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If a
workload remains Pending, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q032. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
disaster-recovery testing, keep credentials outside Git, and define acceptance, rollback, and evidence
before the change. If required traffic is silently dropped or times out, I change only the proven causal
layer and retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q033. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce quota
alignment, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If a request receives 401, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q034. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce artifact
checksum, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q035. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce
vulnerability scanning, keep credentials outside Git, and define acceptance, rollback, and evidence
before the change. If metadata and artifact versions do not match, I change only the proven causal layer
and retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q036. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce secret rotation, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q037. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce change approval, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If a workload
remains Pending, I change only the proven causal layer and retest the original operation plus a negative
case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q038. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
break-glass access, keep credentials outside Git, and define acceptance, rollback, and evidence before
the change. If required traffic is silently dropped or times out, I change only the proven causal layer
and retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q039. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce access
review, keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If
a request receives 401, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q040. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce namespace
isolation, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q041. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce audit
logging, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If metadata and artifact versions do not match, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q042. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce incident evidence, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q043. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce immutable image
digests, keep credentials outside Git, and define acceptance, rollback, and evidence before the change.
If a workload remains Pending, I change only the proven causal layer and retest the original operation
plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q044. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce Git
review, keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If
required traffic is silently dropped or times out, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q045. How would you handle authentication risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `inspect
issuer, audience, token expiry, and Authorization header` to isolate the failing layer. I enforce
rollback readiness, keep credentials outside Git, and define acceptance, rollback, and evidence before
the change. If a request receives 401, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q046. How would you handle gpu discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
node labels; device-plugin logs; nvidia-smi on host` to isolate the failing layer. I enforce retention
policy, keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If
nvidia.com/gpu is not allocatable, I change only the proven causal layer and retest the original
operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q047. How would you handle data integrity risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `compare
registry URI, object version, checksum, and Git commit` to isolate the failing layer. I enforce data
classification, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If metadata and artifact versions do not match, I change only the proven causal layer and retest
the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q048. How would you handle api discovery risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc api-
resources; oc get crd; oc explain` to isolate the failing layer. I enforce SLO definition, keep
credentials outside Git, and define acceptance, rollback, and evidence before the change. If CRD is
missing or the requested API version is not served, I change only the proven causal layer and retest the
original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q049. How would you handle scheduling risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc
describe pod; oc get nodes; oc get events` to isolate the failing layer. I enforce capacity headroom,
keep credentials outside Git, and define acceptance, rollback, and evidence before the change. If a
workload remains Pending, I change only the proven causal layer and retest the original operation plus a
negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

### Q050. How would you handle networkpolicy risk while you monitor gpu using dcgm metrics?

**Model answer:** I start by confirming the supported version, API scope, owner, and dependency health. I
validate the desired object server-side, observe its conditions and generated resources, and use `oc get
networkpolicy; connectivity test from the same pod identity` to isolate the failing layer. I enforce
least privilege, keep credentials outside Git, and define acceptance, rollback, and evidence before the
change. If required traffic is silently dropped or times out, I change only the proven causal layer and
retest the original operation plus a negative case.

**Senior follow-up:** Explain which fields are user-owned, Operator-owned, and GitOps-owned.

## 24. Hands-on practice assignments

### Assignment A001: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the data classification requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the managing Operator is unavailable or degraded.
5. Diagnose it with `oc get csv,subscription,installplan; oc describe csv` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A002: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the Git review requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: resource creation is rejected because quota is exhausted.
5. Diagnose it with `oc describe resourcequota; oc get limitrange` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A003: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the audit logging requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a PVC is Pending or a volume cannot mount.
5. Diagnose it with `oc get pvc,pv,storageclass; oc describe pvc` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A004: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the break-glass access requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a service or external endpoint cannot resolve.
5. Diagnose it with `getent hosts; dig; oc get dns.operator/default` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A005: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the vulnerability scanning requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: required traffic is silently dropped or times out.
5. Diagnose it with `oc get networkpolicy; connectivity test from the same pod identity` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A006: Create warning and critical alerts

1. Build the lab state needed to perform `Create warning and critical alerts`.
2. Apply the disaster-recovery testing requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: liveness or readiness probes repeatedly fail.
5. Diagnose it with `oc describe pod; curl the probe endpoint from the pod` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A007: Test alert routing

1. Build the lab state needed to perform `Test alert routing`.
2. Apply the TLS verification requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the runtime cannot find, parse, or allocate the model.
5. Diagnose it with `storage initializer logs; runtime logs; artifact checksum` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A008: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the backup consistency requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a request receives 403.
5. Diagnose it with `oc auth can-i; inspect policy and group snapshot` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A009: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the SLO definition requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the endpoint returns 503.
5. Diagnose it with `inspect Ready conditions, endpoints, probes, and upstream pods` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A010: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the rollback readiness requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: nvidia.com/gpu is not allocatable.
5. Diagnose it with `oc get node labels; device-plugin logs; nvidia-smi on host` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A011: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the incident evidence requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: metrics are absent or stale.
5. Diagnose it with `inspect exporter, ServiceMonitor, target health, and relabeling` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A012: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the access review requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: backup or restore is PartiallyFailed.
5. Diagnose it with `oc describe backup/restore; inspect volume and plugin logs` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A013: Create warning and critical alerts

1. Build the lab state needed to perform `Create warning and critical alerts`.
2. Apply the secret rotation requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the container is blocked by SCC or cannot write.
5. Diagnose it with `oc get pod -o yaml; oc adm policy who-can use scc; id and mount permissions` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A014: Test alert routing

1. Build the lab state needed to perform `Test alert routing`.
2. Apply the quota alignment requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a previously valid field or workflow changes after upgrade.
5. Diagnose it with `compare served CRD schema, release notes, and generated manifests` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A015: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the observability requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: CRD is missing or the requested API version is not served.
5. Diagnose it with `oc api-resources; oc get crd; oc explain` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A016: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the model approval requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the managing Operator is unavailable or degraded.
5. Diagnose it with `oc get csv,subscription,installplan; oc describe csv` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A017: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the capacity headroom requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: resource creation is rejected because quota is exhausted.
5. Diagnose it with `oc describe resourcequota; oc get limitrange` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A018: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the retention policy requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a PVC is Pending or a volume cannot mount.
5. Diagnose it with `oc get pvc,pv,storageclass; oc describe pvc` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A019: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the immutable image digests requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a service or external endpoint cannot resolve.
5. Diagnose it with `getent hosts; dig; oc get dns.operator/default` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A020: Create warning and critical alerts

1. Build the lab state needed to perform `Create warning and critical alerts`.
2. Apply the namespace isolation requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: required traffic is silently dropped or times out.
5. Diagnose it with `oc get networkpolicy; connectivity test from the same pod identity` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A021: Test alert routing

1. Build the lab state needed to perform `Test alert routing`.
2. Apply the change approval requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: liveness or readiness probes repeatedly fail.
5. Diagnose it with `oc describe pod; curl the probe endpoint from the pod` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A022: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the artifact checksum requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the runtime cannot find, parse, or allocate the model.
5. Diagnose it with `storage initializer logs; runtime logs; artifact checksum` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A023: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the dependency inventory requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a request receives 403.
5. Diagnose it with `oc auth can-i; inspect policy and group snapshot` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A024: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the signature verification requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the endpoint returns 503.
5. Diagnose it with `inspect Ready conditions, endpoints, probes, and upstream pods` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A025: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the least privilege requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: nvidia.com/gpu is not allocatable.
5. Diagnose it with `oc get node labels; device-plugin logs; nvidia-smi on host` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A026: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the data classification requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: metrics are absent or stale.
5. Diagnose it with `inspect exporter, ServiceMonitor, target health, and relabeling` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A027: Create warning and critical alerts

1. Build the lab state needed to perform `Create warning and critical alerts`.
2. Apply the Git review requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: backup or restore is PartiallyFailed.
5. Diagnose it with `oc describe backup/restore; inspect volume and plugin logs` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A028: Test alert routing

1. Build the lab state needed to perform `Test alert routing`.
2. Apply the audit logging requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the container is blocked by SCC or cannot write.
5. Diagnose it with `oc get pod -o yaml; oc adm policy who-can use scc; id and mount permissions` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A029: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the break-glass access requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a previously valid field or workflow changes after upgrade.
5. Diagnose it with `compare served CRD schema, release notes, and generated manifests` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A030: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the vulnerability scanning requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: CRD is missing or the requested API version is not served.
5. Diagnose it with `oc api-resources; oc get crd; oc explain` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A031: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the disaster-recovery testing requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the managing Operator is unavailable or degraded.
5. Diagnose it with `oc get csv,subscription,installplan; oc describe csv` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A032: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the TLS verification requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: resource creation is rejected because quota is exhausted.
5. Diagnose it with `oc describe resourcequota; oc get limitrange` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A033: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the backup consistency requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a PVC is Pending or a volume cannot mount.
5. Diagnose it with `oc get pvc,pv,storageclass; oc describe pvc` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A034: Create warning and critical alerts

1. Build the lab state needed to perform `Create warning and critical alerts`.
2. Apply the SLO definition requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a service or external endpoint cannot resolve.
5. Diagnose it with `getent hosts; dig; oc get dns.operator/default` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A035: Test alert routing

1. Build the lab state needed to perform `Test alert routing`.
2. Apply the rollback readiness requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: required traffic is silently dropped or times out.
5. Diagnose it with `oc get networkpolicy; connectivity test from the same pod identity` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A036: Verify exporter pods and service

1. Build the lab state needed to perform `Verify exporter pods and service`.
2. Apply the incident evidence requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: liveness or readiness probes repeatedly fail.
5. Diagnose it with `oc describe pod; curl the probe endpoint from the pod` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A037: Confirm Prometheus target health

1. Build the lab state needed to perform `Confirm Prometheus target health`.
2. Apply the access review requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the runtime cannot find, parse, or allocate the model.
5. Diagnose it with `storage initializer logs; runtime logs; artifact checksum` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A038: Query core DCGM metrics

1. Build the lab state needed to perform `Query core DCGM metrics`.
2. Apply the secret rotation requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: a request receives 403.
5. Diagnose it with `oc auth can-i; inspect policy and group snapshot` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A039: Correlate pod-to-GPU attribution

1. Build the lab state needed to perform `Correlate pod-to-GPU attribution`.
2. Apply the quota alignment requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: the endpoint returns 503.
5. Diagnose it with `inspect Ready conditions, endpoints, probes, and upstream pods` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

### Assignment A040: Create dashboard panels

1. Build the lab state needed to perform `Create dashboard panels`.
2. Apply the observability requirement.
3. Capture before-and-after YAML, events, logs, and resource usage.
4. Inject this failure: nvidia.com/gpu is not allocatable.
5. Diagnose it with `oc get node labels; device-plugin logs; nvidia-smi on host` and the topic-specific commands.
6. Apply the smallest reversible fix.
7. Prove functional success, a security negative test, and rollback.
8. Write a five-sentence incident summary and one preventive control.

## 25. Day-2 operations runbook

| Frequency | Required action |
|---|---|
| Daily | Check Ready conditions, pod restarts, Warning events, endpoint availability, and capacity. |
| Weekly | Review image and model versions, failed jobs, quota headroom, certificate expiry, and backup status. |
| Monthly | Review access, network flows, storage growth, SLO trends, alert quality, and dependency lifecycle. |
| Quarterly | Test restore, rotate credentials, review known issues, and validate runbooks with another engineer. |
| Before upgrade | Export CRs, read release notes, validate prerequisites, create backups, and define rollback. |
| After upgrade | Confirm served APIs, component versions, generated resources, functional tests, and metrics. |
| During incident | Freeze unrelated changes, capture evidence, establish timeline, apply reversible mitigation. |
| After incident | Document root cause, corrective action, owner, due date, and validation of prevention. |

## 26. Change record template

```text
Change ID:
Topic:
Business reason:
Scope:
Owner:
Reviewer:
Planned start:
Planned end:
Platform version:
Affected namespaces:
Affected external systems:
Risk rating:
Prerequisite evidence:
Implementation commit:
Manifest checksum:
Image digest:
Model artifact URI and checksum:
Backup identifier:
Acceptance tests:
Rollback trigger:
Rollback command or commit:
Monitoring dashboard:
Incident channel:
Result:
Post-change observations:
Follow-up actions:
```

## 27. Incident report template

```text
Incident title:
Start time:
Detection source:
Customer impact:
Affected model/workbench/pipeline:
Current RHOAI and OpenShift versions:
Last known-good state:
Change timeline:
Primary symptoms:
Conditions:
Events:
Logs:
Metrics:
Root cause:
Contributing factors:
Mitigation:
Recovery validation:
Data-integrity validation:
Security impact:
Corrective actions:
Preventive controls:
Owners and due dates:
Evidence location:
```

## 28. Final certification checklist

- [ ] C01: I can explain the architecture without using the dashboard as a black box.
- [ ] C02: I can build the resource from scratch and also through the supported UI.
- [ ] C03: I can discover the served API and validate a CR server-side.
- [ ] C04: I can map the CR to generated workloads and owner references.
- [ ] C05: I can diagnose a failure using conditions, events, logs, metrics, and external dependency tests.
- [ ] C06: I can implement least privilege, TLS, secrets, NetworkPolicy, and quota.
- [ ] C07: I can automate the resource safely with GitOps.
- [ ] C08: I can back up and restore both Kubernetes state and external state.
- [ ] C09: I can define an SLO, alert, and runbook.
- [ ] C10: I can perform a controlled rollback and prove the prior state is restored.
- [ ] C11: I can discuss known limitations and version-specific risks.
- [ ] C12: I can present the design to security, networking, storage, and application teams.

## 29. Glossary

| Term | Meaning |
|---|---|
| CRD | CustomResourceDefinition; extends the Kubernetes API. |
| CR | Custom resource; an instance reconciled by a controller. |
| DSPA | DataSciencePipelinesApplication; namespace-scoped pipelines stack. |
| DSC | DataScienceCluster; controls OpenShift AI component management state. |
| InferenceService | KServe resource describing a model-serving deployment. |
| ServingRuntime | KServe runtime template defining how a model server runs. |
| Hardware profile | Governed compute and accelerator configuration exposed to users. |
| ModelCar | OCI-based model storage format used for faster and portable KServe deployment. |
| RPO | Maximum acceptable data loss measured in time. |
| RTO | Maximum acceptable time to restore service. |
| SLO | Measurable reliability objective for a service. |
| SCC | OpenShift SecurityContextConstraints. |
| NFD | Node Feature Discovery, which labels hardware capabilities. |
| DCGM | NVIDIA Data Center GPU Manager metrics and diagnostics. |
| TrustyAI | OpenShift AI capability for model bias and data-drift monitoring. |

## 30. Official references

- [Red Hat OpenShift AI Self-Managed 3.4 - Release notes](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/release_notes/index)
- [Installing and uninstalling OpenShift AI Self-Managed 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/installing_and_uninstalling_openshift_ai_self-managed/index)
- [Managing OpenShift AI 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/managing_openshift_ai/index)
- [Working on projects 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/working_on_projects/index)
- [Creating a workbench 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/creating_a_workbench/index)
- [Deploying models 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/deploying_models/index)
- [Configuring your model-serving platform 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/configuring_your_model-serving_platform/index)
- [Monitoring your AI systems 3.4](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/monitoring_your_ai_systems/index)
- [OpenShift Container Platform 4.20 hardware accelerators](https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/hardware_accelerators/index)
- [OpenShift Container Platform backup and restore](https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/backup_and_restore/index)
- [OpenShift GitOps](https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/latest)

> Always check the current supported-configuration matrix, lifecycle page, release notes, known issues,
> and the live CRD schema before a production change.

---

End of `23. Monitor GPU using DCGM metrics.md`.

