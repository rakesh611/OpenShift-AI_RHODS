# 04. Design multi-tenant OpenShift AI platform

> Corporate Interview Scenario Guide — Red Hat OpenShift AI L3 / Senior Platform Engineer

- **Production baseline:** Red Hat OpenShift AI Self-Managed 3.4 and OpenShift Container Platform 4.20
- **Generated:** 2026-07-21
- **Audience:** 10+ year Linux, Kubernetes, OpenShift, DevOps and production-support engineers
- **Safety:** Validate fields with `oc api-resources` and `oc explain` before applying manifests.


## Scenario objective

Create a shared OpenShift AI platform that isolates teams, data, credentials, networking, compute and cost while preserving standardized self-service.


## Senior interview answer structure

1. Clarify business impact, data class, tenancy, SLO, RPO, RTO, compliance and operating ownership.
2. Verify the supported OpenShift, OpenShift AI and dependent Operator matrix.
3. Draw identity, request, storage, compute, serving, monitoring and recovery flows.
4. Define namespace, RBAC, quota, NetworkPolicy, secret, image and artifact boundaries.
5. Build a minimum production pattern in non-production.
6. Run functional, security, performance, failure, restore and upgrade tests.
7. Promote through GitOps with approvals and evidence.
8. Operate with SLOs, alerts, capacity, backup, incident and change management.


## Core resources

- `Namespace` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Group` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `RoleBinding` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ResourceQuota` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `LimitRange` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `NetworkPolicy` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `HardwareProfile` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ClusterQueue` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `LocalQueue` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Secret` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.

## Core concepts

- **tenant boundary:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **namespace tenancy:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **showback:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **chargeback:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **golden path:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **platform team:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **data owner:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **shared service:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **quota hierarchy:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **noisy neighbor:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.

## Failure modes

- **cross-project access:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **quota starvation:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **secret exposure:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **policy bypass:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **GPU monopolization:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **orphan resources:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **cost gaps:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.

## Service indicators

- **onboarding time:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **quota utilization:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **GPU fairness:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **policy compliance:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **cost per tenant:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **idle hours:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **support tickets:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.

## Required controls

- **namespace isolation:** implement declaratively, test negatively, review periodically and retain evidence.
- **least privilege:** implement declaratively, test negatively, review periodically and retain evidence.
- **default deny:** implement declaratively, test negatively, review periodically and retain evidence.
- **per-tenant quota:** implement declaratively, test negatively, review periodically and retain evidence.
- **approved profiles:** implement declaratively, test negatively, review periodically and retain evidence.
- **secret scoping:** implement declaratively, test negatively, review periodically and retain evidence.
- **cost labels:** implement declaratively, test negatively, review periodically and retain evidence.

## Reference architecture

1. **Enterprise services:** identity, DNS, NTP, PKI, secrets manager, Git, registry, S3, SIEM, ticketing and backup.
2. **OpenShift:** control plane, ingress or Gateway API, OVN-Kubernetes, monitoring, registry, storage, OAuth and policy.
3. **OpenShift AI:** Operator, DSCInitialization, DataScienceCluster, dashboard, workbenches, pipelines, KServe, registry and TrustyAI as enabled.
4. **Tenant:** namespace, groups, service accounts, connections, workbenches, pipeline server, runs, models, quota and policies.
5. **Operations:** GitOps, CI/CD, vulnerability management, SLOs, capacity, backup, restore, upgrade and incident response.

Every route, runtime, model, bucket, PVC, credential, quota and exception must have a named owner and lifecycle.


## Project creation from scratch

### Phase 0 — supported-version discovery

1. Record cluster version, channel, CSV, dependent Operators, storage classes, ingress, identity, proxy and disconnected status.
2. Read release notes, known issues, deprecated and removed features.
3. Run `oc api-resources` and `oc explain` for every custom resource.
4. Create a RACI across platform, security, network, storage, data science and application teams.

### Phase 1 — requirements

1. Define users, data classes, model type, request pattern, peak concurrency, latency, availability, RPO, RTO and retention.
2. Convert each requirement into a measurable acceptance test.
3. Record constraints such as air gap, private connectivity, HSM, GPU scarcity and regulated change.
4. Document assumptions and explicit exclusions.

### Phase 2 — dependency readiness

1. Validate cluster operators, DNS, NTP, ingress, storage, registry, monitoring, identity, certificate trust and backup.
2. Verify node labels, taints, capacity and scheduling.
3. Verify credentials come from an approved secret system.
4. Create an automated preflight report.

### Phase 3 — tenant foundation

1. Create namespaces with owner, environment, cost center, data class and lifecycle labels.
2. Bind groups rather than individuals where practical.
3. Apply ResourceQuota, LimitRange, default-deny and required allow policies.
4. Register approved images, hardware profiles, runtimes and connections.

### Phase 4 — implementation

1. Deploy the minimum representative workload.
2. Pin images and models by digest or immutable version.
3. Use Git overlays for environment differences.
4. Expose only required endpoints and verify authentication and authorization.

### Phase 5 — validation

1. Run positive and negative functional tests.
2. Test scheduling, images, storage, DNS, TLS, RBAC, policy and quota.
3. Run load, soak and capacity tests.
4. Capture YAML, events, logs, metrics and test output.

### Phase 6 — failure and recovery

1. Inject one dependency failure at a time in non-production.
2. Confirm actionable alerts.
3. Execute remediation and rollback.
4. Measure restoration against SLO and RTO.

### Phase 7 — production promotion

1. Require review, policy checks, artifact verification, approvals and change record.
2. Use canary or controlled traffic migration.
3. Observe a defined bake period.
4. Retain last-known-good manifests and artifacts.

### Phase 8 — day-2 operations

1. Review capacity, latency, errors, security, backup age, certificate expiry and deprecations.
2. Patch and upgrade through rehearsal environments.
3. Review access and exceptions.
4. Improve the runbook after every incident and exercise.


## Command and evidence toolkit

Run read-only commands first and preserve timestamped output.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A
oc get subscriptions.operators.coreos.com -A
oc get installplans -A
oc get datasciencecluster -A -o yaml
oc get dscinitialization -A -o yaml
oc get pods -A --sort-by=.metadata.creationTimestamp
oc get events -A --sort-by=.lastTimestamp
oc get nodes -o wide
oc describe node <node>
oc auth can-i --list -n <project>
oc get resourcequota,limitrange -n <project> -o yaml
oc get networkpolicy -n <project> -o yaml
oc get secrets,pvc -n <project>
oc get routes,services,endpointslices -n <project>
oc logs <pod> -n <project> --all-containers --prefix
oc adm inspect namespace/<project>
oc adm must-gather
```

Evidence path: `<incident>/<timestamp>/<cluster>/<namespace>/<resource>/<command-output>`.

For every command record expected result, actual result and interpretation. Output without interpretation is not diagnosis.

## Implementation manifests

### Version-adaptive preflight

```yaml
#!/usr/bin/env bash
set -euo pipefail
oc version
oc get clusterversion
oc get csv -A
oc get datasciencecluster -A
oc api-resources | grep -Ei 'inferenceservice|servingruntime|hardwareprofile|notebook|trustyai'
oc explain inferenceservice.spec 2>/dev/null || true
oc get co
```

### Project quota

```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: ai-project-quota
  namespace: ai-team-a-dev
spec:
  hard:
    requests.cpu: "32"
    limits.cpu: "64"
    requests.memory: 128Gi
    limits.memory: 256Gi
    requests.nvidia.com/gpu: "4"
    persistentvolumeclaims: "20"
    requests.storage: 5Ti
```

### Default deny

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: ai-team-a-dev
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
```

Examples are patterns, not guaranteed schemas for every release. Confirm installed CRDs and Operator ownership.

## L3 diagnostic methodology

1. Capture exact symptom, timestamp, user, namespace, endpoint, request ID and reproduction.
2. Inspect desired custom resource, generation, observed generation, conditions and owning controller.
3. Inspect Deployment or StatefulSet, ReplicaSet, Pod, init containers, sidecars, probes and termination.
4. Inspect requests, limits, quota, selectors, affinity, taints, tolerations, priority, queue and PVC topology.
5. Inspect image digest, entrypoint, environment, mounts, certificates, runtime flags, GPU visibility and permissions.
6. Inspect DNS, service selector, EndpointSlice, route or gateway, TLS, NetworkPolicy, proxy and firewall.
7. Inspect PVC, S3 endpoint, credentials, bucket policy, object path, capacity, latency and throughput.
8. Correlate logs, metrics, traces, audit and events by time.
9. Correlate recent Git, Operator, node, certificate, secret, quota and policy changes.
10. Apply the smallest reversible remediation with a declared rollback trigger.

Collect events early because they expire. Read init-container and sidecar logs as well as the primary container.


## Security and compliance

1. Use enterprise groups and least privilege.
2. Separate platform admin, tenant admin, model approval, production deployment and audit roles.
3. Use private network paths where required.
4. Encrypt in transit and at rest.
5. Use external or short-lived secrets and test rotation.
6. Pin and sign images and artifacts.
7. Apply default deny and explicit allows.
8. Retain audit and promotion evidence.
9. Mask prompts, tokens and sensitive features in logs.
10. Threat-model model theft, injection, exfiltration and supply-chain compromise.

## Performance and capacity

1. Baseline before tuning.
2. Separate cold start, model load, queue, prefill, decode, storage and network latency.
3. Size CPU, memory, storage and accelerators from measurements.
4. Reserve headroom for failover and upgrades.
5. Use representative prompt, output, batch and concurrency distributions.
6. Track p50, p95, p99, max, errors, saturation and queue.
7. Test noisy-neighbor behavior.
8. Validate GPU and NUMA topology.
9. Correlate utilization, memory, power, temperature and throughput.
10. Tune one variable at a time with rollback thresholds.

## Backup and recovery

1. Inventory every stateful dependency.
2. Define RPO and RTO by dependency.
3. Protect project and required cluster-scoped resources.
4. Snapshot PVCs with supported CSI mechanisms.
5. Replicate or back up S3 objects and versions.
6. Protect external databases consistently.
7. Keep backup credentials outside the cluster.
8. Restore into isolation first.
9. Rotate credentials after restore where required.
10. Validate function, security, integrity and performance.

## Monitoring and SLO design

### Indicator 1: onboarding time

- Define exactly how `onboarding time` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 2: quota utilization

- Define exactly how `quota utilization` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 3: GPU fairness

- Define exactly how `GPU fairness` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 4: policy compliance

- Define exactly how `policy compliance` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 5: cost per tenant

- Define exactly how `cost per tenant` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 6: idle hours

- Define exactly how `idle hours` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 7: support tickets

- Define exactly how `support tickets` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

Use symptom alerts for paging and cause alerts for context. Use multi-window burn-rate alerts for user-facing SLOs.

## Failure-injection lab

1. Block a required egress path with NetworkPolicy. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
2. Use a test-invalid credential. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
3. Scale a dependency to zero. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
4. Exhaust test quota or storage. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
5. Use an invalid image digest. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
6. Use an incompatible runtime or model path. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
7. Drain a worker node. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
8. Expire a test certificate. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
9. Inject dependency latency. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
10. Execute rollback and measure restoration. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.

## L3 operational drill catalog

Each drill is a mini interview case and a production rehearsal. Execute only in approved non-production environments.

### Drill 001 — tenant boundary / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 002 — namespace tenancy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 003 — showback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 004 — chargeback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 005 — golden path / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 006 — platform team / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 007 — data owner / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 008 — shared service / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 009 — quota hierarchy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 010 — noisy neighbor / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 011 — tenant boundary / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 012 — namespace tenancy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 013 — showback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 014 — chargeback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 015 — golden path / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 016 — platform team / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 017 — data owner / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 018 — shared service / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 019 — quota hierarchy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 020 — noisy neighbor / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 021 — tenant boundary / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 022 — namespace tenancy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 023 — showback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 024 — chargeback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 025 — golden path / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 026 — platform team / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 027 — data owner / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 028 — shared service / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 029 — quota hierarchy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 030 — noisy neighbor / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 031 — tenant boundary / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 032 — namespace tenancy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 033 — showback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 034 — chargeback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 035 — golden path / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 036 — platform team / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 037 — data owner / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 038 — shared service / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 039 — quota hierarchy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 040 — noisy neighbor / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 041 — tenant boundary / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 042 — namespace tenancy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 043 — showback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 044 — chargeback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 045 — golden path / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 046 — platform team / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 047 — data owner / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 048 — shared service / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 049 — quota hierarchy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 050 — noisy neighbor / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 051 — tenant boundary / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 052 — namespace tenancy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 053 — showback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 054 — chargeback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 055 — golden path / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 056 — platform team / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 057 — data owner / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 058 — shared service / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 059 — quota hierarchy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 060 — noisy neighbor / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 061 — tenant boundary / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 062 — namespace tenancy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 063 — showback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 064 — chargeback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 065 — golden path / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 066 — platform team / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 067 — data owner / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 068 — shared service / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 069 — quota hierarchy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 070 — noisy neighbor / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 071 — tenant boundary / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 072 — namespace tenancy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 073 — showback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 074 — chargeback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 075 — golden path / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 076 — platform team / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 077 — data owner / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 078 — shared service / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 079 — quota hierarchy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 080 — noisy neighbor / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 081 — tenant boundary / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 082 — namespace tenancy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 083 — showback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 084 — chargeback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 085 — golden path / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 086 — platform team / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 087 — data owner / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 088 — shared service / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 089 — quota hierarchy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 090 — noisy neighbor / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 091 — tenant boundary / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 092 — namespace tenancy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 093 — showback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 094 — chargeback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 095 — golden path / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 096 — platform team / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 097 — data owner / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 098 — shared service / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 099 — quota hierarchy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 100 — noisy neighbor / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 101 — tenant boundary / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 102 — namespace tenancy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 103 — showback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 104 — chargeback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 105 — golden path / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 106 — platform team / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 107 — data owner / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 108 — shared service / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 109 — quota hierarchy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 110 — noisy neighbor / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 111 — tenant boundary / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 112 — namespace tenancy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 113 — showback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 114 — chargeback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 115 — golden path / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 116 — platform team / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 117 — data owner / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 118 — shared service / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 119 — quota hierarchy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 120 — noisy neighbor / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 121 — tenant boundary / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 122 — namespace tenancy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 123 — showback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 124 — chargeback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 125 — golden path / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 126 — platform team / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 127 — data owner / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 128 — shared service / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 129 — quota hierarchy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 130 — noisy neighbor / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 131 — tenant boundary / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 132 — namespace tenancy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 133 — showback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 134 — chargeback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 135 — golden path / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 136 — platform team / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 137 — data owner / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 138 — shared service / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 139 — quota hierarchy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 140 — noisy neighbor / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 141 — tenant boundary / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 142 — namespace tenancy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 143 — showback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 144 — chargeback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 145 — golden path / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 146 — platform team / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 147 — data owner / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 148 — shared service / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 149 — quota hierarchy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 150 — noisy neighbor / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 151 — tenant boundary / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 152 — namespace tenancy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 153 — showback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 154 — chargeback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 155 — golden path / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 156 — platform team / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 157 — data owner / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 158 — shared service / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 159 — quota hierarchy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 160 — noisy neighbor / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 161 — tenant boundary / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 162 — namespace tenancy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 163 — showback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 164 — chargeback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 165 — golden path / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 166 — platform team / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 167 — data owner / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 168 — shared service / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 169 — quota hierarchy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 170 — noisy neighbor / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 171 — tenant boundary / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 172 — namespace tenancy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 173 — showback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 174 — chargeback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 175 — golden path / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 176 — platform team / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 177 — data owner / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 178 — shared service / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 179 — quota hierarchy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 180 — noisy neighbor / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 181 — tenant boundary / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 182 — namespace tenancy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 183 — showback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 184 — chargeback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 185 — golden path / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 186 — platform team / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 187 — data owner / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 188 — shared service / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 189 — quota hierarchy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 190 — noisy neighbor / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 191 — tenant boundary / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 192 — namespace tenancy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 193 — showback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 194 — chargeback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 195 — golden path / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 196 — platform team / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 197 — data owner / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 198 — shared service / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 199 — quota hierarchy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 200 — noisy neighbor / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 201 — tenant boundary / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 202 — namespace tenancy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 203 — showback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 204 — chargeback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 205 — golden path / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 206 — platform team / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 207 — data owner / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 208 — shared service / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 209 — quota hierarchy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 210 — noisy neighbor / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 211 — tenant boundary / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 212 — namespace tenancy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 213 — showback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 214 — chargeback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 215 — golden path / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 216 — platform team / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 217 — data owner / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 218 — shared service / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 219 — quota hierarchy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 220 — noisy neighbor / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 221 — tenant boundary / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 222 — namespace tenancy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 223 — showback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 224 — chargeback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 225 — golden path / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 226 — platform team / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 227 — data owner / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 228 — shared service / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 229 — quota hierarchy / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 230 — noisy neighbor / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 231 — tenant boundary / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 232 — namespace tenancy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 233 — showback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 234 — chargeback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 235 — golden path / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 236 — platform team / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 237 — data owner / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 238 — shared service / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 239 — quota hierarchy / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 240 — noisy neighbor / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 241 — tenant boundary / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 242 — namespace tenancy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 243 — showback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 244 — chargeback / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 245 — golden path / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 246 — platform team / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 247 — data owner / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 248 — shared service / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 249 — quota hierarchy / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 250 — noisy neighbor / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 251 — tenant boundary / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 252 — namespace tenancy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 253 — showback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 254 — chargeback / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 255 — golden path / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 256 — platform team / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 257 — data owner / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 258 — shared service / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 259 — quota hierarchy / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 260 — noisy neighbor / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 261 — tenant boundary / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 262 — namespace tenancy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 263 — showback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 264 — chargeback / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 265 — golden path / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 266 — platform team / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 267 — data owner / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 268 — shared service / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 269 — quota hierarchy / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 270 — noisy neighbor / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 271 — tenant boundary / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 272 — namespace tenancy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 273 — showback / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 274 — chargeback / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 275 — golden path / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 276 — platform team / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `platform team`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `platform team` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 277 — data owner / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `data owner`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `data owner` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 278 — shared service / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `shared service`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `shared service` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 279 — quota hierarchy / policy bypass

1. **Context:** A production-like workload shows `policy bypass` while validating `quota hierarchy`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `quota hierarchy` works.
5. **Metric correlation:** Compare the incident timestamp with `quota utilization` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 280 — noisy neighbor / cost gaps

1. **Context:** A production-like workload shows `cost gaps` while validating `noisy neighbor`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `noisy neighbor` works.
5. **Metric correlation:** Compare the incident timestamp with `support tickets` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 281 — tenant boundary / secret exposure

1. **Context:** A production-like workload shows `secret exposure` while validating `tenant boundary`.
2. **Primary resource:** Inspect `Group`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `tenant boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `cost per tenant` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 282 — namespace tenancy / orphan resources

1. **Context:** A production-like workload shows `orphan resources` while validating `namespace tenancy`.
2. **Primary resource:** Inspect `ResourceQuota`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace tenancy` works.
5. **Metric correlation:** Compare the incident timestamp with `GPU fairness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 283 — showback / quota starvation

1. **Context:** A production-like workload shows `quota starvation` while validating `showback`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `showback` works.
5. **Metric correlation:** Compare the incident timestamp with `onboarding time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 284 — chargeback / GPU monopolization

1. **Context:** A production-like workload shows `GPU monopolization` while validating `chargeback`.
2. **Primary resource:** Inspect `ClusterQueue`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `chargeback` works.
5. **Metric correlation:** Compare the incident timestamp with `idle hours` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 285 — golden path / cross-project access

1. **Context:** A production-like workload shows `cross-project access` while validating `golden path`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `golden path` works.
5. **Metric correlation:** Compare the incident timestamp with `policy compliance` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `cost labels` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.


## Senior interview questions and answer points

### Interview question 001

**Question:** Which requirements must be clarified first? Apply it to `namespace tenancy` and `secret exposure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 002

**Question:** Which layer do you inspect first and why? Apply it to `chargeback` and `orphan resources`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 003

**Question:** How do you separate symptom from root cause? Apply it to `platform team` and `quota starvation`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 004

**Question:** Which objects are Operator managed? Apply it to `shared service` and `GPU monopolization`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 005

**Question:** What is the safest rollback? Apply it to `noisy neighbor` and `cross-project access`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 006

**Question:** How do you prove isolation? Apply it to `namespace tenancy` and `policy bypass`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 007

**Question:** How do you retain audit evidence? Apply it to `chargeback` and `cost gaps`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 008

**Question:** Which metrics prove success? Apply it to `platform team` and `secret exposure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 009

**Question:** How do you test failure safely? Apply it to `shared service` and `orphan resources`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 010

**Question:** What changes in an air gap? Apply it to `noisy neighbor` and `quota starvation`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 011

**Question:** How do quota and scheduling interact? Apply it to `namespace tenancy` and `GPU monopolization`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 012

**Question:** How do you protect credentials? Apply it to `chargeback` and `cross-project access`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 013

**Question:** Which compatibility checks are mandatory? Apply it to `platform team` and `policy bypass`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 014

**Question:** How do you prevent drift? Apply it to `shared service` and `cost gaps`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 015

**Question:** How do you reserve capacity? Apply it to `noisy neighbor` and `secret exposure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 016

**Question:** Which requirements must be clarified first? Apply it to `namespace tenancy` and `orphan resources`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 017

**Question:** Which layer do you inspect first and why? Apply it to `chargeback` and `quota starvation`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 018

**Question:** How do you separate symptom from root cause? Apply it to `platform team` and `GPU monopolization`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 019

**Question:** Which objects are Operator managed? Apply it to `shared service` and `cross-project access`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 020

**Question:** What is the safest rollback? Apply it to `noisy neighbor` and `policy bypass`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 021

**Question:** How do you prove isolation? Apply it to `namespace tenancy` and `cost gaps`.

**Answer points:**

- Begin with business impact and acceptance criteria.

## Size boundary note

Core implementation, drills, interview questions and checklists retained within the requested line range.
