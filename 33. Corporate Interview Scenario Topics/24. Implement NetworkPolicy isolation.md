# 24. Implement NetworkPolicy isolation

> Corporate Interview Scenario Guide — Red Hat OpenShift AI L3 / Senior Platform Engineer

- **Production baseline:** Red Hat OpenShift AI Self-Managed 3.4 and OpenShift Container Platform 4.20
- **Generated:** 2026-07-21
- **Audience:** 10+ year Linux, Kubernetes, OpenShift, DevOps and production-support engineers
- **Safety:** Validate fields with `oc api-resources` and `oc explain` before applying manifests.


## Scenario objective

Establish default-deny tenant isolation and explicitly allow DNS, APIs, object storage, pipeline services, monitoring and approved model consumers.


## Senior interview answer structure

1. Clarify business impact, data class, tenancy, SLO, RPO, RTO, compliance and operating ownership.
2. Verify the supported OpenShift, OpenShift AI and dependent Operator matrix.
3. Draw identity, request, storage, compute, serving, monitoring and recovery flows.
4. Define namespace, RBAC, quota, NetworkPolicy, secret, image and artifact boundaries.
5. Build a minimum production pattern in non-production.
6. Run functional, security, performance, failure, restore and upgrade tests.
7. Promote through GitOps with approvals and evidence.
8. Operate with SLOs, alerts, capacity, backup, incident and change management.


## Core resources

- `NetworkPolicy` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `AdminNetworkPolicy` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Namespace` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Service` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Route` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `EndpointSlice` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Pod` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.

## Core concepts

- **ingress:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **egress:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **default deny:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **namespace selector:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **pod selector:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **DNS:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **API server:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **S3 endpoint:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **monitoring:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **zero trust:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.

## Failure modes

- **DNS blocked:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **S3 blocked:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **dashboard lost:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **endpoint unreachable:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **scrape blocked:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **overbroad allow:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **cross-tenant path:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.

## Service indicators

- **policy coverage:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **denied flows:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **connectivity pass:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **cross-tenant denial:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **incidents:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **exceptions:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **drift:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.

## Required controls

- **default deny:** implement declaratively, test negatively, review periodically and retain evidence.
- **named allows:** implement declaratively, test negatively, review periodically and retain evidence.
- **connectivity tests:** implement declaratively, test negatively, review periodically and retain evidence.
- **namespace labels:** implement declaratively, test negatively, review periodically and retain evidence.
- **change review:** implement declaratively, test negatively, review periodically and retain evidence.
- **flow logs:** implement declaratively, test negatively, review periodically and retain evidence.
- **policy linting:** implement declaratively, test negatively, review periodically and retain evidence.

## Reference architecture

1. **Enterprise services:** identity, DNS, NTP, PKI, secrets manager, Git, registry, S3, SIEM, ticketing and backup.
2. **OpenShift:** control plane, ingress or Gateway API, OVN-Kubernetes, monitoring, registry, storage, OAuth and policy.
3. **OpenShift AI:** Operator, DSCInitialization, DataScienceCluster, dashboard, workbenches, pipelines, KServe, registry and TrustyAI as enabled.
4. **Tenant:** namespace, groups, service accounts, connections, workbenches, pipeline server, runs, models, quota and policies.
5. **Operations:** GitOps, CI/CD, vulnerability management, SLOs, capacity, backup, restore, upgrade and incident response.

Every route, runtime, model, bucket, PVC, credential, quota and exception must have a named owner and lifecycle.


## Project creation from scratch

### Phase 0 — supported-version discovery

1. Record cluster version, channel, CSV, dependent Operators, storage classes, ingress, identity, proxy and disconnected status.
2. Read release notes, known issues, deprecated and removed features.
3. Run `oc api-resources` and `oc explain` for every custom resource.
4. Create a RACI across platform, security, network, storage, data science and application teams.

### Phase 1 — requirements

1. Define users, data classes, model type, request pattern, peak concurrency, latency, availability, RPO, RTO and retention.
2. Convert each requirement into a measurable acceptance test.
3. Record constraints such as air gap, private connectivity, HSM, GPU scarcity and regulated change.
4. Document assumptions and explicit exclusions.

### Phase 2 — dependency readiness

1. Validate cluster operators, DNS, NTP, ingress, storage, registry, monitoring, identity, certificate trust and backup.
2. Verify node labels, taints, capacity and scheduling.
3. Verify credentials come from an approved secret system.
4. Create an automated preflight report.

### Phase 3 — tenant foundation

1. Create namespaces with owner, environment, cost center, data class and lifecycle labels.
2. Bind groups rather than individuals where practical.
3. Apply ResourceQuota, LimitRange, default-deny and required allow policies.
4. Register approved images, hardware profiles, runtimes and connections.

### Phase 4 — implementation

1. Deploy the minimum representative workload.
2. Pin images and models by digest or immutable version.
3. Use Git overlays for environment differences.
4. Expose only required endpoints and verify authentication and authorization.

### Phase 5 — validation

1. Run positive and negative functional tests.
2. Test scheduling, images, storage, DNS, TLS, RBAC, policy and quota.
3. Run load, soak and capacity tests.
4. Capture YAML, events, logs, metrics and test output.

### Phase 6 — failure and recovery

1. Inject one dependency failure at a time in non-production.
2. Confirm actionable alerts.
3. Execute remediation and rollback.
4. Measure restoration against SLO and RTO.

### Phase 7 — production promotion

1. Require review, policy checks, artifact verification, approvals and change record.
2. Use canary or controlled traffic migration.
3. Observe a defined bake period.
4. Retain last-known-good manifests and artifacts.

### Phase 8 — day-2 operations

1. Review capacity, latency, errors, security, backup age, certificate expiry and deprecations.
2. Patch and upgrade through rehearsal environments.
3. Review access and exceptions.
4. Improve the runbook after every incident and exercise.


## Command and evidence toolkit

Run read-only commands first and preserve timestamped output.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A
oc get subscriptions.operators.coreos.com -A
oc get installplans -A
oc get datasciencecluster -A -o yaml
oc get dscinitialization -A -o yaml
oc get pods -A --sort-by=.metadata.creationTimestamp
oc get events -A --sort-by=.lastTimestamp
oc get nodes -o wide
oc describe node <node>
oc auth can-i --list -n <project>
oc get resourcequota,limitrange -n <project> -o yaml
oc get networkpolicy -n <project> -o yaml
oc get secrets,pvc -n <project>
oc get routes,services,endpointslices -n <project>
oc logs <pod> -n <project> --all-containers --prefix
oc adm inspect namespace/<project>
oc adm must-gather
```

Evidence path: `<incident>/<timestamp>/<cluster>/<namespace>/<resource>/<command-output>`.

For every command record expected result, actual result and interpretation. Output without interpretation is not diagnosis.

## Implementation manifests

### Version-adaptive preflight

```yaml
#!/usr/bin/env bash
set -euo pipefail
oc version
oc get clusterversion
oc get csv -A
oc get datasciencecluster -A
oc api-resources | grep -Ei 'inferenceservice|servingruntime|hardwareprofile|notebook|trustyai'
oc explain inferenceservice.spec 2>/dev/null || true
oc get co
```

### Default deny

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: ai-team-a-dev
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
```

Examples are patterns, not guaranteed schemas for every release. Confirm installed CRDs and Operator ownership.

## L3 diagnostic methodology

1. Capture exact symptom, timestamp, user, namespace, endpoint, request ID and reproduction.
2. Inspect desired custom resource, generation, observed generation, conditions and owning controller.
3. Inspect Deployment or StatefulSet, ReplicaSet, Pod, init containers, sidecars, probes and termination.
4. Inspect requests, limits, quota, selectors, affinity, taints, tolerations, priority, queue and PVC topology.
5. Inspect image digest, entrypoint, environment, mounts, certificates, runtime flags, GPU visibility and permissions.
6. Inspect DNS, service selector, EndpointSlice, route or gateway, TLS, NetworkPolicy, proxy and firewall.
7. Inspect PVC, S3 endpoint, credentials, bucket policy, object path, capacity, latency and throughput.
8. Correlate logs, metrics, traces, audit and events by time.
9. Correlate recent Git, Operator, node, certificate, secret, quota and policy changes.
10. Apply the smallest reversible remediation with a declared rollback trigger.

Collect events early because they expire. Read init-container and sidecar logs as well as the primary container.


## Security and compliance

1. Use enterprise groups and least privilege.
2. Separate platform admin, tenant admin, model approval, production deployment and audit roles.
3. Use private network paths where required.
4. Encrypt in transit and at rest.
5. Use external or short-lived secrets and test rotation.
6. Pin and sign images and artifacts.
7. Apply default deny and explicit allows.
8. Retain audit and promotion evidence.
9. Mask prompts, tokens and sensitive features in logs.
10. Threat-model model theft, injection, exfiltration and supply-chain compromise.

## Performance and capacity

1. Baseline before tuning.
2. Separate cold start, model load, queue, prefill, decode, storage and network latency.
3. Size CPU, memory, storage and accelerators from measurements.
4. Reserve headroom for failover and upgrades.
5. Use representative prompt, output, batch and concurrency distributions.
6. Track p50, p95, p99, max, errors, saturation and queue.
7. Test noisy-neighbor behavior.
8. Validate GPU and NUMA topology.
9. Correlate utilization, memory, power, temperature and throughput.
10. Tune one variable at a time with rollback thresholds.

## Backup and recovery

1. Inventory every stateful dependency.
2. Define RPO and RTO by dependency.
3. Protect project and required cluster-scoped resources.
4. Snapshot PVCs with supported CSI mechanisms.
5. Replicate or back up S3 objects and versions.
6. Protect external databases consistently.
7. Keep backup credentials outside the cluster.
8. Restore into isolation first.
9. Rotate credentials after restore where required.
10. Validate function, security, integrity and performance.

## Monitoring and SLO design

### Indicator 1: policy coverage

- Define exactly how `policy coverage` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 2: denied flows

- Define exactly how `denied flows` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 3: connectivity pass

- Define exactly how `connectivity pass` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 4: cross-tenant denial

- Define exactly how `cross-tenant denial` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 5: incidents

- Define exactly how `incidents` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 6: exceptions

- Define exactly how `exceptions` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 7: drift

- Define exactly how `drift` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

Use symptom alerts for paging and cause alerts for context. Use multi-window burn-rate alerts for user-facing SLOs.

## Failure-injection lab

1. Block a required egress path with NetworkPolicy. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
2. Use a test-invalid credential. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
3. Scale a dependency to zero. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
4. Exhaust test quota or storage. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
5. Use an invalid image digest. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
6. Use an incompatible runtime or model path. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
7. Drain a worker node. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
8. Expire a test certificate. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
9. Inject dependency latency. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
10. Execute rollback and measure restoration. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.

## L3 operational drill catalog

Each drill is a mini interview case and a production rehearsal. Execute only in approved non-production environments.

### Drill 001 — ingress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `ingress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 002 — egress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `egress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 003 — default deny / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `default deny`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 004 — namespace selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 005 — pod selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 006 — DNS / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `DNS`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 007 — API server / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `API server`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 008 — S3 endpoint / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 009 — monitoring / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `monitoring`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 010 — zero trust / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 011 — ingress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `ingress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 012 — egress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `egress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 013 — default deny / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `default deny`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 014 — namespace selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `namespace selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 015 — pod selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `pod selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 016 — DNS / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `DNS`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 017 — API server / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `API server`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 018 — S3 endpoint / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 019 — monitoring / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 020 — zero trust / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `zero trust`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 021 — ingress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `ingress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 022 — egress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `egress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 023 — default deny / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `default deny`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 024 — namespace selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 025 — pod selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 026 — DNS / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `DNS`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 027 — API server / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `API server`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 028 — S3 endpoint / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 029 — monitoring / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `monitoring`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 030 — zero trust / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `zero trust`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 031 — ingress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `ingress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 032 — egress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `egress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 033 — default deny / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `default deny`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 034 — namespace selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `namespace selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 035 — pod selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `pod selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 036 — DNS / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `DNS`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 037 — API server / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `API server`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 038 — S3 endpoint / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 039 — monitoring / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 040 — zero trust / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 041 — ingress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `ingress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 042 — egress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `egress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 043 — default deny / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `default deny`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 044 — namespace selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `namespace selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 045 — pod selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 046 — DNS / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `DNS`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 047 — API server / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `API server`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 048 — S3 endpoint / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 049 — monitoring / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `monitoring`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 050 — zero trust / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `zero trust`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 051 — ingress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `ingress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 052 — egress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `egress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 053 — default deny / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `default deny`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 054 — namespace selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 055 — pod selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `pod selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 056 — DNS / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `DNS`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 057 — API server / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `API server`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 058 — S3 endpoint / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 059 — monitoring / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 060 — zero trust / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 061 — ingress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `ingress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 062 — egress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `egress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 063 — default deny / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `default deny`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 064 — namespace selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `namespace selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 065 — pod selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `pod selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 066 — DNS / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `DNS`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 067 — API server / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `API server`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 068 — S3 endpoint / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 069 — monitoring / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `monitoring`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 070 — zero trust / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `zero trust`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 071 — ingress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `ingress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 072 — egress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `egress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 073 — default deny / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `default deny`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 074 — namespace selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 075 — pod selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 076 — DNS / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `DNS`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 077 — API server / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `API server`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 078 — S3 endpoint / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 079 — monitoring / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `monitoring`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 080 — zero trust / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 081 — ingress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `ingress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 082 — egress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `egress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 083 — default deny / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `default deny`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 084 — namespace selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `namespace selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 085 — pod selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `pod selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 086 — DNS / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `DNS`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 087 — API server / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `API server`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 088 — S3 endpoint / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 089 — monitoring / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 090 — zero trust / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `zero trust`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 091 — ingress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `ingress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 092 — egress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `egress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 093 — default deny / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `default deny`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 094 — namespace selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 095 — pod selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 096 — DNS / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `DNS`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 097 — API server / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `API server`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 098 — S3 endpoint / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 099 — monitoring / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `monitoring`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 100 — zero trust / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `zero trust`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 101 — ingress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `ingress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 102 — egress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `egress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 103 — default deny / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `default deny`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 104 — namespace selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `namespace selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 105 — pod selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `pod selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 106 — DNS / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `DNS`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 107 — API server / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `API server`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 108 — S3 endpoint / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 109 — monitoring / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 110 — zero trust / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 111 — ingress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `ingress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 112 — egress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `egress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 113 — default deny / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `default deny`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 114 — namespace selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `namespace selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 115 — pod selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 116 — DNS / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `DNS`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 117 — API server / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `API server`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 118 — S3 endpoint / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 119 — monitoring / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `monitoring`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 120 — zero trust / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `zero trust`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 121 — ingress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `ingress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 122 — egress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `egress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 123 — default deny / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `default deny`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 124 — namespace selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 125 — pod selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `pod selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 126 — DNS / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `DNS`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 127 — API server / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `API server`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 128 — S3 endpoint / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 129 — monitoring / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 130 — zero trust / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 131 — ingress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `ingress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 132 — egress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `egress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 133 — default deny / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `default deny`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 134 — namespace selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `namespace selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 135 — pod selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `pod selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 136 — DNS / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `DNS`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 137 — API server / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `API server`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 138 — S3 endpoint / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 139 — monitoring / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `monitoring`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 140 — zero trust / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `zero trust`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 141 — ingress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `ingress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 142 — egress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `egress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 143 — default deny / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `default deny`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 144 — namespace selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 145 — pod selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 146 — DNS / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `DNS`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 147 — API server / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `API server`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 148 — S3 endpoint / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 149 — monitoring / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `monitoring`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 150 — zero trust / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 151 — ingress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `ingress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 152 — egress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `egress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 153 — default deny / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `default deny`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 154 — namespace selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `namespace selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 155 — pod selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `pod selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 156 — DNS / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `DNS`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 157 — API server / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `API server`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 158 — S3 endpoint / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 159 — monitoring / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 160 — zero trust / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `zero trust`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 161 — ingress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `ingress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 162 — egress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `egress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 163 — default deny / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `default deny`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 164 — namespace selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 165 — pod selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 166 — DNS / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `DNS`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 167 — API server / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `API server`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 168 — S3 endpoint / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 169 — monitoring / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `monitoring`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 170 — zero trust / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `zero trust`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 171 — ingress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `ingress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 172 — egress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `egress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 173 — default deny / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `default deny`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 174 — namespace selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `namespace selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 175 — pod selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `pod selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 176 — DNS / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `DNS`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 177 — API server / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `API server`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 178 — S3 endpoint / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 179 — monitoring / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 180 — zero trust / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 181 — ingress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `ingress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 182 — egress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `egress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 183 — default deny / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `default deny`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 184 — namespace selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `namespace selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 185 — pod selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 186 — DNS / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `DNS`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 187 — API server / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `API server`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 188 — S3 endpoint / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 189 — monitoring / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `monitoring`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 190 — zero trust / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `zero trust`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 191 — ingress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `ingress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 192 — egress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `egress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 193 — default deny / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `default deny`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 194 — namespace selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 195 — pod selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `pod selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 196 — DNS / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `DNS`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 197 — API server / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `API server`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 198 — S3 endpoint / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 199 — monitoring / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 200 — zero trust / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 201 — ingress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `ingress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 202 — egress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `egress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 203 — default deny / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `default deny`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 204 — namespace selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `namespace selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 205 — pod selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `pod selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 206 — DNS / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `DNS`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 207 — API server / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `API server`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 208 — S3 endpoint / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 209 — monitoring / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `monitoring`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 210 — zero trust / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `zero trust`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 211 — ingress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `ingress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 212 — egress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `egress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 213 — default deny / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `default deny`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 214 — namespace selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 215 — pod selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 216 — DNS / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `DNS`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 217 — API server / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `API server`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 218 — S3 endpoint / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 219 — monitoring / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `monitoring`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 220 — zero trust / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 221 — ingress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `ingress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 222 — egress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `egress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 223 — default deny / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `default deny`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 224 — namespace selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `namespace selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 225 — pod selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `pod selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 226 — DNS / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `DNS`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 227 — API server / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `API server`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 228 — S3 endpoint / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 229 — monitoring / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 230 — zero trust / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `zero trust`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 231 — ingress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `ingress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 232 — egress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `egress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 233 — default deny / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `default deny`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 234 — namespace selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 235 — pod selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 236 — DNS / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `DNS`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 237 — API server / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `API server`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 238 — S3 endpoint / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 239 — monitoring / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `monitoring`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 240 — zero trust / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `zero trust`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 241 — ingress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `ingress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 242 — egress / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `egress`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 243 — default deny / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `default deny`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 244 — namespace selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `namespace selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 245 — pod selector / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `pod selector`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 246 — DNS / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `DNS`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 247 — API server / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `API server`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 248 — S3 endpoint / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 249 — monitoring / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 250 — zero trust / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 251 — ingress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `ingress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 252 — egress / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `egress`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 253 — default deny / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `default deny`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 254 — namespace selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `namespace selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 255 — pod selector / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 256 — DNS / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `DNS`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 257 — API server / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `API server`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 258 — S3 endpoint / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 259 — monitoring / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `monitoring`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 260 — zero trust / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `zero trust`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 261 — ingress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `ingress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 262 — egress / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `egress`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 263 — default deny / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `default deny`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 264 — namespace selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 265 — pod selector / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `pod selector`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 266 — DNS / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `DNS`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 267 — API server / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `API server`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 268 — S3 endpoint / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 269 — monitoring / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `monitoring`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 270 — zero trust / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `zero trust`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 271 — ingress / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `ingress`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 272 — egress / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `egress`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 273 — default deny / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `default deny`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 274 — namespace selector / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `namespace selector`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 275 — pod selector / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `pod selector`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 276 — DNS / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `DNS`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `DNS` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 277 — API server / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `API server`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `API server` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 278 — S3 endpoint / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `S3 endpoint`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 279 — monitoring / endpoint unreachable

1. **Context:** A production-like workload shows `endpoint unreachable` while validating `monitoring`.
2. **Primary resource:** Inspect `Route`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `monitoring` works.
5. **Metric correlation:** Compare the incident timestamp with `denied flows` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 280 — zero trust / cross-tenant path

1. **Context:** A production-like workload shows `cross-tenant path` while validating `zero trust`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `zero trust` works.
5. **Metric correlation:** Compare the incident timestamp with `drift` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 281 — ingress / dashboard lost

1. **Context:** A production-like workload shows `dashboard lost` while validating `ingress`.
2. **Primary resource:** Inspect `AdminNetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `ingress` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 282 — egress / overbroad allow

1. **Context:** A production-like workload shows `overbroad allow` while validating `egress`.
2. **Primary resource:** Inspect `Service`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `egress` works.
5. **Metric correlation:** Compare the incident timestamp with `connectivity pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 283 — default deny / S3 blocked

1. **Context:** A production-like workload shows `S3 blocked` while validating `default deny`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `default deny` works.
5. **Metric correlation:** Compare the incident timestamp with `policy coverage` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 284 — namespace selector / scrape blocked

1. **Context:** A production-like workload shows `scrape blocked` while validating `namespace selector`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `namespace selector` works.
5. **Metric correlation:** Compare the incident timestamp with `exceptions` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 285 — pod selector / DNS blocked

1. **Context:** A production-like workload shows `DNS blocked` while validating `pod selector`.
2. **Primary resource:** Inspect `Namespace`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `pod selector` works.
5. **Metric correlation:** Compare the incident timestamp with `cross-tenant denial` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `policy linting` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.


## Senior interview questions and answer points

### Interview question 001

**Question:** Which requirements must be clarified first? Apply it to `egress` and `dashboard lost`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 002

**Question:** Which layer do you inspect first and why? Apply it to `namespace selector` and `overbroad allow`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 003

**Question:** How do you separate symptom from root cause? Apply it to `DNS` and `S3 blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 004

**Question:** Which objects are Operator managed? Apply it to `S3 endpoint` and `scrape blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 005

**Question:** What is the safest rollback? Apply it to `zero trust` and `DNS blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 006

**Question:** How do you prove isolation? Apply it to `egress` and `endpoint unreachable`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 007

**Question:** How do you retain audit evidence? Apply it to `namespace selector` and `cross-tenant path`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 008

**Question:** Which metrics prove success? Apply it to `DNS` and `dashboard lost`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 009

**Question:** How do you test failure safely? Apply it to `S3 endpoint` and `overbroad allow`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 010

**Question:** What changes in an air gap? Apply it to `zero trust` and `S3 blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 011

**Question:** How do quota and scheduling interact? Apply it to `egress` and `scrape blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 012

**Question:** How do you protect credentials? Apply it to `namespace selector` and `DNS blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 013

**Question:** Which compatibility checks are mandatory? Apply it to `DNS` and `endpoint unreachable`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 014

**Question:** How do you prevent drift? Apply it to `S3 endpoint` and `cross-tenant path`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 015

**Question:** How do you reserve capacity? Apply it to `zero trust` and `dashboard lost`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 016

**Question:** Which requirements must be clarified first? Apply it to `egress` and `overbroad allow`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 017

**Question:** Which layer do you inspect first and why? Apply it to `namespace selector` and `S3 blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 018

**Question:** How do you separate symptom from root cause? Apply it to `DNS` and `scrape blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 019

**Question:** Which objects are Operator managed? Apply it to `S3 endpoint` and `DNS blocked`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 020

**Question:** What is the safest rollback? Apply it to `zero trust` and `endpoint unreachable`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 021

**Question:** How do you prove isolation? Apply it to `egress` and `cross-tenant path`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 022

**Question:** How do you retain audit evidence? Apply it to `namespace selector` and `dashboard lost`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 023

## Size boundary note

Core implementation, drills, interview questions and checklists retained within the requested line range.
