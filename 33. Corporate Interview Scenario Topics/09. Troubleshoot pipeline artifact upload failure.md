# 09. Troubleshoot pipeline artifact upload failure

> Corporate Interview Scenario Guide — Red Hat OpenShift AI L3 / Senior Platform Engineer

- **Production baseline:** Red Hat OpenShift AI Self-Managed 3.4 and OpenShift Container Platform 4.20
- **Generated:** 2026-07-21
- **Audience:** 10+ year Linux, Kubernetes, OpenShift, DevOps and production-support engineers
- **Safety:** Validate fields with `oc api-resources` and `oc explain` before applying manifests.


## Scenario objective

Isolate failures in the pipeline artifact path from component output through the artifact-copy sidecar to S3-compatible object storage.


## Senior interview answer structure

1. Clarify business impact, data class, tenancy, SLO, RPO, RTO, compliance and operating ownership.
2. Verify the supported OpenShift, OpenShift AI and dependent Operator matrix.
3. Draw identity, request, storage, compute, serving, monitoring and recovery flows.
4. Define namespace, RBAC, quota, NetworkPolicy, secret, image and artifact boundaries.
5. Build a minimum production pattern in non-production.
6. Run functional, security, performance, failure, restore and upgrade tests.
7. Promote through GitOps with approvals and evidence.
8. Operate with SLOs, alerts, capacity, backup, incident and change management.


## Core resources

- `PipelineRun` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Pod` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Container` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Secret` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `PipelineServer` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `NetworkPolicy` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ServiceAccount` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ConfigMap` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.

## Core concepts

- **step-copy-artifact:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **artifact URI:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **S3 credentials:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **signature:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **CA bundle:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **multipart:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **proxy:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **endpoint:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **bucket policy:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **clock skew:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.

## Failure modes

- **AccessDenied:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **SignatureDoesNotMatch:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **x509 unknown authority:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **timeout:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **NoSuchBucket:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **entity too large:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **DNS failure:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.

## Service indicators

- **artifact success:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **copy-sidecar errors:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **upload duration:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **retries:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **S3 4xx:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **S3 5xx:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **failed runs:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.

## Required controls

- **credential test:** implement declaratively, test negatively, review periodically and retain evidence.
- **bucket policy tests:** implement declaratively, test negatively, review periodically and retain evidence.
- **CA injection:** implement declaratively, test negatively, review periodically and retain evidence.
- **NTP health:** implement declaratively, test negatively, review periodically and retain evidence.
- **connectivity tests:** implement declaratively, test negatively, review periodically and retain evidence.
- **size limits:** implement declaratively, test negatively, review periodically and retain evidence.
- **retry policy:** implement declaratively, test negatively, review periodically and retain evidence.

## Reference architecture

1. **Enterprise services:** identity, DNS, NTP, PKI, secrets manager, Git, registry, S3, SIEM, ticketing and backup.
2. **OpenShift:** control plane, ingress or Gateway API, OVN-Kubernetes, monitoring, registry, storage, OAuth and policy.
3. **OpenShift AI:** Operator, DSCInitialization, DataScienceCluster, dashboard, workbenches, pipelines, KServe, registry and TrustyAI as enabled.
4. **Tenant:** namespace, groups, service accounts, connections, workbenches, pipeline server, runs, models, quota and policies.
5. **Operations:** GitOps, CI/CD, vulnerability management, SLOs, capacity, backup, restore, upgrade and incident response.

Every route, runtime, model, bucket, PVC, credential, quota and exception must have a named owner and lifecycle.


## Project creation from scratch

### Phase 0 — supported-version discovery

1. Record cluster version, channel, CSV, dependent Operators, storage classes, ingress, identity, proxy and disconnected status.
2. Read release notes, known issues, deprecated and removed features.
3. Run `oc api-resources` and `oc explain` for every custom resource.
4. Create a RACI across platform, security, network, storage, data science and application teams.

### Phase 1 — requirements

1. Define users, data classes, model type, request pattern, peak concurrency, latency, availability, RPO, RTO and retention.
2. Convert each requirement into a measurable acceptance test.
3. Record constraints such as air gap, private connectivity, HSM, GPU scarcity and regulated change.
4. Document assumptions and explicit exclusions.

### Phase 2 — dependency readiness

1. Validate cluster operators, DNS, NTP, ingress, storage, registry, monitoring, identity, certificate trust and backup.
2. Verify node labels, taints, capacity and scheduling.
3. Verify credentials come from an approved secret system.
4. Create an automated preflight report.

### Phase 3 — tenant foundation

1. Create namespaces with owner, environment, cost center, data class and lifecycle labels.
2. Bind groups rather than individuals where practical.
3. Apply ResourceQuota, LimitRange, default-deny and required allow policies.
4. Register approved images, hardware profiles, runtimes and connections.

### Phase 4 — implementation

1. Deploy the minimum representative workload.
2. Pin images and models by digest or immutable version.
3. Use Git overlays for environment differences.
4. Expose only required endpoints and verify authentication and authorization.

### Phase 5 — validation

1. Run positive and negative functional tests.
2. Test scheduling, images, storage, DNS, TLS, RBAC, policy and quota.
3. Run load, soak and capacity tests.
4. Capture YAML, events, logs, metrics and test output.

### Phase 6 — failure and recovery

1. Inject one dependency failure at a time in non-production.
2. Confirm actionable alerts.
3. Execute remediation and rollback.
4. Measure restoration against SLO and RTO.

### Phase 7 — production promotion

1. Require review, policy checks, artifact verification, approvals and change record.
2. Use canary or controlled traffic migration.
3. Observe a defined bake period.
4. Retain last-known-good manifests and artifacts.

### Phase 8 — day-2 operations

1. Review capacity, latency, errors, security, backup age, certificate expiry and deprecations.
2. Patch and upgrade through rehearsal environments.
3. Review access and exceptions.
4. Improve the runbook after every incident and exercise.


## Command and evidence toolkit

Run read-only commands first and preserve timestamped output.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A
oc get subscriptions.operators.coreos.com -A
oc get installplans -A
oc get datasciencecluster -A -o yaml
oc get dscinitialization -A -o yaml
oc get pods -A --sort-by=.metadata.creationTimestamp
oc get events -A --sort-by=.lastTimestamp
oc get nodes -o wide
oc describe node <node>
oc auth can-i --list -n <project>
oc get resourcequota,limitrange -n <project> -o yaml
oc get networkpolicy -n <project> -o yaml
oc get secrets,pvc -n <project>
oc get routes,services,endpointslices -n <project>
oc logs <pod> -n <project> --all-containers --prefix
oc adm inspect namespace/<project>
oc adm must-gather
```

Evidence path: `<incident>/<timestamp>/<cluster>/<namespace>/<resource>/<command-output>`.

For every command record expected result, actual result and interpretation. Output without interpretation is not diagnosis.

## Implementation manifests

### Version-adaptive preflight

```yaml
#!/usr/bin/env bash
set -euo pipefail
oc version
oc get clusterversion
oc get csv -A
oc get datasciencecluster -A
oc api-resources | grep -Ei 'inferenceservice|servingruntime|hardwareprofile|notebook|trustyai'
oc explain inferenceservice.spec 2>/dev/null || true
oc get co
```

### S3 connection secret pattern

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-artifact-s3
  namespace: ai-team-a-dev
  labels:
    opendatahub.io/dashboard: "true"
  annotations:
    opendatahub.io/connection-type-ref: s3
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "<external-secret-reference>"
  AWS_SECRET_ACCESS_KEY: "<external-secret-reference>"
  AWS_S3_ENDPOINT: "https://s3.internal.example.com"
  AWS_S3_BUCKET: "ai-team-a-artifacts"
  AWS_DEFAULT_REGION: "us-east-1" 
```

### InferenceService pattern

```yaml
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: example-model
  namespace: ai-team-a-prod
spec:
  predictor:
    model:
      modelFormat:
        name: "<format>"
      runtime: "<validated-runtime>"
      storageUri: "s3://model-bucket/example-model/<immutable-version>/" 
```

Examples are patterns, not guaranteed schemas for every release. Confirm installed CRDs and Operator ownership.

## L3 diagnostic methodology

1. Capture exact symptom, timestamp, user, namespace, endpoint, request ID and reproduction.
2. Inspect desired custom resource, generation, observed generation, conditions and owning controller.
3. Inspect Deployment or StatefulSet, ReplicaSet, Pod, init containers, sidecars, probes and termination.
4. Inspect requests, limits, quota, selectors, affinity, taints, tolerations, priority, queue and PVC topology.
5. Inspect image digest, entrypoint, environment, mounts, certificates, runtime flags, GPU visibility and permissions.
6. Inspect DNS, service selector, EndpointSlice, route or gateway, TLS, NetworkPolicy, proxy and firewall.
7. Inspect PVC, S3 endpoint, credentials, bucket policy, object path, capacity, latency and throughput.
8. Correlate logs, metrics, traces, audit and events by time.
9. Correlate recent Git, Operator, node, certificate, secret, quota and policy changes.
10. Apply the smallest reversible remediation with a declared rollback trigger.

Collect events early because they expire. Read init-container and sidecar logs as well as the primary container.


## Security and compliance

1. Use enterprise groups and least privilege.
2. Separate platform admin, tenant admin, model approval, production deployment and audit roles.
3. Use private network paths where required.
4. Encrypt in transit and at rest.
5. Use external or short-lived secrets and test rotation.
6. Pin and sign images and artifacts.
7. Apply default deny and explicit allows.
8. Retain audit and promotion evidence.
9. Mask prompts, tokens and sensitive features in logs.
10. Threat-model model theft, injection, exfiltration and supply-chain compromise.

## Performance and capacity

1. Baseline before tuning.
2. Separate cold start, model load, queue, prefill, decode, storage and network latency.
3. Size CPU, memory, storage and accelerators from measurements.
4. Reserve headroom for failover and upgrades.
5. Use representative prompt, output, batch and concurrency distributions.
6. Track p50, p95, p99, max, errors, saturation and queue.
7. Test noisy-neighbor behavior.
8. Validate GPU and NUMA topology.
9. Correlate utilization, memory, power, temperature and throughput.
10. Tune one variable at a time with rollback thresholds.

## Backup and recovery

1. Inventory every stateful dependency.
2. Define RPO and RTO by dependency.
3. Protect project and required cluster-scoped resources.
4. Snapshot PVCs with supported CSI mechanisms.
5. Replicate or back up S3 objects and versions.
6. Protect external databases consistently.
7. Keep backup credentials outside the cluster.
8. Restore into isolation first.
9. Rotate credentials after restore where required.
10. Validate function, security, integrity and performance.

## Monitoring and SLO design

### Indicator 1: artifact success

- Define exactly how `artifact success` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 2: copy-sidecar errors

- Define exactly how `copy-sidecar errors` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 3: upload duration

- Define exactly how `upload duration` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 4: retries

- Define exactly how `retries` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 5: S3 4xx

- Define exactly how `S3 4xx` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 6: S3 5xx

- Define exactly how `S3 5xx` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 7: failed runs

- Define exactly how `failed runs` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

Use symptom alerts for paging and cause alerts for context. Use multi-window burn-rate alerts for user-facing SLOs.

## Failure-injection lab

1. Block a required egress path with NetworkPolicy. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
2. Use a test-invalid credential. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
3. Scale a dependency to zero. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
4. Exhaust test quota or storage. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
5. Use an invalid image digest. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
6. Use an incompatible runtime or model path. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
7. Drain a worker node. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
8. Expire a test certificate. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
9. Inject dependency latency. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
10. Execute rollback and measure restoration. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.

## L3 operational drill catalog

Each drill is a mini interview case and a production rehearsal. Execute only in approved non-production environments.

### Drill 001 — step-copy-artifact / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 002 — artifact URI / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 003 — S3 credentials / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 004 — signature / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 005 — CA bundle / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 006 — multipart / timeout

1. **Context:** A production-like workload shows `timeout` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 007 — proxy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 008 — endpoint / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 009 — bucket policy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 010 — clock skew / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 011 — step-copy-artifact / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 012 — artifact URI / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 013 — S3 credentials / timeout

1. **Context:** A production-like workload shows `timeout` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 014 — signature / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 015 — CA bundle / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 016 — multipart / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 017 — proxy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 018 — endpoint / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 019 — bucket policy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 020 — clock skew / timeout

1. **Context:** A production-like workload shows `timeout` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 021 — step-copy-artifact / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 022 — artifact URI / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 023 — S3 credentials / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 024 — signature / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 025 — CA bundle / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 026 — multipart / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 027 — proxy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 028 — endpoint / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 029 — bucket policy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 030 — clock skew / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 031 — step-copy-artifact / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 032 — artifact URI / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 033 — S3 credentials / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 034 — signature / timeout

1. **Context:** A production-like workload shows `timeout` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 035 — CA bundle / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 036 — multipart / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 037 — proxy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 038 — endpoint / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 039 — bucket policy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 040 — clock skew / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 041 — step-copy-artifact / timeout

1. **Context:** A production-like workload shows `timeout` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 042 — artifact URI / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 043 — S3 credentials / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 044 — signature / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 045 — CA bundle / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 046 — multipart / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 047 — proxy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 048 — endpoint / timeout

1. **Context:** A production-like workload shows `timeout` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 049 — bucket policy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 050 — clock skew / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 051 — step-copy-artifact / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 052 — artifact URI / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 053 — S3 credentials / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 054 — signature / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 055 — CA bundle / timeout

1. **Context:** A production-like workload shows `timeout` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 056 — multipart / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 057 — proxy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 058 — endpoint / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 059 — bucket policy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 060 — clock skew / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 061 — step-copy-artifact / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 062 — artifact URI / timeout

1. **Context:** A production-like workload shows `timeout` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 063 — S3 credentials / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 064 — signature / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 065 — CA bundle / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 066 — multipart / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 067 — proxy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 068 — endpoint / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 069 — bucket policy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 070 — clock skew / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 071 — step-copy-artifact / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 072 — artifact URI / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 073 — S3 credentials / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 074 — signature / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 075 — CA bundle / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 076 — multipart / timeout

1. **Context:** A production-like workload shows `timeout` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 077 — proxy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 078 — endpoint / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 079 — bucket policy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 080 — clock skew / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 081 — step-copy-artifact / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 082 — artifact URI / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 083 — S3 credentials / timeout

1. **Context:** A production-like workload shows `timeout` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 084 — signature / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 085 — CA bundle / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 086 — multipart / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 087 — proxy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 088 — endpoint / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 089 — bucket policy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 090 — clock skew / timeout

1. **Context:** A production-like workload shows `timeout` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 091 — step-copy-artifact / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 092 — artifact URI / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 093 — S3 credentials / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 094 — signature / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 095 — CA bundle / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 096 — multipart / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 097 — proxy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 098 — endpoint / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 099 — bucket policy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 100 — clock skew / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 101 — step-copy-artifact / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 102 — artifact URI / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 103 — S3 credentials / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 104 — signature / timeout

1. **Context:** A production-like workload shows `timeout` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 105 — CA bundle / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 106 — multipart / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 107 — proxy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 108 — endpoint / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 109 — bucket policy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 110 — clock skew / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 111 — step-copy-artifact / timeout

1. **Context:** A production-like workload shows `timeout` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 112 — artifact URI / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 113 — S3 credentials / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 114 — signature / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 115 — CA bundle / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 116 — multipart / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 117 — proxy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 118 — endpoint / timeout

1. **Context:** A production-like workload shows `timeout` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 119 — bucket policy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 120 — clock skew / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 121 — step-copy-artifact / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 122 — artifact URI / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 123 — S3 credentials / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 124 — signature / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 125 — CA bundle / timeout

1. **Context:** A production-like workload shows `timeout` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 126 — multipart / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 127 — proxy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 128 — endpoint / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 129 — bucket policy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 130 — clock skew / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 131 — step-copy-artifact / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 132 — artifact URI / timeout

1. **Context:** A production-like workload shows `timeout` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 133 — S3 credentials / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 134 — signature / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 135 — CA bundle / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 136 — multipart / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 137 — proxy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 138 — endpoint / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 139 — bucket policy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 140 — clock skew / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 141 — step-copy-artifact / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 142 — artifact URI / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 143 — S3 credentials / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 144 — signature / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 145 — CA bundle / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 146 — multipart / timeout

1. **Context:** A production-like workload shows `timeout` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 147 — proxy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 148 — endpoint / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 149 — bucket policy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 150 — clock skew / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 151 — step-copy-artifact / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 152 — artifact URI / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 153 — S3 credentials / timeout

1. **Context:** A production-like workload shows `timeout` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 154 — signature / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 155 — CA bundle / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 156 — multipart / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 157 — proxy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 158 — endpoint / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 159 — bucket policy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 160 — clock skew / timeout

1. **Context:** A production-like workload shows `timeout` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 161 — step-copy-artifact / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 162 — artifact URI / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 163 — S3 credentials / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 164 — signature / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 165 — CA bundle / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 166 — multipart / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 167 — proxy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 168 — endpoint / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 169 — bucket policy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 170 — clock skew / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 171 — step-copy-artifact / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 172 — artifact URI / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 173 — S3 credentials / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 174 — signature / timeout

1. **Context:** A production-like workload shows `timeout` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 175 — CA bundle / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 176 — multipart / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 177 — proxy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 178 — endpoint / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 179 — bucket policy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 180 — clock skew / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 181 — step-copy-artifact / timeout

1. **Context:** A production-like workload shows `timeout` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 182 — artifact URI / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 183 — S3 credentials / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 184 — signature / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 185 — CA bundle / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 186 — multipart / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 187 — proxy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 188 — endpoint / timeout

1. **Context:** A production-like workload shows `timeout` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 189 — bucket policy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 190 — clock skew / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 191 — step-copy-artifact / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 192 — artifact URI / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 193 — S3 credentials / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 194 — signature / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 195 — CA bundle / timeout

1. **Context:** A production-like workload shows `timeout` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 196 — multipart / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 197 — proxy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 198 — endpoint / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 199 — bucket policy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 200 — clock skew / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 201 — step-copy-artifact / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 202 — artifact URI / timeout

1. **Context:** A production-like workload shows `timeout` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 203 — S3 credentials / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 204 — signature / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 205 — CA bundle / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 206 — multipart / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 207 — proxy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 208 — endpoint / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 209 — bucket policy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 210 — clock skew / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 211 — step-copy-artifact / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 212 — artifact URI / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 213 — S3 credentials / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 214 — signature / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 215 — CA bundle / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 216 — multipart / timeout

1. **Context:** A production-like workload shows `timeout` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 217 — proxy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 218 — endpoint / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 219 — bucket policy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 220 — clock skew / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 221 — step-copy-artifact / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 222 — artifact URI / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 223 — S3 credentials / timeout

1. **Context:** A production-like workload shows `timeout` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 224 — signature / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 225 — CA bundle / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 226 — multipart / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 227 — proxy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 228 — endpoint / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 229 — bucket policy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 230 — clock skew / timeout

1. **Context:** A production-like workload shows `timeout` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 231 — step-copy-artifact / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 232 — artifact URI / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 233 — S3 credentials / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 234 — signature / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 235 — CA bundle / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 236 — multipart / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 237 — proxy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 238 — endpoint / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 239 — bucket policy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 240 — clock skew / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 241 — step-copy-artifact / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 242 — artifact URI / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 243 — S3 credentials / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 244 — signature / timeout

1. **Context:** A production-like workload shows `timeout` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 245 — CA bundle / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 246 — multipart / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 247 — proxy / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 248 — endpoint / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 249 — bucket policy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 250 — clock skew / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 251 — step-copy-artifact / timeout

1. **Context:** A production-like workload shows `timeout` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 252 — artifact URI / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 253 — S3 credentials / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 254 — signature / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 255 — CA bundle / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 256 — multipart / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 257 — proxy / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 258 — endpoint / timeout

1. **Context:** A production-like workload shows `timeout` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 259 — bucket policy / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 260 — clock skew / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 261 — step-copy-artifact / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 262 — artifact URI / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 263 — S3 credentials / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 264 — signature / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 265 — CA bundle / timeout

1. **Context:** A production-like workload shows `timeout` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 266 — multipart / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `multipart`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 267 — proxy / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `proxy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 268 — endpoint / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `endpoint`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 269 — bucket policy / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `bucket policy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 270 — clock skew / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `clock skew`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 271 — step-copy-artifact / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 272 — artifact URI / timeout

1. **Context:** A production-like workload shows `timeout` while validating `artifact URI`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 273 — S3 credentials / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `S3 credentials`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 274 — signature / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `signature`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 275 — CA bundle / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `CA bundle`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 276 — multipart / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `multipart`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `multipart` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 277 — proxy / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `proxy`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `proxy` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 278 — endpoint / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `endpoint`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 279 — bucket policy / timeout

1. **Context:** A production-like workload shows `timeout` while validating `bucket policy`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `bucket policy` works.
5. **Metric correlation:** Compare the incident timestamp with `copy-sidecar errors` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 280 — clock skew / DNS failure

1. **Context:** A production-like workload shows `DNS failure` while validating `clock skew`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `clock skew` works.
5. **Metric correlation:** Compare the incident timestamp with `failed runs` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 281 — step-copy-artifact / x509 unknown authority

1. **Context:** A production-like workload shows `x509 unknown authority` while validating `step-copy-artifact`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `step-copy-artifact` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 4xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 282 — artifact URI / entity too large

1. **Context:** A production-like workload shows `entity too large` while validating `artifact URI`.
2. **Primary resource:** Inspect `Secret`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `artifact URI` works.
5. **Metric correlation:** Compare the incident timestamp with `upload duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 283 — S3 credentials / SignatureDoesNotMatch

1. **Context:** A production-like workload shows `SignatureDoesNotMatch` while validating `S3 credentials`.
2. **Primary resource:** Inspect `NetworkPolicy`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `S3 credentials` works.
5. **Metric correlation:** Compare the incident timestamp with `artifact success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 284 — signature / NoSuchBucket

1. **Context:** A production-like workload shows `NoSuchBucket` while validating `signature`.
2. **Primary resource:** Inspect `ConfigMap`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `signature` works.
5. **Metric correlation:** Compare the incident timestamp with `S3 5xx` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 285 — CA bundle / AccessDenied

1. **Context:** A production-like workload shows `AccessDenied` while validating `CA bundle`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `CA bundle` works.
5. **Metric correlation:** Compare the incident timestamp with `retries` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `retry policy` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.


## Senior interview questions and answer points

### Interview question 001

**Question:** Which requirements must be clarified first? Apply it to `artifact URI` and `x509 unknown authority`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 002

**Question:** Which layer do you inspect first and why? Apply it to `signature` and `entity too large`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 003

**Question:** How do you separate symptom from root cause? Apply it to `multipart` and `SignatureDoesNotMatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 004

**Question:** Which objects are Operator managed? Apply it to `endpoint` and `NoSuchBucket`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 005

**Question:** What is the safest rollback? Apply it to `clock skew` and `AccessDenied`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 006

**Question:** How do you prove isolation? Apply it to `artifact URI` and `timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 007

**Question:** How do you retain audit evidence? Apply it to `signature` and `DNS failure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 008

**Question:** Which metrics prove success? Apply it to `multipart` and `x509 unknown authority`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 009

**Question:** How do you test failure safely? Apply it to `endpoint` and `entity too large`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 010

**Question:** What changes in an air gap? Apply it to `clock skew` and `SignatureDoesNotMatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 011

**Question:** How do quota and scheduling interact? Apply it to `artifact URI` and `NoSuchBucket`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 012

**Question:** How do you protect credentials? Apply it to `signature` and `AccessDenied`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 013

**Question:** Which compatibility checks are mandatory? Apply it to `multipart` and `timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 014

**Question:** How do you prevent drift? Apply it to `endpoint` and `DNS failure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 015

**Question:** How do you reserve capacity? Apply it to `clock skew` and `x509 unknown authority`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 016

**Question:** Which requirements must be clarified first? Apply it to `artifact URI` and `entity too large`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 017

**Question:** Which layer do you inspect first and why? Apply it to `signature` and `SignatureDoesNotMatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 018

**Question:** How do you separate symptom from root cause? Apply it to `multipart` and `NoSuchBucket`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 019

**Question:** Which objects are Operator managed? Apply it to `endpoint` and `AccessDenied`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 020

**Question:** What is the safest rollback? Apply it to `clock skew` and `timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 021

**Question:** How do you prove isolation? Apply it to `artifact URI` and `DNS failure`.

## Size boundary note

Core implementation, drills, interview questions and checklists retained within the requested line range.
