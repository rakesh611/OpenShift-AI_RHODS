# 20. Handle OpenShift AI upgrade risk

> Corporate Interview Scenario Guide — Red Hat OpenShift AI L3 / Senior Platform Engineer

- **Production baseline:** Red Hat OpenShift AI Self-Managed 3.4 and OpenShift Container Platform 4.20
- **Generated:** 2026-07-21
- **Audience:** 10+ year Linux, Kubernetes, OpenShift, DevOps and production-support engineers
- **Safety:** Validate fields with `oc api-resources` and `oc explain` before applying manifests.


## Scenario objective

Plan, rehearse, execute and validate OpenShift AI upgrades while managing channels, deprecated APIs, dependencies, workload compatibility and rollback boundaries.


## Senior interview answer structure

1. Clarify business impact, data class, tenancy, SLO, RPO, RTO, compliance and operating ownership.
2. Verify the supported OpenShift, OpenShift AI and dependent Operator matrix.
3. Draw identity, request, storage, compute, serving, monitoring and recovery flows.
4. Define namespace, RBAC, quota, NetworkPolicy, secret, image and artifact boundaries.
5. Build a minimum production pattern in non-production.
6. Run functional, security, performance, failure, restore and upgrade tests.
7. Promote through GitOps with approvals and evidence.
8. Operate with SLOs, alerts, capacity, backup, incident and change management.


## Core resources

- `Subscription` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `InstallPlan` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ClusterServiceVersion` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `DataScienceCluster` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `DSCInitialization` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `CatalogSource` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `InferenceService` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `HardwareProfile` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.

## Core concepts

- **upgrade channel:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **EUS:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **compatibility matrix:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **preflight:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **deprecated API:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **backup:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **canary cluster:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **maintenance window:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **rollback boundary:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **known issue:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.

## Failure modes

- **CSV stuck:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **dependency mismatch:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **CRD conversion:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **model server break:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **pipeline incompatibility:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **mirror gap:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **operator degraded:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.

## Service indicators

- **upgrade success:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **duration:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **incidents:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **preflight findings:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **rollback readiness:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **validation pass:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **operator health:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.

## Required controls

- **release review:** implement declaratively, test negatively, review periodically and retain evidence.
- **staging rehearsal:** implement declaratively, test negatively, review periodically and retain evidence.
- **backup:** implement declaratively, test negatively, review periodically and retain evidence.
- **manual approval:** implement declaratively, test negatively, review periodically and retain evidence.
- **deprecation inventory:** implement declaratively, test negatively, review periodically and retain evidence.
- **smoke suite:** implement declaratively, test negatively, review periodically and retain evidence.
- **change freeze:** implement declaratively, test negatively, review periodically and retain evidence.

## Reference architecture

1. **Enterprise services:** identity, DNS, NTP, PKI, secrets manager, Git, registry, S3, SIEM, ticketing and backup.
2. **OpenShift:** control plane, ingress or Gateway API, OVN-Kubernetes, monitoring, registry, storage, OAuth and policy.
3. **OpenShift AI:** Operator, DSCInitialization, DataScienceCluster, dashboard, workbenches, pipelines, KServe, registry and TrustyAI as enabled.
4. **Tenant:** namespace, groups, service accounts, connections, workbenches, pipeline server, runs, models, quota and policies.
5. **Operations:** GitOps, CI/CD, vulnerability management, SLOs, capacity, backup, restore, upgrade and incident response.

Every route, runtime, model, bucket, PVC, credential, quota and exception must have a named owner and lifecycle.


## Project creation from scratch

### Phase 0 — supported-version discovery

1. Record cluster version, channel, CSV, dependent Operators, storage classes, ingress, identity, proxy and disconnected status.
2. Read release notes, known issues, deprecated and removed features.
3. Run `oc api-resources` and `oc explain` for every custom resource.
4. Create a RACI across platform, security, network, storage, data science and application teams.

### Phase 1 — requirements

1. Define users, data classes, model type, request pattern, peak concurrency, latency, availability, RPO, RTO and retention.
2. Convert each requirement into a measurable acceptance test.
3. Record constraints such as air gap, private connectivity, HSM, GPU scarcity and regulated change.
4. Document assumptions and explicit exclusions.

### Phase 2 — dependency readiness

1. Validate cluster operators, DNS, NTP, ingress, storage, registry, monitoring, identity, certificate trust and backup.
2. Verify node labels, taints, capacity and scheduling.
3. Verify credentials come from an approved secret system.
4. Create an automated preflight report.

### Phase 3 — tenant foundation

1. Create namespaces with owner, environment, cost center, data class and lifecycle labels.
2. Bind groups rather than individuals where practical.
3. Apply ResourceQuota, LimitRange, default-deny and required allow policies.
4. Register approved images, hardware profiles, runtimes and connections.

### Phase 4 — implementation

1. Deploy the minimum representative workload.
2. Pin images and models by digest or immutable version.
3. Use Git overlays for environment differences.
4. Expose only required endpoints and verify authentication and authorization.

### Phase 5 — validation

1. Run positive and negative functional tests.
2. Test scheduling, images, storage, DNS, TLS, RBAC, policy and quota.
3. Run load, soak and capacity tests.
4. Capture YAML, events, logs, metrics and test output.

### Phase 6 — failure and recovery

1. Inject one dependency failure at a time in non-production.
2. Confirm actionable alerts.
3. Execute remediation and rollback.
4. Measure restoration against SLO and RTO.

### Phase 7 — production promotion

1. Require review, policy checks, artifact verification, approvals and change record.
2. Use canary or controlled traffic migration.
3. Observe a defined bake period.
4. Retain last-known-good manifests and artifacts.

### Phase 8 — day-2 operations

1. Review capacity, latency, errors, security, backup age, certificate expiry and deprecations.
2. Patch and upgrade through rehearsal environments.
3. Review access and exceptions.
4. Improve the runbook after every incident and exercise.


## Command and evidence toolkit

Run read-only commands first and preserve timestamped output.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A
oc get subscriptions.operators.coreos.com -A
oc get installplans -A
oc get datasciencecluster -A -o yaml
oc get dscinitialization -A -o yaml
oc get pods -A --sort-by=.metadata.creationTimestamp
oc get events -A --sort-by=.lastTimestamp
oc get nodes -o wide
oc describe node <node>
oc auth can-i --list -n <project>
oc get resourcequota,limitrange -n <project> -o yaml
oc get networkpolicy -n <project> -o yaml
oc get secrets,pvc -n <project>
oc get routes,services,endpointslices -n <project>
oc logs <pod> -n <project> --all-containers --prefix
oc adm inspect namespace/<project>
oc adm must-gather
```

Evidence path: `<incident>/<timestamp>/<cluster>/<namespace>/<resource>/<command-output>`.

For every command record expected result, actual result and interpretation. Output without interpretation is not diagnosis.

## Implementation manifests

### Version-adaptive preflight

```yaml
#!/usr/bin/env bash
set -euo pipefail
oc version
oc get clusterversion
oc get csv -A
oc get datasciencecluster -A
oc api-resources | grep -Ei 'inferenceservice|servingruntime|hardwareprofile|notebook|trustyai'
oc explain inferenceservice.spec 2>/dev/null || true
oc get co
```

### OADP backup pattern

```yaml
apiVersion: velero.io/v1
kind: Backup
metadata:
  name: ai-team-a-prechange
  namespace: openshift-adp
spec:
  includedNamespaces:
  - ai-team-a-dev
  - ai-team-a-prod
  snapshotMoveData: true
  ttl: 720h0m0s
```

Examples are patterns, not guaranteed schemas for every release. Confirm installed CRDs and Operator ownership.

## L3 diagnostic methodology

1. Capture exact symptom, timestamp, user, namespace, endpoint, request ID and reproduction.
2. Inspect desired custom resource, generation, observed generation, conditions and owning controller.
3. Inspect Deployment or StatefulSet, ReplicaSet, Pod, init containers, sidecars, probes and termination.
4. Inspect requests, limits, quota, selectors, affinity, taints, tolerations, priority, queue and PVC topology.
5. Inspect image digest, entrypoint, environment, mounts, certificates, runtime flags, GPU visibility and permissions.
6. Inspect DNS, service selector, EndpointSlice, route or gateway, TLS, NetworkPolicy, proxy and firewall.
7. Inspect PVC, S3 endpoint, credentials, bucket policy, object path, capacity, latency and throughput.
8. Correlate logs, metrics, traces, audit and events by time.
9. Correlate recent Git, Operator, node, certificate, secret, quota and policy changes.
10. Apply the smallest reversible remediation with a declared rollback trigger.

Collect events early because they expire. Read init-container and sidecar logs as well as the primary container.


## Security and compliance

1. Use enterprise groups and least privilege.
2. Separate platform admin, tenant admin, model approval, production deployment and audit roles.
3. Use private network paths where required.
4. Encrypt in transit and at rest.
5. Use external or short-lived secrets and test rotation.
6. Pin and sign images and artifacts.
7. Apply default deny and explicit allows.
8. Retain audit and promotion evidence.
9. Mask prompts, tokens and sensitive features in logs.
10. Threat-model model theft, injection, exfiltration and supply-chain compromise.

## Performance and capacity

1. Baseline before tuning.
2. Separate cold start, model load, queue, prefill, decode, storage and network latency.
3. Size CPU, memory, storage and accelerators from measurements.
4. Reserve headroom for failover and upgrades.
5. Use representative prompt, output, batch and concurrency distributions.
6. Track p50, p95, p99, max, errors, saturation and queue.
7. Test noisy-neighbor behavior.
8. Validate GPU and NUMA topology.
9. Correlate utilization, memory, power, temperature and throughput.
10. Tune one variable at a time with rollback thresholds.

## Backup and recovery

1. Inventory every stateful dependency.
2. Define RPO and RTO by dependency.
3. Protect project and required cluster-scoped resources.
4. Snapshot PVCs with supported CSI mechanisms.
5. Replicate or back up S3 objects and versions.
6. Protect external databases consistently.
7. Keep backup credentials outside the cluster.
8. Restore into isolation first.
9. Rotate credentials after restore where required.
10. Validate function, security, integrity and performance.

## Monitoring and SLO design

### Indicator 1: upgrade success

- Define exactly how `upgrade success` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 2: duration

- Define exactly how `duration` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 3: incidents

- Define exactly how `incidents` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 4: preflight findings

- Define exactly how `preflight findings` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 5: rollback readiness

- Define exactly how `rollback readiness` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 6: validation pass

- Define exactly how `validation pass` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 7: operator health

- Define exactly how `operator health` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

Use symptom alerts for paging and cause alerts for context. Use multi-window burn-rate alerts for user-facing SLOs.

## Failure-injection lab

1. Block a required egress path with NetworkPolicy. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
2. Use a test-invalid credential. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
3. Scale a dependency to zero. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
4. Exhaust test quota or storage. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
5. Use an invalid image digest. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
6. Use an incompatible runtime or model path. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
7. Drain a worker node. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
8. Expire a test certificate. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
9. Inject dependency latency. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
10. Execute rollback and measure restoration. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.

## L3 operational drill catalog

Each drill is a mini interview case and a production rehearsal. Execute only in approved non-production environments.

### Drill 001 — upgrade channel / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 002 — EUS / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 003 — compatibility matrix / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 004 — preflight / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 005 — deprecated API / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 006 — backup / model server break

1. **Context:** A production-like workload shows `model server break` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 007 — canary cluster / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 008 — maintenance window / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 009 — rollback boundary / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 010 — known issue / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 011 — upgrade channel / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 012 — EUS / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 013 — compatibility matrix / model server break

1. **Context:** A production-like workload shows `model server break` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 014 — preflight / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 015 — deprecated API / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 016 — backup / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 017 — canary cluster / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 018 — maintenance window / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 019 — rollback boundary / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 020 — known issue / model server break

1. **Context:** A production-like workload shows `model server break` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 021 — upgrade channel / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 022 — EUS / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 023 — compatibility matrix / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 024 — preflight / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 025 — deprecated API / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 026 — backup / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 027 — canary cluster / model server break

1. **Context:** A production-like workload shows `model server break` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 028 — maintenance window / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 029 — rollback boundary / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 030 — known issue / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 031 — upgrade channel / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 032 — EUS / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 033 — compatibility matrix / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 034 — preflight / model server break

1. **Context:** A production-like workload shows `model server break` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 035 — deprecated API / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 036 — backup / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 037 — canary cluster / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 038 — maintenance window / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 039 — rollback boundary / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 040 — known issue / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 041 — upgrade channel / model server break

1. **Context:** A production-like workload shows `model server break` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 042 — EUS / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 043 — compatibility matrix / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 044 — preflight / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 045 — deprecated API / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 046 — backup / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 047 — canary cluster / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 048 — maintenance window / model server break

1. **Context:** A production-like workload shows `model server break` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 049 — rollback boundary / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 050 — known issue / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 051 — upgrade channel / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 052 — EUS / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 053 — compatibility matrix / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 054 — preflight / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 055 — deprecated API / model server break

1. **Context:** A production-like workload shows `model server break` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 056 — backup / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 057 — canary cluster / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 058 — maintenance window / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 059 — rollback boundary / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 060 — known issue / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 061 — upgrade channel / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 062 — EUS / model server break

1. **Context:** A production-like workload shows `model server break` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 063 — compatibility matrix / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 064 — preflight / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 065 — deprecated API / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 066 — backup / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 067 — canary cluster / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 068 — maintenance window / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 069 — rollback boundary / model server break

1. **Context:** A production-like workload shows `model server break` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 070 — known issue / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 071 — upgrade channel / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 072 — EUS / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 073 — compatibility matrix / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 074 — preflight / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 075 — deprecated API / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 076 — backup / model server break

1. **Context:** A production-like workload shows `model server break` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 077 — canary cluster / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 078 — maintenance window / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 079 — rollback boundary / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 080 — known issue / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 081 — upgrade channel / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 082 — EUS / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 083 — compatibility matrix / model server break

1. **Context:** A production-like workload shows `model server break` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 084 — preflight / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 085 — deprecated API / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 086 — backup / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 087 — canary cluster / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 088 — maintenance window / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 089 — rollback boundary / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 090 — known issue / model server break

1. **Context:** A production-like workload shows `model server break` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 091 — upgrade channel / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 092 — EUS / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 093 — compatibility matrix / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 094 — preflight / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 095 — deprecated API / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 096 — backup / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 097 — canary cluster / model server break

1. **Context:** A production-like workload shows `model server break` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 098 — maintenance window / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 099 — rollback boundary / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 100 — known issue / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 101 — upgrade channel / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 102 — EUS / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 103 — compatibility matrix / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 104 — preflight / model server break

1. **Context:** A production-like workload shows `model server break` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 105 — deprecated API / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 106 — backup / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 107 — canary cluster / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 108 — maintenance window / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 109 — rollback boundary / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 110 — known issue / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 111 — upgrade channel / model server break

1. **Context:** A production-like workload shows `model server break` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 112 — EUS / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 113 — compatibility matrix / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 114 — preflight / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 115 — deprecated API / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 116 — backup / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 117 — canary cluster / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 118 — maintenance window / model server break

1. **Context:** A production-like workload shows `model server break` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 119 — rollback boundary / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 120 — known issue / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 121 — upgrade channel / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 122 — EUS / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 123 — compatibility matrix / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 124 — preflight / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 125 — deprecated API / model server break

1. **Context:** A production-like workload shows `model server break` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 126 — backup / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 127 — canary cluster / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 128 — maintenance window / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 129 — rollback boundary / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 130 — known issue / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 131 — upgrade channel / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 132 — EUS / model server break

1. **Context:** A production-like workload shows `model server break` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 133 — compatibility matrix / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 134 — preflight / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 135 — deprecated API / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 136 — backup / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 137 — canary cluster / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 138 — maintenance window / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 139 — rollback boundary / model server break

1. **Context:** A production-like workload shows `model server break` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 140 — known issue / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 141 — upgrade channel / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 142 — EUS / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 143 — compatibility matrix / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 144 — preflight / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 145 — deprecated API / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 146 — backup / model server break

1. **Context:** A production-like workload shows `model server break` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 147 — canary cluster / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 148 — maintenance window / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 149 — rollback boundary / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 150 — known issue / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 151 — upgrade channel / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 152 — EUS / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 153 — compatibility matrix / model server break

1. **Context:** A production-like workload shows `model server break` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 154 — preflight / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 155 — deprecated API / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 156 — backup / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 157 — canary cluster / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 158 — maintenance window / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 159 — rollback boundary / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 160 — known issue / model server break

1. **Context:** A production-like workload shows `model server break` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 161 — upgrade channel / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 162 — EUS / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 163 — compatibility matrix / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 164 — preflight / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 165 — deprecated API / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 166 — backup / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 167 — canary cluster / model server break

1. **Context:** A production-like workload shows `model server break` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 168 — maintenance window / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 169 — rollback boundary / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 170 — known issue / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 171 — upgrade channel / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 172 — EUS / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 173 — compatibility matrix / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 174 — preflight / model server break

1. **Context:** A production-like workload shows `model server break` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 175 — deprecated API / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 176 — backup / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 177 — canary cluster / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 178 — maintenance window / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 179 — rollback boundary / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 180 — known issue / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 181 — upgrade channel / model server break

1. **Context:** A production-like workload shows `model server break` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 182 — EUS / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 183 — compatibility matrix / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 184 — preflight / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 185 — deprecated API / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 186 — backup / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 187 — canary cluster / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 188 — maintenance window / model server break

1. **Context:** A production-like workload shows `model server break` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 189 — rollback boundary / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 190 — known issue / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 191 — upgrade channel / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 192 — EUS / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 193 — compatibility matrix / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 194 — preflight / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 195 — deprecated API / model server break

1. **Context:** A production-like workload shows `model server break` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 196 — backup / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 197 — canary cluster / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 198 — maintenance window / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 199 — rollback boundary / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 200 — known issue / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 201 — upgrade channel / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 202 — EUS / model server break

1. **Context:** A production-like workload shows `model server break` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 203 — compatibility matrix / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 204 — preflight / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 205 — deprecated API / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 206 — backup / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 207 — canary cluster / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 208 — maintenance window / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 209 — rollback boundary / model server break

1. **Context:** A production-like workload shows `model server break` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 210 — known issue / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 211 — upgrade channel / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 212 — EUS / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 213 — compatibility matrix / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 214 — preflight / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 215 — deprecated API / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 216 — backup / model server break

1. **Context:** A production-like workload shows `model server break` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 217 — canary cluster / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 218 — maintenance window / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 219 — rollback boundary / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 220 — known issue / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 221 — upgrade channel / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 222 — EUS / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 223 — compatibility matrix / model server break

1. **Context:** A production-like workload shows `model server break` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 224 — preflight / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 225 — deprecated API / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 226 — backup / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 227 — canary cluster / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 228 — maintenance window / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 229 — rollback boundary / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 230 — known issue / model server break

1. **Context:** A production-like workload shows `model server break` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 231 — upgrade channel / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 232 — EUS / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 233 — compatibility matrix / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 234 — preflight / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 235 — deprecated API / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 236 — backup / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 237 — canary cluster / model server break

1. **Context:** A production-like workload shows `model server break` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 238 — maintenance window / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 239 — rollback boundary / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 240 — known issue / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 241 — upgrade channel / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 242 — EUS / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 243 — compatibility matrix / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 244 — preflight / model server break

1. **Context:** A production-like workload shows `model server break` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 245 — deprecated API / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 246 — backup / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 247 — canary cluster / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 248 — maintenance window / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 249 — rollback boundary / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 250 — known issue / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 251 — upgrade channel / model server break

1. **Context:** A production-like workload shows `model server break` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 252 — EUS / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 253 — compatibility matrix / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 254 — preflight / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 255 — deprecated API / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 256 — backup / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 257 — canary cluster / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 258 — maintenance window / model server break

1. **Context:** A production-like workload shows `model server break` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 259 — rollback boundary / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 260 — known issue / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 261 — upgrade channel / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 262 — EUS / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 263 — compatibility matrix / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 264 — preflight / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 265 — deprecated API / model server break

1. **Context:** A production-like workload shows `model server break` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 266 — backup / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `backup`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 267 — canary cluster / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `canary cluster`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 268 — maintenance window / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `maintenance window`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 269 — rollback boundary / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `rollback boundary`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 270 — known issue / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `known issue`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 271 — upgrade channel / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `upgrade channel`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 272 — EUS / model server break

1. **Context:** A production-like workload shows `model server break` while validating `EUS`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 273 — compatibility matrix / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 274 — preflight / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `preflight`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 275 — deprecated API / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `deprecated API`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 276 — backup / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `backup`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backup` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 277 — canary cluster / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `canary cluster`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `canary cluster` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 278 — maintenance window / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `maintenance window`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `maintenance window` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 279 — rollback boundary / model server break

1. **Context:** A production-like workload shows `model server break` while validating `rollback boundary`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `rollback boundary` works.
5. **Metric correlation:** Compare the incident timestamp with `duration` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 280 — known issue / operator degraded

1. **Context:** A production-like workload shows `operator degraded` while validating `known issue`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `known issue` works.
5. **Metric correlation:** Compare the incident timestamp with `operator health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 281 — upgrade channel / CRD conversion

1. **Context:** A production-like workload shows `CRD conversion` while validating `upgrade channel`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `upgrade channel` works.
5. **Metric correlation:** Compare the incident timestamp with `rollback readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 282 — EUS / mirror gap

1. **Context:** A production-like workload shows `mirror gap` while validating `EUS`.
2. **Primary resource:** Inspect `DataScienceCluster`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `EUS` works.
5. **Metric correlation:** Compare the incident timestamp with `incidents` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 283 — compatibility matrix / dependency mismatch

1. **Context:** A production-like workload shows `dependency mismatch` while validating `compatibility matrix`.
2. **Primary resource:** Inspect `CatalogSource`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `compatibility matrix` works.
5. **Metric correlation:** Compare the incident timestamp with `upgrade success` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 284 — preflight / pipeline incompatibility

1. **Context:** A production-like workload shows `pipeline incompatibility` while validating `preflight`.
2. **Primary resource:** Inspect `HardwareProfile`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `preflight` works.
5. **Metric correlation:** Compare the incident timestamp with `validation pass` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 285 — deprecated API / CSV stuck

1. **Context:** A production-like workload shows `CSV stuck` while validating `deprecated API`.
2. **Primary resource:** Inspect `InstallPlan`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `deprecated API` works.
5. **Metric correlation:** Compare the incident timestamp with `preflight findings` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `change freeze` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.


## Senior interview questions and answer points

### Interview question 001

**Question:** Which requirements must be clarified first? Apply it to `EUS` and `CRD conversion`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 002

**Question:** Which layer do you inspect first and why? Apply it to `preflight` and `mirror gap`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 003

**Question:** How do you separate symptom from root cause? Apply it to `backup` and `dependency mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 004

**Question:** Which objects are Operator managed? Apply it to `maintenance window` and `pipeline incompatibility`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 005

**Question:** What is the safest rollback? Apply it to `known issue` and `CSV stuck`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 006

**Question:** How do you prove isolation? Apply it to `EUS` and `model server break`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 007

**Question:** How do you retain audit evidence? Apply it to `preflight` and `operator degraded`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 008

**Question:** Which metrics prove success? Apply it to `backup` and `CRD conversion`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 009

**Question:** How do you test failure safely? Apply it to `maintenance window` and `mirror gap`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 010

**Question:** What changes in an air gap? Apply it to `known issue` and `dependency mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 011

**Question:** How do quota and scheduling interact? Apply it to `EUS` and `pipeline incompatibility`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 012

**Question:** How do you protect credentials? Apply it to `preflight` and `CSV stuck`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 013

**Question:** Which compatibility checks are mandatory? Apply it to `backup` and `model server break`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 014

**Question:** How do you prevent drift? Apply it to `maintenance window` and `operator degraded`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 015

**Question:** How do you reserve capacity? Apply it to `known issue` and `CRD conversion`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 016

**Question:** Which requirements must be clarified first? Apply it to `EUS` and `mirror gap`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 017

**Question:** Which layer do you inspect first and why? Apply it to `preflight` and `dependency mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 018

**Question:** How do you separate symptom from root cause? Apply it to `backup` and `pipeline incompatibility`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 019

**Question:** Which objects are Operator managed? Apply it to `maintenance window` and `CSV stuck`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 020

**Question:** What is the safest rollback? Apply it to `known issue` and `model server break`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 021

**Question:** How do you prove isolation? Apply it to `EUS` and `operator degraded`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 022

**Question:** How do you retain audit evidence? Apply it to `preflight` and `CRD conversion`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.

## Size boundary note

Core implementation, drills, interview questions and checklists retained within the requested line range.
