# 10. Troubleshoot KServe model endpoint 503

> Corporate Interview Scenario Guide — Red Hat OpenShift AI L3 / Senior Platform Engineer

- **Production baseline:** Red Hat OpenShift AI Self-Managed 3.4 and OpenShift Container Platform 4.20
- **Generated:** 2026-07-21
- **Audience:** 10+ year Linux, Kubernetes, OpenShift, DevOps and production-support engineers
- **Safety:** Validate fields with `oc api-resources` and `oc explain` before applying manifests.


## Scenario objective

Trace HTTP 503 responses through route or gateway, service endpoints, KServe conditions, predictor deployment, model loading and backend readiness.


## Senior interview answer structure

1. Clarify business impact, data class, tenancy, SLO, RPO, RTO, compliance and operating ownership.
2. Verify the supported OpenShift, OpenShift AI and dependent Operator matrix.
3. Draw identity, request, storage, compute, serving, monitoring and recovery flows.
4. Define namespace, RBAC, quota, NetworkPolicy, secret, image and artifact boundaries.
5. Build a minimum production pattern in non-production.
6. Run functional, security, performance, failure, restore and upgrade tests.
7. Promote through GitOps with approvals and evidence.
8. Operate with SLOs, alerts, capacity, backup, incident and change management.


## Core resources

- `InferenceService` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `ServingRuntime` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Deployment` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Pod` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Service` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `EndpointSlice` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Route` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Gateway` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `HTTPRoute` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.
- `Event` — inspect ownership, reconciliation, status conditions, events, dependencies and rollback behavior.

## Core concepts

- **Ready condition:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **PredictorReady:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **RoutesReady:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **backend endpoint:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **readiness probe:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **model storage:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **runtime compatibility:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **timeout:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **gateway:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.
- **service selector:** define purpose, configuration, healthy evidence, failure evidence, owner and lifecycle.

## Failure modes

- **no endpoints:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **model load timeout:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **runtime crash:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **route misrouting:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **selector mismatch:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **GPU pending:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.
- **storage-init failure:** document symptom, scope, first evidence, alternative causes, safe remediation and prevention.

## Service indicators

- **503 rate:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **predictor readiness:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **model load time:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **restarts:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **endpoint count:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **route health:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.
- **inference latency:** specify metric source, labels, aggregation, threshold, alert window, SLO and runbook.

## Required controls

- **synthetic probes:** implement declaratively, test negatively, review periodically and retain evidence.
- **condition alerts:** implement declaratively, test negatively, review periodically and retain evidence.
- **runtime validation:** implement declaratively, test negatively, review periodically and retain evidence.
- **model checksum:** implement declaratively, test negatively, review periodically and retain evidence.
- **startup probes:** implement declaratively, test negatively, review periodically and retain evidence.
- **capacity headroom:** implement declaratively, test negatively, review periodically and retain evidence.
- **rollback:** implement declaratively, test negatively, review periodically and retain evidence.

## Reference architecture

1. **Enterprise services:** identity, DNS, NTP, PKI, secrets manager, Git, registry, S3, SIEM, ticketing and backup.
2. **OpenShift:** control plane, ingress or Gateway API, OVN-Kubernetes, monitoring, registry, storage, OAuth and policy.
3. **OpenShift AI:** Operator, DSCInitialization, DataScienceCluster, dashboard, workbenches, pipelines, KServe, registry and TrustyAI as enabled.
4. **Tenant:** namespace, groups, service accounts, connections, workbenches, pipeline server, runs, models, quota and policies.
5. **Operations:** GitOps, CI/CD, vulnerability management, SLOs, capacity, backup, restore, upgrade and incident response.

Every route, runtime, model, bucket, PVC, credential, quota and exception must have a named owner and lifecycle.


## Project creation from scratch

### Phase 0 — supported-version discovery

1. Record cluster version, channel, CSV, dependent Operators, storage classes, ingress, identity, proxy and disconnected status.
2. Read release notes, known issues, deprecated and removed features.
3. Run `oc api-resources` and `oc explain` for every custom resource.
4. Create a RACI across platform, security, network, storage, data science and application teams.

### Phase 1 — requirements

1. Define users, data classes, model type, request pattern, peak concurrency, latency, availability, RPO, RTO and retention.
2. Convert each requirement into a measurable acceptance test.
3. Record constraints such as air gap, private connectivity, HSM, GPU scarcity and regulated change.
4. Document assumptions and explicit exclusions.

### Phase 2 — dependency readiness

1. Validate cluster operators, DNS, NTP, ingress, storage, registry, monitoring, identity, certificate trust and backup.
2. Verify node labels, taints, capacity and scheduling.
3. Verify credentials come from an approved secret system.
4. Create an automated preflight report.

### Phase 3 — tenant foundation

1. Create namespaces with owner, environment, cost center, data class and lifecycle labels.
2. Bind groups rather than individuals where practical.
3. Apply ResourceQuota, LimitRange, default-deny and required allow policies.
4. Register approved images, hardware profiles, runtimes and connections.

### Phase 4 — implementation

1. Deploy the minimum representative workload.
2. Pin images and models by digest or immutable version.
3. Use Git overlays for environment differences.
4. Expose only required endpoints and verify authentication and authorization.

### Phase 5 — validation

1. Run positive and negative functional tests.
2. Test scheduling, images, storage, DNS, TLS, RBAC, policy and quota.
3. Run load, soak and capacity tests.
4. Capture YAML, events, logs, metrics and test output.

### Phase 6 — failure and recovery

1. Inject one dependency failure at a time in non-production.
2. Confirm actionable alerts.
3. Execute remediation and rollback.
4. Measure restoration against SLO and RTO.

### Phase 7 — production promotion

1. Require review, policy checks, artifact verification, approvals and change record.
2. Use canary or controlled traffic migration.
3. Observe a defined bake period.
4. Retain last-known-good manifests and artifacts.

### Phase 8 — day-2 operations

1. Review capacity, latency, errors, security, backup age, certificate expiry and deprecations.
2. Patch and upgrade through rehearsal environments.
3. Review access and exceptions.
4. Improve the runbook after every incident and exercise.


## Command and evidence toolkit

Run read-only commands first and preserve timestamped output.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A
oc get subscriptions.operators.coreos.com -A
oc get installplans -A
oc get datasciencecluster -A -o yaml
oc get dscinitialization -A -o yaml
oc get pods -A --sort-by=.metadata.creationTimestamp
oc get events -A --sort-by=.lastTimestamp
oc get nodes -o wide
oc describe node <node>
oc auth can-i --list -n <project>
oc get resourcequota,limitrange -n <project> -o yaml
oc get networkpolicy -n <project> -o yaml
oc get secrets,pvc -n <project>
oc get routes,services,endpointslices -n <project>
oc logs <pod> -n <project> --all-containers --prefix
oc adm inspect namespace/<project>
oc adm must-gather
```

Evidence path: `<incident>/<timestamp>/<cluster>/<namespace>/<resource>/<command-output>`.

For every command record expected result, actual result and interpretation. Output without interpretation is not diagnosis.

## Implementation manifests

### Version-adaptive preflight

```yaml
#!/usr/bin/env bash
set -euo pipefail
oc version
oc get clusterversion
oc get csv -A
oc get datasciencecluster -A
oc api-resources | grep -Ei 'inferenceservice|servingruntime|hardwareprofile|notebook|trustyai'
oc explain inferenceservice.spec 2>/dev/null || true
oc get co
```

### S3 connection secret pattern

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-artifact-s3
  namespace: ai-team-a-dev
  labels:
    opendatahub.io/dashboard: "true"
  annotations:
    opendatahub.io/connection-type-ref: s3
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "<external-secret-reference>"
  AWS_SECRET_ACCESS_KEY: "<external-secret-reference>"
  AWS_S3_ENDPOINT: "https://s3.internal.example.com"
  AWS_S3_BUCKET: "ai-team-a-artifacts"
  AWS_DEFAULT_REGION: "us-east-1" 
```

### InferenceService pattern

```yaml
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: example-model
  namespace: ai-team-a-prod
spec:
  predictor:
    model:
      modelFormat:
        name: "<format>"
      runtime: "<validated-runtime>"
      storageUri: "s3://model-bucket/example-model/<immutable-version>/" 
```

Examples are patterns, not guaranteed schemas for every release. Confirm installed CRDs and Operator ownership.

## L3 diagnostic methodology

1. Capture exact symptom, timestamp, user, namespace, endpoint, request ID and reproduction.
2. Inspect desired custom resource, generation, observed generation, conditions and owning controller.
3. Inspect Deployment or StatefulSet, ReplicaSet, Pod, init containers, sidecars, probes and termination.
4. Inspect requests, limits, quota, selectors, affinity, taints, tolerations, priority, queue and PVC topology.
5. Inspect image digest, entrypoint, environment, mounts, certificates, runtime flags, GPU visibility and permissions.
6. Inspect DNS, service selector, EndpointSlice, route or gateway, TLS, NetworkPolicy, proxy and firewall.
7. Inspect PVC, S3 endpoint, credentials, bucket policy, object path, capacity, latency and throughput.
8. Correlate logs, metrics, traces, audit and events by time.
9. Correlate recent Git, Operator, node, certificate, secret, quota and policy changes.
10. Apply the smallest reversible remediation with a declared rollback trigger.

Collect events early because they expire. Read init-container and sidecar logs as well as the primary container.


## Security and compliance

1. Use enterprise groups and least privilege.
2. Separate platform admin, tenant admin, model approval, production deployment and audit roles.
3. Use private network paths where required.
4. Encrypt in transit and at rest.
5. Use external or short-lived secrets and test rotation.
6. Pin and sign images and artifacts.
7. Apply default deny and explicit allows.
8. Retain audit and promotion evidence.
9. Mask prompts, tokens and sensitive features in logs.
10. Threat-model model theft, injection, exfiltration and supply-chain compromise.

## Performance and capacity

1. Baseline before tuning.
2. Separate cold start, model load, queue, prefill, decode, storage and network latency.
3. Size CPU, memory, storage and accelerators from measurements.
4. Reserve headroom for failover and upgrades.
5. Use representative prompt, output, batch and concurrency distributions.
6. Track p50, p95, p99, max, errors, saturation and queue.
7. Test noisy-neighbor behavior.
8. Validate GPU and NUMA topology.
9. Correlate utilization, memory, power, temperature and throughput.
10. Tune one variable at a time with rollback thresholds.

## Backup and recovery

1. Inventory every stateful dependency.
2. Define RPO and RTO by dependency.
3. Protect project and required cluster-scoped resources.
4. Snapshot PVCs with supported CSI mechanisms.
5. Replicate or back up S3 objects and versions.
6. Protect external databases consistently.
7. Keep backup credentials outside the cluster.
8. Restore into isolation first.
9. Rotate credentials after restore where required.
10. Validate function, security, integrity and performance.

## Monitoring and SLO design

### Indicator 1: 503 rate

- Define exactly how `503 rate` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 2: predictor readiness

- Define exactly how `predictor readiness` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 3: model load time

- Define exactly how `model load time` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 4: restarts

- Define exactly how `restarts` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 5: endpoint count

- Define exactly how `endpoint count` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 6: route health

- Define exactly how `route health` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

### Indicator 7: inference latency

- Define exactly how `inference latency` is calculated.
- Identify Prometheus metric, log query, object condition, API or synthetic probe.
- Control label cardinality; never use request IDs, user IDs, prompts or object keys as labels.
- Derive warning and critical thresholds from SLO and load tests.
- Assign owner, escalation and runbook.
- Validate that the alert detects user impact and clears after recovery.

Use symptom alerts for paging and cause alerts for context. Use multi-window burn-rate alerts for user-facing SLOs.

## Failure-injection lab

1. Block a required egress path with NetworkPolicy. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
2. Use a test-invalid credential. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
3. Scale a dependency to zero. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
4. Exhaust test quota or storage. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
5. Use an invalid image digest. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
6. Use an incompatible runtime or model path. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
7. Drain a worker node. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
8. Expire a test certificate. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
9. Inject dependency latency. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.
10. Execute rollback and measure restoration. Capture hypothesis, expected signal, actual signal, alert, diagnosis, remediation, rollback and elapsed time.

## L3 operational drill catalog

Each drill is a mini interview case and a production rehearsal. Execute only in approved non-production environments.

### Drill 001 — Ready condition / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 002 — PredictorReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 003 — RoutesReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 004 — backend endpoint / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 005 — readiness probe / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 006 — model storage / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 007 — runtime compatibility / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 008 — timeout / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 009 — gateway / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 010 — service selector / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 011 — Ready condition / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 012 — PredictorReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 013 — RoutesReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 014 — backend endpoint / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 015 — readiness probe / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 016 — model storage / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 017 — runtime compatibility / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 018 — timeout / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 019 — gateway / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 020 — service selector / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 021 — Ready condition / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 022 — PredictorReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 023 — RoutesReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 024 — backend endpoint / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 025 — readiness probe / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 026 — model storage / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 027 — runtime compatibility / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 028 — timeout / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 029 — gateway / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 030 — service selector / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 031 — Ready condition / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 032 — PredictorReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 033 — RoutesReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 034 — backend endpoint / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 035 — readiness probe / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 036 — model storage / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 037 — runtime compatibility / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 038 — timeout / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 039 — gateway / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 040 — service selector / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 041 — Ready condition / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 042 — PredictorReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 043 — RoutesReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 044 — backend endpoint / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 045 — readiness probe / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 046 — model storage / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 047 — runtime compatibility / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 048 — timeout / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 049 — gateway / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 050 — service selector / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 051 — Ready condition / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 052 — PredictorReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 053 — RoutesReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 054 — backend endpoint / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 055 — readiness probe / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 056 — model storage / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 057 — runtime compatibility / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 058 — timeout / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 059 — gateway / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 060 — service selector / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 061 — Ready condition / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 062 — PredictorReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 063 — RoutesReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 064 — backend endpoint / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 065 — readiness probe / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 066 — model storage / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 067 — runtime compatibility / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 068 — timeout / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 069 — gateway / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 070 — service selector / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 071 — Ready condition / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 072 — PredictorReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 073 — RoutesReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 074 — backend endpoint / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 075 — readiness probe / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 076 — model storage / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 077 — runtime compatibility / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 078 — timeout / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 079 — gateway / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 080 — service selector / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 081 — Ready condition / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 082 — PredictorReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 083 — RoutesReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 084 — backend endpoint / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 085 — readiness probe / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 086 — model storage / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 087 — runtime compatibility / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 088 — timeout / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 089 — gateway / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 090 — service selector / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 091 — Ready condition / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 092 — PredictorReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 093 — RoutesReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 094 — backend endpoint / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 095 — readiness probe / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 096 — model storage / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 097 — runtime compatibility / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 098 — timeout / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 099 — gateway / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 100 — service selector / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 101 — Ready condition / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 102 — PredictorReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 103 — RoutesReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 104 — backend endpoint / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 105 — readiness probe / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 106 — model storage / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 107 — runtime compatibility / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 108 — timeout / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 109 — gateway / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 110 — service selector / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 111 — Ready condition / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 112 — PredictorReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 113 — RoutesReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 114 — backend endpoint / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 115 — readiness probe / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 116 — model storage / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 117 — runtime compatibility / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 118 — timeout / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 119 — gateway / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 120 — service selector / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 121 — Ready condition / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 122 — PredictorReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 123 — RoutesReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 124 — backend endpoint / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 125 — readiness probe / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 126 — model storage / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 127 — runtime compatibility / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 128 — timeout / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 129 — gateway / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 130 — service selector / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 131 — Ready condition / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 132 — PredictorReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 133 — RoutesReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 134 — backend endpoint / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 135 — readiness probe / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 136 — model storage / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 137 — runtime compatibility / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 138 — timeout / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 139 — gateway / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 140 — service selector / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 141 — Ready condition / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 142 — PredictorReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 143 — RoutesReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 144 — backend endpoint / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 145 — readiness probe / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 146 — model storage / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 147 — runtime compatibility / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 148 — timeout / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 149 — gateway / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 150 — service selector / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 151 — Ready condition / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 152 — PredictorReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 153 — RoutesReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 154 — backend endpoint / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 155 — readiness probe / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 156 — model storage / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 157 — runtime compatibility / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 158 — timeout / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 159 — gateway / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 160 — service selector / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 161 — Ready condition / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 162 — PredictorReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 163 — RoutesReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 164 — backend endpoint / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 165 — readiness probe / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 166 — model storage / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 167 — runtime compatibility / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 168 — timeout / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 169 — gateway / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 170 — service selector / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 171 — Ready condition / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 172 — PredictorReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 173 — RoutesReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 174 — backend endpoint / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 175 — readiness probe / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 176 — model storage / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 177 — runtime compatibility / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 178 — timeout / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 179 — gateway / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 180 — service selector / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 181 — Ready condition / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 182 — PredictorReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 183 — RoutesReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 184 — backend endpoint / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 185 — readiness probe / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 186 — model storage / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 187 — runtime compatibility / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 188 — timeout / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 189 — gateway / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 190 — service selector / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 191 — Ready condition / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 192 — PredictorReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 193 — RoutesReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 194 — backend endpoint / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 195 — readiness probe / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 196 — model storage / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 197 — runtime compatibility / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 198 — timeout / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 199 — gateway / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 200 — service selector / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 201 — Ready condition / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 202 — PredictorReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 203 — RoutesReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 204 — backend endpoint / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 205 — readiness probe / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 206 — model storage / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 207 — runtime compatibility / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 208 — timeout / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 209 — gateway / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 210 — service selector / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 211 — Ready condition / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 212 — PredictorReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 213 — RoutesReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 214 — backend endpoint / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 215 — readiness probe / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 216 — model storage / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 217 — runtime compatibility / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 218 — timeout / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 219 — gateway / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 220 — service selector / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 221 — Ready condition / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 222 — PredictorReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 223 — RoutesReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 224 — backend endpoint / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 225 — readiness probe / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 226 — model storage / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 227 — runtime compatibility / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 228 — timeout / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 229 — gateway / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 230 — service selector / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 231 — Ready condition / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 232 — PredictorReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 233 — RoutesReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 234 — backend endpoint / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 235 — readiness probe / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 236 — model storage / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 237 — runtime compatibility / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 238 — timeout / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 239 — gateway / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 240 — service selector / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 241 — Ready condition / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 242 — PredictorReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 243 — RoutesReady / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 244 — backend endpoint / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 245 — readiness probe / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 246 — model storage / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 247 — runtime compatibility / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 248 — timeout / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 249 — gateway / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 250 — service selector / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 251 — Ready condition / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 252 — PredictorReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 253 — RoutesReady / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 254 — backend endpoint / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 255 — readiness probe / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 256 — model storage / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 257 — runtime compatibility / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 258 — timeout / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 259 — gateway / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 260 — service selector / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 261 — Ready condition / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 262 — PredictorReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 263 — RoutesReady / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 264 — backend endpoint / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 265 — readiness probe / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 266 — model storage / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get installplans -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 267 — runtime compatibility / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get routes,services,endpointslices -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 268 — timeout / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get dscinitialization -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 269 — gateway / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc adm inspect namespace/<project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 270 — service selector / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get events -A --sort-by=.lastTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 271 — Ready condition / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc whoami`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 272 — PredictorReady / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc describe node <node>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 273 — RoutesReady / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc get clusterversion`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 274 — backend endpoint / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get resourcequota,limitrange -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 275 — readiness probe / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get subscriptions.operators.coreos.com -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 276 — model storage / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `model storage`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get secrets,pvc -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `model storage` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 277 — runtime compatibility / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `runtime compatibility`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc get datasciencecluster -A -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `runtime compatibility` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 278 — timeout / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `timeout`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc logs <pod> -n <project> --all-containers --prefix`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `timeout` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 279 — gateway / route misrouting

1. **Context:** A production-like workload shows `route misrouting` while validating `gateway`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get pods -A --sort-by=.metadata.creationTimestamp`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `gateway` works.
5. **Metric correlation:** Compare the incident timestamp with `predictor readiness` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 280 — service selector / storage-init failure

1. **Context:** A production-like workload shows `storage-init failure` while validating `service selector`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc adm must-gather`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `service selector` works.
5. **Metric correlation:** Compare the incident timestamp with `inference latency` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 281 — Ready condition / runtime crash

1. **Context:** A production-like workload shows `runtime crash` while validating `Ready condition`.
2. **Primary resource:** Inspect `ServingRuntime`, its owner and dependencies.
3. **First command:** `oc get nodes -o wide`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `Ready condition` works.
5. **Metric correlation:** Compare the incident timestamp with `endpoint count` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 282 — PredictorReady / GPU pending

1. **Context:** A production-like workload shows `GPU pending` while validating `PredictorReady`.
2. **Primary resource:** Inspect `Pod`, its owner and dependencies.
3. **First command:** `oc version`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `PredictorReady` works.
5. **Metric correlation:** Compare the incident timestamp with `model load time` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 283 — RoutesReady / model load timeout

1. **Context:** A production-like workload shows `model load timeout` while validating `RoutesReady`.
2. **Primary resource:** Inspect `EndpointSlice`, its owner and dependencies.
3. **First command:** `oc auth can-i --list -n <project>`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `RoutesReady` works.
5. **Metric correlation:** Compare the incident timestamp with `503 rate` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 284 — backend endpoint / selector mismatch

1. **Context:** A production-like workload shows `selector mismatch` while validating `backend endpoint`.
2. **Primary resource:** Inspect `Gateway`, its owner and dependencies.
3. **First command:** `oc get csv -A`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `backend endpoint` works.
5. **Metric correlation:** Compare the incident timestamp with `route health` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.

### Drill 285 — readiness probe / no endpoints

1. **Context:** A production-like workload shows `no endpoints` while validating `readiness probe`.
2. **Primary resource:** Inspect `Event`, its owner and dependencies.
3. **First command:** `oc get networkpolicy -n <project> -o yaml`.
4. **Healthy evidence:** Status, events, logs, endpoints and usage agree that `readiness probe` works.
5. **Metric correlation:** Compare the incident timestamp with `restarts` and one dependency metric.
6. **Negative test:** Reproduce from an in-cluster debug pod and from the intended client path.
7. **Branches:** drift; dependency; identity; DNS or policy; storage; scheduling; runtime; capacity.
8. **Safe remediation:** Restore `rollback` with the smallest reversible change.
9. **Validation:** Repeat the request, inspect conditions, verify SLO recovery and check collateral impact.
10. **Rollback:** Roll back if SLO is not restored, blast radius grows or another tenant is affected.
11. **Evidence:** Save before/after YAML, events, logs, metrics, Git diff, approver and timestamps.
12. **Interview point:** Explain why this order reduces guesswork and protects production.


## Senior interview questions and answer points

### Interview question 001

**Question:** Which requirements must be clarified first? Apply it to `PredictorReady` and `runtime crash`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 002

**Question:** Which layer do you inspect first and why? Apply it to `backend endpoint` and `GPU pending`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 003

**Question:** How do you separate symptom from root cause? Apply it to `model storage` and `model load timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 004

**Question:** Which objects are Operator managed? Apply it to `timeout` and `selector mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 005

**Question:** What is the safest rollback? Apply it to `service selector` and `no endpoints`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 006

**Question:** How do you prove isolation? Apply it to `PredictorReady` and `route misrouting`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 007

**Question:** How do you retain audit evidence? Apply it to `backend endpoint` and `storage-init failure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 008

**Question:** Which metrics prove success? Apply it to `model storage` and `runtime crash`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 009

**Question:** How do you test failure safely? Apply it to `timeout` and `GPU pending`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 010

**Question:** What changes in an air gap? Apply it to `service selector` and `model load timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 011

**Question:** How do quota and scheduling interact? Apply it to `PredictorReady` and `selector mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 012

**Question:** How do you protect credentials? Apply it to `backend endpoint` and `no endpoints`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 013

**Question:** Which compatibility checks are mandatory? Apply it to `model storage` and `route misrouting`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 014

**Question:** How do you prevent drift? Apply it to `timeout` and `storage-init failure`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 015

**Question:** How do you reserve capacity? Apply it to `service selector` and `runtime crash`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 016

**Question:** Which requirements must be clarified first? Apply it to `PredictorReady` and `GPU pending`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 017

**Question:** Which layer do you inspect first and why? Apply it to `backend endpoint` and `model load timeout`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 018

**Question:** How do you separate symptom from root cause? Apply it to `model storage` and `selector mismatch`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 019

**Question:** Which objects are Operator managed? Apply it to `timeout` and `no endpoints`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 020

**Question:** What is the safest rollback? Apply it to `service selector` and `route misrouting`.

**Answer points:**

- Begin with business impact and acceptance criteria.
- Verify versions and installed CRDs rather than relying on memory.
- Follow controller to pod to dependency.
- Preserve security controls during troubleshooting.
- Make a reversible change with rollback defined first.
- Validate with request, conditions, logs, events and metrics.
- Convert the lesson into GitOps, policy, monitoring or preflight automation.

### Interview question 021

## Size boundary note

Core implementation, drills, interview questions and checklists retained within the requested line range.
