---
title: "29. Alert rules for pipeline failures"
product_baseline: "Red Hat OpenShift AI Self-Managed 3.5"
openshift_monitoring_baseline: "Monitoring stack for Red Hat OpenShift 4.19"
audience: "Corporate L3 / senior platform engineer"
status: "Training and implementation guide"
---

# 29. Alert rules for pipeline failures

## 1. Scope and outcome

Implement alerts for pipeline server unavailability, run failures, task pod failures, pending tasks, image pulls, artifact storage, and recurring schedule misses.

By the end of this guide, you will have a deployable reference project, validation commands, PromQL, dashboard design, alert rules, production controls, and a troubleshooting runbook.

> **Version and support note:** The examples are written for OpenShift AI 3.5 and OpenShift monitoring 4.19. Verify compatibility with your installed minor releases. The dedicated OpenShift AI centralized observability stack in 3.5 is Technology Preview; use UWM and supported platform monitoring as the production baseline unless Red Hat explicitly supports your target design.

## 2. Why this topic matters at Corporate L3

- Alert rules for pipeline failures is not only a dashboard task; it is a reliability, security, capacity, and supportability discipline.
- A senior engineer must understand where a signal originates, how it is scraped or forwarded, what labels are attached, how it is retained, and who acts on it.
- The implementation must survive upgrades, multi-tenancy, runtime changes, node replacement, certificate rotation, disconnected environments, and incident conditions.
- Where the backend lacks stable metrics, generate bounded custom metrics from run status or use CR status collectors. Do not alert directly from log text as the only signal.
- The project therefore uses layered signals: resource state, application metrics, synthetic checks, events, logs, and—where valuable—traces.

## 3. Learning objectives

1. Explain the architecture and data flow for Alert rules for pipeline failures.
2. Identify authoritative, derived, and synthetic signals.
3. Enable user workload monitoring safely.
4. Expose or discover a metrics endpoint.
5. Create ServiceMonitor or PodMonitor resources.
6. Write bounded, efficient PromQL queries.
7. Create recording rules and actionable alerts.
8. Build an operational dashboard with drill-down paths.
9. Validate no-data, partial-failure, and upgrade scenarios.
10. Troubleshoot from symptom to root cause using a repeatable evidence chain.
11. Apply capacity, security, governance, and GitOps controls.
12. Present findings to platform, data science, SRE, and management stakeholders.

## 4. Architecture and data flow

### 4.1 Core components

- **pipeline backend metrics:** role in the telemetry or monitored workload path.
- **Kubernetes workload metrics:** role in the telemetry or monitored workload path.
- **event metrics:** role in the telemetry or monitored workload path.
- **synthetic object-store probe:** role in the telemetry or monitored workload path.
- **PrometheusRule:** role in the telemetry or monitored workload path.

### 4.2 Logical flow

1. The workload or exporter exposes an HTTP metrics endpoint, usually `/metrics`.
2. A Service and named port provide stable discovery for the endpoint.
3. A ServiceMonitor or PodMonitor selects the target and defines path, interval, scheme, TLS, and authentication.
4. Prometheus discovers the monitor resource, applies relabeling, and stores time series.
5. Recording rules precompute expensive or standardized service-level expressions.
6. PrometheusRule objects evaluate alert conditions and send firing alerts to Alertmanager.
7. Alertmanager groups, inhibits, routes, and notifies receivers.
8. OpenShift Console or Grafana queries Prometheus/Thanos and renders operational views.
9. Logs, events, and traces provide high-cardinality diagnostic context that should not be forced into metric labels.
10. Runbooks connect alerts and dashboards to deterministic triage and remediation steps.

### 4.3 Signal model

- `run failure ratio` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `failed jobs` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `pending pods` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `ImagePullBackOff` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `artifact failure` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `schedule delay` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.
- `server down` — define source, unit, labels, freshness, aggregation, threshold, owner, and retention.

### 4.4 Production architecture decisions

- Use platform monitoring only for supported core namespaces and components.
- Use UWM for project-owned AI applications and custom exporters.
- Use the OpenShift AI dedicated observability stack only according to its support status and your risk acceptance.
- Choose one long-term metrics architecture: in-cluster retention, remote write, or a federated/global system.
- Define a cluster label and environment label before multi-cluster aggregation.
- Use a controlled metric contract for custom runtimes and exporters.
- Keep page-generating alerts close to the user-visible failure and use lower-severity signals for diagnosis.
- Use synthetic checks for dependencies that expose no reliable server-side metric.

## 5. Prerequisites and access

- [ ] A healthy OpenShift cluster compatible with the installed OpenShift AI release.
- [ ] Red Hat OpenShift AI installed and reconciled.
- [ ] OpenShift CLI `oc` installed.
- [ ] Cluster-admin for cluster-wide configuration; project admin for namespaced examples.
- [ ] User workload monitoring enabled for project workloads.
- [ ] A Git repository for manifests, dashboards, rules, and runbooks.
- [ ] A test namespace and non-production receiver for alert testing.
- [ ] Approved network egress, proxy, CA bundle, and secret-management design.

### 5.1 RBAC checks

```bash
oc auth can-i get pods -A
oc auth can-i create servicemonitor.monitoring.coreos.com -n ai-monitoring-29
oc auth can-i create prometheusrule.monitoring.coreos.com -n ai-monitoring-29
oc auth can-i update configmap/cluster-monitoring-config -n openshift-monitoring
oc auth can-i get --raw /api/v1/namespaces/openshift-monitoring/services/https:thanos-querier:9091/proxy/api/v1/query
```

## 6. Discovery before implementation

Capture this evidence before changing the cluster. Store output with the change ticket.

```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A | egrep -i 'rhods|openshift-ai|monitoring|observability|gpu|logging'
oc get dscinitialization -A -o yaml
oc get datasciencecluster -A -o yaml
oc get pods -n openshift-monitoring
oc get pods -n openshift-user-workload-monitoring
oc get pods -A | egrep 'redhat-ods|rhods|modelmesh|kserve|trustyai|pipeline|dcgm'
oc get servicemonitor,podmonitor,prometheusrule -A
oc get all -n ai-monitoring-29
oc get events -n ai-monitoring-29 --sort-by=.lastTimestamp
oc get route -A
oc get pvc -A
oc get networkpolicy -A
```

### 6.1 Live metric discovery

```bash
# Locate services and ports that may expose metrics
oc get svc -n ai-monitoring-29 -o wide
oc get svc -n ai-monitoring-29 -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{range .spec.ports[*]}{.name}={.port} {end}{"\n"}{end}'
# Port-forward the target service and inspect the actual exposition format
oc -n ai-monitoring-29 port-forward svc/alert-rules-for-pipeline-failures-metrics 9095:8080
curl -fsS http://127.0.0.1:9095/metrics | head -100
curl -fsS http://127.0.0.1:9095/metrics | grep '^# HELP' | head -50
```

## 7. Project from scratch

Project name: **29-alert-rules-for-pipeline-failures-lab**

### 7.1 Create a project and ownership labels

Create a dedicated namespace, owner labels, environment labels, and quota boundaries.

**Exit criterion:** Evidence proves that phase 1 is complete and repeatable from Git.

### 7.2 Enable monitoring prerequisites

Confirm UWM pods are running and monitoring CRDs are available.

**Exit criterion:** Evidence proves that phase 2 is complete and repeatable from Git.

### 7.3 Deploy a safe sample exporter

Use a tiny application that exposes a bounded metric set.

**Exit criterion:** Evidence proves that phase 3 is complete and repeatable from Git.

### 7.4 Create Service and ServiceMonitor

Use a named service port and a monitor selector in the same project.

**Exit criterion:** Evidence proves that phase 4 is complete and repeatable from Git.

### 7.5 Validate target discovery

Inspect target status and raw samples before building dashboards.

**Exit criterion:** Evidence proves that phase 5 is complete and repeatable from Git.

### 7.6 Define recording rules

Standardize rate, error ratio, percentile, and saturation calculations.

**Exit criterion:** Evidence proves that phase 6 is complete and repeatable from Git.

### 7.7 Define alerts

Add warning and critical thresholds with `for` durations and runbook annotations.

**Exit criterion:** Evidence proves that phase 7 is complete and repeatable from Git.

### 7.8 Add dashboard-as-code

Create OpenShift Console or Grafana dashboard resources from Git.

**Exit criterion:** Evidence proves that phase 8 is complete and repeatable from Git.

### 7.9 Configure notification routing

Use a non-production receiver and verify end-to-end delivery.

**Exit criterion:** Evidence proves that phase 9 is complete and repeatable from Git.

### 7.10 Run failure injection

Stop a target, return errors, add latency, and simulate capacity pressure.

**Exit criterion:** Evidence proves that phase 10 is complete and repeatable from Git.

### 7.11 Collect incident evidence

Save queries, alerts, logs, events, and deployment state.

**Exit criterion:** Evidence proves that phase 11 is complete and repeatable from Git.

### 7.12 Harden and promote

Apply RBAC, NetworkPolicy, secrets, retention, ownership, and change controls.

**Exit criterion:** Evidence proves that phase 12 is complete and repeatable from Git.

## 8. Base manifests

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: ai-monitoring-29
  labels:
    app.kubernetes.io/part-of: openshift-ai-observability
    observability.company.example/owner: ai-platform
    observability.company.example/environment: lab
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alert-rules-for-pipeline-failures-monitor
  namespace: ai-monitoring-29
```

### 8.1 Example metrics workload

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-rules-for-pipeline-failures-exporter
  namespace: ai-monitoring-29
  labels:
    app: alert-rules-for-pipeline-failures-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-rules-for-pipeline-failures-exporter
  template:
    metadata:
      labels:
        app: alert-rules-for-pipeline-failures-exporter
        monitoring.opendatahub.io/scrape: "true"
    spec:
      serviceAccountName: alert-rules-for-pipeline-failures-monitor
      containers:
        - name: exporter
          image: quay.io/prometheuscommunity/json-exporter:v0.7.0
          args: ["--config.file=/config/config.yml"]
          ports:
            - name: metrics
              containerPort: 7979
          resources:
            requests: {cpu: 20m, memory: 32Mi}
            limits: {cpu: 100m, memory: 128Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: alert-rules-for-pipeline-failures-metrics
  namespace: ai-monitoring-29
  labels:
    app: alert-rules-for-pipeline-failures-exporter
spec:
  selector:
    app: alert-rules-for-pipeline-failures-exporter
  ports:
    - name: metrics
      port: 7979
      targetPort: metrics
```

> Replace the sample image with an approved internal image in disconnected or regulated environments. The sample exists only to demonstrate discovery; the project-specific metrics should come from the real component or a purpose-built exporter.

### 8.2 ServiceMonitor

```yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alert-rules-for-pipeline-failures
  namespace: ai-monitoring-29
  labels:
    app.kubernetes.io/part-of: openshift-ai-observability
spec:
  selector:
    matchLabels:
      app: alert-rules-for-pipeline-failures-exporter
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
      honorLabels: false
```

### 8.3 NetworkPolicy for Prometheus scraping

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-user-workload-monitoring
  namespace: ai-monitoring-29
spec:
  podSelector:
    matchLabels:
      app: alert-rules-for-pipeline-failures-exporter
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: openshift-user-workload-monitoring
      ports:
        - protocol: TCP
          port: 7979
```

## 9. PromQL query catalog

Run each query in Observe → Metrics or against Thanos Querier. Replace example label matchers with labels present in your cluster.

| # | Purpose | PromQL | Notes |
|---:|---|---|---|
| 1 | Inspect `kube_job_status_failed` | `kube_job_status_failed  # Verify labels and availability on the deployed version` | Validate units, labels, and freshness. |
| 2 | Inspect `kube_pod_container_status_waiting_reason` | `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` | Validate units, labels, and freshness. |
| 3 | Inspect `kube_pod_status_phase` | `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` | Validate units, labels, and freshness. |
| 4 | Inspect `probe_success` | `min by (instance, module) (probe_success)` | Validate units, labels, and freshness. |
| 5 | Inspect `up` | `min by (namespace, job) (up{namespace=~"$namespace"})` | Validate units, labels, and freshness. |
| 6 | Inspect `kube_pod_container_status_restarts_total` | `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` | Validate units, labels, and freshness. |
| 7 | Target health | `min by (job, namespace) (up)` | Use bounded filters in production. |
| 8 | Scrape sample volume | `sum by (job) (scrape_samples_scraped)` | Use bounded filters in production. |
| 9 | Series added rate | `sum by (job) (rate(scrape_series_added[5m]))` | Use bounded filters in production. |
| 10 | Pod restart trend | `sum by (namespace, pod) (increase(kube_pod_container_status_restarts_total[1h]))` | Use bounded filters in production. |
| 11 | Active firing alerts | `sum by (severity) (ALERTS{alertstate="firing"})` | Use bounded filters in production. |
| 12 | Data freshness | `time() - max by (job) (timestamp(up))` | Use bounded filters in production. |

### 9.1 Query engineering rules

- Always identify the metric type and unit before applying rate or aggregation.
- Apply rate/increase to counters before aggregation unless reset behavior is intentionally handled another way.
- Aggregate histogram buckets by `le` and required dimensions before `histogram_quantile`.
- Use `clamp_min` to avoid division by zero in ratios.
- Add a minimum-traffic guard to error-ratio and latency alerts.
- Avoid broad regex over high-cardinality labels.
- Create recording rules for queries used by multiple panels or alerts.
- Show explicit no-data behavior; absence can mean healthy idle, broken scrape, or missing instrumentation.
- Use `timestamp()` and `time()` to verify freshness.
- Benchmark query duration before publishing a fleet-wide dashboard.

## 10. Recording rules

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: alert-rules-for-pipeline-failures-recording
  namespace: ai-monitoring-29
spec:
  groups:
    - name: alert-rules-for-pipeline-failures.recording
      interval: 30s
      rules:
        - record: ai:alert-rules-for-pipeline-failures:target_availability:ratio5m
          expr: avg_over_time(up{namespace="ai-monitoring-29"}[5m])
          labels:
            service: alert-rules-for-pipeline-failures
            owner: ai-platform
        - record: ai:alert-rules-for-pipeline-failures:pod_restarts:sum15m
          expr: sum(increase(kube_pod_container_status_restarts_total{namespace="ai-monitoring-29"}[15m]))
          labels:
            service: alert-rules-for-pipeline-failures
        - record: ai:alert-rules-for-pipeline-failures:signal_present
          expr: count(kube_job_status_failed) > bool 0
          labels:
            service: alert-rules-for-pipeline-failures
```

## 11. Alert rules

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: alert-rules-for-pipeline-failures-alerts
  namespace: ai-monitoring-29
spec:
  groups:
    - name: alert-rules-for-pipeline-failures.alerts
      rules:

      - alert: PipelineServerDown
        expr: absent(up{namespace="ai-monitoring-29"}) == 1
        for: 5m
        labels:
          severity: critical
          service: alert-rules-for-pipeline-failures
          owner: ai-platform
        annotations:
          summary: "PipelineServerDown for Alert rules for pipeline failures"
          description: "Signal up/absence crossed the validated threshold. Check the linked dashboard and runbook before remediation."
          runbook_url: "https://runbooks.example.com/openshift-ai/alert-rules-for-pipeline-failures/pipelineserverdown"

      - alert: PipelineRunFailuresHigh
        expr: absent(up{namespace="ai-monitoring-29"}) == 1
        for: 10m
        labels:
          severity: warning
          service: alert-rules-for-pipeline-failures
          owner: ai-platform
        annotations:
          summary: "PipelineRunFailuresHigh for Alert rules for pipeline failures"
          description: "Signal up/absence crossed the validated threshold. Check the linked dashboard and runbook before remediation."
          runbook_url: "https://runbooks.example.com/openshift-ai/alert-rules-for-pipeline-failures/pipelinerunfailureshigh"

      - alert: PipelineTaskPending
        expr: absent(up{namespace="ai-monitoring-29"}) == 1
        for: 10m
        labels:
          severity: critical
          service: alert-rules-for-pipeline-failures
          owner: ai-platform
        annotations:
          summary: "PipelineTaskPending for Alert rules for pipeline failures"
          description: "Signal up/absence crossed the validated threshold. Check the linked dashboard and runbook before remediation."
          runbook_url: "https://runbooks.example.com/openshift-ai/alert-rules-for-pipeline-failures/pipelinetaskpending"

      - alert: PipelineImagePullFailure
        expr: absent(up{namespace="ai-monitoring-29"}) == 1
        for: 10m
        labels:
          severity: warning
          service: alert-rules-for-pipeline-failures
          owner: ai-platform
        annotations:
          summary: "PipelineImagePullFailure for Alert rules for pipeline failures"
          description: "Signal up/absence crossed the validated threshold. Check the linked dashboard and runbook before remediation."
          runbook_url: "https://runbooks.example.com/openshift-ai/alert-rules-for-pipeline-failures/pipelineimagepullfailure"

      - alert: PipelineArtifactStoreDown
        expr: absent(up{namespace="ai-monitoring-29"}) == 1
        for: 10m
        labels:
          severity: warning
          service: alert-rules-for-pipeline-failures
          owner: ai-platform
        annotations:
          summary: "PipelineArtifactStoreDown for Alert rules for pipeline failures"
          description: "Signal up/absence crossed the validated threshold. Check the linked dashboard and runbook before remediation."
          runbook_url: "https://runbooks.example.com/openshift-ai/alert-rules-for-pipeline-failures/pipelineartifactstoredown"
```

### 11.1 Alert acceptance criteria

- [ ] Expression returns the intended series in normal, warning, and failure states.
- [ ] The `for` duration suppresses rollout and scrape transients.
- [ ] The alert has service, owner, severity, environment, and cluster context.
- [ ] Summary and description explain user impact, not only a metric threshold.
- [ ] Runbook is reachable by the on-call team.
- [ ] Receiver delivery is tested and evidenced.
- [ ] Silence and inhibition behavior is documented.
- [ ] Recovery is automatically verified after remediation.

## 12. Dashboard design

### 12.1 Recommended layout

1. **Pipeline alerts** — show current value, trend, threshold, owner, and drill-down link.
2. **Failure ratio** — show current value, trend, threshold, owner, and drill-down link.
3. **Failed tasks** — show current value, trend, threshold, owner, and drill-down link.
4. **Pending tasks** — show current value, trend, threshold, owner, and drill-down link.
5. **Image pulls** — show current value, trend, threshold, owner, and drill-down link.
6. **Artifact store** — show current value, trend, threshold, owner, and drill-down link.
7. **Server health** — show current value, trend, threshold, owner, and drill-down link.
8. **Schedule lag** — show current value, trend, threshold, owner, and drill-down link.

### 12.2 Dashboard variables

- `$cluster` — populate from a bounded label query and set a safe default.
- `$environment` — populate from a bounded label query and set a safe default.
- `$namespace` — populate from a bounded label query and set a safe default.
- `$workload` — populate from a bounded label query and set a safe default.
- `$model` — populate from a bounded label query and set a safe default.
- `$model_version` — populate from a bounded label query and set a safe default.
- `$node` — populate from a bounded label query and set a safe default.
- `$gpu_uuid` — populate from a bounded label query and set a safe default.
- `$time_range` — populate from a bounded label query and set a safe default.

### 12.3 OpenShift Console dashboard ConfigMap pattern

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-alert-rules-for-pipeline-failures
  namespace: openshift-config-managed
  labels:
    console.openshift.io/dashboard: "true"
data:
  alert-rules-for-pipeline-failures.json: |-
    {
      "annotations": {
        "list": []
      },
      "editable": false,
      "panels": [
        {
          "type": "stat",
          "title": "Alert rules for pipeline failures target availability",
          "targets": [
            {
              "expr": "avg(up{namespace=\"ai-monitoring-29\"})",
              "refId": "A"
            }
          ],
          "gridPos": {
            "h": 6,
            "w": 6,
            "x": 0,
            "y": 0
          }
        },
        {
          "type": "timeseries",
          "title": "Pod restarts",
          "targets": [
            {
              "expr": "sum(increase(kube_pod_container_status_restarts_total{namespace=\"ai-monitoring-29\"}[15m]))",
              "refId": "A"
            }
          ],
          "gridPos": {
            "h": 8,
            "w": 12,
            "x": 6,
            "y": 0
          }
        }
      ],
      "schemaVersion": 39,
      "tags": [
        "openshift-ai",
        "corporate-l3",
        "alert-rules-for-pipeline-failures"
      ],
      "templating": {
        "list": []
      },
      "time": {
        "from": "now-6h",
        "to": "now"
      },
      "title": "29. Alert rules for pipeline failures",
      "uid": "rhoai-29-alert-rules-for-pipeline-failur",
      "version": 1
    }
```

## 13. Validation procedure

1. oc apply -f manifests/ -n ai-monitoring-29
2. oc get servicemonitor,podmonitor,prometheusrule -n ai-monitoring-29
3. oc get endpointslice -n ai-monitoring-29
4. oc get pods -n ai-monitoring-29 -o wide
5. oc get events -n ai-monitoring-29 --sort-by=.lastTimestamp
6. Open Observe → Targets and verify target state is Up.
7. Open Observe → Metrics and run `up`, then run each topic query.
8. Open Observe → Alerting and verify rule evaluation status.
9. Open the dashboard and verify every panel has a defined no-data behavior.
10. Capture screenshots or API output as implementation evidence.

### 13.1 API query through Thanos Querier

```bash
TOKEN=$(oc whoami -t)
QUERY='up'
curl -sk -H "Authorization: Bearer ${TOKEN}" --get \
  --data-urlencode "query=${QUERY}" \
  https://thanos-querier-openshift-monitoring.apps.example.com/api/v1/query | jq .
```

## 14. Failure-injection lab

| Test | Action | Expected evidence |
|---|---|---|
| Target failure | `oc -n ai-monitoring-29 scale deploy/alert-rules-for-pipeline-failures-exporter --replicas=0` | Target-down alert enters pending and then firing. |
| Recovery | `oc -n ai-monitoring-29 scale deploy/alert-rules-for-pipeline-failures-exporter --replicas=1` | Alert resolves and target returns Up. |
| Selector failure | `oc -n ai-monitoring-29 label svc/alert-rules-for-pipeline-failures-metrics app-` | Target disappears; monitoring-object troubleshooting identifies mismatch. |
| Restore selector | `oc -n ai-monitoring-29 label svc/alert-rules-for-pipeline-failures-metrics app=alert-rules-for-pipeline-failures-exporter` | Target returns without recreating Prometheus. |
| Network failure | `oc -n ai-monitoring-29 apply -f deny-monitoring.yaml` | Target shows connectivity timeout; NetworkPolicy evidence confirms cause. |
| High cardinality simulation | `Deploy a lab-only exporter with bounded synthetic labels, then remove it.` | Series growth is visible and cleanup is verified. |
| Alert delivery test | `Temporarily use a safe `vector(1)` test rule with a dedicated receiver.` | Receiver receives and resolves the test alert. |
| Dashboard no-data test | `Select a namespace with no matching workload.` | Panels show an intentional no-data message rather than misleading zero. |

## 15. Troubleshooting decision matrix

| Symptom | Likely causes | L3 evidence and action |
|---|---|---|
| No target appears | ServiceMonitor/PodMonitor selector mismatch; wrong namespace; no named service port | Compare labels and selectors; inspect Prometheus targets; verify RBAC and network policy |
| Target is down | Wrong path/port/scheme; TLS/auth failure; application not listening | curl the endpoint from a debug pod; inspect target error; verify certificate and bearer token |
| Metric is absent | Exporter disabled; metric renamed; relabeling dropped it; scrape timeout | Inspect raw /metrics; query metadata; compare image version and exporter configuration |
| Dashboard shows N/A | Wrong variable value; label mismatch; data-source permission; time range too short | Run panel query directly; inspect labels; broaden time; check data-source status |
| PromQL is slow | Unbounded regex; high cardinality; long range; missing recording rule | Use bounded matchers; pre-aggregate; reduce range; create recording rules |
| Alert never fires | Expression wrong; no data; for duration not elapsed; rule not selected | Query expression; inspect rule status; use promtool; verify PrometheusRule labels |
| Alert flaps | Threshold too tight; sparse traffic; no hysteresis; rollout transients | Add for duration, minimum traffic, separate warning/critical, or use multi-window burn |
| Notifications fail | Receiver secret/config invalid; egress/TLS/DNS issue | Inspect Alertmanager status/logs; test endpoint; check proxy and NetworkPolicy |
| Cardinality grows | Dynamic labels such as request ID, path, user, model hash | Drop/relabel labels; redesign instrumentation; cap dimensions |
| Metrics are stale | Exporter hung; target unreachable; clock issue; scrape disabled | Check timestamp(), up, scrape duration, pod health, node time sync |
| Pod metrics missing | Pod terminated; kubelet scrape issue; namespace access | Check pod UID/time range; kubelet targets; UWM namespace permissions |
| PVC metrics missing | CSI driver does not expose volume stats or volume not mounted | Check kubelet volume stats and CSI capability; use storage backend metrics |
| GPU metrics missing | dcgm-exporter absent; GPU node selector; ServiceMonitor not discovered | Check ClusterPolicy, DaemonSet, service, endpoint and raw /metrics |
| Route probe fails | DNS, TLS, router, OAuth redirect, backend endpoint | Run dig/curl -vk from internal and external probes; inspect route and endpoints |
| Model metrics missing | Runtime does not expose metrics or service port not declared | Inspect runtime docs and /metrics; patch Service/ServiceMonitor; avoid assuming KServe metrics |
| Pipeline status unclear | Backend metric names changed; task labels inconsistent | Use Kubernetes status and backend API; standardize labels and run IDs |
| Logs and metrics disagree | Different time zones/windows; sampling; pod replacement | Normalize UTC; use pod UID; correlate deployment and event timeline |
| Remote write backlog | Receiver unavailable; network issue; queue undersized | Inspect queue metrics; test endpoint; tune shards/capacity only after fixing receiver |
| Monitoring PVC fills | Retention too long; cardinality high; no capacity planning | Reduce cardinality/retention or expand PVC according to supported configuration |
| Access forbidden | RBAC or namespace isolation prevents query/target access | Use oc auth can-i; validate cluster roles and project permissions |

### 15.1 Deterministic target troubleshooting

1. Confirm the workload pod is Ready and listening on the expected port.
2. Confirm the Service selector resolves to EndpointSlices.
3. Confirm the Service port has the exact name referenced by ServiceMonitor.
4. Confirm ServiceMonitor selector matches Service labels.
5. Confirm the monitor resource is in a namespace watched by UWM.
6. Confirm NetworkPolicy allows Prometheus ingress.
7. Confirm TLS/auth settings by curling from a pod in the monitoring namespace.
8. Inspect Prometheus target error text.
9. Inspect Prometheus Operator logs for rejected configuration.
10. Inspect sample/label limits and metric relabeling.

## 16. Capacity and performance engineering

- Measure active series, samples per second, scrape payload bytes, rule duration, query duration, and retention growth.
- Estimate daily storage from measured ingestion and compression instead of a generic bytes-per-sample assumption.
- Set scrape intervals according to decision latency; do not scrape every exporter at 5 seconds without need.
- Use recording rules to reduce repeated high-cost range queries.
- Keep dashboard refresh intervals slower than scrape intervals unless a specific use case requires otherwise.
- Separate high-frequency GPU/runtime metrics from low-frequency inventory metrics.
- Load-test remote-write receivers and network paths.
- Plan PVC expansion and monitoring storage IOPS before retention changes.
- Use query logs temporarily to find expensive PromQL, then disable extra logging.
- Review top label names and values during every capacity review.

## 17. Security and compliance controls

- [ ] Use least-privilege RBAC for reading metrics, editing monitors, and managing alert routes.
- [ ] Do not expose Prometheus, Alertmanager, Grafana, exporter, or debug endpoints without authentication and TLS controls.
- [ ] Store receiver tokens, webhooks, and client credentials in Secrets, not ConfigMaps or Git.
- [ ] Apply NetworkPolicy between monitoring components, workloads, and external receivers.
- [ ] Use approved CA bundles and certificate-rotation processes.
- [ ] Redact prompts, inputs, outputs, user identifiers, credentials, and object keys.
- [ ] Log administrative changes to monitoring rules and routing.
- [ ] Protect dashboard folders and data sources by team and environment.
- [ ] Define telemetry retention and deletion based on policy.
- [ ] Review exporter images, signatures, vulnerabilities, and software bill of materials.

## 18. Production readiness checklist

- [ ] Record OpenShift, OpenShift AI, runtime, GPU Operator, CSI, and logging versions.
- [ ] Classify each feature as Generally Available, Technology Preview, or custom/unsupported.
- [ ] Use GitOps for ServiceMonitor, PodMonitor, PrometheusRule, dashboards, and routing configuration.
- [ ] Define service owner, on-call group, severity, escalation, and runbook URL for every page.
- [ ] Use namespace, workload, model, version, environment, cluster, and owner labels with bounded values.
- [ ] Never use request ID, prompt, user ID, object key, exception text, or raw URL as a metric label.
- [ ] Set explicit scrape interval, timeout, sample limit, target limit, and label limit where supported.
- [ ] Create recording rules for expensive dashboard and SLO queries.
- [ ] Size monitoring CPU, memory, storage, IOPS, and retention from measured series and ingestion.
- [ ] Protect metrics endpoints with network policy, TLS, and least-privilege RBAC where applicable.
- [ ] Separate production alerting from lab/demo receivers.
- [ ] Test alert delivery quarterly and after any routing, proxy, certificate, or receiver change.
- [ ] Validate dashboard JSON and rule syntax in CI before deployment.
- [ ] Keep dashboard variables bounded and provide safe defaults.
- [ ] Display data freshness and no-data states explicitly.
- [ ] Correlate dashboards with deployment, configuration, and incident annotations.
- [ ] Use multi-window burn-rate alerts for SLO paging.
- [ ] Use warning alerts for capacity lead time and critical alerts for imminent service impact.
- [ ] Document maintenance windows and inhibition behavior.
- [ ] Back up monitoring configuration and dashboard source, not only the UI state.
- [ ] Forward long-term metrics only after label governance and cost review.
- [ ] Redact credentials, tokens, prompts, model inputs, model outputs, and PII from telemetry.
- [ ] Define retention by operational, security, and compliance need.
- [ ] Run failure-injection tests for target down, latency, 5xx, storage, GPU, and receiver failure.
- [ ] Review noisy, stale, or unactionable alerts every sprint or month.

## 19. Operational runbook

1. **Acknowledge:** Record alert name, start time, affected cluster/namespace/model, and current user impact.
2. **Validate signal:** Run the alert expression and compare with dashboard, synthetic, logs, and events.
3. **Scope impact:** Identify affected models, versions, tenants, nodes, GPUs, storage, and routes.
4. **Check recent change:** Review deployment, operator reconciliation, certificate, policy, quota, and configuration changes.
5. **Stabilize:** Rollback, scale, reroute, restart only when evidence supports the action; preserve diagnostics first.
6. **Verify recovery:** Confirm target, SLI, alerts, logs, and synthetic checks are healthy for a defined observation window.
7. **Communicate:** Update incident channel with impact, action, risk, and next checkpoint.
8. **Close:** Document root cause, detection gap, corrective action, owner, and due date.

### 19.1 Evidence bundle commands

```bash
mkdir -p evidence/29-alert-rules-for-pipeline-failures
oc get all -n ai-monitoring-29 -o yaml > evidence/29-alert-rules-for-pipeline-failures/resources.yaml
oc get events -n ai-monitoring-29 --sort-by=.lastTimestamp > evidence/29-alert-rules-for-pipeline-failures/events.txt
oc get servicemonitor,podmonitor,prometheusrule -n ai-monitoring-29 -o yaml > evidence/29-alert-rules-for-pipeline-failures/monitoring-crs.yaml
oc logs -n ai-monitoring-29 deploy/alert-rules-for-pipeline-failures-exporter --all-containers --tail=1000 > evidence/29-alert-rules-for-pipeline-failures/logs.txt
oc adm inspect ns/ai-monitoring-29 --dest-dir=evidence/29-alert-rules-for-pipeline-failures/inspect
```

## 20. Cleanup and rollback

```bash
oc delete namespace ai-monitoring-29
oc delete configmap dashboard-alert-rules-for-pipeline-failures -n openshift-config-managed --ignore-not-found
# Revert any cluster-monitoring-config or UWM changes from the Git-controlled previous version.
# Remove temporary test silences and receivers.
# Verify no orphaned targets, rules, secrets, routes, PVCs, or remote-write series remain.
```

## 21. Project deliverables

- [ ] Architecture diagram and data-flow explanation.
- [ ] Version and supportability matrix.
- [ ] Git repository with manifests and overlays.
- [ ] Metric catalog with type, unit, labels, owner, and retention.
- [ ] PromQL query catalog and recording rules.
- [ ] Dashboard JSON/CR and screenshots.
- [ ] Alert rules, routing configuration, and delivery evidence.
- [ ] Failure-injection test report.
- [ ] Capacity estimate and cardinality review.
- [ ] Security and compliance review.
- [ ] Troubleshooting runbook and evidence bundle.
- [ ] Operational handover and acceptance sign-off.

## 22. Senior-level assessment questions

1. Explain the difference between platform monitoring and user workload monitoring in OpenShift.
2. Why can a ServiceMonitor exist while no target appears in Prometheus?
3. How do you identify a metric-cardinality incident?
4. Why is histogram_quantile preferred over averaging pod-level quantiles?
5. How do you distinguish no traffic from an unavailable endpoint?
6. What should be in an actionable alert annotation?
7. How do you prove that an alert route works end to end?
8. When do you use a recording rule?
9. How do you monitor an AI runtime whose metric names change between versions?
10. How do you monitor a route from both inside and outside the cluster?

## 23. Topic-specific design review

1. What is the authoritative source for each Alert rules for pipeline failures signal?
2. Which metrics are stable across upgrades, and which are runtime-specific?
3. What does zero mean, and how is it different from no data?
4. What labels are required for ownership and drill-down?
5. What is the expected traffic/load baseline and seasonality?
6. Which thresholds are derived from SLOs, capacity, vendor guidance, or historical baseline?
7. What failure mode is not visible through metrics alone?
8. How will the design work in disconnected and multi-cluster environments?
9. What part is supported by Red Hat, vendor-supported, Technology Preview, or custom?
10. What is the rollback plan if the monitoring change increases load or cardinality?

## 24. Corporate L3 deep-dive scenario workbook

Use these drills as implementation exercises, interview practice, incident simulations, and change-review tests. Each drill must produce evidence, not only a verbal answer.

### 24.1 Scenario 001: run failure ratio on pipeline backend metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a target-discovery mismatch; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.2 Scenario 002: failed jobs on Kubernetes workload metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a NetworkPolicy change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.3 Scenario 003: pending pods on event metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a certificate rotation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.4 Scenario 004: ImagePullBackOff on synthetic object-store probe

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume an exporter upgrade; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.5 Scenario 005: artifact failure on PrometheusRule

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume a runtime metric rename; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.6 Scenario 006: schedule delay on pipeline backend metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a high-cardinality label; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.7 Scenario 007: server down on Kubernetes workload metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a storage latency spike; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.8 Scenario 008: run failure ratio on event metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a node drain; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.9 Scenario 009: failed jobs on synthetic object-store probe

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume a quota change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.10 Scenario 010: pending pods on PrometheusRule

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume a failed rollout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.11 Scenario 011: ImagePullBackOff on pipeline backend metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a proxy or no-proxy error; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.12 Scenario 012: artifact failure on Kubernetes workload metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a DNS degradation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.13 Scenario 013: schedule delay on event metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a receiver outage; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.14 Scenario 014: server down on synthetic object-store probe

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume a remote-write backlog; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.15 Scenario 015: run failure ratio on PrometheusRule

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume an expired credential; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.16 Scenario 016: failed jobs on pipeline backend metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a GPU hardware fault; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.17 Scenario 017: pending pods on Kubernetes workload metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a model load regression; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.18 Scenario 018: ImagePullBackOff on event metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a pipeline image pull failure; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.19 Scenario 019: artifact failure on synthetic object-store probe

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume an object-store timeout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.20 Scenario 020: schedule delay on PrometheusRule

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume an RBAC change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.21 Scenario 021: server down on pipeline backend metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a target-discovery mismatch; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.22 Scenario 022: run failure ratio on Kubernetes workload metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a NetworkPolicy change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.23 Scenario 023: failed jobs on event metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a certificate rotation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.24 Scenario 024: pending pods on synthetic object-store probe

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume an exporter upgrade; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.25 Scenario 025: ImagePullBackOff on PrometheusRule

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume a runtime metric rename; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.26 Scenario 026: artifact failure on pipeline backend metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a high-cardinality label; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.27 Scenario 027: schedule delay on Kubernetes workload metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a storage latency spike; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.28 Scenario 028: server down on event metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a node drain; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.29 Scenario 029: run failure ratio on synthetic object-store probe

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume a quota change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.30 Scenario 030: failed jobs on PrometheusRule

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume a failed rollout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.31 Scenario 031: pending pods on pipeline backend metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a proxy or no-proxy error; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.32 Scenario 032: ImagePullBackOff on Kubernetes workload metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a DNS degradation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.33 Scenario 033: artifact failure on event metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a receiver outage; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.34 Scenario 034: schedule delay on synthetic object-store probe

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume a remote-write backlog; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.35 Scenario 035: server down on PrometheusRule

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume an expired credential; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.36 Scenario 036: run failure ratio on pipeline backend metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a GPU hardware fault; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.37 Scenario 037: failed jobs on Kubernetes workload metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a model load regression; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.38 Scenario 038: pending pods on event metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a pipeline image pull failure; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.39 Scenario 039: ImagePullBackOff on synthetic object-store probe

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume an object-store timeout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.40 Scenario 040: artifact failure on PrometheusRule

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume an RBAC change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.41 Scenario 041: schedule delay on pipeline backend metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a target-discovery mismatch; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.42 Scenario 042: server down on Kubernetes workload metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a NetworkPolicy change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.43 Scenario 043: run failure ratio on event metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a certificate rotation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.44 Scenario 044: failed jobs on synthetic object-store probe

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume an exporter upgrade; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.45 Scenario 045: pending pods on PrometheusRule

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume a runtime metric rename; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.46 Scenario 046: ImagePullBackOff on pipeline backend metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a high-cardinality label; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.47 Scenario 047: artifact failure on Kubernetes workload metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a storage latency spike; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.48 Scenario 048: schedule delay on event metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a node drain; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.49 Scenario 049: server down on synthetic object-store probe

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume a quota change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.50 Scenario 050: run failure ratio on PrometheusRule

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume a failed rollout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.51 Scenario 051: failed jobs on pipeline backend metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a proxy or no-proxy error; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.52 Scenario 052: pending pods on Kubernetes workload metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a DNS degradation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.53 Scenario 053: ImagePullBackOff on event metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a receiver outage; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.54 Scenario 054: artifact failure on synthetic object-store probe

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume a remote-write backlog; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.55 Scenario 055: schedule delay on PrometheusRule

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume an expired credential; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.56 Scenario 056: server down on pipeline backend metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a GPU hardware fault; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.57 Scenario 057: run failure ratio on Kubernetes workload metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a model load regression; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.58 Scenario 058: failed jobs on event metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a pipeline image pull failure; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.59 Scenario 059: pending pods on synthetic object-store probe

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume an object-store timeout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.60 Scenario 060: ImagePullBackOff on PrometheusRule

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume an RBAC change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.61 Scenario 061: artifact failure on pipeline backend metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a target-discovery mismatch; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.62 Scenario 062: schedule delay on Kubernetes workload metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a NetworkPolicy change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.63 Scenario 063: server down on event metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a certificate rotation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.64 Scenario 064: run failure ratio on synthetic object-store probe

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume an exporter upgrade; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.65 Scenario 065: failed jobs on PrometheusRule

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume a runtime metric rename; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.66 Scenario 066: pending pods on pipeline backend metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a high-cardinality label; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.67 Scenario 067: ImagePullBackOff on Kubernetes workload metrics

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a storage latency spike; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.68 Scenario 068: artifact failure on event metrics

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a node drain; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.69 Scenario 069: schedule delay on synthetic object-store probe

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume a quota change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.70 Scenario 070: server down on PrometheusRule

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume a failed rollout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.71 Scenario 071: run failure ratio on pipeline backend metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes partial user impact.
- **Injected cause:** Assume a proxy or no-proxy error; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.72 Scenario 072: failed jobs on Kubernetes workload metrics

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes total endpoint unavailability.
- **Injected cause:** Assume a DNS degradation; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.73 Scenario 073: pending pods on event metrics

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes silent monitoring blindness.
- **Injected cause:** Assume a receiver outage; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.74 Scenario 074: ImagePullBackOff on synthetic object-store probe

- **Situation:** During a production window, `ImagePullBackOff` for **Alert rules for pipeline failures** changes unexpectedly and causes increased tail latency.
- **Injected cause:** Assume a remote-write backlog; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.75 Scenario 075: artifact failure on PrometheusRule

- **Situation:** During a production window, `artifact failure` for **Alert rules for pipeline failures** changes unexpectedly and causes false-positive paging.
- **Injected cause:** Assume an expired credential; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_status_phase`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.76 Scenario 076: schedule delay on pipeline backend metrics

- **Situation:** During a production window, `schedule delay` for **Alert rules for pipeline failures** changes unexpectedly and causes missing dashboard data.
- **Injected cause:** Assume a GPU hardware fault; do not accept this as root cause until evidence proves it.
- **Primary signal:** `probe_success`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (instance, module) (probe_success)` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.77 Scenario 077: server down on Kubernetes workload metrics

- **Situation:** During a production window, `server down` for **Alert rules for pipeline failures** changes unexpectedly and causes capacity exhaustion risk.
- **Injected cause:** Assume a model load regression; do not accept this as root cause until evidence proves it.
- **Primary signal:** `up`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `min by (namespace, job) (up{namespace=~"$namespace"})` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.78 Scenario 078: run failure ratio on event metrics

- **Situation:** During a production window, `run failure ratio` for **Alert rules for pipeline failures** changes unexpectedly and causes cross-team escalation.
- **Injected cause:** Assume a pipeline image pull failure; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_restarts_total`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.79 Scenario 079: failed jobs on synthetic object-store probe

- **Situation:** During a production window, `failed jobs` for **Alert rules for pipeline failures** changes unexpectedly and causes SLO error-budget burn.
- **Injected cause:** Assume an object-store timeout; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_job_status_failed`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_job_status_failed  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

### 24.80 Scenario 080: pending pods on PrometheusRule

- **Situation:** During a production window, `pending pods` for **Alert rules for pipeline failures** changes unexpectedly and causes compliance evidence loss.
- **Injected cause:** Assume an RBAC change; do not accept this as root cause until evidence proves it.
- **Primary signal:** `kube_pod_container_status_waiting_reason`. Confirm metric type, unit, labels, timestamp, and scrape target before interpretation.
- **First command:** `oc get pods,svc,endpointslice -n ai-monitoring-29 -o wide` and capture the output with UTC time.
- **PromQL task:** Start with `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version` and add only bounded labels needed to scope the incident.
- **Logs/events task:** Correlate the metric transition with pod logs, Warning events, rollout history, and operator reconciliation.
- **Hypothesis test:** State one expected observation that would confirm the cause and one that would falsify it.
- **Stabilization:** Choose the least disruptive action that restores service while preserving diagnostic evidence.
- **Rollback:** Define the exact manifest, image, route, policy, certificate, or configuration version to restore.
- **Recovery proof:** Require the primary signal, synthetic check, alert state, and user-visible SLI to remain healthy for a defined window.
- **Prevention:** Add one control in CI, GitOps, capacity planning, alert design, or upgrade testing.
- **Deliverable:** Produce a one-page incident record with timeline, evidence links, root cause, action owner, and due date.

## 25. PromQL and recording-rule engineering drills

### 25.1 Query drill 001: availability ratio by `namespace`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate availability ratio grouped by `namespace` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:availability-ratio:namespace`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.2 Query drill 002: five-minute rate by `pod`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate five-minute rate grouped by `pod` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:five-minute-rate:pod`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.3 Query drill 003: one-hour increase by `workload`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate one-hour increase grouped by `workload` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:one-hour-increase:workload`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.4 Query drill 004: P95 latency by `model_name`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate P95 latency grouped by `model_name` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p95-latency:model-name`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.5 Query drill 005: P99 latency by `model_version`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate P99 latency grouped by `model_version` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p99-latency:model-version`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.6 Query drill 006: error ratio by `node`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate error ratio grouped by `node` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:error-ratio:node`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.7 Query drill 007: capacity percentage by `cluster`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate capacity percentage grouped by `cluster` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:capacity-percentage:cluster`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.8 Query drill 008: saturation by `environment`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate saturation grouped by `environment` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:saturation:environment`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.9 Query drill 009: freshness by `owner`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate freshness grouped by `owner` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:freshness:owner`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.10 Query drill 010: top-N offenders by `response_code`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate top-N offenders grouped by `response_code` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:top-n-offenders:response-code`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.11 Query drill 011: availability ratio by `namespace`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate availability ratio grouped by `namespace` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:availability-ratio:namespace`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.12 Query drill 012: five-minute rate by `pod`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate five-minute rate grouped by `pod` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:five-minute-rate:pod`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.13 Query drill 013: one-hour increase by `workload`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate one-hour increase grouped by `workload` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:one-hour-increase:workload`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.14 Query drill 014: P95 latency by `model_name`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate P95 latency grouped by `model_name` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p95-latency:model-name`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.15 Query drill 015: P99 latency by `model_version`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate P99 latency grouped by `model_version` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p99-latency:model-version`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.16 Query drill 016: error ratio by `node`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate error ratio grouped by `node` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:error-ratio:node`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.17 Query drill 017: capacity percentage by `cluster`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate capacity percentage grouped by `cluster` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:capacity-percentage:cluster`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.18 Query drill 018: saturation by `environment`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate saturation grouped by `environment` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:saturation:environment`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.19 Query drill 019: freshness by `owner`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate freshness grouped by `owner` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:freshness:owner`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.20 Query drill 020: top-N offenders by `response_code`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate top-N offenders grouped by `response_code` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:top-n-offenders:response-code`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.21 Query drill 021: availability ratio by `namespace`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate availability ratio grouped by `namespace` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:availability-ratio:namespace`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.22 Query drill 022: five-minute rate by `pod`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate five-minute rate grouped by `pod` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:five-minute-rate:pod`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.23 Query drill 023: one-hour increase by `workload`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate one-hour increase grouped by `workload` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:one-hour-increase:workload`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.24 Query drill 024: P95 latency by `model_name`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate P95 latency grouped by `model_name` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p95-latency:model-name`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.25 Query drill 025: P99 latency by `model_version`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate P99 latency grouped by `model_version` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p99-latency:model-version`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.26 Query drill 026: error ratio by `node`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate error ratio grouped by `node` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:error-ratio:node`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.27 Query drill 027: capacity percentage by `cluster`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate capacity percentage grouped by `cluster` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:capacity-percentage:cluster`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.28 Query drill 028: saturation by `environment`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate saturation grouped by `environment` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:saturation:environment`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.29 Query drill 029: freshness by `owner`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate freshness grouped by `owner` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:freshness:owner`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.30 Query drill 030: top-N offenders by `response_code`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate top-N offenders grouped by `response_code` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:top-n-offenders:response-code`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.31 Query drill 031: availability ratio by `namespace`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate availability ratio grouped by `namespace` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:availability-ratio:namespace`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.32 Query drill 032: five-minute rate by `pod`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate five-minute rate grouped by `pod` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:five-minute-rate:pod`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.33 Query drill 033: one-hour increase by `workload`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate one-hour increase grouped by `workload` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:one-hour-increase:workload`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.34 Query drill 034: P95 latency by `model_name`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate P95 latency grouped by `model_name` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p95-latency:model-name`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.35 Query drill 035: P99 latency by `model_version`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate P99 latency grouped by `model_version` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p99-latency:model-version`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.36 Query drill 036: error ratio by `node`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate error ratio grouped by `node` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:error-ratio:node`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.37 Query drill 037: capacity percentage by `cluster`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate capacity percentage grouped by `cluster` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:capacity-percentage:cluster`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.38 Query drill 038: saturation by `environment`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate saturation grouped by `environment` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:saturation:environment`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.39 Query drill 039: freshness by `owner`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate freshness grouped by `owner` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:freshness:owner`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.40 Query drill 040: top-N offenders by `response_code`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate top-N offenders grouped by `response_code` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:top-n-offenders:response-code`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.41 Query drill 041: availability ratio by `namespace`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate availability ratio grouped by `namespace` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:availability-ratio:namespace`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.42 Query drill 042: five-minute rate by `pod`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate five-minute rate grouped by `pod` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:five-minute-rate:pod`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.43 Query drill 043: one-hour increase by `workload`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate one-hour increase grouped by `workload` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:one-hour-increase:workload`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.44 Query drill 044: P95 latency by `model_name`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate P95 latency grouped by `model_name` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p95-latency:model-name`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.45 Query drill 045: P99 latency by `model_version`

- **Source metric:** `kube_pod_status_phase`
- **Base expression:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Task:** Rewrite the expression to calculate P99 latency grouped by `model_version` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:p99-latency:model-version`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.46 Query drill 046: error ratio by `node`

- **Source metric:** `probe_success`
- **Base expression:** `min by (instance, module) (probe_success)`
- **Task:** Rewrite the expression to calculate error ratio grouped by `node` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:error-ratio:node`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.47 Query drill 047: capacity percentage by `cluster`

- **Source metric:** `up`
- **Base expression:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Task:** Rewrite the expression to calculate capacity percentage grouped by `cluster` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:capacity-percentage:cluster`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.48 Query drill 048: saturation by `environment`

- **Source metric:** `kube_pod_container_status_restarts_total`
- **Base expression:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Task:** Rewrite the expression to calculate saturation grouped by `environment` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:saturation:environment`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.49 Query drill 049: freshness by `owner`

- **Source metric:** `kube_job_status_failed`
- **Base expression:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate freshness grouped by `owner` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:freshness:owner`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

### 25.50 Query drill 050: top-N offenders by `response_code`

- **Source metric:** `kube_pod_container_status_waiting_reason`
- **Base expression:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Task:** Rewrite the expression to calculate top-N offenders grouped by `response_code` while preserving correct counter or histogram semantics.
- **Guardrail:** Add a bounded namespace/environment matcher and prevent division by zero where applicable.
- **No-data test:** Explain how the result differs when the workload is idle, the target is down, or the metric is absent.
- **Performance test:** Measure query duration for 1h, 6h, 24h, and 7d ranges and decide whether a recording rule is required.
- **Recording-rule name:** `ai:alert-rules-for-pipeline-failures:top-n-offenders:response-code`
- **Acceptance evidence:** Save query result, label set, execution time, and a screenshot from the intended dashboard time range.

## 26. Alert-engineering drills

### 26.1 Alert drill 001: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.2 Alert drill 002: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.3 Alert drill 003: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.4 Alert drill 004: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.5 Alert drill 005: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.6 Alert drill 006: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.7 Alert drill 007: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.8 Alert drill 008: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.9 Alert drill 009: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.10 Alert drill 010: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.11 Alert drill 011: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.12 Alert drill 012: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.13 Alert drill 013: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.14 Alert drill 014: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.15 Alert drill 015: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.16 Alert drill 016: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.17 Alert drill 017: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.18 Alert drill 018: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.19 Alert drill 019: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.20 Alert drill 020: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.21 Alert drill 021: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.22 Alert drill 022: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.23 Alert drill 023: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.24 Alert drill 024: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.25 Alert drill 025: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.26 Alert drill 026: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.27 Alert drill 027: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.28 Alert drill 028: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.29 Alert drill 029: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.30 Alert drill 030: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.31 Alert drill 031: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.32 Alert drill 032: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.33 Alert drill 033: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.34 Alert drill 034: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.35 Alert drill 035: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.36 Alert drill 036: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.37 Alert drill 037: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.38 Alert drill 038: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.39 Alert drill 039: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.40 Alert drill 040: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.41 Alert drill 041: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.42 Alert drill 042: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.43 Alert drill 043: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.44 Alert drill 044: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.45 Alert drill 045: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.46 Alert drill 046: PipelineServerDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 5m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.47 Alert drill 047: PipelineRunFailuresHigh

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 10m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.48 Alert drill 048: PipelineTaskPending

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `critical` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 15m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.49 Alert drill 049: PipelineImagePullFailure

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `info` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 30m` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

### 26.50 Alert drill 050: PipelineArtifactStoreDown

- **Candidate expression:** `absent(up{namespace="ai-monitoring-29"}) == 1`
- **Primary signal:** `up/absence`
- **Severity exercise:** Justify `warning` based on urgency, user impact, and required response time.
- **Persistence:** Test a `for: 1h` window and document which transient failures it suppresses.
- **Traffic guard:** Add minimum-volume logic when the rule is a rate, ratio, or latency alert.
- **Annotations:** Write summary, user impact, probable scopes, first three checks, and runbook URL.
- **Routing:** Specify owner, receiver, escalation, maintenance inhibition, and after-hours policy.
- **Failure injection:** Create a reversible lab condition that makes the rule pending, firing, and resolved.
- **Noise review:** Identify duplicate alerts and determine parent/child inhibition or aggregation.
- **Acceptance evidence:** Attach rule status, Alertmanager route match, notification receipt, and recovery notification.

## 27. Dashboard panel engineering drills

### 27.1 Panel drill 001: Pipeline alerts

- **Visualization:** `stat`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.2 Panel drill 002: Failure ratio

- **Visualization:** `time series`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.3 Panel drill 003: Failed tasks

- **Visualization:** `table`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.4 Panel drill 004: Pending tasks

- **Visualization:** `heatmap`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.5 Panel drill 005: Image pulls

- **Visualization:** `state timeline`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.6 Panel drill 006: Artifact store

- **Visualization:** `gauge`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.7 Panel drill 007: Server health

- **Visualization:** `bar chart`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.8 Panel drill 008: Schedule lag

- **Visualization:** `logs panel`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.9 Panel drill 009: Pipeline alerts

- **Visualization:** `stat`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.10 Panel drill 010: Failure ratio

- **Visualization:** `time series`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.11 Panel drill 011: Failed tasks

- **Visualization:** `table`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.12 Panel drill 012: Pending tasks

- **Visualization:** `heatmap`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.13 Panel drill 013: Image pulls

- **Visualization:** `state timeline`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.14 Panel drill 014: Artifact store

- **Visualization:** `gauge`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.15 Panel drill 015: Server health

- **Visualization:** `bar chart`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.16 Panel drill 016: Schedule lag

- **Visualization:** `logs panel`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.17 Panel drill 017: Pipeline alerts

- **Visualization:** `stat`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.18 Panel drill 018: Failure ratio

- **Visualization:** `time series`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.19 Panel drill 019: Failed tasks

- **Visualization:** `table`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.20 Panel drill 020: Pending tasks

- **Visualization:** `heatmap`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.21 Panel drill 021: Image pulls

- **Visualization:** `state timeline`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.22 Panel drill 022: Artifact store

- **Visualization:** `gauge`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.23 Panel drill 023: Server health

- **Visualization:** `bar chart`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.24 Panel drill 024: Schedule lag

- **Visualization:** `logs panel`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.25 Panel drill 025: Pipeline alerts

- **Visualization:** `stat`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.26 Panel drill 026: Failure ratio

- **Visualization:** `time series`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.27 Panel drill 027: Failed tasks

- **Visualization:** `table`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.28 Panel drill 028: Pending tasks

- **Visualization:** `heatmap`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.29 Panel drill 029: Image pulls

- **Visualization:** `state timeline`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.30 Panel drill 030: Artifact store

- **Visualization:** `gauge`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.31 Panel drill 031: Server health

- **Visualization:** `bar chart`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.32 Panel drill 032: Schedule lag

- **Visualization:** `logs panel`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.33 Panel drill 033: Pipeline alerts

- **Visualization:** `stat`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.34 Panel drill 034: Failure ratio

- **Visualization:** `time series`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.35 Panel drill 035: Failed tasks

- **Visualization:** `table`
- **Primary metric/query:** `min by (namespace, job) (up{namespace=~"$namespace"})`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.36 Panel drill 036: Pending tasks

- **Visualization:** `heatmap`
- **Primary metric/query:** `sum by (namespace, pod, container) (increase(kube_pod_container_status_restarts_total[15m]))`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.37 Panel drill 037: Image pulls

- **Visualization:** `state timeline`
- **Primary metric/query:** `kube_job_status_failed  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.38 Panel drill 038: Artifact store

- **Visualization:** `gauge`
- **Primary metric/query:** `kube_pod_container_status_waiting_reason  # Verify labels and availability on the deployed version`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.39 Panel drill 039: Server health

- **Visualization:** `bar chart`
- **Primary metric/query:** `sum by (namespace, phase) (kube_pod_status_phase{phase!="Succeeded"} == 1)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

### 27.40 Panel drill 040: Schedule lag

- **Visualization:** `logs panel`
- **Primary metric/query:** `min by (instance, module) (probe_success)`
- **Audience:** State whether this is for incident response, service owner, capacity planner, data scientist, or executive.
- **Units and thresholds:** Configure units, decimals, warning/critical thresholds, and color semantics from policy or baseline.
- **Variables:** Add only variables that materially change the decision; use bounded defaults.
- **No-data behavior:** Display a meaningful state for idle workload, missing metric, and failed target.
- **Drill-down:** Link to the resource, logs, events, detailed dashboard, and runbook.
- **Performance:** Check query duration, dashboard refresh, panel repetition, and time-range cost.
- **Acceptance evidence:** Review with the target audience and record the decision the panel enables.

## 28. Change, upgrade, and disaster-recovery exercises

### 28.1 Exercise 001

- **Task:** Upgrade the monitored runtime and create a before/after metric compatibility report.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.2 Exercise 002

- **Task:** Rotate the metrics endpoint certificate and prove uninterrupted scraping.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.3 Exercise 003

- **Task:** Move the workload to another node and prove label continuity or document intentional identity changes.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.4 Exercise 004

- **Task:** Restore monitoring manifests into a clean project by using GitOps only.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.5 Exercise 005

- **Task:** Simulate loss of the monitoring PVC and execute the supported recovery path.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.6 Exercise 006

- **Task:** Disable an exporter metric and prove dashboards and alerts fail safely.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.7 Exercise 007

- **Task:** Change scrape interval and quantify ingestion, storage, and detection-latency impact.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.8 Exercise 008

- **Task:** Migrate a dashboard from manual UI state to dashboard-as-code.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.9 Exercise 009

- **Task:** Change Alertmanager receiver credentials and test old-token revocation.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.10 Exercise 010

- **Task:** Export metrics to a secondary observability platform and validate cluster labels.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.11 Exercise 011

- **Task:** Simulate a disconnected registry and prove monitoring images remain deployable.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.12 Exercise 012

- **Task:** Run must-gather or inspect collection and verify the evidence contains the required resources.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.13 Exercise 013

- **Task:** Restore rule and dashboard configuration from backup after accidental deletion.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.14 Exercise 014

- **Task:** Test a multi-cluster label collision and correct the external-label design.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.15 Exercise 015

- **Task:** Conduct a game day covering target loss, route failure, storage latency, and receiver failure.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.16 Exercise 016

- **Task:** Upgrade the monitored runtime and create a before/after metric compatibility report.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.17 Exercise 017

- **Task:** Rotate the metrics endpoint certificate and prove uninterrupted scraping.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.18 Exercise 018

- **Task:** Move the workload to another node and prove label continuity or document intentional identity changes.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.19 Exercise 019

- **Task:** Restore monitoring manifests into a clean project by using GitOps only.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.20 Exercise 020

- **Task:** Simulate loss of the monitoring PVC and execute the supported recovery path.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.21 Exercise 021

- **Task:** Disable an exporter metric and prove dashboards and alerts fail safely.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.22 Exercise 022

- **Task:** Change scrape interval and quantify ingestion, storage, and detection-latency impact.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.23 Exercise 023

- **Task:** Migrate a dashboard from manual UI state to dashboard-as-code.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.24 Exercise 024

- **Task:** Change Alertmanager receiver credentials and test old-token revocation.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.25 Exercise 025

- **Task:** Export metrics to a secondary observability platform and validate cluster labels.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.26 Exercise 026

- **Task:** Simulate a disconnected registry and prove monitoring images remain deployable.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.27 Exercise 027

- **Task:** Run must-gather or inspect collection and verify the evidence contains the required resources.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.28 Exercise 028

- **Task:** Restore rule and dashboard configuration from backup after accidental deletion.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.29 Exercise 029

- **Task:** Test a multi-cluster label collision and correct the external-label design.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.30 Exercise 030

- **Task:** Conduct a game day covering target loss, route failure, storage latency, and receiver failure.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.31 Exercise 031

- **Task:** Upgrade the monitored runtime and create a before/after metric compatibility report.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.32 Exercise 032

- **Task:** Rotate the metrics endpoint certificate and prove uninterrupted scraping.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.33 Exercise 033

- **Task:** Move the workload to another node and prove label continuity or document intentional identity changes.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.34 Exercise 034

- **Task:** Restore monitoring manifests into a clean project by using GitOps only.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.35 Exercise 035

- **Task:** Simulate loss of the monitoring PVC and execute the supported recovery path.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.36 Exercise 036

- **Task:** Disable an exporter metric and prove dashboards and alerts fail safely.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.37 Exercise 037

- **Task:** Change scrape interval and quantify ingestion, storage, and detection-latency impact.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.38 Exercise 038

- **Task:** Migrate a dashboard from manual UI state to dashboard-as-code.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.39 Exercise 039

- **Task:** Change Alertmanager receiver credentials and test old-token revocation.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `ImagePullBackOff`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.40 Exercise 040

- **Task:** Export metrics to a secondary observability platform and validate cluster labels.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `artifact failure`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.41 Exercise 041

- **Task:** Simulate a disconnected registry and prove monitoring images remain deployable.
- **Scope:** Alert rules for pipeline failures; component `pipeline backend metrics`; signal `schedule delay`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.42 Exercise 042

- **Task:** Run must-gather or inspect collection and verify the evidence contains the required resources.
- **Scope:** Alert rules for pipeline failures; component `Kubernetes workload metrics`; signal `server down`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.43 Exercise 043

- **Task:** Restore rule and dashboard configuration from backup after accidental deletion.
- **Scope:** Alert rules for pipeline failures; component `event metrics`; signal `run failure ratio`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.44 Exercise 044

- **Task:** Test a multi-cluster label collision and correct the external-label design.
- **Scope:** Alert rules for pipeline failures; component `synthetic object-store probe`; signal `failed jobs`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

### 28.45 Exercise 045

- **Task:** Conduct a game day covering target loss, route failure, storage latency, and receiver failure.
- **Scope:** Alert rules for pipeline failures; component `PrometheusRule`; signal `pending pods`.
- **Pre-check:** Record versions, health, alerts, active series, query performance, and current Git revision.
- **Change control:** Define approval, maintenance window, rollback trigger, and communication owner.
- **Execution:** Apply the smallest reversible change and preserve command output.
- **Validation:** Compare targets, metrics, dashboards, alerts, logs, events, and user-visible behavior.
- **Rollback test:** Execute or dry-run the rollback and verify configuration convergence.
- **Post-check:** Confirm no orphaned resources, stale alerts, duplicate series, or secret exposure.
- **Deliverable:** Produce a change record with quantified before/after impact and lessons learned.

## 29. Security, governance, and cost-review exercises

### 29.1 Governance drill 001

- **Review objective:** Review RBAC for metric readers, monitoring editors, alert routers, and dashboard administrators.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.2 Governance drill 002

- **Review objective:** Scan all metric labels for PII, credentials, prompt content, object keys, or unbounded identifiers.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.3 Governance drill 003

- **Review objective:** Review exporter image source, signature, vulnerabilities, and SBOM.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.4 Governance drill 004

- **Review objective:** Test NetworkPolicy for allowed scrapers and denied unauthorized namespaces.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.5 Governance drill 005

- **Review objective:** Review telemetry retention against legal, security, and operational requirements.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.6 Governance drill 006

- **Review objective:** Estimate monthly storage and remote-write cost from measured ingestion.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.7 Governance drill 007

- **Review objective:** Find unused dashboards, stale rules, and alerts without owners.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.8 Governance drill 008

- **Review objective:** Validate audit records for monitoring configuration changes.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.9 Governance drill 009

- **Review objective:** Review external receiver TLS, proxy, DNS, egress, and secret rotation.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.10 Governance drill 010

- **Review objective:** Perform a tenant-isolation test for metrics and dashboards.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.11 Governance drill 011

- **Review objective:** Review RBAC for metric readers, monitoring editors, alert routers, and dashboard administrators.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.12 Governance drill 012

- **Review objective:** Scan all metric labels for PII, credentials, prompt content, object keys, or unbounded identifiers.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.13 Governance drill 013

- **Review objective:** Review exporter image source, signature, vulnerabilities, and SBOM.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.14 Governance drill 014

- **Review objective:** Test NetworkPolicy for allowed scrapers and denied unauthorized namespaces.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.15 Governance drill 015

- **Review objective:** Review telemetry retention against legal, security, and operational requirements.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.16 Governance drill 016

- **Review objective:** Estimate monthly storage and remote-write cost from measured ingestion.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.17 Governance drill 017

- **Review objective:** Find unused dashboards, stale rules, and alerts without owners.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.18 Governance drill 018

- **Review objective:** Validate audit records for monitoring configuration changes.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.19 Governance drill 019

- **Review objective:** Review external receiver TLS, proxy, DNS, egress, and secret rotation.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.20 Governance drill 020

- **Review objective:** Perform a tenant-isolation test for metrics and dashboards.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.21 Governance drill 021

- **Review objective:** Review RBAC for metric readers, monitoring editors, alert routers, and dashboard administrators.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.22 Governance drill 022

- **Review objective:** Scan all metric labels for PII, credentials, prompt content, object keys, or unbounded identifiers.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.23 Governance drill 023

- **Review objective:** Review exporter image source, signature, vulnerabilities, and SBOM.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.24 Governance drill 024

- **Review objective:** Test NetworkPolicy for allowed scrapers and denied unauthorized namespaces.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.25 Governance drill 025

- **Review objective:** Review telemetry retention against legal, security, and operational requirements.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.26 Governance drill 026

- **Review objective:** Estimate monthly storage and remote-write cost from measured ingestion.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.27 Governance drill 027

- **Review objective:** Find unused dashboards, stale rules, and alerts without owners.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.28 Governance drill 028

- **Review objective:** Validate audit records for monitoring configuration changes.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.29 Governance drill 029

- **Review objective:** Review external receiver TLS, proxy, DNS, egress, and secret rotation.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.30 Governance drill 030

- **Review objective:** Perform a tenant-isolation test for metrics and dashboards.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.31 Governance drill 031

- **Review objective:** Review RBAC for metric readers, monitoring editors, alert routers, and dashboard administrators.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.32 Governance drill 032

- **Review objective:** Scan all metric labels for PII, credentials, prompt content, object keys, or unbounded identifiers.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.33 Governance drill 033

- **Review objective:** Review exporter image source, signature, vulnerabilities, and SBOM.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.34 Governance drill 034

- **Review objective:** Test NetworkPolicy for allowed scrapers and denied unauthorized namespaces.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.35 Governance drill 035

- **Review objective:** Review telemetry retention against legal, security, and operational requirements.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.36 Governance drill 036

- **Review objective:** Estimate monthly storage and remote-write cost from measured ingestion.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.37 Governance drill 037

- **Review objective:** Find unused dashboards, stale rules, and alerts without owners.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.38 Governance drill 038

- **Review objective:** Validate audit records for monitoring configuration changes.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.39 Governance drill 039

- **Review objective:** Review external receiver TLS, proxy, DNS, egress, and secret rotation.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

### 29.40 Governance drill 040

- **Review objective:** Perform a tenant-isolation test for metrics and dashboards.
- **Target:** `Alert rules for pipeline failures` resources in namespace `ai-monitoring-29` and any cluster-scoped dependencies.
- **Evidence:** Export RBAC, labels, image digests, policies, secrets metadata, retention, and cost inputs without exposing secret values.
- **Risk rating:** Assign likelihood, impact, control strength, residual risk, and owner.
- **Required remediation:** Define the exact Git change, policy update, or operational action.
- **Verification:** Prove the control with `oc auth can-i`, network tests, metric queries, audit searches, or cost calculations.
- **Exception process:** Record expiry date, approver, compensating controls, and review cadence.
- **Deliverable:** Attach a signed review checklist to the service or platform readiness record.

## 24. References

- [Red Hat OpenShift AI 3.5 — Managing observability](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_openshift_ai/managing-observability_managing-rhoai)
- [Red Hat OpenShift AI 3.5 — Managing and monitoring models](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_and_monitoring_models/index)
- [Monitoring stack for Red Hat OpenShift 4.19 — User workload monitoring](https://docs.redhat.com/en/documentation/monitoring_stack_for_red_hat_openshift/4.19/html-single/configuring_user_workload_monitoring/index)
- [Monitoring stack for Red Hat OpenShift 4.19 — About monitoring](https://docs.redhat.com/en/documentation/monitoring_stack_for_red_hat_openshift/4.19/html-single/about_monitoring/)
- [NVIDIA DCGM Exporter documentation](https://docs.nvidia.com/datacenter/dcgm/latest/gpu-telemetry/dcgm-exporter.html)
- [NVIDIA GPU monitoring dashboard for OpenShift](https://docs.nvidia.com/datacenter/cloud-native/openshift/latest/enable-gpu-monitoring-dashboard.html)

## 25. Final L3 notes

- Do not copy metric names from a blog into production without checking the live exporter.
- Do not treat a green dashboard as proof of end-user availability; include synthetic and SLO views.
- Do not page on every technical symptom; page on urgent user impact and route diagnostic warnings appropriately.
- Do not solve cardinality or storage incidents only by adding resources; fix label and query design.
- Do not make manual monitoring changes without Git history and rollback.
- Do not close an incident until monitoring confirms stable recovery and the detection gap is reviewed.

