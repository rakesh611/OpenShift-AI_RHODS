# 20. GPU-enabled workbench

> **Track:** GPU and Accelerator Management for Red Hat OpenShift AI
> **Baseline:** OpenShift Container Platform 4.21 and Red Hat OpenShift AI Self-Managed 3.5
> **Audience:** Corporate/L3 platform engineers, OpenShift administrators, MLOps engineers, SREs, and senior support engineers
> **Primary lab outcome:** Build a GPU-enabled Jupyter workbench, verify framework access, run a sample training task, and validate shutdown releases the accelerator.
> **Generated:** 2026-07-21

## Version and support notice

This chapter is a production-oriented learning guide, not a substitute for the product support matrix. Before implementation, verify the exact OpenShift, OpenShift AI, operator, firmware, driver, runtime image, accelerator model, hypervisor, and architecture combination in the current Red Hat and vendor documentation. Commands containing angle-bracket values are templates and must be adapted to the target cluster.

Current OpenShift AI releases use **hardware profiles** for governed resource selection. The separate chapter on accelerator profiles preserves legacy knowledge for upgrades and older environments. IBM Spyre, AMD, Intel Gaudi, vGPU, and multi-node designs have platform- and release-specific boundaries; never infer support from generic Kubernetes functionality alone.

## Executive summary

Create OpenShift AI workbenches that request accelerators through hardware profiles, use supported CUDA or ROCm images, mount storage and data connections, and expose health and utilization safely.

The central operational object in this chapter is `nvidia.com/gpu or amd.com/gpu`. The primary controller or integration is **OpenShift AI workbench controller**, and the representative namespace or scope is `data-science-project`.

- Start at the physical and firmware layer before assuming Kubernetes is at fault.
- Separate discovery, driver, runtime, scheduling, and application layers.
- Use node capacity and allocatable fields as the scheduler source of truth.
- Use pod events before reading long application logs.
- Correlate changes with MachineConfigPool, kernel, operator, and image updates.
- Preserve evidence before rebooting or deleting failed operands.
- Validate remediation with a minimal vendor smoke workload and the real application.
- Prefer reversible GitOps changes with explicit rollback steps.
- Measure queue time, utilization, memory headroom, and failure rate together.
- Treat accelerator support matrices as release-specific contracts.

## Learning objectives

1. Explain GPU-enabled workbench to an architecture review board without relying on dashboard screenshots.
2. Identify every control-plane and node-level component involved in GPU-enabled workbench.
3. Create the build a gpu-enabled jupyter workbench, verify framework access, run a sample training task, and validate shutdown releases the accelerator.
4. Validate `nvidia.com/gpu or amd.com/gpu` from discovery through workload execution.
5. Differentiate a hardware problem from a driver, device-plugin, runtime, scheduler, quota, storage, network, or application problem.
6. Design change, rollback, maintenance, monitoring, and escalation procedures.
7. Apply least privilege, tenant isolation, capacity governance, and audit controls.
8. Create evidence suitable for an L3 support case without destroying the original failure state.
9. Translate dashboard symptoms into oc, node, kernel, runtime, and application-level checks.
10. Explain version-sensitive and support-sensitive decisions explicitly.

## Architecture and control flow

```text
[Physical accelerator / firmware / PCIe or virtual device]
                         |
                         v
[Node OS and kernel] -> [Vendor driver or guest driver]
                         |
                         v
[NFD / feature discovery] ----> [Node labels and policy facts]
                         |
                         v
[Vendor device plugin] --------> [Node capacity and allocatable: nvidia.com/gpu or amd.com/gpu]
                         |
                         v
[OpenShift scheduler + quota + affinity + taints]
                         |
                         v
[CRI-O + vendor container toolkit / CDI / OCI hooks]
                         |
                         v
[Workbench, training pod, KServe runtime, or vLLM workload]
                         |
                         v
[DCGM/vendor telemetry + Prometheus + alerts + SLO dashboards]
```

- The physical or virtual accelerator must first be discoverable by the operating system.
- A compatible kernel driver binds the device and creates the required character devices or vendor interfaces.
- Feature discovery labels facts; labels do not allocate a device by themselves.
- A device plugin registers with kubelet and publishes a vendor-qualified extended resource.
- The scheduler places only a pod whose resource request, affinity, tolerations, and quota can all be satisfied.
- The container runtime injects device nodes and matching user-space libraries or CDI specifications.
- The application framework performs a final enumeration and initialization step.
- Telemetry must observe both infrastructure health and workload-level service indicators.

## Deep concepts

### Device identity

Trace stable identity across PCI address, UUID, node label, `nvidia.com/gpu or amd.com/gpu` allocation, and application-visible device ordinal.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Capacity versus allocatable

Capacity represents discovered resources; allocatable is the scheduler-usable quantity after kubelet and health decisions.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Extended resource semantics

Accelerators are vendor-qualified integer resources and are not CPU-style overcommittable resources.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Locality

CPU socket, NUMA node, PCIe root complex, NIC, storage, and accelerator placement can materially affect performance.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Health

A device may be physically present yet excluded from allocatable capacity because the device plugin or driver marks it unhealthy.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Isolation

Exclusive allocation, MIG, time slicing, vGPU, and application multiplexing provide different isolation guarantees.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Lifecycle

Kernel updates, driver changes, firmware changes, hypervisor changes, and operator upgrades form one compatibility chain.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Governance

Hardware profiles, quotas, taints, admission policy, and GitOps provide controlled user access to scarce devices.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Observability

Allocation without utilization data hides waste; utilization without service metrics hides inefficient applications.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.

### Supportability

A configuration can be technically possible but unsupported; production standards must require explicit matrix validation.

- L3 consideration 1: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 2: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 3: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 4: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.
- L3 consideration 5: apply this concept to `GPU-enabled workbench` and record the expected evidence, owner, and rollback condition.


## Prerequisite and readiness matrix

| Layer | Required evidence | Pass condition | Typical owner |
|---|---|---|---|
| Hardware | Supported accelerator model, firmware, power, cooling, PCIe topology | Vendor inventory and diagnostics pass | Infrastructure |
| Platform | Supported OpenShift and OpenShift AI versions | Compatibility matrix verified | Platform engineering |
| Node OS | RHCOS/kernel level and MachineConfigPool health | MCP Updated=True and Degraded=False | OpenShift admin |
| Discovery | NFD and vendor labels | Expected nodes have stable labels | Platform engineering |
| Driver | Kernel module, version, device nodes | Driver loads and vendor CLI works | Accelerator admin |
| Device plugin | `nvidia.com/gpu or amd.com/gpu` capacity and allocatable | Expected integer resource is advertised | OpenShift admin |
| Runtime | CRI-O and vendor toolkit or CDI | Container can enumerate assigned device | Platform engineering |
| Scheduling | Labels, taints, affinity, quota | Test pod is admitted and scheduled | Platform engineering |
| Application | Supported framework and image | Smoke workload completes | ML engineering |
| Monitoring | Metrics, alerts, dashboard, runbook | Telemetry and test alert verified | SRE |

## Project from scratch

**Project goal:** Build a GPU-enabled Jupyter workbench, verify framework access, run a sample training task, and validate shutdown releases the accelerator.

### Phase 0 - Architecture decision record

1. Record business workload, model or training framework, target SLO, concurrency, data sensitivity, and expected growth.
2. Identify supported accelerator models and the exact platform architecture.
3. Choose exclusive, shared, MIG, vGPU, passthrough, or vendor-native allocation only after isolation and support review.
4. Define failure domain, maintenance window, rollback trigger, capacity reserve, and ownership.

### Phase 1 - Cluster and node preparation

1. Verify cluster operators and MachineConfigPools are healthy before introducing accelerator changes.
2. Confirm firmware, IOMMU, Secure Boot, PCIe, NUMA, and hypervisor settings as applicable.
3. Create or identify a dedicated worker pool and approve labels and taints through GitOps.
4. Capture a before-state inventory for nodes, kernel, PCI devices, and installed operators.

### Phase 2 - Operator and operand installation

1. Install and verify OpenShift AI workbench controller in the approved namespace and channel.
2. Create the required custom resource only from current vendor or Red Hat examples.
3. Observe daemonsets, deployments, jobs, validation pods, events, and ClusterServiceVersion conditions.
4. Do not proceed until desired, current, ready, and available operand counts agree.

### Phase 3 - Resource publication and governance

1. Verify node capacity and allocatable include `nvidia.com/gpu or amd.com/gpu` or the release-specific equivalent.
2. Create stable policy labels separately from automatically generated discovery labels.
3. Apply taints, tolerations, namespace quota, and hardware profiles.
4. Validate RBAC so ordinary users cannot change cluster-scoped accelerator configuration.

### Phase 4 - Minimal smoke workload

1. Run the smallest vendor-supported image and discovery command before testing a large model.
2. Confirm the pod was assigned to the expected node and received exactly the requested device count.
3. Collect the vendor CLI output and framework enumeration output from inside the container.
4. Delete the pod and verify the resource returns to allocatable capacity.

### Phase 5 - OpenShift AI workload

1. Create or select a hardware profile with the required resource identifier and scheduling policy.
2. Create a GPU-enabled workbench or model deployment with a supported image and storage path.
3. Validate readiness, logs, endpoint behavior, performance, and resource release during shutdown.
4. Run a representative workload rather than relying only on a synthetic discovery command.

### Phase 6 - Monitoring and production acceptance

1. Expose vendor telemetry to Prometheus using supported ServiceMonitor integration.
2. Create alerts for device loss, memory pressure, sustained thermal issues, Xid or vendor errors, and long Pending duration.
3. Define operational SLOs for queue time, workload readiness, inference latency, throughput, and accelerator health.
4. Complete rollback, backup, must-gather, and escalation rehearsals before production sign-off.


## Reference lab manifest

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: data-science-project
  labels:
    purpose: gpu-accelerator-l3-lab
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: accelerator-quota
  namespace: data-science-project
spec:
  hard:
    requests.nvidia.com/gpu: "2"  # Replace for AMD, Gaudi, Spyre, or MIG resources.
    requests.cpu: "16"
    requests.memory: 64Gi
    limits.cpu: "32"
    limits.memory: 128Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: accelerator-smoke-test
  namespace: data-science-project
  labels:
    app: accelerator-smoke-test
spec:
  restartPolicy: Never
  nodeSelector:
    accelerator.platform.example.com/enabled: "true"  # Replace with approved label.
  tolerations:
  - key: accelerator.platform.example.com/dedicated
    operator: Equal
    value: "true"
    effect: NoSchedule
  containers:
  - name: test
    image: <approved-vendor-runtime-image>
    imagePullPolicy: IfNotPresent
    command: ["/bin/bash", "-lc"]
    args:
    - |
      set -euxo pipefail
      echo "Run vendor-specific discovery here"
      sleep 30
    resources:
      requests:
        cpu: "1"
        memory: 2Gi
        nvidia.com/gpu: "1"
      limits:
        cpu: "2"
        memory: 4Gi
        nvidia.com/gpu: "1"
```

### Apply and validate

```bash
oc apply -f accelerator-lab.yaml
```

```bash
oc get resourcequota -n data-science-project
```

```bash
oc describe pod accelerator-smoke-test -n data-science-project
```

```bash
oc get pod accelerator-smoke-test -n data-science-project -o wide
```

```bash
oc describe node <assigned-node> | sed -n '/Capacity:/,/System Info:/p'
```


## Command and evidence toolkit

### Command 01

```bash
oc get nodes -o wide
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 02

```bash
oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 03

```bash
oc describe node <gpu-node>
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 04

```bash
oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 05

```bash
oc get events -A --sort-by=.lastTimestamp | tail -100
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 06

```bash
oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 07

```bash
oc debug node/<gpu-node>
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 08

```bash
chroot /host
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 09

```bash
lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 10

```bash
lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 11

```bash
oc get csv -A
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 12

```bash
oc get subscriptions.operators.coreos.com -A
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 13

```bash
oc get installplans.operators.coreos.com -A
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 14

```bash
oc get mcp
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 15

```bash
oc get co
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 16

```bash
oc get resourcequota -A
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 17

```bash
oc get pod <pod> -n <namespace> -o yaml
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 18

```bash
oc describe pod <pod> -n <namespace>
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 19

```bash
oc logs <pod> -n <namespace> --all-containers --tail=300
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.

### Command 20

```bash
oc adm must-gather
```

**Purpose:** Use this command while investigating `GPU-enabled workbench`. Record timestamp, cluster, node, namespace, command, output location, and interpretation.

**Evidence rule:** Do not paste only the successful line. Preserve surrounding conditions, events, versions, and resource state.


## Topic-specific validation checks

- [ ] Validation 001: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/001>`.
- [ ] Validation 002: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/002>`.
- [ ] Validation 003: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/003>`.
- [ ] Validation 004: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/004>`.
- [ ] Validation 005: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/005>`.
- [ ] Validation 006: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/006>`.
- [ ] Validation 007: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/007>`.
- [ ] Validation 008: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/008>`.
- [ ] Validation 009: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/009>`.
- [ ] Validation 010: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/010>`.
- [ ] Validation 011: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/011>`.
- [ ] Validation 012: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/012>`.
- [ ] Validation 013: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/013>`.
- [ ] Validation 014: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/014>`.
- [ ] Validation 015: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/015>`.
- [ ] Validation 016: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/016>`.
- [ ] Validation 017: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/017>`.
- [ ] Validation 018: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/018>`.
- [ ] Validation 019: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/019>`.
- [ ] Validation 020: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/020>`.
- [ ] Validation 021: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/021>`.
- [ ] Validation 022: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/022>`.
- [ ] Validation 023: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/023>`.
- [ ] Validation 024: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/024>`.
- [ ] Validation 025: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/025>`.
- [ ] Validation 026: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/026>`.
- [ ] Validation 027: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/027>`.
- [ ] Validation 028: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/028>`.
- [ ] Validation 029: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/029>`.
- [ ] Validation 030: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/030>`.
- [ ] Validation 031: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/031>`.
- [ ] Validation 032: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/032>`.
- [ ] Validation 033: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/033>`.
- [ ] Validation 034: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/034>`.
- [ ] Validation 035: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/035>`.
- [ ] Validation 036: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/036>`.
- [ ] Validation 037: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/037>`.
- [ ] Validation 038: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/038>`.
- [ ] Validation 039: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/039>`.
- [ ] Validation 040: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/040>`.
- [ ] Validation 041: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/041>`.
- [ ] Validation 042: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/042>`.
- [ ] Validation 043: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/043>`.
- [ ] Validation 044: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/044>`.
- [ ] Validation 045: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/045>`.
- [ ] Validation 046: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/046>`.
- [ ] Validation 047: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/047>`.
- [ ] Validation 048: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/048>`.
- [ ] Validation 049: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/049>`.
- [ ] Validation 050: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/050>`.
- [ ] Validation 051: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/051>`.
- [ ] Validation 052: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/052>`.
- [ ] Validation 053: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/053>`.
- [ ] Validation 054: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/054>`.
- [ ] Validation 055: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/055>`.
- [ ] Validation 056: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/056>`.
- [ ] Validation 057: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/057>`.
- [ ] Validation 058: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/058>`.
- [ ] Validation 059: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/059>`.
- [ ] Validation 060: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/060>`.
- [ ] Validation 061: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/061>`.
- [ ] Validation 062: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/062>`.
- [ ] Validation 063: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/063>`.
- [ ] Validation 064: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/064>`.
- [ ] Validation 065: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/065>`.
- [ ] Validation 066: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/066>`.
- [ ] Validation 067: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/067>`.
- [ ] Validation 068: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/068>`.
- [ ] Validation 069: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/069>`.
- [ ] Validation 070: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/070>`.
- [ ] Validation 071: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/071>`.
- [ ] Validation 072: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/072>`.
- [ ] Validation 073: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/073>`.
- [ ] Validation 074: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/074>`.
- [ ] Validation 075: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/075>`.
- [ ] Validation 076: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/076>`.
- [ ] Validation 077: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/077>`.
- [ ] Validation 078: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/078>`.
- [ ] Validation 079: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/079>`.
- [ ] Validation 080: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/080>`.
- [ ] Validation 081: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/081>`.
- [ ] Validation 082: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/082>`.
- [ ] Validation 083: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/083>`.
- [ ] Validation 084: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/084>`.
- [ ] Validation 085: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/085>`.
- [ ] Validation 086: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/086>`.
- [ ] Validation 087: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/087>`.
- [ ] Validation 088: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/088>`.
- [ ] Validation 089: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/089>`.
- [ ] Validation 090: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/090>`.
- [ ] Validation 091: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/091>`.
- [ ] Validation 092: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/092>`.
- [ ] Validation 093: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/093>`.
- [ ] Validation 094: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/094>`.
- [ ] Validation 095: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/095>`.
- [ ] Validation 096: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/096>`.
- [ ] Validation 097: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/097>`.
- [ ] Validation 098: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/098>`.
- [ ] Validation 099: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/099>`.
- [ ] Validation 100: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/100>`.
- [ ] Validation 101: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/101>`.
- [ ] Validation 102: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/102>`.
- [ ] Validation 103: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/103>`.
- [ ] Validation 104: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/104>`.
- [ ] Validation 105: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/105>`.
- [ ] Validation 106: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/106>`.
- [ ] Validation 107: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/107>`.
- [ ] Validation 108: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/108>`.
- [ ] Validation 109: Confirm the expected controller for GPU-enabled workbench is installed and reports a healthy status. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/109>`.
- [ ] Validation 110: Confirm `nvidia.com/gpu or amd.com/gpu` is spelled exactly as advertised by the device plugin or profile. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/110>`.
- [ ] Validation 111: Compare desired, current, ready, available, and updated daemonset counts. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/111>`.
- [ ] Validation 112: Inspect pod events before application logs. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/112>`.
- [ ] Validation 113: Verify the assigned node has the expected vendor, product, memory, and policy labels. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/113>`.
- [ ] Validation 114: Verify all taints are intentionally tolerated. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/114>`.
- [ ] Validation 115: Verify quota is sufficient and uses the requests prefix for extended resources where required. Evidence owner: `<owner-1>`; evidence path: `<evidence/gpu-enabled-workbench/115>`.
- [ ] Validation 116: Verify container image architecture and accelerator libraries match the node. Evidence owner: `<owner-2>`; evidence path: `<evidence/gpu-enabled-workbench/116>`.
- [ ] Validation 117: Verify the accelerator is visible inside the container and through the intended framework. Evidence owner: `<owner-3>`; evidence path: `<evidence/gpu-enabled-workbench/117>`.
- [ ] Validation 118: Verify metrics are emitted and attributed to the correct node, GPU, namespace, pod, or model. Evidence owner: `<owner-4>`; evidence path: `<evidence/gpu-enabled-workbench/118>`.
- [ ] Validation 119: Verify deleting or stopping the workload releases the allocated device. Evidence owner: `<owner-5>`; evidence path: `<evidence/gpu-enabled-workbench/119>`.
- [ ] Validation 120: Verify rollback restores the before-state without leaving stale labels or operands. Evidence owner: `<owner-6>`; evidence path: `<evidence/gpu-enabled-workbench/120>`.

## Security, governance, and compliance

### Control 01: Use vendor-supported operators and signed images from approved registries.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 02: Restrict privileged operand permissions to the operator-managed namespaces.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 03: Protect GPU worker nodes with taints and explicit tolerations.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 04: Use namespace quotas for extended accelerator resources.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 05: Apply least-privilege RBAC for hardware profile administration.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 06: Review custom images for CUDA, ROCm, Gaudi, or Spyre library provenance.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 07: Do not expose node-debug or hostPath capabilities to ordinary users.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 08: Use NetworkPolicy for model-serving and notebook namespaces.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 09: Separate platform credentials from user workload secrets.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 10: Pin and scan runtime images according to the enterprise release process.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 11: Audit changes to ClusterPolicy, NodeFeatureDiscovery, hardware profiles, and taints.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.

### Control 12: Require change approval for disruptive MIG, driver, firmware, and vGPU reconfiguration.

- **Why it matters for GPU-enabled workbench:** accelerator components commonly require elevated node access, and a configuration error can affect every workload on the node.
- **Implementation evidence:** Git commit, approved change record, RBAC export, operator custom resource, policy result, and validation output.
- **Failure signal:** unexpected privileged workload, unmanaged image, manual node drift, resource bypass, or missing audit trail.
- **Review cadence:** before release, after upgrade, after incident, and at least quarterly for production clusters.


## Capacity and performance engineering

| Dimension | Question | Evidence | Decision |
|---|---|---|---|
| Workload Memory Footprint | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Model Weights And Kv Cache | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Batch Size And Concurrency | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Tensor Or Data Parallelism | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Pcie And Numa Locality | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Network Bandwidth For Distributed Workloads | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Storage Download And Model Initialization | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Sharing Or Partitioning Overhead | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Thermal And Power Limits | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Queueing And Reserved Headroom | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Failure-Domain Capacity | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |
| Upgrade Surge Capacity | What is the measured requirement for GPU-enabled workbench? | Benchmark, telemetry, and load-test report | Record target, limit, and reserve |

- Capacity checkpoint 001: correlate **workbench startup time** with workload class `1`, concurrency tier `1`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 002: correlate **GPU utilization** with workload class `2`, concurrency tier `2`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 003: correlate **idle GPU hours** with workload class `3`, concurrency tier `3`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 004: correlate **kernel failures** with workload class `4`, concurrency tier `4`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 005: correlate **PVC latency** with workload class `5`, concurrency tier `5`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 006: correlate **workbench restart count** with workload class `1`, concurrency tier `6`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 007: correlate **workbench startup time** with workload class `2`, concurrency tier `7`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 008: correlate **GPU utilization** with workload class `3`, concurrency tier `8`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 009: correlate **idle GPU hours** with workload class `4`, concurrency tier `1`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 010: correlate **kernel failures** with workload class `5`, concurrency tier `2`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 011: correlate **PVC latency** with workload class `1`, concurrency tier `3`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 012: correlate **workbench restart count** with workload class `2`, concurrency tier `4`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 013: correlate **workbench startup time** with workload class `3`, concurrency tier `5`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 014: correlate **GPU utilization** with workload class `4`, concurrency tier `6`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 015: correlate **idle GPU hours** with workload class `5`, concurrency tier `7`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 016: correlate **kernel failures** with workload class `1`, concurrency tier `8`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 017: correlate **PVC latency** with workload class `2`, concurrency tier `1`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 018: correlate **workbench restart count** with workload class `3`, concurrency tier `2`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 019: correlate **workbench startup time** with workload class `4`, concurrency tier `3`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 020: correlate **GPU utilization** with workload class `5`, concurrency tier `4`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 021: correlate **idle GPU hours** with workload class `1`, concurrency tier `5`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 022: correlate **kernel failures** with workload class `2`, concurrency tier `6`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 023: correlate **PVC latency** with workload class `3`, concurrency tier `7`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 024: correlate **workbench restart count** with workload class `4`, concurrency tier `8`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 025: correlate **workbench startup time** with workload class `5`, concurrency tier `1`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 026: correlate **GPU utilization** with workload class `1`, concurrency tier `2`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 027: correlate **idle GPU hours** with workload class `2`, concurrency tier `3`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 028: correlate **kernel failures** with workload class `3`, concurrency tier `4`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 029: correlate **PVC latency** with workload class `4`, concurrency tier `5`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 030: correlate **workbench restart count** with workload class `5`, concurrency tier `6`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 031: correlate **workbench startup time** with workload class `1`, concurrency tier `7`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 032: correlate **GPU utilization** with workload class `2`, concurrency tier `8`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 033: correlate **idle GPU hours** with workload class `3`, concurrency tier `1`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 034: correlate **kernel failures** with workload class `4`, concurrency tier `2`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 035: correlate **PVC latency** with workload class `5`, concurrency tier `3`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 036: correlate **workbench restart count** with workload class `1`, concurrency tier `4`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 037: correlate **workbench startup time** with workload class `2`, concurrency tier `5`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 038: correlate **GPU utilization** with workload class `3`, concurrency tier `6`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 039: correlate **idle GPU hours** with workload class `4`, concurrency tier `7`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 040: correlate **kernel failures** with workload class `5`, concurrency tier `8`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 041: correlate **PVC latency** with workload class `1`, concurrency tier `1`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 042: correlate **workbench restart count** with workload class `2`, concurrency tier `2`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 043: correlate **workbench startup time** with workload class `3`, concurrency tier `3`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 044: correlate **GPU utilization** with workload class `4`, concurrency tier `4`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 045: correlate **idle GPU hours** with workload class `5`, concurrency tier `5`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 046: correlate **kernel failures** with workload class `1`, concurrency tier `6`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 047: correlate **PVC latency** with workload class `2`, concurrency tier `7`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 048: correlate **workbench restart count** with workload class `3`, concurrency tier `8`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 049: correlate **workbench startup time** with workload class `4`, concurrency tier `1`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 050: correlate **GPU utilization** with workload class `5`, concurrency tier `2`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 051: correlate **idle GPU hours** with workload class `1`, concurrency tier `3`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 052: correlate **kernel failures** with workload class `2`, concurrency tier `4`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 053: correlate **PVC latency** with workload class `3`, concurrency tier `5`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 054: correlate **workbench restart count** with workload class `4`, concurrency tier `6`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 055: correlate **workbench startup time** with workload class `5`, concurrency tier `7`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 056: correlate **GPU utilization** with workload class `1`, concurrency tier `8`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 057: correlate **idle GPU hours** with workload class `2`, concurrency tier `1`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 058: correlate **kernel failures** with workload class `3`, concurrency tier `2`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 059: correlate **PVC latency** with workload class `4`, concurrency tier `3`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 060: correlate **workbench restart count** with workload class `5`, concurrency tier `4`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 061: correlate **workbench startup time** with workload class `1`, concurrency tier `5`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 062: correlate **GPU utilization** with workload class `2`, concurrency tier `6`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 063: correlate **idle GPU hours** with workload class `3`, concurrency tier `7`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 064: correlate **kernel failures** with workload class `4`, concurrency tier `8`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 065: correlate **PVC latency** with workload class `5`, concurrency tier `1`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 066: correlate **workbench restart count** with workload class `1`, concurrency tier `2`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 067: correlate **workbench startup time** with workload class `2`, concurrency tier `3`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 068: correlate **GPU utilization** with workload class `3`, concurrency tier `4`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 069: correlate **idle GPU hours** with workload class `4`, concurrency tier `5`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 070: correlate **kernel failures** with workload class `5`, concurrency tier `6`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 071: correlate **PVC latency** with workload class `1`, concurrency tier `7`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 072: correlate **workbench restart count** with workload class `2`, concurrency tier `8`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 073: correlate **workbench startup time** with workload class `3`, concurrency tier `1`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 074: correlate **GPU utilization** with workload class `4`, concurrency tier `2`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 075: correlate **idle GPU hours** with workload class `5`, concurrency tier `3`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 076: correlate **kernel failures** with workload class `1`, concurrency tier `4`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 077: correlate **PVC latency** with workload class `2`, concurrency tier `5`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 078: correlate **workbench restart count** with workload class `3`, concurrency tier `6`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 079: correlate **workbench startup time** with workload class `4`, concurrency tier `7`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 080: correlate **GPU utilization** with workload class `5`, concurrency tier `8`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 081: correlate **idle GPU hours** with workload class `1`, concurrency tier `1`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 082: correlate **kernel failures** with workload class `2`, concurrency tier `2`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 083: correlate **PVC latency** with workload class `3`, concurrency tier `3`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 084: correlate **workbench restart count** with workload class `4`, concurrency tier `4`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 085: correlate **workbench startup time** with workload class `5`, concurrency tier `5`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 086: correlate **GPU utilization** with workload class `1`, concurrency tier `6`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 087: correlate **idle GPU hours** with workload class `2`, concurrency tier `7`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 088: correlate **kernel failures** with workload class `3`, concurrency tier `8`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 089: correlate **PVC latency** with workload class `4`, concurrency tier `1`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 090: correlate **workbench restart count** with workload class `5`, concurrency tier `2`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 091: correlate **workbench startup time** with workload class `1`, concurrency tier `3`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 092: correlate **GPU utilization** with workload class `2`, concurrency tier `4`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 093: correlate **idle GPU hours** with workload class `3`, concurrency tier `5`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 094: correlate **kernel failures** with workload class `4`, concurrency tier `6`, and failure reserve `30%`; record saturation evidence and scaling action.
- Capacity checkpoint 095: correlate **PVC latency** with workload class `5`, concurrency tier `7`, and failure reserve `35%`; record saturation evidence and scaling action.
- Capacity checkpoint 096: correlate **workbench restart count** with workload class `1`, concurrency tier `8`, and failure reserve `10%`; record saturation evidence and scaling action.
- Capacity checkpoint 097: correlate **workbench startup time** with workload class `2`, concurrency tier `1`, and failure reserve `15%`; record saturation evidence and scaling action.
- Capacity checkpoint 098: correlate **GPU utilization** with workload class `3`, concurrency tier `2`, and failure reserve `20%`; record saturation evidence and scaling action.
- Capacity checkpoint 099: correlate **idle GPU hours** with workload class `4`, concurrency tier `3`, and failure reserve `25%`; record saturation evidence and scaling action.
- Capacity checkpoint 100: correlate **kernel failures** with workload class `5`, concurrency tier `4`, and failure reserve `30%`; record saturation evidence and scaling action.

## Monitoring design

### Core metrics

- workbench startup time
- GPU utilization
- idle GPU hours
- kernel failures
- PVC latency
- workbench restart count

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: accelerator-health-rules
  namespace: data-science-project
spec:
  groups:
  - name: accelerator.rules
    rules:
    - alert: AcceleratorNodeHasNoAllocatableDevice
      expr: kube_node_status_allocatable{resource="nvidia_com_gpu_or_amd_com_gpu"} == 0
      for: 15m
      labels:
        severity: warning
      annotations:
        summary: "Accelerator node reports zero allocatable devices"
        runbook_url: "https://runbooks.example.com/accelerators/no-allocatable-device"
    - alert: AcceleratorPodPendingTooLong
      expr: max_over_time(kube_pod_status_phase{phase="Pending",namespace="data-science-project"}[30m]) > 0
      for: 20m
      labels:
        severity: warning
      annotations:
        summary: "Accelerator workload has remained Pending"
```

### Dashboard layout

- Fleet accelerator inventory by vendor and product
- Node capacity, allocatable, requested, and active workload count
- GPU or accelerator utilization by node and workload
- Memory used, free, and high-water mark
- Temperature, power, clocks, throttling, and health
- Pending workloads and scheduler reason
- Operator and daemonset readiness
- Driver and runtime version drift
- Xid, ECC, PCIe, reset, and vendor error events
- Inference or training service indicators

## L3 troubleshooting method

```text
1. Scope the blast radius.
2. Identify the first failing layer.
3. Preserve before-state evidence.
4. Check recent changes and version drift.
5. Reproduce with the smallest supported test.
6. Form one falsifiable hypothesis.
7. Run the least disruptive test.
8. Remediate with rollback prepared.
9. Validate platform and real workload.
10. Document prevention and monitoring improvements.
```

### Layer 1: Physical device and firmware

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 2: Hypervisor or passthrough layer

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 3: Node OS and kernel

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 4: Vendor driver

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 5: Feature discovery and labels

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 6: Device plugin and kubelet

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 7: Scheduler, quota, affinity, and taints

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 8: CRI-O, toolkit, CDI, and libraries

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 9: Workbench, KServe, vLLM, or training runtime

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.

### Layer 10: Application model, data, and framework

- **Question:** Can this layer independently explain the observed `GPU-enabled workbench` symptom?
- **Evidence:** versions, status, events, logs, node state, resource state, and a minimal test.
- **Do not do first:** reboot, reinstall, delete all operands, or change multiple variables simultaneously.
- **Exit criterion:** either prove the layer healthy or identify a concrete failure and owner.


## Scenario-based L3 drills

### Scenario 001: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 002: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 003: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 004: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 005: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 006: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 007: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 008: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 009: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 010: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 011: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 012: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 013: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 014: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 015: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 016: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 017: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 018: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 019: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 020: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 021: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 022: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 023: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 024: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 025: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 026: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 027: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 028: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 029: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 030: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 031: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 032: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 033: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 034: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 035: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 036: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 037: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 038: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 039: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 040: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 041: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 042: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 043: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 044: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 045: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 046: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 047: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 048: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 049: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 050: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 051: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 052: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 053: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 054: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 055: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 056: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 057: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 058: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 059: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 060: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 061: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 062: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 063: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 064: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 065: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 066: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 067: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 068: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 069: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 070: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 071: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 072: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 073: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 074: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 075: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 076: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 077: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 078: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 079: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 080: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 081: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 082: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 083: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 084: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 085: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 086: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 087: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 088: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 089: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 090: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 091: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 092: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 093: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 094: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 095: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 096: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 097: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 098: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 099: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 100: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 101: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 102: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 103: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 104: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 105: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 106: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 107: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 108: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 109: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 110: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 111: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get csv -A` followed by `oc describe pod <pod> -n <namespace>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 112: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get subscriptions.operators.coreos.com -A` followed by `oc logs <pod> -n <namespace> --all-containers --tail=300`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 113: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get installplans.operators.coreos.com -A` followed by `oc adm must-gather`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 114: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc get mcp` followed by `oc get nodes -o wide`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 115: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get co` followed by `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 116: metrics show utilization but application throughput is low

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get resourcequota -A` followed by `oc describe node <gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 117: device is intermittently marked unhealthy

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc get pod <pod> -n <namespace> -o yaml` followed by `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 118: only one tenant experiences the issue

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc describe pod <pod> -n <namespace>` followed by `oc get events -A --sort-by=.lastTimestamp | tail -100`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 119: failure appears only under concurrency

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc logs <pod> -n <namespace> --all-containers --tail=300` followed by `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 120: rollback does not restore previous behavior

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc adm must-gather` followed by `oc debug node/<gpu-node>`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 121: workbench pod Pending

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** missing node label or toleration.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc get nodes -o wide` followed by `chroot /host`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 122: CUDA image mismatch

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** device-plugin registration or health issue.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `oc get node <gpu-node> -o json | jq '.status.capacity,.status.allocatable'` followed by `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 123: PVC mount delays startup

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** unsupported hardware, platform, or release combination.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `oc describe node <gpu-node>` followed by `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 124: GPU remains allocated to idle workbench

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** incorrect resource name or profile identifier.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `oc get pods -A -o wide | grep -Ei 'gpu|nvidia|amd|habana|spyre|nfd'` followed by `oc get csv -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 125: notebook kernel cannot load framework

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** driver and user-space incompatibility.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench startup time`.
- **Command path:** `oc get events -A --sort-by=.lastTimestamp | tail -100` followed by `oc get subscriptions.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 126: works on one GPU node but not another

- **Context:** `GPU-enabled workbench` in workload tier `1` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** application memory or topology problem.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `workbench restart count`.
- **Command path:** `oc adm node-logs <gpu-node> --path=journal | grep -Ei 'nvidia|amdgpu|habana|xid|vfio|iommu'` followed by `oc get installplans.operators.coreos.com -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 127: works on host but not in container

- **Context:** `GPU-enabled workbench` in workload tier `2` on accelerator pool `gpu-pool-3`.
- **Primary hypothesis:** version drift between nodes.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `PVC latency`.
- **Command path:** `oc debug node/<gpu-node>` followed by `oc get mcp`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 128: works in smoke pod but not in OpenShift AI workbench

- **Context:** `GPU-enabled workbench` in workload tier `3` on accelerator pool `gpu-pool-4`.
- **Primary hypothesis:** quota or admission rejection.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `kernel failures`.
- **Command path:** `chroot /host` followed by `oc get co`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 129: works after reboot but fails after upgrade

- **Context:** `GPU-enabled workbench` in workload tier `4` on accelerator pool `gpu-pool-1`.
- **Primary hypothesis:** runtime injection or CDI failure.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `idle GPU hours`.
- **Command path:** `lspci -nn | grep -Ei 'vga|3d|display|nvidia|amd|habana|ibm'` followed by `oc get resourcequota -A`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.

### Scenario 130: capacity exists but scheduler reports insufficient resource

- **Context:** `GPU-enabled workbench` in workload tier `5` on accelerator pool `gpu-pool-2`.
- **Primary hypothesis:** stale configuration after operator reconciliation.
- **First evidence:** pod events, node allocatable `nvidia.com/gpu or amd.com/gpu`, operand readiness, recent changes, and `GPU utilization`.
- **Command path:** `lsmod | grep -Ei 'nvidia|amdgpu|vfio|habana'` followed by `oc get pod <pod> -n <namespace> -o yaml`.
- **Safe test:** run a minimal supported accelerator container on the same node without changing the production workload.
- **Remediation rule:** change one layer at a time and keep a tested rollback manifest or node-state recovery step.
- **Validation:** prove device visibility, resource allocation, representative workload success, metrics recovery, and no regression on another node.
- **Prevention:** add a preflight check, drift alert, canary node, or admission control matching the discovered cause.
- **Escalation evidence:** must-gather, operator logs, node journal, hardware diagnostics, exact versions, timestamps, and reproducer.


## Production operating procedures

### SOP 01: New GPU node onboarding

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 02: Operator installation

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 03: Driver upgrade

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 04: OpenShift upgrade

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 05: OpenShift AI upgrade

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 06: Firmware upgrade

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 07: MIG or sharing reconfiguration

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 08: vGPU profile change

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 09: Node drain and maintenance

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 10: Accelerator incident response

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 11: Capacity expansion

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 12: Node decommission

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 13: Disaster recovery validation

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 14: Quarterly access review

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.

### SOP 15: Performance regression investigation

1. Confirm approved change record, support matrix, maintenance window, owner, and rollback trigger.
2. Capture cluster, operator, node, driver, firmware, label, taint, quota, and workload before-state.
3. Drain or quiesce workloads according to disruption and data-protection requirements.
4. Apply the smallest approved change through GitOps or the supported operator interface.
5. Observe reconciliation, daemonsets, node readiness, allocatable resources, and vendor diagnostics.
6. Run minimal smoke, OpenShift AI workload, telemetry, and SLO validation.
7. Rollback when trigger conditions are met; do not continue stacking unverified changes.
8. Close with evidence, lessons learned, configuration drift check, and runbook update.


## Operational checklist catalogue

- [ ] Control 001 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-001`.
- [ ] Control 002 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-002`.
- [ ] Control 003 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-003`.
- [ ] Control 004 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-004`.
- [ ] Control 005 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-005`.
- [ ] Control 006 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-006`.
- [ ] Control 007 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-007`.
- [ ] Control 008 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-008`.
- [ ] Control 009 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-009`.
- [ ] Control 010 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-010`.
- [ ] Control 011 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-011`.
- [ ] Control 012 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-012`.
- [ ] Control 013 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-013`.
- [ ] Control 014 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-014`.
- [ ] Control 015 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-015`.
- [ ] Control 016 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-016`.
- [ ] Control 017 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-017`.
- [ ] Control 018 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-018`.
- [ ] Control 019 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-019`.
- [ ] Control 020 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-020`.
- [ ] Control 021 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-021`.
- [ ] Control 022 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-022`.
- [ ] Control 023 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-023`.
- [ ] Control 024 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-024`.
- [ ] Control 025 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-025`.
- [ ] Control 026 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-026`.
- [ ] Control 027 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-027`.
- [ ] Control 028 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-028`.
- [ ] Control 029 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-029`.
- [ ] Control 030 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-030`.
- [ ] Control 031 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-031`.
- [ ] Control 032 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-032`.
- [ ] Control 033 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-033`.
- [ ] Control 034 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-034`.
- [ ] Control 035 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-035`.
- [ ] Control 036 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-036`.
- [ ] Control 037 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-037`.
- [ ] Control 038 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-038`.
- [ ] Control 039 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-039`.
- [ ] Control 040 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-040`.
- [ ] Control 041 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-041`.
- [ ] Control 042 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-042`.
- [ ] Control 043 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-043`.
- [ ] Control 044 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-044`.
- [ ] Control 045 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-045`.
- [ ] Control 046 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-046`.
- [ ] Control 047 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-047`.
- [ ] Control 048 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-048`.
- [ ] Control 049 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-049`.
- [ ] Control 050 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-050`.
- [ ] Control 051 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-051`.
- [ ] Control 052 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-052`.
- [ ] Control 053 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-053`.
- [ ] Control 054 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-054`.
- [ ] Control 055 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-055`.
- [ ] Control 056 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-056`.
- [ ] Control 057 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-057`.
- [ ] Control 058 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-058`.
- [ ] Control 059 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-059`.
- [ ] Control 060 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-060`.
- [ ] Control 061 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-061`.
- [ ] Control 062 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-062`.
- [ ] Control 063 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-063`.
- [ ] Control 064 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-064`.
- [ ] Control 065 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-065`.
- [ ] Control 066 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-066`.
- [ ] Control 067 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-067`.
- [ ] Control 068 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-068`.
- [ ] Control 069 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-069`.
- [ ] Control 070 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-070`.
- [ ] Control 071 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-071`.
- [ ] Control 072 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-072`.
- [ ] Control 073 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-073`.
- [ ] Control 074 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-074`.
- [ ] Control 075 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-075`.
- [ ] Control 076 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-076`.
- [ ] Control 077 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-077`.
- [ ] Control 078 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-078`.
- [ ] Control 079 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-079`.
- [ ] Control 080 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-080`.
- [ ] Control 081 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-081`.
- [ ] Control 082 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-082`.
- [ ] Control 083 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-083`.
- [ ] Control 084 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-084`.
- [ ] Control 085 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-085`.
- [ ] Control 086 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-086`.
- [ ] Control 087 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-087`.
- [ ] Control 088 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-088`.
- [ ] Control 089 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-089`.
- [ ] Control 090 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-090`.
- [ ] Control 091 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-091`.
- [ ] Control 092 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-092`.
- [ ] Control 093 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-093`.
- [ ] Control 094 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-094`.
- [ ] Control 095 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-095`.
- [ ] Control 096 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-096`.
- [ ] Control 097 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-097`.
- [ ] Control 098 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-098`.
- [ ] Control 099 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-099`.
- [ ] Control 100 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-100`.
- [ ] Control 101 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-101`.
- [ ] Control 102 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-102`.
- [ ] Control 103 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-103`.
- [ ] Control 104 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-104`.
- [ ] Control 105 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-105`.
- [ ] Control 106 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-106`.
- [ ] Control 107 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-107`.
- [ ] Control 108 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-108`.
- [ ] Control 109 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-109`.
- [ ] Control 110 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-110`.
- [ ] Control 111 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-111`.
- [ ] Control 112 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-112`.
- [ ] Control 113 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-113`.
- [ ] Control 114 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-114`.
- [ ] Control 115 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-115`.
- [ ] Control 116 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-116`.
- [ ] Control 117 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-117`.
- [ ] Control 118 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-118`.
- [ ] Control 119 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-119`.
- [ ] Control 120 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-120`.
- [ ] Control 121 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-121`.
- [ ] Control 122 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-122`.
- [ ] Control 123 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-123`.
- [ ] Control 124 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-124`.
- [ ] Control 125 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-125`.
- [ ] Control 126 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-126`.
- [ ] Control 127 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-127`.
- [ ] Control 128 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-128`.
- [ ] Control 129 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-129`.
- [ ] Control 130 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-130`.
- [ ] Control 131 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-131`.
- [ ] Control 132 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-132`.
- [ ] Control 133 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-133`.
- [ ] Control 134 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-134`.
- [ ] Control 135 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-135`.
- [ ] Control 136 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-136`.
- [ ] Control 137 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-137`.
- [ ] Control 138 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-138`.
- [ ] Control 139 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-139`.
- [ ] Control 140 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-140`.
- [ ] Control 141 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-141`.
- [ ] Control 142 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-142`.
- [ ] Control 143 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-143`.
- [ ] Control 144 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-144`.
- [ ] Control 145 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-145`.
- [ ] Control 146 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-146`.
- [ ] Control 147 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-147`.
- [ ] Control 148 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-148`.
- [ ] Control 149 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-149`.
- [ ] Control 150 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-150`.
- [ ] Control 151 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-151`.
- [ ] Control 152 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-152`.
- [ ] Control 153 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-153`.
- [ ] Control 154 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-154`.
- [ ] Control 155 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-155`.
- [ ] Control 156 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-156`.
- [ ] Control 157 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-157`.
- [ ] Control 158 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-158`.
- [ ] Control 159 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-159`.
- [ ] Control 160 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-160`.
- [ ] Control 161 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-161`.
- [ ] Control 162 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-162`.
- [ ] Control 163 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-163`.
- [ ] Control 164 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-164`.
- [ ] Control 165 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-165`.
- [ ] Control 166 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-166`.
- [ ] Control 167 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-167`.
- [ ] Control 168 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-168`.
- [ ] Control 169 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-169`.
- [ ] Control 170 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-170`.
- [ ] Control 171 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-171`.
- [ ] Control 172 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-172`.
- [ ] Control 173 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-173`.
- [ ] Control 174 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-174`.
- [ ] Control 175 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-175`.
- [ ] Control 176 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-176`.
- [ ] Control 177 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-177`.
- [ ] Control 178 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-178`.
- [ ] Control 179 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-179`.
- [ ] Control 180 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-180`.
- [ ] Control 181 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-181`.
- [ ] Control 182 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-182`.
- [ ] Control 183 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-183`.
- [ ] Control 184 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-184`.
- [ ] Control 185 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-185`.
- [ ] Control 186 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-186`.
- [ ] Control 187 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-187`.
- [ ] Control 188 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-188`.
- [ ] Control 189 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-189`.
- [ ] Control 190 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-190`.
- [ ] Control 191 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-191`.
- [ ] Control 192 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-192`.
- [ ] Control 193 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-193`.
- [ ] Control 194 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-194`.
- [ ] Control 195 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-195`.
- [ ] Control 196 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-196`.
- [ ] Control 197 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-197`.
- [ ] Control 198 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-198`.
- [ ] Control 199 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-199`.
- [ ] Control 200 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-200`.
- [ ] Control 201 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-201`.
- [ ] Control 202 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-202`.
- [ ] Control 203 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-203`.
- [ ] Control 204 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-204`.
- [ ] Control 205 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-205`.
- [ ] Control 206 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-206`.
- [ ] Control 207 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-207`.
- [ ] Control 208 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-208`.
- [ ] Control 209 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-209`.
- [ ] Control 210 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-210`.
- [ ] Control 211 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-211`.
- [ ] Control 212 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-212`.
- [ ] Control 213 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-213`.
- [ ] Control 214 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-214`.
- [ ] Control 215 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-215`.
- [ ] Control 216 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-216`.
- [ ] Control 217 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-217`.
- [ ] Control 218 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-218`.
- [ ] Control 219 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-219`.
- [ ] Control 220 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-220`.
- [ ] Control 221 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-221`.
- [ ] Control 222 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-222`.
- [ ] Control 223 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-223`.
- [ ] Control 224 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-224`.
- [ ] Control 225 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-225`.
- [ ] Control 226 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-226`.
- [ ] Control 227 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-227`.
- [ ] Control 228 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-228`.
- [ ] Control 229 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-229`.
- [ ] Control 230 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-230`.
- [ ] Control 231 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-231`.
- [ ] Control 232 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-232`.
- [ ] Control 233 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-233`.
- [ ] Control 234 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-234`.
- [ ] Control 235 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-235`.
- [ ] Control 236 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-236`.
- [ ] Control 237 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-237`.
- [ ] Control 238 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-238`.
- [ ] Control 239 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-239`.
- [ ] Control 240 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-240`.
- [ ] Control 241 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-241`.
- [ ] Control 242 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-242`.
- [ ] Control 243 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-243`.
- [ ] Control 244 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-244`.
- [ ] Control 245 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-245`.
- [ ] Control 246 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-246`.
- [ ] Control 247 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-247`.
- [ ] Control 248 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-248`.
- [ ] Control 249 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-249`.
- [ ] Control 250 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-250`.
- [ ] Control 251 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-251`.
- [ ] Control 252 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-252`.
- [ ] Control 253 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-253`.
- [ ] Control 254 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-254`.
- [ ] Control 255 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-255`.
- [ ] Control 256 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-256`.
- [ ] Control 257 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-257`.
- [ ] Control 258 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-258`.
- [ ] Control 259 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-259`.
- [ ] Control 260 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-260`.
- [ ] Control 261 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-261`.
- [ ] Control 262 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-262`.
- [ ] Control 263 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-263`.
- [ ] Control 264 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-264`.
- [ ] Control 265 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-265`.
- [ ] Control 266 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-266`.
- [ ] Control 267 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-267`.
- [ ] Control 268 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-268`.
- [ ] Control 269 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-269`.
- [ ] Control 270 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-270`.
- [ ] Control 271 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-271`.
- [ ] Control 272 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-272`.
- [ ] Control 273 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-273`.
- [ ] Control 274 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-274`.
- [ ] Control 275 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-275`.
- [ ] Control 276 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-276`.
- [ ] Control 277 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-277`.
- [ ] Control 278 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-278`.
- [ ] Control 279 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-279`.
- [ ] Control 280 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-280`.
- [ ] Control 281 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-281`.
- [ ] Control 282 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-282`.
- [ ] Control 283 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-283`.
- [ ] Control 284 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-284`.
- [ ] Control 285 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-285`.
- [ ] Control 286 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-286`.
- [ ] Control 287 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-287`.
- [ ] Control 288 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-288`.
- [ ] Control 289 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-289`.
- [ ] Control 290 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-290`.
- [ ] Control 291 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-291`.
- [ ] Control 292 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-292`.
- [ ] Control 293 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-293`.
- [ ] Control 294 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-294`.
- [ ] Control 295 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-295`.
- [ ] Control 296 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-296`.
- [ ] Control 297 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-297`.
- [ ] Control 298 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-298`.
- [ ] Control 299 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-299`.
- [ ] Control 300 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-300`.
- [ ] Control 301 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-301`.
- [ ] Control 302 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-302`.
- [ ] Control 303 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-303`.
- [ ] Control 304 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-304`.
- [ ] Control 305 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-305`.
- [ ] Control 306 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-306`.
- [ ] Control 307 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-307`.
- [ ] Control 308 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-308`.
- [ ] Control 309 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-309`.
- [ ] Control 310 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-310`.
- [ ] Control 311 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-311`.
- [ ] Control 312 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-312`.
- [ ] Control 313 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-313`.
- [ ] Control 314 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-314`.
- [ ] Control 315 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-315`.
- [ ] Control 316 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-316`.
- [ ] Control 317 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-317`.
- [ ] Control 318 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-318`.
- [ ] Control 319 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-319`.
- [ ] Control 320 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-320`.
- [ ] Control 321 (monthly): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-321`.
- [ ] Control 322 (before change): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-322`.
- [ ] Control 323 (after change): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-323`.
- [ ] Control 324 (quarterly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-324`.
- [ ] Control 325 (daily): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-325`.
- [ ] Control 326 (weekly): Verify minimal smoke workload success. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-326`.
- [ ] Control 327 (monthly): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-327`.
- [ ] Control 328 (before change): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-328`.
- [ ] Control 329 (after change): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-329`.
- [ ] Control 330 (quarterly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-330`.
- [ ] Control 331 (daily): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-331`.
- [ ] Control 332 (weekly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-332`.
- [ ] Control 333 (monthly): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-333`.
- [ ] Control 334 (before change): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-334`.
- [ ] Control 335 (after change): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-335`.
- [ ] Control 336 (quarterly): Verify minimal smoke workload success. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-336`.
- [ ] Control 337 (daily): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-337`.
- [ ] Control 338 (weekly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-338`.
- [ ] Control 339 (monthly): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-339`.
- [ ] Control 340 (before change): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-340`.
- [ ] Control 341 (after change): Verify support matrix for the exact release and hardware combination. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-341`.
- [ ] Control 342 (quarterly): Verify the responsible operator reports healthy conditions. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-342`.
- [ ] Control 343 (daily): Verify `nvidia.com/gpu or amd.com/gpu` capacity and allocatable values. Record result for scope `gpu-domain-7` and attach evidence reference `ACC-20-343`.
- [ ] Control 344 (weekly): Verify no unexpected node label or taint drift. Record result for scope `gpu-domain-8` and attach evidence reference `ACC-20-344`.
- [ ] Control 345 (monthly): Verify namespace quota and hardware-profile mapping. Record result for scope `gpu-domain-1` and attach evidence reference `ACC-20-345`.
- [ ] Control 346 (before change): Verify minimal smoke workload success. Record result for scope `gpu-domain-2` and attach evidence reference `ACC-20-346`.
- [ ] Control 347 (after change): Verify representative OpenShift AI workload success. Record result for scope `gpu-domain-3` and attach evidence reference `ACC-20-347`.
- [ ] Control 348 (quarterly): Verify telemetry scrape, dashboard, and alerts. Record result for scope `gpu-domain-4` and attach evidence reference `ACC-20-348`.
- [ ] Control 349 (daily): Verify rollback artifacts and evidence bundle are current. Record result for scope `gpu-domain-5` and attach evidence reference `ACC-20-349`.
- [ ] Control 350 (weekly): Verify ownership, escalation path, and maintenance documentation. Record result for scope `gpu-domain-6` and attach evidence reference `ACC-20-350`.

## Hands-on exercises

### Exercise 001: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 002: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 003: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 004: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 005: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 006: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 007: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 008: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 009: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 010: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 011: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 012: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 013: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 014: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 015: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 016: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 017: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 018: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 019: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 020: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 021: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 022: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 023: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 024: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 025: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 026: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 027: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 028: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 029: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 030: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 031: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 032: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 033: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 034: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 035: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 036: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 037: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 038: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 039: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 040: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 041: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 042: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 043: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 044: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 045: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 046: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 047: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 048: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 049: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 050: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 051: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 052: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 053: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 054: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 055: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 056: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 057: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 058: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 059: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 060: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 061: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 062: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 063: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 064: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 065: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 066: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 067: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 068: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 069: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 070: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 071: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 072: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 073: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 074: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 075: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 076: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 077: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 078: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 079: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 080: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 081: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 082: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 083: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 084: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 085: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 086: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 087: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 088: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 089: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 090: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 091: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 092: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 093: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 094: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 095: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 096: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 097: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 098: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 099: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 100: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 101: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 102: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 103: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 104: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 105: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 106: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 107: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 108: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 109: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 110: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 111: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 112: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 113: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 114: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 115: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 116: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 117: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 118: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 119: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 120: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 121: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 122: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 123: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 124: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 125: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 126: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 127: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 128: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 129: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 130: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 131: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 132: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 133: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 134: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 135: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 136: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 137: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 138: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 139: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 140: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 141: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 142: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 143: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 144: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 145: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 146: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 147: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 148: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 149: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 150: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 151: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 152: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 153: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 154: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 155: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 156: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 157: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 158: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 159: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 160: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 161: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 162: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 163: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 164: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 165: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 166: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 167: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 168: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 169: Inventory The Accelerator Path

- **Objective:** inventory the accelerator path for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 170: Break And Repair A Node Label

- **Objective:** break and repair a node label for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 171: Create A Taint And Matching Toleration

- **Objective:** create a taint and matching toleration for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 172: Apply And Exhaust A Gpu Quota

- **Objective:** apply and exhaust a GPU quota for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 173: Trace A Device-Plugin Allocation

- **Objective:** trace a device-plugin allocation for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 174: Compare Host And Container Visibility

- **Objective:** compare host and container visibility for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 175: Collect Operator And Node Evidence

- **Objective:** collect operator and node evidence for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 176: Build A Memory Or Utilization Dashboard

- **Objective:** build a memory or utilization dashboard for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 177: Create A Pending Workload And Resolve It

- **Objective:** create a Pending workload and resolve it for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-1>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 178: Perform A Rollback Rehearsal

- **Objective:** perform a rollback rehearsal for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-2>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 179: Write An Architecture Decision Record

- **Objective:** write an architecture decision record for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-3>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.

### Exercise 180: Create A Support Escalation Bundle

- **Objective:** create a support escalation bundle for `GPU-enabled workbench` without modifying unrelated accelerator nodes.
- **Environment:** namespace `data-science-project`, resource `nvidia.com/gpu or amd.com/gpu`, test node `<gpu-node-4>`.
- **Deliverable:** commands, manifests, before/after evidence, root-cause statement, validation, rollback, and prevention control.
- **Pass condition:** another senior engineer can reproduce the result using only the documented evidence.
- **Safety rule:** never simulate hardware faults or destructive driver changes on a production node.


## Interview and design-review questions

1. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
2. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
3. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
4. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
5. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
6. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
7. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
8. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
9. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
10. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
11. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
12. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
13. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
14. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
15. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
16. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
17. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
18. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
19. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
20. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
21. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
22. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
23. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
24. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
25. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
26. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
27. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
28. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
29. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
30. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
31. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
32. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
33. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
34. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
35. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
36. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
37. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
38. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
39. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
40. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
41. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
42. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
43. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
44. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
45. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
46. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
47. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
48. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
49. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
50. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
51. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
52. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
53. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
54. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
55. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
56. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
57. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
58. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
59. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
60. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
61. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
62. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
63. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
64. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
65. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
66. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
67. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
68. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
69. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
70. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
71. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
72. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
73. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
74. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
75. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
76. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
77. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
78. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
79. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
80. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
81. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
82. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
83. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
84. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
85. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
86. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
87. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
88. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
89. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
90. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
91. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
92. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
93. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
94. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
95. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
96. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
97. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
98. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
99. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
100. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
101. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
102. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
103. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
104. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
105. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
106. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
107. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
108. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
109. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
110. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
111. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
112. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
113. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
114. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
115. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
116. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
117. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
118. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
119. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
120. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
121. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
122. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
123. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
124. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
125. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
126. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
127. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
128. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
129. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
130. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
131. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
132. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
133. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
134. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
135. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
136. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
137. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
138. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
139. How would you design canary and rollback procedures? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
140. What metrics prove efficient accelerator use? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
141. What support-matrix checks are mandatory? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
142. How do you build an escalation-ready evidence bundle? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
143. When should you use exclusive GPUs, MIG, time slicing, vGPU, or passthrough? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
144. How do you reduce Pending time without over-allocating expensive capacity? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
145. Explain the complete control path for GPU-enabled workbench. Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
146. What evidence proves `nvidia.com/gpu or amd.com/gpu` is schedulable? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
147. How do you separate a driver failure from a runtime injection failure? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
148. Why can a GPU be visible on the host but absent inside the pod? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
149. How do quotas, taints, affinity, and hardware profiles interact? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.
150. What changes during a kernel or OpenShift upgrade? Include architecture, commands, failure modes, validation, rollback, monitoring, and security in the answer.

## Troubleshooting quick reference

| Symptom | First checks | Likely layer | Safe next action |
|---|---|---|---|
| No PCI device | lspci, firmware, hypervisor assignment | Hardware or virtualization | Correct assignment or firmware; do not reinstall operator first |
| Driver pod failing | daemonset logs, kernel, MCP, Secure Boot | Driver | Resolve exact build/load error |
| Allocatable is zero | device plugin, NVML/vendor API, health | Device plugin or driver | Run vendor smoke on host and inspect plugin logs |
| Pod Pending | events, quota, selectors, taints, capacity | Scheduling or admission | Fix only the reported predicate or quota |
| GPU absent in container | resource request, assigned node, CDI/hooks, libraries | Runtime | Run minimal container and compare mounts/devices |
| CUDA or framework error | driver compatibility, memory, Xid, image | Application or compatibility | Reproduce with supported base image |
| Low utilization | queue, batch, data feed, CPU, storage, topology | Application or system bottleneck | Correlate accelerator and service metrics |
| High temperature | power, clocks, fan, ambient, neighboring nodes | Hardware and facility | Reduce load safely and inspect cooling |
| Intermittent unhealthy device | Xid, ECC, PCIe, reset, thermal | Hardware or driver | Preserve logs and run vendor diagnostics |
| Works only on one node | version, labels, firmware, topology, images | Configuration drift | Diff node state before changing anything |

## Glossary

- **Accelerator:** A specialized compute device used to speed machine-learning, inference, training, or data-processing workloads.
- **Allocatable:** The quantity of a node resource available for pod scheduling after system reservations and health decisions.
- **CDI:** Container Device Interface, a specification for describing device injection into containers.
- **CUDA:** NVIDIA parallel-computing platform and programming model.
- **DCGM:** NVIDIA Data Center GPU Manager used for health and telemetry.
- **Device plugin:** A kubelet extension that discovers, advertises, allocates, and health-checks specialized devices.
- **Extended resource:** A vendor-qualified integer resource such as nvidia.com/gpu or amd.com/gpu.
- **GPU Feature Discovery:** NVIDIA component that uses NFD to create GPU-specific node labels.
- **Hardware profile:** OpenShift AI abstraction used to present governed compute and accelerator choices to users.
- **MIG:** NVIDIA Multi-Instance GPU, hardware partitioning on supported GPUs.
- **NFD:** Node Feature Discovery, a Kubernetes add-on that labels node hardware and system features.
- **NUMA:** Non-uniform memory access topology that affects CPU, memory, NIC, and GPU locality.
- **ROCm:** AMD software platform for GPU computing.
- **ServingRuntime:** KServe resource describing how a model server runs and which model formats it supports.
- **vGPU:** A virtual GPU profile exposed to a virtual machine through a hypervisor and vendor software stack.
- **Xid:** An NVIDIA driver error report written to the kernel or system log.

## Official references

- [Red Hat OpenShift AI 3.5 - Working with accelerators](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/)
- [Red Hat OpenShift AI - Workbenches](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/getting_started_with_red_hat_openshift_ai_self-managed/)
- [Red Hat OpenShift AI - Model serving](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/configuring_your_model-serving_platform/)
- [OpenShift Container Platform 4.21 - Specialized hardware and driver enablement](https://docs.redhat.com/en/documentation/openshift_container_platform/4.21/html/specialized_hardware_and_driver_enablement/)
- [OpenShift Container Platform 4.21 - Hardware accelerators](https://docs.redhat.com/en/documentation/openshift_container_platform/4.21/html/hardware_accelerators/)
- [OpenShift Container Platform 4.21 - Resource quotas](https://docs.redhat.com/en/documentation/openshift_container_platform/4.21/html/scalability_and_performance/compute-resource-quotas)
- [Kubernetes - Device plugins](https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/device-plugins/)
- [Kubernetes - Schedule GPUs](https://kubernetes.io/docs/tasks/manage-gpus/scheduling-gpus/)
- [NVIDIA GPU Operator](https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/latest/)
- [NVIDIA GPU telemetry and DCGM Exporter](https://docs.nvidia.com/datacenter/cloud-native/gpu-telemetry/latest/)
- [NVIDIA CUDA compatibility](https://docs.nvidia.com/deploy/cuda-compatibility/latest/)
- [NVIDIA Xid errors](https://docs.nvidia.com/deploy/xid-errors/index.html)
- [AMD GPU Operator](https://instinct.docs.amd.com/projects/gpu-operator/en/latest/)
- [AMD GPU device plugin](https://instinct.docs.amd.com/projects/k8s-device-plugin/en/latest/)
- [Intel Gaudi documentation](https://docs.habana.ai/en/latest/)
- [IBM Spyre Accelerator support content](https://www.ibm.com/support/z-content-solutions/spyre-accelerator-z-and-linuxone/)

## Final L3 acceptance statement

A production implementation of **GPU-enabled workbench** is complete only when the hardware-to-application path is proven, `nvidia.com/gpu or amd.com/gpu` is governed, a representative OpenShift AI workload succeeds, metrics and alerts work, rollback is rehearsed, and the exact combination is verified as supported.
