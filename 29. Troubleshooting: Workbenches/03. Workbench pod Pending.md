---
title: "03. Workbench pod Pending"
series: "Troubleshooting: Workbenches"
product: "Red Hat OpenShift AI Self-Managed"
level: "Corporate L3 / Senior Platform Engineer"
baseline: "OpenShift AI 3.4 GA; notes identify 3.5 or environment-specific behavior"
generated: "2026-07-21"
document_type: "hands-on troubleshooting project and production runbook"
---

# 03. Workbench pod Pending

> This guide is written for senior OpenShift, Kubernetes, platform engineering, SRE, and MLOps teams.
> Commands use placeholders and must be validated in a non-production environment before production execution.
> Resource names and APIs can differ by OpenShift AI release, enabled components, and whether the workbench is a basic workbench or a project workbench.
> Discover the installed APIs first; do not assume that a resource name from another release exists in your cluster.

## Document goals

- Primary symptom: A workbench Pod exists but the scheduler cannot place or initialize it.
- Build a reproducible lab from scratch.
- Separate dashboard symptoms from controller, Kubernetes, node, image, storage, network, identity, and external dependency failures.
- Collect an escalation-quality evidence bundle before deleting transient evidence.
- Restore service with the least risky reversible change.
- Prove recovery with technical and user-level validation.
- Convert the incident into monitoring, policy, automation, and capacity improvements.

## Version and support position

- The stable operational baseline in this guide is Red Hat OpenShift AI Self-Managed 3.4 documentation.
- OpenShift AI 3.5 documentation is used only for forward-looking differences and must not be treated as a supported production baseline unless your subscription and release channel support it.
- Custom images can be imported and made selectable, but Red Hat support does not repair unsupported content inside a customer-built image.
- In OpenShift AI 3.4, idle workbench culling is disabled by default until configured.
- The cluster-wide user session duration can take precedence over the idle workbench timeout.
- OpenShift AI 3.4 verifies culling through the `notebook-controller-culler-config` ConfigMap in `redhat-ods-applications`.
- Kubernetes `ImagePullBackOff` means the kubelet cannot pull an image and retries with increasing backoff.
- Never edit an operator-managed generated resource as the permanent fix unless the product documentation explicitly instructs you to do so.

## Table of contents

1. [Executive diagnosis](#executive-diagnosis)
2. [Architecture and failure boundaries](#architecture-and-failure-boundaries)
3. [Create the troubleshooting project from scratch](#create-the-troubleshooting-project-from-scratch)
4. [Baseline validation](#baseline-validation)
5. [Controlled fault injection](#controlled-fault-injection)
6. [Evidence collection bundle](#evidence-collection-bundle)
7. [Root-cause matrix](#root-cause-matrix)
8. [Layer-by-layer diagnosis](#layer-by-layer-diagnosis)
9. [Detailed remediation runbooks](#detailed-remediation-runbooks)
10. [Decision tree workbook](#decision-tree-workbook)
11. [Command interpretation drills](#command-interpretation-drills)
12. [Production case studies](#production-case-studies)
13. [Automation toolkit](#automation-toolkit)
14. [Observability and alerting](#observability-and-alerting)
15. [Capacity and performance](#capacity-and-performance)
16. [Security and compliance](#security-and-compliance)
17. [Incident response and escalation](#incident-response-and-escalation)
18. [Change, rollback, and validation](#change-rollback-and-validation)
19. [Preventive engineering](#preventive-engineering)
20. [Senior interview and review questions](#senior-interview-and-review-questions)
21. [References](#references)

## Executive diagnosis

### What the symptom means

A workbench Pod exists but the scheduler cannot place or initialize it.

The dashboard status is an aggregation, not the root cause.
A senior engineer proves where progress stopped by following ownership from the user action to the generated workload.
The first irreversible action must not be deleting the Pod; first preserve events, current logs, previous logs, generated YAML, controller logs, and timestamps.
The first comparison should be between one failing workbench and one known-good workbench in the same cluster.
The second comparison should be between the same workbench before and after the last change.
The third comparison should be between the requested configuration and the object actually admitted by the API server.

### Fast severity model

| Severity | Scope | Immediate action |
|---|---|---|
| SEV-1 | Many users or all workbenches fail; business-critical development or incident response is blocked. | Activate incident command, freeze unrelated changes, collect cluster-wide evidence. |
| SEV-2 | A team, project, node pool, image family, storage class, or registry path is affected. | Assign platform, storage, network, or identity owner and begin scoped mitigation. |
| SEV-3 | One workbench or one user is affected and a workaround exists. | Preserve evidence, compare with known-good, repair safely. |
| SEV-4 | Cosmetic, intermittent, or non-production symptom. | Track as problem-management or engineering backlog. |

### Five-minute triage

```bash
export NS="<data-science-project-namespace>"
export POD="<workbench-pod>"
export PVC="<workbench-pvc>"
oc get pod -n "$NS" -o wide
oc get events -n "$NS" --sort-by=.lastTimestamp | tail -n 80
oc describe pod -n "$NS" "$POD"
oc logs -n "$NS" "$POD" --all-containers --tail=200
oc logs -n "$NS" "$POD" --all-containers --previous --tail=200
oc get pvc,svc,route,httproute -n "$NS"
oc get resourcequota,limitrange,networkpolicy -n "$NS"
oc adm top pod -n "$NS" --containers
```

### Triage stop conditions

- Stop broad investigation when evidence conclusively identifies `insufficient requested CPU`.
- Record why cause candidate 1 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `insufficient requested memory`.
- Record why cause candidate 2 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `GPU resource unavailable`.
- Record why cause candidate 3 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `node selector matches no nodes`.
- Record why cause candidate 4 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `taint lacks a matching toleration`.
- Record why cause candidate 5 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `pod affinity or topology rule is unsatisfiable`.
- Record why cause candidate 6 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `ResourceQuota blocks admission`.
- Record why cause candidate 7 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `LimitRange default conflicts with the profile`.
- Record why cause candidate 8 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `PVC is unbound`.
- Record why cause candidate 9 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `volume node affinity conflict`.
- Record why cause candidate 10 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `Kueue admission is pending`.
- Record why cause candidate 11 is confirmed or rejected.
- Stop broad investigation when evidence conclusively identifies `maximum pod density or node pressure`.
- Record why cause candidate 12 is confirmed or rejected.

## Architecture and failure boundaries

A workbench request crosses several independently owned control planes.
A dashboard button can succeed while a downstream controller, admission plugin, scheduler, kubelet, CSI driver, registry, or ingress path fails.

### Boundary 1: User, dashboard, and identity

Responsibility: user-selected project, allowed groups, OAuth session, dashboard validation, ownership, and browser redirects.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the user, dashboard, and identity boundary.
Common false assumption: a healthy adjacent layer proves that the user, dashboard, and identity boundary is healthy.
Escalation owner: identify the team with change authority over user, dashboard, and identity.
Rollback rule: reverse only the smallest change within user, dashboard, and identity that explains the failure.

### Boundary 2: OpenShift AI controllers

Responsibility: reconciliation of workbench, image, connection, culling, generated workload, and displayed status.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the openshift ai controllers boundary.
Common false assumption: a healthy adjacent layer proves that the openshift ai controllers boundary is healthy.
Escalation owner: identify the team with change authority over openshift ai controllers.
Rollback rule: reverse only the smallest change within openshift ai controllers that explains the failure.

### Boundary 3: Kubernetes API, admission, and policy

Responsibility: RBAC, ResourceQuota, LimitRange, SCC, policy engines, webhooks, and object validation.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the kubernetes api, admission, and policy boundary.
Common false assumption: a healthy adjacent layer proves that the kubernetes api, admission, and policy boundary is healthy.
Escalation owner: identify the team with change authority over kubernetes api, admission, and policy.
Rollback rule: reverse only the smallest change within kubernetes api, admission, and policy that explains the failure.

### Boundary 4: Scheduler, node, and accelerator

Responsibility: requests, selectors, affinity, taints, Kueue, node pressure, kubelet, device plugins, GPU drivers, and allocatable resources.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the scheduler, node, and accelerator boundary.
Common false assumption: a healthy adjacent layer proves that the scheduler, node, and accelerator boundary is healthy.
Escalation owner: identify the team with change authority over scheduler, node, and accelerator.
Rollback rule: reverse only the smallest change within scheduler, node, and accelerator that explains the failure.

### Boundary 5: Image, storage, and external dependencies

Responsibility: registry, pull secret, proxy, certificates, package mirrors, PVC, CSI, filesystem, object storage, and identity dependencies.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the image, storage, and external dependencies boundary.
Common false assumption: a healthy adjacent layer proves that the image, storage, and external dependencies boundary is healthy.
Escalation owner: identify the team with change authority over image, storage, and external dependencies.
Rollback rule: reverse only the smallest change within image, storage, and external dependencies that explains the failure.

### Boundary 6: Networking, access path, and observability

Responsibility: Service, EndpointSlice, Route or Gateway API, DNS, TLS, OAuth proxy, NetworkPolicy, events, logs, metrics, and audit records.
Primary proof: inspect objects, status conditions, events, logs, and metrics owned by the networking, access path, and observability boundary.
Common false assumption: a healthy adjacent layer proves that the networking, access path, and observability boundary is healthy.
Escalation owner: identify the team with change authority over networking, access path, and observability.
Rollback rule: reverse only the smallest change within networking, access path, and observability that explains the failure.

### Resource discovery before diagnosis

```bash
oc api-resources | grep -Ei 'notebook|workbench|hardwareprofile|accelerator|connection'
oc get crd | grep -Ei 'notebook|kubeflow|opendatahub|hardwareprofile'
oc get datasciencecluster,dscinitialization -A -o wide
oc get csv -A | grep -Ei 'rhods|opendatahub|openshift-ai'
oc get pods -n redhat-ods-operator
oc get pods -n redhat-ods-applications
```
Interpret the installed discovery output before copying any YAML from this guide.

## Create the troubleshooting project from scratch

### Lab objective

Create a safe project that reproduces `Workbench pod Pending` without modifying a production workbench.
Use a dedicated namespace, dedicated service account, explicit ResourceQuota, explicit LimitRange, small PVC, and disposable test workload.
Do not inject failures into shared operator namespaces.

### Step 1: Create the project and labels

```bash
export LAB_NS="rhoai-wb-03-workbench-pod-pending"
oc new-project "$LAB_NS"
oc label namespace "$LAB_NS"   app.kubernetes.io/part-of=rhoai-workbench-troubleshooting   troubleshooting.redhat.com/topic="workbench-pod-pending"   environment=lab --overwrite
```
Expected result: the project exists and the current user can create namespaced resources.
Failure meaning: project creation failure is a prerequisite or RBAC problem, not a workbench problem.

### Step 2: Create a bounded service account and pull-secret placeholder

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wb-lab
---
apiVersion: v1
kind: Secret
metadata:
  name: wb-lab-config
type: Opaque
stringData:
  LAB_MODE: "true"
  EXPECTED_RESULT: "baseline-running"
```
```bash
oc apply -n "$LAB_NS" -f service-account-and-secret.yaml
oc get sa,secret -n "$LAB_NS"
```

### Step 3: Create ResourceQuota and LimitRange

```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: wb-lab-quota
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "5"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: wb-lab-limits
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 250m
      memory: 512Mi
    default:
      cpu: "1"
      memory: 2Gi
```
This deliberately exposes quota and defaulting behavior for troubleshooting.

### Step 4: Create baseline storage

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wb-home
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
```
If the cluster has no default StorageClass, set `storageClassName` explicitly.
Wait for `Bound` before treating a later mount failure as a kubelet or filesystem problem.

### Step 5: Create a baseline workload that tests the same platform dependencies

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wb-baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wb-baseline
  template:
    metadata:
      labels:
        app: wb-baseline
    spec:
      serviceAccountName: wb-lab
      containers:
      - name: terminal
        image: registry.access.redhat.com/ubi9/ubi-minimal:latest
        command: ["/bin/sh","-c"]
        args:
        - |
          id
          test -w /workspace
          echo baseline > /workspace/baseline.txt
          sleep infinity
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        envFrom:
        - secretRef:
            name: wb-lab-config
        volumeMounts:
        - name: home
          mountPath: /workspace
      volumes:
      - name: home
        persistentVolumeClaim:
          claimName: wb-home
```
The baseline does not replace an OpenShift AI workbench.
It isolates core scheduling, image, secret, PVC, arbitrary-UID, and node behavior before the product controller is involved.

### Step 6: Create a real OpenShift AI project workbench

1. Open the OpenShift AI dashboard.
2. Create or select a data science project that maps to the lab namespace.
3. Create a workbench with a Red Hat-provided image first.
4. Select the smallest suitable CPU and memory profile.
5. Attach the lab PVC or create a new lab PVC.
6. Do not attach optional data connections until the baseline starts.
7. Start the workbench.
8. Record the API resources created in the namespace.
9. Save their YAML before fault injection.
10. Record the generated Pod, Service, Route or HTTPRoute, PVC, and controller status conditions.

### Step 7: Capture the golden baseline

```bash
mkdir -p evidence/golden
oc get all -n "$LAB_NS" -o yaml > evidence/golden/all.yaml
oc get events -n "$LAB_NS" --sort-by=.lastTimestamp > evidence/golden/events.txt
oc get pvc,secret,configmap,role,rolebinding,networkpolicy -n "$LAB_NS" -o yaml > evidence/golden/supporting.yaml
oc api-resources > evidence/golden/api-resources.txt
oc get node -o wide > evidence/golden/nodes.txt
```

## Baseline validation

| Check | Pass condition | Evidence |
|---|---|---|
| API | All required CRDs and namespaced APIs are discoverable. | Save command output with UTC timestamp. |
| Operator | Relevant OpenShift AI components report a ready state. | Save command output with UTC timestamp. |
| Namespace | The user and service account have required namespaced permissions. | Save command output with UTC timestamp. |
| Quota | Requested resources fit within quota and LimitRange policy. | Save command output with UTC timestamp. |
| Scheduling | At least one eligible Ready node can satisfy selectors, taints, affinity, and resource requests. | Save command output with UTC timestamp. |
| Image | The selected image reference resolves and is pullable from eligible nodes. | Save command output with UTC timestamp. |
| Storage | PVC is Bound and the CSI path is healthy. | Save command output with UTC timestamp. |
| Secret | Required secret exists in the same namespace and expected keys are present. | Save command output with UTC timestamp. |
| Pod | Containers become Running and Ready without repeated restarts. | Save command output with UTC timestamp. |
| Service | Service selectors produce ready EndpointSlices. | Save command output with UTC timestamp. |
| Access | Route or Gateway status is admitted and the authenticated user reaches the IDE. | Save command output with UTC timestamp. |
| Application | A notebook cell can read and write persistent data. | Save command output with UTC timestamp. |

### Baseline check: API

Pass condition: All required CRDs and namespaced APIs are discoverable.
Negative test: deliberately invalidate only the api dependency in the lab.
Recovery proof: restore the dependency and verify the same api evidence changes to healthy.

### Baseline check: Operator

Pass condition: Relevant OpenShift AI components report a ready state.
Negative test: deliberately invalidate only the operator dependency in the lab.
Recovery proof: restore the dependency and verify the same operator evidence changes to healthy.

### Baseline check: Namespace

Pass condition: The user and service account have required namespaced permissions.
Negative test: deliberately invalidate only the namespace dependency in the lab.
Recovery proof: restore the dependency and verify the same namespace evidence changes to healthy.

### Baseline check: Quota

Pass condition: Requested resources fit within quota and LimitRange policy.
Negative test: deliberately invalidate only the quota dependency in the lab.
Recovery proof: restore the dependency and verify the same quota evidence changes to healthy.

### Baseline check: Scheduling

Pass condition: At least one eligible Ready node can satisfy selectors, taints, affinity, and resource requests.
Negative test: deliberately invalidate only the scheduling dependency in the lab.
Recovery proof: restore the dependency and verify the same scheduling evidence changes to healthy.

### Baseline check: Image

Pass condition: The selected image reference resolves and is pullable from eligible nodes.
Negative test: deliberately invalidate only the image dependency in the lab.
Recovery proof: restore the dependency and verify the same image evidence changes to healthy.

### Baseline check: Storage

Pass condition: PVC is Bound and the CSI path is healthy.
Negative test: deliberately invalidate only the storage dependency in the lab.
Recovery proof: restore the dependency and verify the same storage evidence changes to healthy.

### Baseline check: Secret

Pass condition: Required secret exists in the same namespace and expected keys are present.
Negative test: deliberately invalidate only the secret dependency in the lab.
Recovery proof: restore the dependency and verify the same secret evidence changes to healthy.

### Baseline check: Pod

Pass condition: Containers become Running and Ready without repeated restarts.
Negative test: deliberately invalidate only the pod dependency in the lab.
Recovery proof: restore the dependency and verify the same pod evidence changes to healthy.

### Baseline check: Service

Pass condition: Service selectors produce ready EndpointSlices.
Negative test: deliberately invalidate only the service dependency in the lab.
Recovery proof: restore the dependency and verify the same service evidence changes to healthy.

### Baseline check: Access

Pass condition: Route or Gateway status is admitted and the authenticated user reaches the IDE.
Negative test: deliberately invalidate only the access dependency in the lab.
Recovery proof: restore the dependency and verify the same access evidence changes to healthy.

### Baseline check: Application

Pass condition: A notebook cell can read and write persistent data.
Negative test: deliberately invalidate only the application dependency in the lab.
Recovery proof: restore the dependency and verify the same application evidence changes to healthy.

## Controlled fault injection

Fault injection is allowed only in the lab namespace and must have a rollback command.
### Fault 1: insufficient requested CPU

Objective: reproduce how `insufficient requested CPU` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `insufficient requested CPU`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 2: insufficient requested memory

Objective: reproduce how `insufficient requested memory` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `insufficient requested memory`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 3: GPU resource unavailable

Objective: reproduce how `GPU resource unavailable` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `GPU resource unavailable`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 4: node selector matches no nodes

Objective: reproduce how `node selector matches no nodes` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `node selector matches no nodes`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 5: taint lacks a matching toleration

Objective: reproduce how `taint lacks a matching toleration` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `taint lacks a matching toleration`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 6: pod affinity or topology rule is unsatisfiable

Objective: reproduce how `pod affinity or topology rule is unsatisfiable` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `pod affinity or topology rule is unsatisfiable`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 7: ResourceQuota blocks admission

Objective: reproduce how `ResourceQuota blocks admission` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `ResourceQuota blocks admission`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 8: LimitRange default conflicts with the profile

Objective: reproduce how `LimitRange default conflicts with the profile` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `LimitRange default conflicts with the profile`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 9: PVC is unbound

Objective: reproduce how `PVC is unbound` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `PVC is unbound`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 10: volume node affinity conflict

Objective: reproduce how `volume node affinity conflict` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `volume node affinity conflict`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 11: Kueue admission is pending

Objective: reproduce how `Kueue admission is pending` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `Kueue admission is pending`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Fault 12: maximum pod density or node pressure

Objective: reproduce how `maximum pod density or node pressure` can surface as `Workbench pod Pending`.
Precondition: golden baseline is healthy and evidence is saved.
Injection principle: change one field or dependency that represents `maximum pod density or node pressure`.
Observe: dashboard status, resource condition, event reason, container state, and controller log.
Preserve: YAML before and after, event timeline, current logs, previous logs, and node name.
Rollback: revert the single injected change or reapply the golden manifest.
Validation: user-level access and technical health must both recover.

### Example safe injections

| Injection | Method | Rollback |
|---|---|---|
| Impossible node selector | Patch the lab Deployment with `nodeSelector: {troubleshooting.invalid/eligible: "true"}`. | Remove the selector. |
| Missing secret | Reference a nonexistent optional=false secret in the lab Deployment. | Restore the correct secret name. |
| Bad image tag | Patch the lab image to a nonexistent tag. | Restore the known-good digest or tag. |
| Memory failure | Run a bounded memory allocator above a small lab limit. | Restore the baseline command and resource limit. |
| Permission failure | Mount a directory that is not writable by the arbitrary UID. | Fix image or volume permissions; do not grant privileged SCC. |
| Service failure | Change a lab Service selector so no endpoints match. | Restore the selector from the golden manifest. |

## Evidence collection bundle

### Evidence rules

- Use UTC timestamps.
- Redact tokens, passwords, access keys, cookies, and private registry credentials.
- Do not redact resource names, event reasons, status conditions, image digests, node names, or timestamps unless policy requires it.
- Collect previous container logs before deleting or restarting the Pod.
- Collect namespace events before their retention window expires.
- Record the exact user action and browser-visible error.
- Record the last known good time and first known bad time.
- Record changes to images, quotas, profiles, storage classes, operators, certificates, proxy, DNS, and identity.
- Compare failing and known-good objects with `oc diff` or normalized YAML.
- Include cluster and OpenShift AI versions in every escalation.

### Collection script

```bash
#!/usr/bin/env bash
set -euo pipefail
NS="${1:?namespace required}"
POD="${2:-}"
STAMP="$(date -u +%Y%m%dT%H%M%SZ)"
OUT="rhoai-workbench-evidence-${NS}-${STAMP}"
mkdir -p "$OUT"

oc version > "$OUT/oc-version.txt" 2>&1 || true
oc get clusterversion -o yaml > "$OUT/clusterversion.yaml" 2>&1 || true
oc get datasciencecluster,dscinitialization -A -o yaml > "$OUT/rhoai-control-plane.yaml" 2>&1 || true
oc get csv -A > "$OUT/csv.txt" 2>&1 || true
oc api-resources > "$OUT/api-resources.txt" 2>&1 || true
oc get all -n "$NS" -o wide > "$OUT/all-wide.txt" 2>&1 || true
oc get all -n "$NS" -o yaml > "$OUT/all.yaml" 2>&1 || true
oc get events -n "$NS" --sort-by=.lastTimestamp > "$OUT/events.txt" 2>&1 || true
oc get pvc,secret,configmap,serviceaccount,role,rolebinding,networkpolicy -n "$NS" -o yaml > "$OUT/supporting.yaml" 2>&1 || true
oc get route,httproute,service,endpoints,endpointslice -n "$NS" -o yaml > "$OUT/network.yaml" 2>&1 || true
oc get resourcequota,limitrange -n "$NS" -o yaml > "$OUT/policy.yaml" 2>&1 || true
oc get nodes -o wide > "$OUT/nodes.txt" 2>&1 || true
oc adm top nodes > "$OUT/top-nodes.txt" 2>&1 || true
oc adm top pods -n "$NS" --containers > "$OUT/top-pods.txt" 2>&1 || true

if [[ -n "$POD" ]]; then
  oc describe pod -n "$NS" "$POD" > "$OUT/pod-describe.txt" 2>&1 || true
  oc get pod -n "$NS" "$POD" -o yaml > "$OUT/pod.yaml" 2>&1 || true
  oc logs -n "$NS" "$POD" --all-containers --timestamps --tail=-1 > "$OUT/pod-current.log" 2>&1 || true
  oc logs -n "$NS" "$POD" --all-containers --previous --timestamps --tail=-1 > "$OUT/pod-previous.log" 2>&1 || true
fi

tar -czf "${OUT}.tar.gz" "$OUT"
printf 'Created %s\n' "${OUT}.tar.gz"
```
Review the archive for secrets before attaching it to a ticket.

## Root-cause matrix

| ID | Candidate root cause | Primary evidence | Common misleading symptom | Safe first action |
|---|---|---|---|---|
| RC-01 | insufficient requested CPU | controller reconcile error at the Image, storage, and external dependencies boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-02 | insufficient requested memory | FailedMount or volume event at the Networking, access path, and observability boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-03 | GPU resource unavailable | controller reconcile error at the User, dashboard, and identity boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-04 | node selector matches no nodes | FailedMount or volume event at the OpenShift AI controllers boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-05 | taint lacks a matching toleration | controller reconcile error at the Kubernetes API, admission, and policy boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-06 | pod affinity or topology rule is unsatisfiable | FailedMount or volume event at the Scheduler, node, and accelerator boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-07 | ResourceQuota blocks admission | controller reconcile error at the Image, storage, and external dependencies boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-08 | LimitRange default conflicts with the profile | FailedMount or volume event at the Networking, access path, and observability boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-09 | PVC is unbound | controller reconcile error at the User, dashboard, and identity boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-10 | volume node affinity conflict | FailedMount or volume event at the OpenShift AI controllers boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-11 | Kueue admission is pending | controller reconcile error at the Kubernetes API, admission, and policy boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |
| RC-12 | maximum pod density or node pressure | FailedMount or volume event at the Scheduler, node, and accelerator boundary | Dashboard remains `Workbench pod Pending` | Preserve evidence and validate the owning object. |

### RC-01: insufficient requested CPU

Definition: `insufficient requested CPU` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `insufficient requested CPU`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-02: insufficient requested memory

Definition: `insufficient requested memory` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `insufficient requested memory`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-03: GPU resource unavailable

Definition: `GPU resource unavailable` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `GPU resource unavailable`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-04: node selector matches no nodes

Definition: `node selector matches no nodes` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `node selector matches no nodes`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-05: taint lacks a matching toleration

Definition: `taint lacks a matching toleration` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `taint lacks a matching toleration`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-06: pod affinity or topology rule is unsatisfiable

Definition: `pod affinity or topology rule is unsatisfiable` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `pod affinity or topology rule is unsatisfiable`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-07: ResourceQuota blocks admission

Definition: `ResourceQuota blocks admission` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `ResourceQuota blocks admission`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-08: LimitRange default conflicts with the profile

Definition: `LimitRange default conflicts with the profile` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `LimitRange default conflicts with the profile`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-09: PVC is unbound

Definition: `PVC is unbound` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `PVC is unbound`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-10: volume node affinity conflict

Definition: `volume node affinity conflict` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `volume node affinity conflict`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-11: Kueue admission is pending

Definition: `Kueue admission is pending` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `Kueue admission is pending`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

### RC-12: maximum pod density or node pressure

Definition: `maximum pod density or node pressure` prevents the expected workbench lifecycle from completing.
Why it maps to this symptom: the dashboard cannot distinguish every downstream failure and can display `Workbench pod Pending`.
Strong evidence: a repeatable event, status condition, log entry, metric, or API denial directly names `maximum pod density or node pressure`.
Weak evidence: temporal correlation without a matching object-level failure.
Disproof: the same dependency works for the same Pod on the same node with the same identity and configuration.
Containment: choose a known-good profile, image, node pool, storage class, or access path only when the substitution is allowed.
Permanent correction: repair the source configuration or dependency rather than patching a generated child object.
Rollback: restore the previously approved configuration and confirm controller reconciliation.
Prevention: add preflight policy, alerting, capacity controls, and a tested runbook.

## Layer-by-layer diagnosis

### Layer 1: User, dashboard, and identity

Scope: user-selected project, allowed groups, OAuth session, dashboard validation, ownership, and browser redirects.
#### User, dashboard, and identity check 1: insufficient requested CPU

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 2: insufficient requested memory

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 3: GPU resource unavailable

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 4: node selector matches no nodes

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 5: taint lacks a matching toleration

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 6: pod affinity or topology rule is unsatisfiable

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 7: ResourceQuota blocks admission

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 8: LimitRange default conflicts with the profile

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 9: PVC is unbound

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 10: volume node affinity conflict

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 11: Kueue admission is pending

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### User, dashboard, and identity check 12: maximum pod density or node pressure

Question: can the user, dashboard, and identity layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same user, dashboard, and identity conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

### Layer 2: OpenShift AI controllers

Scope: reconciliation of workbench, image, connection, culling, generated workload, and displayed status.
#### OpenShift AI controllers check 1: insufficient requested CPU

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 2: insufficient requested memory

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 3: GPU resource unavailable

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 4: node selector matches no nodes

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 5: taint lacks a matching toleration

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 6: pod affinity or topology rule is unsatisfiable

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 7: ResourceQuota blocks admission

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 8: LimitRange default conflicts with the profile

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 9: PVC is unbound

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 10: volume node affinity conflict

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 11: Kueue admission is pending

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### OpenShift AI controllers check 12: maximum pod density or node pressure

Question: can the openshift ai controllers layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same openshift ai controllers conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

### Layer 3: Kubernetes API, admission, and policy

Scope: RBAC, ResourceQuota, LimitRange, SCC, policy engines, webhooks, and object validation.
#### Kubernetes API, admission, and policy check 1: insufficient requested CPU

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 2: insufficient requested memory

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 3: GPU resource unavailable

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 4: node selector matches no nodes

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 5: taint lacks a matching toleration

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 6: pod affinity or topology rule is unsatisfiable

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 7: ResourceQuota blocks admission

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 8: LimitRange default conflicts with the profile

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 9: PVC is unbound

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 10: volume node affinity conflict

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 11: Kueue admission is pending

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### Kubernetes API, admission, and policy check 12: maximum pod density or node pressure

Question: can the kubernetes api, admission, and policy layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same kubernetes api, admission, and policy conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

### Layer 4: Scheduler, node, and accelerator

Scope: requests, selectors, affinity, taints, Kueue, node pressure, kubelet, device plugins, GPU drivers, and allocatable resources.
#### Scheduler, node, and accelerator check 1: insufficient requested CPU

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 2: insufficient requested memory

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 3: GPU resource unavailable

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 4: node selector matches no nodes

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 5: taint lacks a matching toleration

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 6: pod affinity or topology rule is unsatisfiable

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 7: ResourceQuota blocks admission

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 8: LimitRange default conflicts with the profile

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 9: PVC is unbound

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 10: volume node affinity conflict

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 11: Kueue admission is pending

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### Scheduler, node, and accelerator check 12: maximum pod density or node pressure

Question: can the scheduler, node, and accelerator layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same scheduler, node, and accelerator conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

### Layer 5: Image, storage, and external dependencies

Scope: registry, pull secret, proxy, certificates, package mirrors, PVC, CSI, filesystem, object storage, and identity dependencies.
#### Image, storage, and external dependencies check 1: insufficient requested CPU

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 2: insufficient requested memory

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 3: GPU resource unavailable

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 4: node selector matches no nodes

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 5: taint lacks a matching toleration

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 6: pod affinity or topology rule is unsatisfiable

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 7: ResourceQuota blocks admission

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 8: LimitRange default conflicts with the profile

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 9: PVC is unbound

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 10: volume node affinity conflict

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 11: Kueue admission is pending

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### Image, storage, and external dependencies check 12: maximum pod density or node pressure

Question: can the image, storage, and external dependencies layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same image, storage, and external dependencies conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

### Layer 6: Networking, access path, and observability

Scope: Service, EndpointSlice, Route or Gateway API, DNS, TLS, OAuth proxy, NetworkPolicy, events, logs, metrics, and audit records.
#### Networking, access path, and observability check 1: insufficient requested CPU

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `insufficient requested CPU`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-01 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 2: insufficient requested memory

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `insufficient requested memory`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-02 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 3: GPU resource unavailable

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `GPU resource unavailable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-03 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 4: node selector matches no nodes

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `node selector matches no nodes`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-04 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 5: taint lacks a matching toleration

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `taint lacks a matching toleration`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-05 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 6: pod affinity or topology rule is unsatisfiable

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `pod affinity or topology rule is unsatisfiable`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-06 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 7: ResourceQuota blocks admission

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `ResourceQuota blocks admission`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-07 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 8: LimitRange default conflicts with the profile

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `LimitRange default conflicts with the profile`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-08 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 9: PVC is unbound

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `PVC is unbound`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-09 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 10: volume node affinity conflict

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `volume node affinity conflict`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-10 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 11: Kueue admission is pending

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `Kueue admission is pending`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-11 confirmed, rejected, or unproven; never leave the result implicit.

#### Networking, access path, and observability check 12: maximum pod density or node pressure

Question: can the networking, access path, and observability layer produce evidence that confirms or rejects `maximum pod density or node pressure`?
Collect: relevant object YAML, status conditions, events, logs, metrics, and audit records for the time window.
Compare: failing workbench versus a known-good workbench under the same networking, access path, and observability conditions.
Decision: mark RC-12 confirmed, rejected, or unproven; never leave the result implicit.

## Detailed remediation runbooks

### Runbook 1: Correct insufficient requested CPU

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `insufficient requested CPU`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `insufficient requested CPU`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `insufficient requested CPU` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 2: Correct insufficient requested memory

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `insufficient requested memory`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `insufficient requested memory`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `insufficient requested memory` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 3: Correct GPU resource unavailable

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `GPU resource unavailable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `GPU resource unavailable`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `GPU resource unavailable` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 4: Correct node selector matches no nodes

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `node selector matches no nodes`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `node selector matches no nodes`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `node selector matches no nodes` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 5: Correct taint lacks a matching toleration

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `taint lacks a matching toleration`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `taint lacks a matching toleration`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `taint lacks a matching toleration` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 6: Correct pod affinity or topology rule is unsatisfiable

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `pod affinity or topology rule is unsatisfiable`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `pod affinity or topology rule is unsatisfiable`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `pod affinity or topology rule is unsatisfiable` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 7: Correct ResourceQuota blocks admission

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `ResourceQuota blocks admission`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `ResourceQuota blocks admission`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `ResourceQuota blocks admission` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 8: Correct LimitRange default conflicts with the profile

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `LimitRange default conflicts with the profile`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `LimitRange default conflicts with the profile`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `LimitRange default conflicts with the profile` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 9: Correct PVC is unbound

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `PVC is unbound`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `PVC is unbound`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `PVC is unbound` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 10: Correct volume node affinity conflict

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `volume node affinity conflict`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `volume node affinity conflict`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `volume node affinity conflict` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 11: Correct Kueue admission is pending

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `Kueue admission is pending`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `Kueue admission is pending`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `Kueue admission is pending` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

### Runbook 12: Correct maximum pod density or node pressure

#### Preconditions

- Evidence archive exists.
- The failing resource and its controller owner are identified.
- The last known good configuration is available.
- Change authority and user impact are understood.
- A rollback path is tested or documented.

#### Diagnosis sequence

1. Run `oc get events -n "$NS" --sort-by=.lastTimestamp`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
2. Run `oc get pod -n "$NS" -o wide`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
3. Run `oc describe pod -n "$NS" "$POD"`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
4. Run `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
5. Run `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
6. Run `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
7. Run `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.
8. Run `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
   - Purpose: obtain evidence relevant to `maximum pod density or node pressure`.
   - Positive finding: output directly identifies the dependency or policy blocking progress.
   - Negative finding: continue without altering unrelated components.

#### Remediation principles

- Correct the declarative source responsible for `maximum pod density or node pressure`.
- Prefer a supported dashboard, CR, Operator, MachineConfig, policy, storage, or registry configuration.
- Do not grant cluster-admin, privileged SCC, hostPath, or broad secret access as a shortcut.
- If a generated Pod is deleted, confirm that the owner recreates it and that the original source problem is fixed.
- For custom images, rebuild and publish an immutable digest; do not repair a running container manually.

#### Validation

- The event or condition naming `maximum pod density or node pressure` no longer recurs.
- The workbench reaches Running and Ready.
- Service endpoints are populated.
- The intended user opens the IDE.
- A notebook kernel starts.
- Persistent data survives a controlled restart.
- Monitoring remains clean for the agreed soak period.

#### Rollback

- Reapply the last approved manifest or restore the prior image digest/profile/configuration.
- Confirm the controller converges to the old state.
- Re-run the same validation checks.
- Preserve evidence of both failed remediation and successful rollback.

## Decision tree workbook

### Decision D-001

Context: production multi-tenant cluster.
Observed signal: controller reconcile error.
Most relevant hypothesis: pod affinity or topology rule is unsatisfiable.
Owning boundary to inspect first: Scheduler, node, and accelerator.
First proof command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-002

Context: production multi-tenant cluster.
Observed signal: admission or policy denial.
Most relevant hypothesis: ResourceQuota blocks admission.
Owning boundary to inspect first: Image, storage, and external dependencies.
First proof command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-003

Context: production multi-tenant cluster.
Observed signal: FailedScheduling event.
Most relevant hypothesis: LimitRange default conflicts with the profile.
Owning boundary to inspect first: Networking, access path, and observability.
First proof command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-004

Context: production multi-tenant cluster.
Observed signal: FailedMount or volume event.
Most relevant hypothesis: PVC is unbound.
Owning boundary to inspect first: User, dashboard, and identity.
First proof command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-005

Context: production multi-tenant cluster.
Observed signal: container waiting or restart reason.
Most relevant hypothesis: volume node affinity conflict.
Owning boundary to inspect first: OpenShift AI controllers.
First proof command: `oc get events -n "$NS" --sort-by=.lastTimestamp`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-006

Context: production multi-tenant cluster.
Observed signal: access-path HTTP or TLS failure.
Most relevant hypothesis: Kueue admission is pending.
Owning boundary to inspect first: Kubernetes API, admission, and policy.
First proof command: `oc get pod -n "$NS" -o wide`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-007

Context: disconnected or restricted-network cluster.
Observed signal: controller reconcile error.
Most relevant hypothesis: ResourceQuota blocks admission.
Owning boundary to inspect first: Networking, access path, and observability.
First proof command: `oc describe pod -n "$NS" "$POD"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-008

Context: disconnected or restricted-network cluster.
Observed signal: admission or policy denial.
Most relevant hypothesis: LimitRange default conflicts with the profile.
Owning boundary to inspect first: User, dashboard, and identity.
First proof command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-009

Context: disconnected or restricted-network cluster.
Observed signal: FailedScheduling event.
Most relevant hypothesis: PVC is unbound.
Owning boundary to inspect first: OpenShift AI controllers.
First proof command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-010

Context: disconnected or restricted-network cluster.
Observed signal: FailedMount or volume event.
Most relevant hypothesis: volume node affinity conflict.
Owning boundary to inspect first: Kubernetes API, admission, and policy.
First proof command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-011

Context: disconnected or restricted-network cluster.
Observed signal: container waiting or restart reason.
Most relevant hypothesis: Kueue admission is pending.
Owning boundary to inspect first: Scheduler, node, and accelerator.
First proof command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

### Decision D-012

Context: disconnected or restricted-network cluster.
Observed signal: access-path HTTP or TLS failure.
Most relevant hypothesis: maximum pod density or node pressure.
Owning boundary to inspect first: Image, storage, and external dependencies.
First proof command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`.
If confirmed: contain impact, repair the declarative source, reconcile, and validate.
If rejected: move to the next boundary; do not repeat the same test without new evidence.
Escalate when: the owning component is degraded, the repair requires unsupported changes, or multiple tenants are affected.

## Command interpretation drills

### Drill C-001

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-002

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-003

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-004

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-005

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-006

Command: `oc get events -n "$NS" --sort-by=.lastTimestamp`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-007

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-008

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-009

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-010

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-011

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-012

Command: `oc get pod -n "$NS" -o wide`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-013

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-014

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-015

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-016

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-017

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-018

Command: `oc describe pod -n "$NS" "$POD"`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-019

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-020

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-021

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-022

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-023

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-024

Command: `oc logs -n "$NS" "$POD" --all-containers --tail=300`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-025

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-026

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-027

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-028

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-029

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-030

Command: `oc logs -n "$NS" "$POD" --all-containers --previous --tail=300`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-031

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-032

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-033

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-034

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-035

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-036

Command: `oc get sts,deploy,svc,route,httproute,endpoints,endpointslice -n "$NS"`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-037

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-038

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-039

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-040

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-041

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-042

Command: `oc get pvc,pv -n "$NS" && oc describe pvc -n "$NS" "$PVC"`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-043

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `insufficient requested CPU`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-044

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `insufficient requested memory`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-045

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `GPU resource unavailable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-046

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `node selector matches no nodes`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-047

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `taint lacks a matching toleration`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

### Drill C-048

Command: `oc get resourcequota,limitrange,networkpolicy -n "$NS" -o yaml && oc auth can-i --list -n "$NS"`
Hypothesis under test: `pod affinity or topology rule is unsatisfiable`.
Healthy interpretation: output contains no condition that blocks `Workbench pod Pending`.
Unhealthy interpretation: output contains a direct failure reason, absent dependency, denial, or non-progressing condition.
Required note: copy the exact reason, object, namespace, node, and timestamp into the incident timeline.
Next action: test the nearest owning layer rather than applying a generic restart.

## Production case studies

### Case study P-001: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: container waiting or restart reason.
Primary hypothesis: insufficient requested CPU.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `insufficient requested CPU`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `insufficient requested CPU` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-002: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: access-path HTTP or TLS failure.
Primary hypothesis: insufficient requested memory.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `insufficient requested memory`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `insufficient requested memory` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-003: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: controller reconcile error.
Primary hypothesis: GPU resource unavailable.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `GPU resource unavailable`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `GPU resource unavailable` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-004: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: admission or policy denial.
Primary hypothesis: node selector matches no nodes.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `node selector matches no nodes`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `node selector matches no nodes` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-005: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: FailedScheduling event.
Primary hypothesis: taint lacks a matching toleration.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `taint lacks a matching toleration`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `taint lacks a matching toleration` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-006: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: FailedMount or volume event.
Primary hypothesis: pod affinity or topology rule is unsatisfiable.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `pod affinity or topology rule is unsatisfiable`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `pod affinity or topology rule is unsatisfiable` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-007: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: container waiting or restart reason.
Primary hypothesis: ResourceQuota blocks admission.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `ResourceQuota blocks admission`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `ResourceQuota blocks admission` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-008: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: access-path HTTP or TLS failure.
Primary hypothesis: LimitRange default conflicts with the profile.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `LimitRange default conflicts with the profile`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `LimitRange default conflicts with the profile` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-009: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: controller reconcile error.
Primary hypothesis: PVC is unbound.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `PVC is unbound`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `PVC is unbound` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-010: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: admission or policy denial.
Primary hypothesis: volume node affinity conflict.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `volume node affinity conflict`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `volume node affinity conflict` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-011: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: FailedScheduling event.
Primary hypothesis: Kueue admission is pending.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `Kueue admission is pending`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `Kueue admission is pending` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-012: production multi-tenant cluster

Initial report: `Workbench pod Pending` affects a workbench in a production multi-tenant cluster.
First visible signal: FailedMount or volume event.
Primary hypothesis: maximum pod density or node pressure.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `maximum pod density or node pressure`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `maximum pod density or node pressure` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-013: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: container waiting or restart reason.
Primary hypothesis: insufficient requested CPU.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `insufficient requested CPU`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `insufficient requested CPU` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-014: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: access-path HTTP or TLS failure.
Primary hypothesis: insufficient requested memory.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `insufficient requested memory`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `insufficient requested memory` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-015: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: controller reconcile error.
Primary hypothesis: GPU resource unavailable.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `GPU resource unavailable`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `GPU resource unavailable` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-016: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: admission or policy denial.
Primary hypothesis: node selector matches no nodes.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `node selector matches no nodes`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `node selector matches no nodes` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-017: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: FailedScheduling event.
Primary hypothesis: taint lacks a matching toleration.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `taint lacks a matching toleration`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `taint lacks a matching toleration` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-018: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: FailedMount or volume event.
Primary hypothesis: pod affinity or topology rule is unsatisfiable.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `pod affinity or topology rule is unsatisfiable`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `pod affinity or topology rule is unsatisfiable` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-019: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: container waiting or restart reason.
Primary hypothesis: ResourceQuota blocks admission.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `ResourceQuota blocks admission`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `ResourceQuota blocks admission` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-020: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: access-path HTTP or TLS failure.
Primary hypothesis: LimitRange default conflicts with the profile.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `LimitRange default conflicts with the profile`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `LimitRange default conflicts with the profile` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-021: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: controller reconcile error.
Primary hypothesis: PVC is unbound.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `PVC is unbound`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `PVC is unbound` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-022: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: admission or policy denial.
Primary hypothesis: volume node affinity conflict.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `volume node affinity conflict`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `volume node affinity conflict` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-023: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: FailedScheduling event.
Primary hypothesis: Kueue admission is pending.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `Kueue admission is pending`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `Kueue admission is pending` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

### Case study P-024: disconnected or restricted-network cluster

Initial report: `Workbench pod Pending` affects a workbench in a disconnected or restricted-network cluster.
First visible signal: FailedMount or volume event.
Primary hypothesis: maximum pod density or node pressure.
Mistake avoided: deleting every Pod before collecting previous logs and events.
Evidence path: dashboard → controller → admitted workload → scheduler/kubelet → dependency responsible for `maximum pod density or node pressure`.
Containment: move only the affected workload to an approved known-good configuration when policy allows.
Permanent fix: correct the source of `maximum pod density or node pressure` and allow the controller to regenerate children.
Validation: user access, kernel start, persistent write, restart survival, and alert clearance.
Problem-management action: add a synthetic check or policy that detects the same precondition earlier.
Lesson: dashboard state is an entry point; object-level evidence determines the root cause.

## Automation toolkit

### Namespace health snapshot

```bash
#!/usr/bin/env bash
set -euo pipefail
NS="${1:?namespace required}"
printf '\n== Workloads ==\n'
oc get pod,sts,deploy,job -n "$NS" -o wide
printf '\n== Storage ==\n'
oc get pvc -n "$NS"
printf '\n== Access path ==\n'
oc get svc,endpoints,endpointslice,route,httproute -n "$NS"
printf '\n== Policy ==\n'
oc get resourcequota,limitrange,networkpolicy -n "$NS"
printf '\n== Warning events ==\n'
oc get events -n "$NS" --field-selector type=Warning --sort-by=.lastTimestamp
```
### Failed container detector

```bash
oc get pod -A -o json | jq -r '
.items[] as $p |
($p.status.containerStatuses // [])[]? |
select(.state.waiting.reason != null or .lastState.terminated.reason != null) |
[
  $p.metadata.namespace,
  $p.metadata.name,
  .name,
  (.state.waiting.reason // .lastState.terminated.reason),
  (.restartCount|tostring)
] | @tsv'
```
### Pending reason report

```bash
oc get pod -A --field-selector=status.phase=Pending -o json | jq -r '
.items[] |
[
 .metadata.namespace,
 .metadata.name,
 ([.status.conditions[]? | select(.type=="PodScheduled") | .reason] | join(",")),
 ([.status.conditions[]? | select(.type=="PodScheduled") | .message] | join(" "))
] | @tsv'
```
### Workbench preflight pseudocode

```python
def preflight(request, cluster):
    assert request.namespace_exists
    assert cluster.rhoai_components_ready
    assert request.user_can_create_workbench
    assert request.profile.resources_fit_quota
    assert cluster.has_eligible_node(request.profile)
    assert cluster.can_pull(request.image_digest)
    assert request.pvc_is_bound_or_provisionable
    assert request.secret_references_exist
    assert cluster.ingress_path_available
    return "approved"
```

## Observability and alerting

### Metric: `kube_pod_status_phase`

Purpose: determine whether `kube_pod_status_phase` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_pod_container_status_waiting_reason`

Purpose: determine whether `kube_pod_container_status_waiting_reason` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_pod_container_status_restarts_total`

Purpose: determine whether `kube_pod_container_status_restarts_total` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_pod_container_status_last_terminated_reason`

Purpose: determine whether `kube_pod_container_status_last_terminated_reason` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_persistentvolumeclaim_status_phase`

Purpose: determine whether `kube_persistentvolumeclaim_status_phase` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_pod_status_unschedulable`

Purpose: determine whether `kube_pod_status_unschedulable` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `container_memory_working_set_bytes`

Purpose: determine whether `container_memory_working_set_bytes` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `container_spec_memory_limit_bytes`

Purpose: determine whether `container_spec_memory_limit_bytes` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `container_cpu_cfs_throttled_seconds_total`

Purpose: determine whether `container_cpu_cfs_throttled_seconds_total` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `container_cpu_usage_seconds_total`

Purpose: determine whether `container_cpu_usage_seconds_total` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_node_status_condition`

Purpose: determine whether `kube_node_status_condition` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_endpoint_address_available`

Purpose: determine whether `kube_endpoint_address_available` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_resourcequota`

Purpose: determine whether `kube_resourcequota` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Metric: `kube_pod_container_resource_requests`

Purpose: determine whether `kube_pod_container_resource_requests` provides an early or confirming signal for `Workbench pod Pending`.
Label caution: verify actual labels and scrape availability in your OpenShift monitoring configuration.
Cardinality rule: aggregate by namespace, workload, reason, and cluster; avoid unbounded user-provided labels.
Runbook link: point alerts to the exact evidence and remediation section of this file.

### Example PrometheusRule skeleton

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: rhoai-workbench-03
spec:
  groups:
  - name: rhoai-workbench-workbench-pod-pending
    rules:
    - alert: RHOAIWorkbenchWorkbenchpodPending
      expr: |
        max_over_time(
          kube_pod_container_status_waiting_reason{reason=~"CrashLoopBackOff|ImagePullBackOff"}[10m]
        ) > 0
      for: 10m
      labels:
        severity: warning
      annotations:
        summary: "OpenShift AI workbench requires investigation"
        description: "Use the namespace, pod, reason, events, previous logs, and controller status before restart."
```
Tune selectors so the rule matches only workbench workloads in your environment.

## Capacity and performance

### Capacity dimension: CPU requests

Measure CPU requests for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for CPU requests.
Include CPU requests in profile design and capacity forecasting.
Do not infer CPU requests from dashboard status alone.

### Capacity dimension: CPU limits

Measure CPU limits for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for CPU limits.
Include CPU limits in profile design and capacity forecasting.
Do not infer CPU limits from dashboard status alone.

### Capacity dimension: memory requests

Measure memory requests for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for memory requests.
Include memory requests in profile design and capacity forecasting.
Do not infer memory requests from dashboard status alone.

### Capacity dimension: memory limits

Measure memory limits for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for memory limits.
Include memory limits in profile design and capacity forecasting.
Do not infer memory limits from dashboard status alone.

### Capacity dimension: ephemeral storage

Measure ephemeral storage for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for ephemeral storage.
Include ephemeral storage in profile design and capacity forecasting.
Do not infer ephemeral storage from dashboard status alone.

### Capacity dimension: PVC capacity

Measure PVC capacity for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for PVC capacity.
Include PVC capacity in profile design and capacity forecasting.
Do not infer PVC capacity from dashboard status alone.

### Capacity dimension: PVC IOPS

Measure PVC IOPS for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for PVC IOPS.
Include PVC IOPS in profile design and capacity forecasting.
Do not infer PVC IOPS from dashboard status alone.

### Capacity dimension: registry bandwidth

Measure registry bandwidth for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for registry bandwidth.
Include registry bandwidth in profile design and capacity forecasting.
Do not infer registry bandwidth from dashboard status alone.

### Capacity dimension: image size

Measure image size for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for image size.
Include image size in profile design and capacity forecasting.
Do not infer image size from dashboard status alone.

### Capacity dimension: node image filesystem

Measure node image filesystem for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for node image filesystem.
Include node image filesystem in profile design and capacity forecasting.
Do not infer node image filesystem from dashboard status alone.

### Capacity dimension: GPU allocatable

Measure GPU allocatable for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for GPU allocatable.
Include GPU allocatable in profile design and capacity forecasting.
Do not infer GPU allocatable from dashboard status alone.

### Capacity dimension: GPU memory

Measure GPU memory for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for GPU memory.
Include GPU memory in profile design and capacity forecasting.
Do not infer GPU memory from dashboard status alone.

### Capacity dimension: pod density

Measure pod density for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for pod density.
Include pod density in profile design and capacity forecasting.
Do not infer pod density from dashboard status alone.

### Capacity dimension: ingress capacity

Measure ingress capacity for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for ingress capacity.
Include ingress capacity in profile design and capacity forecasting.
Do not infer ingress capacity from dashboard status alone.

### Capacity dimension: DNS latency

Measure DNS latency for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for DNS latency.
Include DNS latency in profile design and capacity forecasting.
Do not infer DNS latency from dashboard status alone.

### Capacity dimension: object storage latency

Measure object storage latency for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for object storage latency.
Include object storage latency in profile design and capacity forecasting.
Do not infer object storage latency from dashboard status alone.

### Capacity dimension: API server admission latency

Measure API server admission latency for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for API server admission latency.
Include API server admission latency in profile design and capacity forecasting.
Do not infer API server admission latency from dashboard status alone.

### Capacity dimension: controller reconcile latency

Measure controller reconcile latency for both failing and known-good workbenches.
Define warning, saturation, and rejection thresholds for controller reconcile latency.
Include controller reconcile latency in profile design and capacity forecasting.
Do not infer controller reconcile latency from dashboard status alone.

### CPU throttling ratio example

```promql
sum by (namespace,pod,container) (rate(container_cpu_cfs_throttled_periods_total[5m]))
/
sum by (namespace,pod,container) (rate(container_cpu_cfs_periods_total[5m]))
```
### Memory headroom example

```promql
1 -
(
  container_memory_working_set_bytes
  /
  clamp_min(container_spec_memory_limit_bytes, 1)
)
```

## Security and compliance

### Control: Least privilege for users and service accounts

Requirement: demonstrate how `Least privilege for users and service accounts` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: No broad secret read permission

Requirement: demonstrate how `No broad secret read permission` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: No privileged SCC workaround

Requirement: demonstrate how `No privileged SCC workaround` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: No hostPath workaround

Requirement: demonstrate how `No hostPath workaround` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Arbitrary non-root UID compatibility

Requirement: demonstrate how `Arbitrary non-root UID compatibility` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Read-only secret volume expectations

Requirement: demonstrate how `Read-only secret volume expectations` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: TLS validation for registries and object stores

Requirement: demonstrate how `TLS validation for registries and object stores` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Proxy and noProxy change approval

Requirement: demonstrate how `Proxy and noProxy change approval` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Image digest pinning and provenance

Requirement: demonstrate how `Image digest pinning and provenance` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Vulnerability and malware scanning

Requirement: demonstrate how `Vulnerability and malware scanning` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Audit trail for workbench access and changes

Requirement: demonstrate how `Audit trail for workbench access and changes` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Separation of admin and data scientist duties

Requirement: demonstrate how `Separation of admin and data scientist duties` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: PII and model/data classification

Requirement: demonstrate how `PII and model/data classification` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Evidence redaction before support upload

Requirement: demonstrate how `Evidence redaction before support upload` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

### Control: Credential rotation after suspected exposure

Requirement: demonstrate how `Credential rotation after suspected exposure` applies during diagnosis of `Workbench pod Pending`.
Evidence: approved policy, object YAML, audit record, image attestation, or access review.
Anti-pattern: weaken the control to make the workbench start.
Correct approach: repair ownership, permissions, image construction, or dependency configuration within policy.

## Incident response and escalation

### Incident timeline template

- Detection time: `<UTC timestamp>`
- First user impact: `<UTC timestamp>`
- Last known good: `<UTC timestamp>`
- Change window: `<UTC timestamp>`
- Triage start: `<UTC timestamp>`
- Root cause confirmed: `<UTC timestamp>`
- Containment: `<UTC timestamp>`
- Permanent fix: `<UTC timestamp>`
- Validation complete: `<UTC timestamp>`
- Incident closed: `<UTC timestamp>`

### Escalation package

- Business impact and affected scope
- OpenShift and OpenShift AI versions
- Namespace and workbench identifier
- Exact browser-visible message
- Start of impact and last known good time
- Recent change list
- Resource YAML and status conditions
- Namespace events
- Current and previous logs for all containers
- Relevant controller/operator logs
- Node and storage details
- Image reference and immutable digest
- Route/Gateway, Service, and EndpointSlice status
- Quota, LimitRange, SCC, and RBAC evidence
- Reproduction steps in the lab
- Actions already attempted and their results

### Support boundary

Escalate product controller or supported image defects with complete evidence.
Escalate storage, registry, network, identity, and accelerator defects to the owning platform team in parallel.
For custom image content, provide the image Dockerfile, entrypoint, UID/permission design, package lock file, and reproducible build logs to the internal image owner.

## Change, rollback, and validation

1. State the confirmed root cause and evidence.
2. Define the smallest declarative change.
3. Identify affected users and maintenance need.
4. Record the last approved configuration.
5. Define technical rollback commands.
6. Define user-level validation.
7. Apply in a lab or canary project.
8. Observe reconciliation and events.
9. Validate access, kernel, storage, and restart behavior.
10. Roll out to remaining scope.
11. Monitor the soak window.
12. Close with evidence and update the runbook.

### Validation matrix

| Domain | Pass condition | Proof |
|---|---|---|
| Controller | Desired resource reconciles without error. | Status conditions and controller logs. |
| Pod | Running, Ready, stable restart count. | Pod status and events. |
| Storage | Read/write and persistence across restart. | File checksum before and after. |
| Secret | Expected keys are available without exposing values. | Key names and application behavior. |
| Network | Service has endpoints and URL returns expected authenticated response. | EndpointSlice, route status, curl headers. |
| IDE | JupyterLab or selected IDE loads. | User test. |
| Kernel | Kernel starts and executes a basic cell. | Notebook output and kernel logs. |
| Resource | No OOM, excessive throttling, eviction, or GPU allocation error. | Metrics and container state. |
| Cleanup | Stop/delete behavior matches retention policy. | Remaining object inventory. |

## Preventive engineering

- Maintain one known-good Red Hat-provided workbench canary per hardware and storage class.
- Pin approved custom images by digest and retain rollback digests.
- Test arbitrary UID, writable paths, base URL, probes, certificates, and package lock files in CI.
- Run a registry pull test from every eligible node pool.
- Continuously verify PVC provisioning and mount latency.
- Validate hardware profiles against actual node labels, taints, allocatable resources, and quota.
- Alert on unschedulable workbench Pods and prolonged image pulls.
- Alert on repeated container restarts and OOMKilled terminations.
- Monitor CPU throttling and memory headroom by profile.
- Validate Service endpoints and authenticated workbench URL synthetically.
- Review user group, project RoleBinding, and secret access regularly.
- Document idle timeout and cluster session timeout precedence.
- Automate orphan-resource inventory and cleanup approval.
- Test disconnected mirrors, proxy, noProxy, and custom CA changes before rollout.
- Keep an evidence collection script in the operations repository.
- Run quarterly game days using the fault injections in this guide.

## Senior interview and review questions

### Question Q-001

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-002

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-003

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-004

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-005

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-006

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-007

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-008

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-009

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-010

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `insufficient requested CPU` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-011

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-012

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-013

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-014

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-015

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-016

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-017

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-018

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-019

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-020

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `insufficient requested memory` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-021

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-022

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-023

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-024

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-025

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-026

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-027

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-028

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-029

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-030

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `GPU resource unavailable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-031

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-032

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-033

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-034

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-035

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-036

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-037

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-038

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-039

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-040

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `node selector matches no nodes` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-041

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-042

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-043

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-044

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-045

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-046

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-047

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-048

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-049

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-050

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `taint lacks a matching toleration` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-051

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-052

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-053

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-054

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-055

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-056

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-057

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-058

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-059

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-060

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `pod affinity or topology rule is unsatisfiable` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-061

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-062

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-063

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-064

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-065

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-066

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-067

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-068

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-069

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-070

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `ResourceQuota blocks admission` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-071

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-072

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-073

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-074

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-075

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-076

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-077

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-078

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-079

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-080

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `LimitRange default conflicts with the profile` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-081

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-082

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-083

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-084

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-085

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-086

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-087

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-088

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-089

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-090

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `PVC is unbound` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-091

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-092

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-093

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-094

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-095

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-096

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-097

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-098

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-099

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-100

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `volume node affinity conflict` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-101

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-102

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-103

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-104

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-105

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-106

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-107

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-108

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-109

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-110

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `Kueue admission is pending` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-111

How do you prove the dashboard is not the failing layer?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-112

Which evidence must be collected before deleting a Pod?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-113

How do you distinguish admission failure from scheduling failure?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-114

How do you distinguish PVC binding failure from node mount failure?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-115

Why is patching a generated Pod usually not a permanent fix?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-116

How do ResourceQuota and LimitRange interact with a selected hardware profile?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-117

How do you validate arbitrary UID compatibility in a custom image?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-118

How do you compare a failing workbench with a known-good workbench?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-119

When is a Pod restart useful, and when does it destroy evidence?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

### Question Q-120

How do you prove a Route problem is actually a Service endpoint problem?
Apply the answer specifically to the root-cause candidate `maximum pod density or node pressure` and the symptom `Workbench pod Pending`.
A strong answer names the object, command, evidence, decision, safe change, rollback, and validation.

## References

Use the documentation version that matches the installed product and supported lifecycle.
- Red Hat OpenShift AI Self-Managed 3.4 — Managing resources: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/managing_resources/index
- Red Hat OpenShift AI Self-Managed 3.4 — Managing OpenShift AI: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html-single/managing_openshift_ai/index
- Red Hat OpenShift AI Self-Managed 3.5 — Managing OpenShift AI: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_openshift_ai/index
- Red Hat OpenShift AI Self-Managed 3.5 — Managing resources: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_resources/index
- OpenShift Container Platform — Working with pods: https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/nodes/working-with-pods
- Kubernetes — Images and ImagePullBackOff: https://kubernetes.io/docs/concepts/containers/images/
- Kubernetes — Debug pods: https://kubernetes.io/docs/tasks/debug/debug-application/debug-pods/
- Kubernetes — Resource management for pods and containers: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
- Kubernetes — Persistent volumes: https://kubernetes.io/docs/concepts/storage/persistent-volumes/

## End-of-guide competency checklist

- [ ] I can create a safe workbench troubleshooting lab.
- [ ] I can discover the installed workbench APIs rather than assume them.
- [ ] I can preserve evidence before restarting.
- [ ] I can follow ownership from dashboard to controller to Pod and dependencies.
- [ ] I can interpret events and status conditions.
- [ ] I can diagnose scheduling, storage, image, secret, access, route, GPU, memory, CPU, and permission failures.
- [ ] I can repair the declarative source rather than a generated child.
- [ ] I can validate at both platform and user levels.
- [ ] I can execute and prove rollback.
- [ ] I can produce an escalation-quality evidence package.
- [ ] I can add alerts, canaries, policies, and game days to prevent recurrence.

