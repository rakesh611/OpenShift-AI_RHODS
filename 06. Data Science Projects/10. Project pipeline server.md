# 10. Project pipeline server

> **Level:** Red Hat OpenShift AI Corporate/L3 — intended for engineers with approximately 10+ years of Linux, Kubernetes, OpenShift, DevOps, SRE, or platform experience.
>
> **Release baseline:** Written against the current Red Hat OpenShift AI 3.5 documentation family and OpenShift Container Platform concepts. Always confirm installed CRD versions with `oc api-resources` and `oc explain`.
>
> **Safety rule:** Never apply production examples unchanged. Replace placeholders, review permissions, protect secrets, and run the validation and rollback steps in a non-production project first.

## 1. Executive objective

Deploy and operate an AI pipeline server attached to a project with dedicated artifact storage, database configuration, credentials, RBAC, observability, backup, and lifecycle management.

**Corporate design position:** Create the project pipeline server before creating Elyra-enabled workbenches when automatic runtime configuration is required. Use a separate artifact bucket from general project data where practical.

### 1.1 Learning outcomes
1. Explain and implement **artifact bucket** in a controlled OpenShift AI project.
2. Explain and implement **database** in a controlled OpenShift AI project.
3. Explain and implement **connection secret** in a controlled OpenShift AI project.
4. Explain and implement **server creation** in a controlled OpenShift AI project.
5. Explain and implement **SDK access** in a controlled OpenShift AI project.
6. Explain and implement **pipeline versions** in a controlled OpenShift AI project.
7. Explain and implement **scheduled runs** in a controlled OpenShift AI project.
8. Explain and implement **resource sizing** in a controlled OpenShift AI project.
9. Explain and implement **backup** in a controlled OpenShift AI project.
10. Explain and implement **troubleshooting** in a controlled OpenShift AI project.
11. Convert the design into Git-managed manifests and an auditable operating procedure.
12. Diagnose authorization, scheduling, storage, networking, operator, and lifecycle failures using evidence rather than guesswork.

### 1.2 Core resources in scope
- `DataSciencePipelinesApplication`
- `Pipeline server pods`
- `MariaDB or external DB`
- `S3 bucket`
- `Secret`
- `Service`
- `Route`
- `ServiceAccount`
- `Job`

### 1.3 Assumptions
- Red Hat OpenShift AI Self-Managed is installed and its dashboard is reachable.
- The reader has OpenShift CLI access and can authenticate with `oc login`.
- Administrative operations are performed through controlled elevated access, not a permanent personal cluster-admin binding.
- Names, storage classes, identity-provider groups, ingress domains, and accelerator resource names must be adapted to the target cluster.
- Examples are validated in a non-production project before promotion through GitOps or change control.

## 2. Conceptual architecture

```text
Identity Provider / OpenShift OAuth
            |
            v
OpenShift Groups ---- RoleBindings / Custom Roles
            |
            v
OpenShift AI Project = OpenShift Namespace Boundary
  |-- Workbenches / Notebook pods / PVCs
  |-- Data connections / Secrets / ServiceAccounts
  |-- Pipeline server / runs / artifact storage
  |-- Model deployments / runtimes / routes or gateways
  |-- Quotas / LimitRanges / NetworkPolicies
  `-- Monitoring, audit, backup, governance evidence
            |
            v
Shared services: object storage, registry, Git, identity, monitoring, backup
```

### 2.1 Control-plane and data-plane reasoning
- The OpenShift AI dashboard is the supported user workflow and orchestration interface.
- Kubernetes and OpenShift APIs are the enforcement and reconciliation layer.
- Project resources are namespace scoped unless a shared platform component is explicitly cluster or platform scoped.
- External systems such as S3-compatible object storage, databases, Git, identity providers, and registries remain separate failure and security domains.
- GitOps is the preferred enterprise mechanism for repeatable baselines, while dynamic user workloads may remain dashboard managed with policy controls.

### 2.2 Resource relationship map
- **DataSciencePipelinesApplication:** participates in artifact bucket; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **Pipeline server pods:** participates in database; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **MariaDB or external DB:** participates in connection secret; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **S3 bucket:** participates in server creation; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **Secret:** participates in SDK access; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **Service:** participates in pipeline versions; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **Route:** participates in scheduled runs; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **ServiceAccount:** participates in resource sizing; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.
- **Job:** participates in backup; define ownership, lifecycle, access, telemetry, backup scope, and deletion behavior.

## 3. Enterprise requirements and decision record

| Decision area | Required question | Evidence | Approval owner |
|---|---|---|---|
| Artifact Bucket | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Platform owner |
| Database | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Security/Data owner |
| Connection Secret | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Application owner |
| Server Creation | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Platform owner |
| Sdk Access | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Security/Data owner |
| Pipeline Versions | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Application owner |
| Scheduled Runs | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Platform owner |
| Resource Sizing | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Security/Data owner |
| Backup | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Application owner |
| Troubleshooting | What exact business and technical requirement justifies this configuration? | Ticket, architecture record, manifest, test result | Platform owner |

### 3.1 Non-functional requirements
1. **Availability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
2. **Recoverability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
3. **Security:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
4. **Performance:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
5. **Scalability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
6. **Auditability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
7. **Maintainability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
8. **Portability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
9. **Cost Control:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.
10. **Supportability:** define a measurable target, the test method, the monitoring signal, the owner, and the exception process.

## 4. Prerequisite discovery

Run discovery before creating or changing resources:
```bash
oc whoami
oc version
oc get clusterversion
oc get csv -A | grep -Ei "opendatahub|openshift-ai|rhods|kserve|pipeline|gpu|oadp"
oc api-resources | grep -Ei "notebook|pipeline|inference|servingruntime|modelregistry|backup|restore"
oc get storageclass
oc get nodes -L node-role.kubernetes.io/worker,nvidia.com/gpu.present
oc get groups
oc auth can-i create projects
oc auth can-i create rolebindings -n ds-credit-risk-dev
```

### 4.1 Topic-specific discovery
```bash
oc api-resources | grep -i pipeline
oc get datasciencepipelinesapplications -n ds-credit-risk-dev
oc get pods -n ds-credit-risk-dev | grep -E "pipeline|mariadb|scheduledworkflow"
oc get events -n ds-credit-risk-dev --sort-by=.lastTimestamp
```

### 4.2 Discovery interpretation
- For **artifact bucket**, confirm whether `DataSciencePipelinesApplication` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **database**, confirm whether `Pipeline server pods` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **connection secret**, confirm whether `MariaDB or external DB` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **server creation**, confirm whether `S3 bucket` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **SDK access**, confirm whether `Secret` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **pipeline versions**, confirm whether `Service` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **scheduled runs**, confirm whether `Route` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **resource sizing**, confirm whether `ServiceAccount` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **backup**, confirm whether `Job` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.
- For **troubleshooting**, confirm whether `DataSciencePipelinesApplication` is namespaced, who can create it, which operator reconciles it, and whether the API is supported or version-sensitive.

## 5. From-scratch implementation workflow

**Primary dashboard path:** `OpenShift AI dashboard → Projects → selected project → Pipelines → Configure pipeline server`

1. Open an approved service request containing business purpose, data classification, environments, expected users, resource sizing, recovery objectives, and expiry/review date.
2. Validate that the requested project name follows DNS-compatible enterprise naming and does not expose sensitive information.
3. Map identity-provider groups to project administrator, contributor, auditor, and automation roles.
4. Create or select the OpenShift project namespace and apply mandatory ownership, environment, cost-center, lifecycle, and classification metadata.
5. Apply baseline RBAC and test positive and negative access using `oc auth can-i --as`.
6. Apply ResourceQuota and LimitRange values based on measured workload estimates and shared-cluster capacity.
7. Apply default-deny network policy and explicit allow rules for DNS, image registry, object storage, pipelines, serving, monitoring, and approved APIs.
8. Provision project storage and data connections without exposing credentials in Git, tickets, notebooks, or shell history.
9. Create the required workbench, pipeline, registry integration, or serving resource through the supported dashboard workflow.
10. Export version-sensitive resources, sanitize generated fields, and place only safe declarative configuration in Git.
11. Run functional, security, performance, backup, restore, and rollback validation appropriate to the topic.
12. Capture evidence and hand over the project with ownership, support, monitoring, cost, and lifecycle responsibilities documented.

### 5.1 Topic-focused implementation details
#### 5.1.1 Artifact Bucket

- **Intent:** implement artifact bucket using `DataSciencePipelinesApplication` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for artifact bucket.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with artifact bucket.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `DataSciencePipelinesApplication`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.2 Database

- **Intent:** implement database using `Pipeline server pods` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for database.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with database.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `Pipeline server pods`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.3 Connection Secret

- **Intent:** implement connection secret using `MariaDB or external DB` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for connection secret.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with connection secret.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `MariaDB or external DB`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.4 Server Creation

- **Intent:** implement server creation using `S3 bucket` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for server creation.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with server creation.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `S3 bucket`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.5 Sdk Access

- **Intent:** implement SDK access using `Secret` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for SDK access.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with SDK access.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `Secret`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.6 Pipeline Versions

- **Intent:** implement pipeline versions using `Service` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for pipeline versions.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with pipeline versions.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `Service`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.7 Scheduled Runs

- **Intent:** implement scheduled runs using `Route` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for scheduled runs.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with scheduled runs.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `Route`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.8 Resource Sizing

- **Intent:** implement resource sizing using `ServiceAccount` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for resource sizing.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with resource sizing.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `ServiceAccount`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.9 Backup

- **Intent:** implement backup using `Job` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for backup.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with backup.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `Job`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

#### 5.1.10 Troubleshooting

- **Intent:** implement troubleshooting using `DataSciencePipelinesApplication` and related project resources.
- **Design:** define the source of truth, owner, allowed change mechanism, default state, exception state, and evidence retained for troubleshooting.
- **Execution:** create the minimum configuration required, validate reconciliation, and verify the user-visible behavior in the OpenShift AI dashboard.
- **Security:** identify credentials, sensitive metadata, cross-project access, and escalation paths associated with troubleshooting.
- **Operations:** monitor capacity, errors, age, ownership, and drift for `DataSciencePipelinesApplication`.
- **Rollback:** restore the previous manifest or remove the newly introduced binding/configuration only after confirming dependencies.

## 6. Reference manifest

```yaml
# DataSciencePipelinesApplication fields vary by OpenShift AI release.
# Discover the installed API and export a dashboard-created instance:
#   oc api-resources | grep -i datasciencepipeline
#   oc get datasciencepipelinesapplications -n <project> -o yaml
# Keep object-store credentials in a Secret and avoid committing secret values.
apiVersion: datasciencepipelinesapplications.opendatahub.io/<discover-installed-version>
kind: DataSciencePipelinesApplication
metadata:
  name: dspa
  namespace: ds-credit-risk-dev
spec:
  # Populate from an exported supported instance for your release.
  # Use a dedicated S3-compatible artifact bucket.
  objectStorage: <release-specific-configuration>
```

### 6.1 Safe manifest handling
- Use `oc explain <resource> --recursive` against the target cluster before editing fields.
- Export a supported dashboard-created resource when the API schema is release-sensitive.
- Remove `status`, `managedFields`, `uid`, `resourceVersion`, `generation`, and `creationTimestamp` before committing.
- Do not commit Secret data, bearer tokens, S3 keys, database passwords, kubeconfigs, or signed URLs.
- Use namespace overlays for environment-specific names, endpoints, StorageClasses, and quota sizes.
- Run server-side dry-run and policy tests before merge.
- Require peer review from platform and security owners for RBAC, network, SCC, and external exposure changes.
- Record the tested OpenShift and OpenShift AI versions in the repository README.

## 7. Validation plan

| Test | Pass condition | Suggested evidence |
|---|---|---|
| Existence | The expected resource exists in the intended namespace and has the approved name and labels. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Reconciliation | Operator-managed resources report ready status and do not loop on errors. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Positive authorization | Approved users and service accounts can perform required actions. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Negative authorization | Unapproved users cannot read, change, connect to, or delete protected resources. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Quota behavior | A valid workload is admitted and an intentionally oversized workload is rejected with a clear quota event. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Network behavior | Approved traffic succeeds and non-approved cross-project or egress traffic fails. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Storage behavior | Data persists across pod/workbench restart and expected access modes work. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Credential behavior | Credentials are injected only into authorized workloads and rotation does not require rebuilding images. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Observability | Logs, events, metrics, and alerts provide enough evidence to diagnose failures. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Recovery | Backup and restore procedures reconstitute the required state within target RPO/RTO. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Lifecycle | Stop, update, rollback, archive, cleanup, and deletion workflows behave as documented. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |
| Audit | Ticket, Git change, approvals, test evidence, and ownership metadata are complete. | Command output, screenshot, event/log extract, pipeline run, prediction response, or restore record |

### 7.1 Core validation commands
```bash
oc get namespace ds-credit-risk-dev --show-labels
oc get rolebindings,roles,resourcequota,limitrange,networkpolicy -n ds-credit-risk-dev
oc get pods,pvc,secrets,serviceaccounts -n ds-credit-risk-dev
oc get events -n ds-credit-risk-dev --sort-by=.lastTimestamp
oc auth can-i --list -n ds-credit-risk-dev --as=alice
oc adm policy who-can delete projects
oc get namespace ds-credit-risk-dev -o yaml > evidence-namespace.yaml
```

## 8. Troubleshooting method

Use the following evidence-first sequence:
1. Restate the failed user action, expected result, actual result, time, namespace, identity, and affected resource.
2. Confirm login identity and active project with `oc whoami` and `oc project`.
3. Check authorization with `oc auth can-i` using the real user or service-account identity.
4. Inspect the custom resource, owner references, conditions, finalizers, and events.
5. Identify the reconciling operator and inspect its deployment health and logs.
6. Inspect generated pods, services, routes/gateways, PVCs, Secrets, and service accounts.
7. Check quota, LimitRange, scheduling, node labels, taints, tolerations, accelerator availability, and image pulls.
8. Check NetworkPolicy, DNS, proxy, certificate trust, object-store endpoint, database endpoint, and egress controls.
9. Compare current state with Git and the last known-good manifest.
10. Apply the smallest reversible change, capture evidence, and retest the original user action.

### 8.1 Command toolbox
```bash
oc get events -n ds-credit-risk-dev --sort-by=.lastTimestamp
oc get pods -n ds-credit-risk-dev -o wide
oc describe pod <pod> -n ds-credit-risk-dev
oc logs <pod> -n ds-credit-risk-dev --all-containers --tail=200
oc get <resource> <name> -n ds-credit-risk-dev -o yaml
oc get <resource> <name> -n ds-credit-risk-dev -o jsonpath="{.status.conditions}"
oc auth can-i <verb> <resource> --as=<user> -n ds-credit-risk-dev
oc get rolebindings -n ds-credit-risk-dev -o yaml
oc describe resourcequota -n ds-credit-risk-dev
oc get networkpolicy -n ds-credit-risk-dev -o yaml
oc get pvc,pv -n ds-credit-risk-dev
oc get nodes -o wide --show-labels
oc adm must-gather --dest-dir=must-gather.local
```

## 9. Security and compliance controls

1. Use group-based RBAC and prohibit shared human accounts.
2. Separate project administration from platform administration.
3. Minimize Secret read permissions; Kubernetes `edit` may be broader than some enterprises permit.
4. Use short-lived credentials or workload identity where supported.
5. Apply default-deny network policies and explicit allow rules.
6. Enforce approved images, signed artifacts, vulnerability scanning, and supported runtimes.
7. Encrypt storage and object-store traffic; validate custom CA trust explicitly.
8. Apply data-classification labels and retention rules to PVCs, buckets, and model artifacts.
9. Capture OpenShift audit records and application-specific audit evidence.
10. Review service accounts, tokens, role bindings, routes, and external credentials periodically.
11. Require expiry dates and compensating controls for exceptions.
12. Test revocation, user offboarding, and compromised-credential response.

## 10. Performance, capacity, and cost engineering

| Dimension | Engineering practice |
|---|---|
| CPU | Measure request-to-usage ratio and throttling; right-size after representative runs. |
| Memory | Measure working set, OOM events, model loading peaks, and pipeline concurrency. |
| GPU | Track allocated versus active accelerator time, memory use, queue time, and idle workbenches. |
| Storage capacity | Forecast dataset, checkpoint, notebook home, pipeline artifact, and model growth. |
| Storage performance | Measure throughput, latency, metadata operations, and concurrent client behavior. |
| Network | Measure image pulls, S3 throughput, model latency, and cross-zone or proxy effects. |
| Object storage | Control version growth, incomplete uploads, lifecycle rules, and request costs. |
| Model serving | Define latency, throughput, concurrency, cold-start, scale-up, and error-rate targets. |
| Pipelines | Define run concurrency, queueing, retry cost, artifact retention, and database growth. |
| Chargeback | Tag namespace, buckets, PVCs, and shared services using cost center and owner metadata. |

## 11. Observability and SRE operations

- **Signal 01 — resource readiness:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 02 — pod restart rate:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 03 — scheduling latency:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 04 — quota utilization:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 05 — PVC capacity and health:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 06 — object-store errors and latency:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 07 — pipeline run success and duration:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 08 — model request rate/error/latency:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 09 — GPU allocation and utilization:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 10 — authorization denials:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 11 — network policy drops:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 12 — certificate expiry:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 13 — backup success:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 14 — restore test success:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.
- **Signal 15 — orphaned resource age:** define source metric/log/event, threshold, dashboard panel, alert route, runbook, and service owner.

### 11.1 SLO example
```text
Service: project model endpoint
SLI: successful authenticated inference responses / valid authenticated requests
Target: 99.9% over 30 days, excluding approved maintenance
Latency objective: 95% of requests below the approved model-specific threshold
Error budget policy: stop non-essential releases when the burn-rate threshold is exceeded
```

## 12. Backup, restore, and disaster recovery

1. Inventory all namespaced resources and distinguish declarative configuration from mutable data.
2. Protect PVC data with supported snapshots or data movement according to the storage backend.
3. Protect external object storage with versioning, replication, lifecycle protection, or native backup.
4. Protect pipeline metadata databases and artifact buckets consistently.
5. Protect model registry metadata and artifact backends according to registry architecture.
6. Store manifests in Git, but do not assume Git is a backup for PVCs, databases, or object data.
7. Document storage-class, identity-provider, route/gateway, certificate, and endpoint mappings for alternate-cluster restore.
8. Rotate credentials after a recovery event when confidentiality cannot be guaranteed.
9. Run scheduled restore tests and capture measured RPO/RTO.
10. Obtain business acceptance using representative notebooks, pipelines, and model predictions.

## 13. GitOps repository design

```text
rhoai-projects/
├── platform-baseline/
│   ├── quotas/
│   ├── limitranges/
│   ├── networkpolicies/
│   └── rbac/
├── projects/credit-risk/base/
│   ├── namespace.yaml
│   ├── serviceaccounts.yaml
│   ├── storage.yaml
│   └── kustomization.yaml
├── projects/credit-risk/overlays/dev/
├── projects/credit-risk/overlays/test/
├── projects/credit-risk/overlays/prod/
├── policies/
├── tests/
└── docs/runbooks/
```

### 13.1 Pipeline gates
1. Gate: **YAML syntax** — fail the merge when mandatory evidence is missing.
2. Gate: **Kubernetes schema** — fail the merge when mandatory evidence is missing.
3. Gate: **server-side dry-run** — fail the merge when mandatory evidence is missing.
4. Gate: **policy-as-code** — fail the merge when mandatory evidence is missing.
5. Gate: **RBAC diff** — fail the merge when mandatory evidence is missing.
6. Gate: **network exposure review** — fail the merge when mandatory evidence is missing.
7. Gate: **Secret detection** — fail the merge when mandatory evidence is missing.
8. Gate: **image policy** — fail the merge when mandatory evidence is missing.
9. Gate: **quota sanity** — fail the merge when mandatory evidence is missing.
10. Gate: **naming and labels** — fail the merge when mandatory evidence is missing.
11. Gate: **backup annotation** — fail the merge when mandatory evidence is missing.
12. Gate: **rollback plan** — fail the merge when mandatory evidence is missing.
13. Gate: **peer approval** — fail the merge when mandatory evidence is missing.

## 14. Production runbook

### 14.1 Pre-change
- [ ] Pre-change task 1: verify artifact bucket, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 2: verify database, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 3: verify connection secret, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 4: verify server creation, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 5: verify SDK access, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 6: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 7: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Pre-change task 8: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

### 14.2 Change execution
- [ ] Change execution task 1: verify database, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 2: verify connection secret, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 3: verify server creation, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 4: verify SDK access, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 5: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 6: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 7: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Change execution task 8: verify backup, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

### 14.3 Immediate validation
- [ ] Immediate validation task 1: verify connection secret, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 2: verify server creation, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 3: verify SDK access, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 4: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 5: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 6: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 7: verify backup, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Immediate validation task 8: verify troubleshooting, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

### 14.4 Observation period
- [ ] Observation period task 1: verify server creation, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 2: verify SDK access, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 3: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 4: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 5: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 6: verify backup, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 7: verify troubleshooting, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Observation period task 8: verify artifact bucket, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

### 14.5 Rollback
- [ ] Rollback task 1: verify SDK access, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 2: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 3: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 4: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 5: verify backup, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 6: verify troubleshooting, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 7: verify artifact bucket, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Rollback task 8: verify database, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

### 14.6 Closure
- [ ] Closure task 1: verify pipeline versions, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 2: verify scheduled runs, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 3: verify resource sizing, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 4: verify backup, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 5: verify troubleshooting, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 6: verify artifact bucket, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 7: verify database, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.
- [ ] Closure task 8: verify connection secret, record operator/user identity, timestamp, command or dashboard action, result, and evidence link.

## 15. Detailed design review checklist

### DR-001: Artifact Bucket / MariaDB or external DB
- **Question:** Is the artifact bucket design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-002: Database / Service
- **Question:** Is the database design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-003: Connection Secret / Job
- **Question:** Is the connection secret design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-004: Server Creation / MariaDB or external DB
- **Question:** Is the server creation design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-005: Sdk Access / Service
- **Question:** Is the SDK access design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-006: Pipeline Versions / Job
- **Question:** Is the pipeline versions design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-007: Scheduled Runs / MariaDB or external DB
- **Question:** Is the scheduled runs design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-008: Resource Sizing / Service
- **Question:** Is the resource sizing design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-009: Backup / Job
- **Question:** Is the backup design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-010: Troubleshooting / MariaDB or external DB
- **Question:** Is the troubleshooting design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-011: Artifact Bucket / Service
- **Question:** Is the artifact bucket design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-012: Database / Job
- **Question:** Is the database design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-013: Connection Secret / MariaDB or external DB
- **Question:** Is the connection secret design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-014: Server Creation / Service
- **Question:** Is the server creation design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-015: Sdk Access / Job
- **Question:** Is the SDK access design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-016: Pipeline Versions / MariaDB or external DB
- **Question:** Is the pipeline versions design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-017: Scheduled Runs / Service
- **Question:** Is the scheduled runs design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-018: Resource Sizing / Job
- **Question:** Is the resource sizing design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-019: Backup / MariaDB or external DB
- **Question:** Is the backup design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-020: Troubleshooting / Service
- **Question:** Is the troubleshooting design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-021: Artifact Bucket / Job
- **Question:** Is the artifact bucket design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-022: Database / MariaDB or external DB
- **Question:** Is the database design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-023: Connection Secret / Service
- **Question:** Is the connection secret design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-024: Server Creation / Job
- **Question:** Is the server creation design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-025: Sdk Access / MariaDB or external DB
- **Question:** Is the SDK access design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-026: Pipeline Versions / Service
- **Question:** Is the pipeline versions design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-027: Scheduled Runs / Job
- **Question:** Is the scheduled runs design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-028: Resource Sizing / MariaDB or external DB
- **Question:** Is the resource sizing design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-029: Backup / Service
- **Question:** Is the backup design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-030: Troubleshooting / Job
- **Question:** Is the troubleshooting design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-031: Artifact Bucket / MariaDB or external DB
- **Question:** Is the artifact bucket design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-032: Database / Service
- **Question:** Is the database design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-033: Connection Secret / Job
- **Question:** Is the connection secret design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-034: Server Creation / MariaDB or external DB
- **Question:** Is the server creation design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-035: Sdk Access / Service
- **Question:** Is the SDK access design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-036: Pipeline Versions / Job
- **Question:** Is the pipeline versions design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-037: Scheduled Runs / MariaDB or external DB
- **Question:** Is the scheduled runs design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-038: Resource Sizing / Service
- **Question:** Is the resource sizing design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-039: Backup / Job
- **Question:** Is the backup design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-040: Troubleshooting / MariaDB or external DB
- **Question:** Is the troubleshooting design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-041: Artifact Bucket / Service
- **Question:** Is the artifact bucket design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-042: Database / Job
- **Question:** Is the database design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-043: Connection Secret / MariaDB or external DB
- **Question:** Is the connection secret design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-044: Server Creation / Service
- **Question:** Is the server creation design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-045: Sdk Access / Job
- **Question:** Is the SDK access design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-046: Pipeline Versions / MariaDB or external DB
- **Question:** Is the pipeline versions design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-047: Scheduled Runs / Service
- **Question:** Is the scheduled runs design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-048: Resource Sizing / Job
- **Question:** Is the resource sizing design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-049: Backup / MariaDB or external DB
- **Question:** Is the backup design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-050: Troubleshooting / Service
- **Question:** Is the troubleshooting design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-051: Artifact Bucket / Job
- **Question:** Is the artifact bucket design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-052: Database / MariaDB or external DB
- **Question:** Is the database design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-053: Connection Secret / Service
- **Question:** Is the connection secret design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-054: Server Creation / Job
- **Question:** Is the server creation design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-055: Sdk Access / MariaDB or external DB
- **Question:** Is the SDK access design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-056: Pipeline Versions / Service
- **Question:** Is the pipeline versions design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-057: Scheduled Runs / Job
- **Question:** Is the scheduled runs design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-058: Resource Sizing / MariaDB or external DB
- **Question:** Is the resource sizing design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-059: Backup / Service
- **Question:** Is the backup design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-060: Troubleshooting / Job
- **Question:** Is the troubleshooting design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-061: Artifact Bucket / MariaDB or external DB
- **Question:** Is the artifact bucket design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-062: Database / Service
- **Question:** Is the database design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-063: Connection Secret / Job
- **Question:** Is the connection secret design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-064: Server Creation / MariaDB or external DB
- **Question:** Is the server creation design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-065: Sdk Access / Service
- **Question:** Is the SDK access design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-066: Pipeline Versions / Job
- **Question:** Is the pipeline versions design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-067: Scheduled Runs / MariaDB or external DB
- **Question:** Is the scheduled runs design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-068: Resource Sizing / Service
- **Question:** Is the resource sizing design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-069: Backup / Job
- **Question:** Is the backup design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-070: Troubleshooting / MariaDB or external DB
- **Question:** Is the troubleshooting design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-071: Artifact Bucket / Service
- **Question:** Is the artifact bucket design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-072: Database / Job
- **Question:** Is the database design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-073: Connection Secret / MariaDB or external DB
- **Question:** Is the connection secret design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-074: Server Creation / Service
- **Question:** Is the server creation design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-075: Sdk Access / Job
- **Question:** Is the SDK access design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-076: Pipeline Versions / MariaDB or external DB
- **Question:** Is the pipeline versions design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-077: Scheduled Runs / Service
- **Question:** Is the scheduled runs design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-078: Resource Sizing / Job
- **Question:** Is the resource sizing design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-079: Backup / MariaDB or external DB
- **Question:** Is the backup design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-080: Troubleshooting / Service
- **Question:** Is the troubleshooting design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-081: Artifact Bucket / Job
- **Question:** Is the artifact bucket design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-082: Database / MariaDB or external DB
- **Question:** Is the database design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-083: Connection Secret / Service
- **Question:** Is the connection secret design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-084: Server Creation / Job
- **Question:** Is the server creation design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-085: Sdk Access / MariaDB or external DB
- **Question:** Is the SDK access design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-086: Pipeline Versions / Service
- **Question:** Is the pipeline versions design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-087: Scheduled Runs / Job
- **Question:** Is the scheduled runs design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-088: Resource Sizing / MariaDB or external DB
- **Question:** Is the resource sizing design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-089: Backup / Service
- **Question:** Is the backup design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-090: Troubleshooting / Job
- **Question:** Is the troubleshooting design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-091: Artifact Bucket / MariaDB or external DB
- **Question:** Is the artifact bucket design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-092: Database / Service
- **Question:** Is the database design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-093: Connection Secret / Job
- **Question:** Is the connection secret design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-094: Server Creation / MariaDB or external DB
- **Question:** Is the server creation design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-095: Sdk Access / Service
- **Question:** Is the SDK access design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-096: Pipeline Versions / Job
- **Question:** Is the pipeline versions design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-097: Scheduled Runs / MariaDB or external DB
- **Question:** Is the scheduled runs design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-098: Resource Sizing / Service
- **Question:** Is the resource sizing design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-099: Backup / Job
- **Question:** Is the backup design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-100: Troubleshooting / MariaDB or external DB
- **Question:** Is the troubleshooting design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-101: Artifact Bucket / Service
- **Question:** Is the artifact bucket design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-102: Database / Job
- **Question:** Is the database design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-103: Connection Secret / MariaDB or external DB
- **Question:** Is the connection secret design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-104: Server Creation / Service
- **Question:** Is the server creation design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-105: Sdk Access / Job
- **Question:** Is the SDK access design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-106: Pipeline Versions / MariaDB or external DB
- **Question:** Is the pipeline versions design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-107: Scheduled Runs / Service
- **Question:** Is the scheduled runs design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-108: Resource Sizing / Job
- **Question:** Is the resource sizing design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-109: Backup / MariaDB or external DB
- **Question:** Is the backup design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-110: Troubleshooting / Service
- **Question:** Is the troubleshooting design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-111: Artifact Bucket / Job
- **Question:** Is the artifact bucket design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale artifact bucket creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-112: Database / MariaDB or external DB
- **Question:** Is the database design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale database creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-113: Connection Secret / Service
- **Question:** Is the connection secret design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale connection secret creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-114: Server Creation / Job
- **Question:** Is the server creation design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale server creation creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-115: Sdk Access / MariaDB or external DB
- **Question:** Is the SDK access design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale SDK access creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-116: Pipeline Versions / Service
- **Question:** Is the pipeline versions design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale pipeline versions creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-117: Scheduled Runs / Job
- **Question:** Is the scheduled runs design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale scheduled runs creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-118: Resource Sizing / MariaDB or external DB
- **Question:** Is the resource sizing design for `MariaDB or external DB` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale resource sizing creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `MariaDB or external DB` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-119: Backup / Service
- **Question:** Is the backup design for `Service` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale backup creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Service` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

### DR-120: Troubleshooting / Job
- **Question:** Is the troubleshooting design for `Job` explicit, least-privilege, supportable, and consistent with the project classification?
- **Evidence required:** approved manifest or dashboard configuration, owner, test result, rollback method, and lifecycle date.
- **Failure mode:** missing or stale troubleshooting creates authorization, cost, availability, data-loss, or audit risk.
- **Review action:** compare live `Job` state with Git, policy, ticket, and current capacity assumptions.
- **Pass condition:** no unexplained drift; ownership and next review date are recorded.

## 16. Validation test catalog

### VT-001: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-002: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-003: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-004: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-005: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-006: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-007: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-008: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-009: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-010: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-011: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-012: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-013: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-014: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-015: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-016: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-017: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-018: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-019: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-020: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-021: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-022: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-023: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-024: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-025: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-026: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-027: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-028: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-029: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-030: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-031: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-032: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-033: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-034: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-035: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-036: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-037: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-038: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-039: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-040: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-041: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-042: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-043: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-044: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-045: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-046: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-047: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-048: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-049: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-050: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-051: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-052: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-053: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-054: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-055: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-056: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-057: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-058: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-059: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-060: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-061: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-062: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-063: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-064: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-065: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-066: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-067: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-068: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-069: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-070: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-071: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-072: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-073: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-074: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-075: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-076: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-077: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-078: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-079: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-080: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-081: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-082: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-083: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-084: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-085: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-086: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-087: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-088: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-089: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-090: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-091: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-092: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-093: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-094: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-095: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-096: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-097: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-098: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-099: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-100: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-101: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-102: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-103: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-104: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-105: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-106: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-107: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-108: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-109: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-110: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-111: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-112: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `S3 bucket`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `S3 bucket` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-113: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Secret`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Secret` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-114: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Service`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Service` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-115: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Route`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Route` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-116: Validate database
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `ServiceAccount`.
- **Action:** perform the approved database operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `ServiceAccount` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-117: Validate server creation
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Job`.
- **Action:** perform the approved server creation operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Job` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-118: Validate pipeline versions
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `DataSciencePipelinesApplication`.
- **Action:** perform the approved pipeline versions operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `DataSciencePipelinesApplication` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-119: Validate resource sizing
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `Pipeline server pods`.
- **Action:** perform the approved resource sizing operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `Pipeline server pods` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

### VT-120: Validate troubleshooting
- **Setup:** use a disposable test identity or workload in `ds-credit-risk-dev` with known expected access to `MariaDB or external DB`.
- **Action:** perform the approved troubleshooting operation and one deliberately unauthorized or oversized variation.
- **Expected positive result:** supported operation succeeds and `MariaDB or external DB` reports healthy/ready state.
- **Expected negative result:** unauthorized or policy-violating operation is denied without affecting existing workloads.
- **Evidence:** capture command, identity, timestamp, response, events, relevant logs, and final resource state.

## 17. Troubleshooting scenario catalog

### TS-001: Forbidden error during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-002: resource remains NotReady during database
- **Likely cause to test:** wrong Secret key affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-003: pod Pending during connection secret
- **Likely cause to test:** operator degradation affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-004: ImagePullBackOff during server creation
- **Likely cause to test:** finalizer dependency affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-005: PVC Pending during SDK access
- **Likely cause to test:** drift from Git baseline affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-006: connection timeout during pipeline versions
- **Likely cause to test:** insufficient quota affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-007: TLS validation failure during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-008: quota exceeded during resource sizing
- **Likely cause to test:** operator degradation affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-009: workload crash loop during backup
- **Likely cause to test:** finalizer dependency affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-010: route returns 503 during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-011: pipeline run fails during artifact bucket
- **Likely cause to test:** insufficient quota affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-012: model prediction fails during database
- **Likely cause to test:** wrong Secret key affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-013: resource stuck terminating during connection secret
- **Likely cause to test:** operator degradation affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-014: unexpected cross-project access during server creation
- **Likely cause to test:** finalizer dependency affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-015: restore completes with missing data during SDK access
- **Likely cause to test:** drift from Git baseline affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-016: Forbidden error during pipeline versions
- **Likely cause to test:** insufficient quota affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-017: resource remains NotReady during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-018: pod Pending during resource sizing
- **Likely cause to test:** operator degradation affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-019: ImagePullBackOff during backup
- **Likely cause to test:** finalizer dependency affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-020: PVC Pending during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-021: connection timeout during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-022: TLS validation failure during database
- **Likely cause to test:** wrong Secret key affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-023: quota exceeded during connection secret
- **Likely cause to test:** operator degradation affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-024: workload crash loop during server creation
- **Likely cause to test:** finalizer dependency affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-025: route returns 503 during SDK access
- **Likely cause to test:** drift from Git baseline affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-026: pipeline run fails during pipeline versions
- **Likely cause to test:** insufficient quota affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-027: model prediction fails during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-028: resource stuck terminating during resource sizing
- **Likely cause to test:** operator degradation affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-029: unexpected cross-project access during backup
- **Likely cause to test:** finalizer dependency affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-030: restore completes with missing data during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-031: Forbidden error during artifact bucket
- **Likely cause to test:** insufficient quota affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-032: resource remains NotReady during database
- **Likely cause to test:** wrong Secret key affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-033: pod Pending during connection secret
- **Likely cause to test:** operator degradation affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-034: ImagePullBackOff during server creation
- **Likely cause to test:** finalizer dependency affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-035: PVC Pending during SDK access
- **Likely cause to test:** drift from Git baseline affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-036: connection timeout during pipeline versions
- **Likely cause to test:** insufficient quota affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-037: TLS validation failure during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-038: quota exceeded during resource sizing
- **Likely cause to test:** operator degradation affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-039: workload crash loop during backup
- **Likely cause to test:** finalizer dependency affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-040: route returns 503 during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-041: pipeline run fails during artifact bucket
- **Likely cause to test:** insufficient quota affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-042: model prediction fails during database
- **Likely cause to test:** wrong Secret key affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-043: resource stuck terminating during connection secret
- **Likely cause to test:** operator degradation affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-044: unexpected cross-project access during server creation
- **Likely cause to test:** finalizer dependency affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-045: restore completes with missing data during SDK access
- **Likely cause to test:** drift from Git baseline affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-046: Forbidden error during pipeline versions
- **Likely cause to test:** insufficient quota affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-047: resource remains NotReady during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-048: pod Pending during resource sizing
- **Likely cause to test:** operator degradation affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-049: ImagePullBackOff during backup
- **Likely cause to test:** finalizer dependency affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-050: PVC Pending during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-051: connection timeout during artifact bucket
- **Likely cause to test:** insufficient quota affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-052: TLS validation failure during database
- **Likely cause to test:** wrong Secret key affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-053: quota exceeded during connection secret
- **Likely cause to test:** operator degradation affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-054: workload crash loop during server creation
- **Likely cause to test:** finalizer dependency affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-055: route returns 503 during SDK access
- **Likely cause to test:** drift from Git baseline affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-056: pipeline run fails during pipeline versions
- **Likely cause to test:** insufficient quota affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-057: model prediction fails during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-058: resource stuck terminating during resource sizing
- **Likely cause to test:** operator degradation affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-059: unexpected cross-project access during backup
- **Likely cause to test:** finalizer dependency affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-060: restore completes with missing data during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-061: Forbidden error during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-062: resource remains NotReady during database
- **Likely cause to test:** wrong Secret key affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-063: pod Pending during connection secret
- **Likely cause to test:** operator degradation affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-064: ImagePullBackOff during server creation
- **Likely cause to test:** finalizer dependency affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-065: PVC Pending during SDK access
- **Likely cause to test:** drift from Git baseline affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-066: connection timeout during pipeline versions
- **Likely cause to test:** insufficient quota affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-067: TLS validation failure during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-068: quota exceeded during resource sizing
- **Likely cause to test:** operator degradation affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-069: workload crash loop during backup
- **Likely cause to test:** finalizer dependency affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-070: route returns 503 during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-071: pipeline run fails during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-072: model prediction fails during database
- **Likely cause to test:** wrong Secret key affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-073: resource stuck terminating during connection secret
- **Likely cause to test:** operator degradation affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-074: unexpected cross-project access during server creation
- **Likely cause to test:** finalizer dependency affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-075: restore completes with missing data during SDK access
- **Likely cause to test:** drift from Git baseline affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-076: Forbidden error during pipeline versions
- **Likely cause to test:** insufficient quota affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-077: resource remains NotReady during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-078: pod Pending during resource sizing
- **Likely cause to test:** operator degradation affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-079: ImagePullBackOff during backup
- **Likely cause to test:** finalizer dependency affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-080: PVC Pending during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-081: connection timeout during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-082: TLS validation failure during database
- **Likely cause to test:** wrong Secret key affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-083: quota exceeded during connection secret
- **Likely cause to test:** operator degradation affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-084: workload crash loop during server creation
- **Likely cause to test:** finalizer dependency affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-085: route returns 503 during SDK access
- **Likely cause to test:** drift from Git baseline affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-086: pipeline run fails during pipeline versions
- **Likely cause to test:** insufficient quota affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-087: model prediction fails during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-088: resource stuck terminating during resource sizing
- **Likely cause to test:** operator degradation affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-089: unexpected cross-project access during backup
- **Likely cause to test:** finalizer dependency affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-090: restore completes with missing data during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-091: Forbidden error during artifact bucket
- **Likely cause to test:** insufficient quota affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-092: resource remains NotReady during database
- **Likely cause to test:** wrong Secret key affecting `S3 bucket`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-093: pod Pending during connection secret
- **Likely cause to test:** operator degradation affecting `Service`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-094: ImagePullBackOff during server creation
- **Likely cause to test:** finalizer dependency affecting `ServiceAccount`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-095: PVC Pending during SDK access
- **Likely cause to test:** drift from Git baseline affecting `DataSciencePipelinesApplication`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-096: connection timeout during pipeline versions
- **Likely cause to test:** insufficient quota affecting `MariaDB or external DB`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-097: TLS validation failure during scheduled runs
- **Likely cause to test:** wrong Secret key affecting `Secret`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-098: quota exceeded during resource sizing
- **Likely cause to test:** operator degradation affecting `Route`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-099: workload crash loop during backup
- **Likely cause to test:** finalizer dependency affecting `Job`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get pods -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

### TS-100: route returns 503 during troubleshooting
- **Likely cause to test:** drift from Git baseline affecting `Pipeline server pods`.
- **First evidence:** user identity, namespace, resource YAML, conditions, events, operator ownership, and related pod logs.
- **Diagnostic command:** `oc get events -n ds-credit-risk-dev -o wide` plus `oc auth can-i` for the exact verb/resource.
- **Corrective action:** change only the confirmed failed dependency; avoid broad cluster-admin, disabled policy, or unrestricted egress workarounds.
- **Prevention:** add a preflight test, alert, policy, documentation step, or capacity threshold for this failure mode.

## 18. Operational drill catalog

### OD-001: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-002: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-003: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-004: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-005: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-006: Workbench Image Update
- **Objective:** execute workbench image update while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-007: Pipeline Server Restart
- **Objective:** execute pipeline server restart while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-008: Model Rollback
- **Objective:** execute model rollback while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-009: Network Policy Change
- **Objective:** execute network policy change while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-010: Backup Execution
- **Objective:** execute backup execution while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-011: Restore Test
- **Objective:** execute restore test while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-012: Owner Change
- **Objective:** execute owner change while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-013: Project Suspension
- **Objective:** execute project suspension while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-014: Incident Evidence Collection
- **Objective:** execute incident evidence collection while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-015: Project Retirement
- **Objective:** execute project retirement while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-016: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-017: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-018: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-019: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-020: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-021: Workbench Image Update
- **Objective:** execute workbench image update while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-022: Pipeline Server Restart
- **Objective:** execute pipeline server restart while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-023: Model Rollback
- **Objective:** execute model rollback while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-024: Network Policy Change
- **Objective:** execute network policy change while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-025: Backup Execution
- **Objective:** execute backup execution while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-026: Restore Test
- **Objective:** execute restore test while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-027: Owner Change
- **Objective:** execute owner change while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-028: Project Suspension
- **Objective:** execute project suspension while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-029: Incident Evidence Collection
- **Objective:** execute incident evidence collection while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-030: Project Retirement
- **Objective:** execute project retirement while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-031: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-032: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-033: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-034: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-035: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-036: Workbench Image Update
- **Objective:** execute workbench image update while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-037: Pipeline Server Restart
- **Objective:** execute pipeline server restart while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-038: Model Rollback
- **Objective:** execute model rollback while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-039: Network Policy Change
- **Objective:** execute network policy change while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-040: Backup Execution
- **Objective:** execute backup execution while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-041: Restore Test
- **Objective:** execute restore test while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-042: Owner Change
- **Objective:** execute owner change while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-043: Project Suspension
- **Objective:** execute project suspension while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-044: Incident Evidence Collection
- **Objective:** execute incident evidence collection while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-045: Project Retirement
- **Objective:** execute project retirement while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-046: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-047: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-048: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-049: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-050: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-051: Workbench Image Update
- **Objective:** execute workbench image update while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-052: Pipeline Server Restart
- **Objective:** execute pipeline server restart while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-053: Model Rollback
- **Objective:** execute model rollback while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-054: Network Policy Change
- **Objective:** execute network policy change while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-055: Backup Execution
- **Objective:** execute backup execution while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-056: Restore Test
- **Objective:** execute restore test while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-057: Owner Change
- **Objective:** execute owner change while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-058: Project Suspension
- **Objective:** execute project suspension while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-059: Incident Evidence Collection
- **Objective:** execute incident evidence collection while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-060: Project Retirement
- **Objective:** execute project retirement while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-061: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-062: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-063: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-064: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-065: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-066: Workbench Image Update
- **Objective:** execute workbench image update while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-067: Pipeline Server Restart
- **Objective:** execute pipeline server restart while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-068: Model Rollback
- **Objective:** execute model rollback while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-069: Network Policy Change
- **Objective:** execute network policy change while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-070: Backup Execution
- **Objective:** execute backup execution while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-071: Restore Test
- **Objective:** execute restore test while preserving required service, data, audit evidence, and rollback capability for connection secret.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-072: Owner Change
- **Objective:** execute owner change while preserving required service, data, audit evidence, and rollback capability for pipeline versions.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-073: Project Suspension
- **Objective:** execute project suspension while preserving required service, data, audit evidence, and rollback capability for backup.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-074: Incident Evidence Collection
- **Objective:** execute incident evidence collection while preserving required service, data, audit evidence, and rollback capability for database.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-075: Project Retirement
- **Objective:** execute project retirement while preserving required service, data, audit evidence, and rollback capability for SDK access.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-076: Access Revocation
- **Objective:** execute access revocation while preserving required service, data, audit evidence, and rollback capability for resource sizing.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-077: Credential Rotation
- **Objective:** execute credential rotation while preserving required service, data, audit evidence, and rollback capability for artifact bucket.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-078: Quota Increase
- **Objective:** execute quota increase while preserving required service, data, audit evidence, and rollback capability for server creation.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-079: Quota Decrease
- **Objective:** execute quota decrease while preserving required service, data, audit evidence, and rollback capability for scheduled runs.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

### OD-080: Storage Expansion
- **Objective:** execute storage expansion while preserving required service, data, audit evidence, and rollback capability for troubleshooting.
- **Preconditions:** approved change, current backup, named owner, tested command or dashboard path, and defined success criteria.
- **Execution:** make one controlled change, observe reconciliation, and avoid parallel unrelated modifications.
- **Validation:** test user workflow, authorization, workload readiness, data access, monitoring, and dependent services.
- **Evidence:** capture before/after state, timestamps, approver, operator logs when relevant, and business acceptance.

## 19. Corporate interview and review questions

### Q001. Why is an OpenShift AI data science project also an OpenShift namespace boundary?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q002. Which controls are enforced by the dashboard and which are enforced by Kubernetes/OpenShift?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q003. How do Admin, Contributor, custom roles, and service-account access differ?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q004. Why can granting `edit` be too broad in a high-security project?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q005. How do ResourceQuota and LimitRange complement each other?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q006. Why must GPU requests and limits be explicit?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q007. How would you select RWO versus RWX storage for workbenches?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q008. Why should pipeline artifacts be separated from general project data?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q009. How do you rotate a data connection without editing notebook code?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q010. How do you automate a version-sensitive dashboard-created resource safely?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q011. What is the difference between a project model server and the shared model registry?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q012. What must be backed up outside the namespace?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q013. How do you prove project isolation?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q014. What evidence is required before deleting a project?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q015. How would you investigate a user who can see a project but cannot start a workbench?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q016. How would you diagnose a model deployment stuck in NotReady?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q017. How do you manage dormant workbenches and idle GPUs?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q018. How do you implement access recertification for hundreds of projects?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q019. How do you recover the project on a cluster with different StorageClass names?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q020. Which project controls should be policy-as-code?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q021. Why is an OpenShift AI data science project also an OpenShift namespace boundary?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q022. Which controls are enforced by the dashboard and which are enforced by Kubernetes/OpenShift?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q023. How do Admin, Contributor, custom roles, and service-account access differ?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q024. Why can granting `edit` be too broad in a high-security project?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q025. How do ResourceQuota and LimitRange complement each other?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q026. Why must GPU requests and limits be explicit?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q027. How would you select RWO versus RWX storage for workbenches?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q028. Why should pipeline artifacts be separated from general project data?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q029. How do you rotate a data connection without editing notebook code?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q030. How do you automate a version-sensitive dashboard-created resource safely?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q031. What is the difference between a project model server and the shared model registry?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q032. What must be backed up outside the namespace?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q033. How do you prove project isolation?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q034. What evidence is required before deleting a project?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q035. How would you investigate a user who can see a project but cannot start a workbench?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q036. How would you diagnose a model deployment stuck in NotReady?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q037. How do you manage dormant workbenches and idle GPUs?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q038. How do you implement access recertification for hundreds of projects?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q039. How do you recover the project on a cluster with different StorageClass names?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q040. Which project controls should be policy-as-code?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q041. Why is an OpenShift AI data science project also an OpenShift namespace boundary?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q042. Which controls are enforced by the dashboard and which are enforced by Kubernetes/OpenShift?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q043. How do Admin, Contributor, custom roles, and service-account access differ?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q044. Why can granting `edit` be too broad in a high-security project?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q045. How do ResourceQuota and LimitRange complement each other?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q046. Why must GPU requests and limits be explicit?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q047. How would you select RWO versus RWX storage for workbenches?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q048. Why should pipeline artifacts be separated from general project data?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q049. How do you rotate a data connection without editing notebook code?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q050. How do you automate a version-sensitive dashboard-created resource safely?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q051. What is the difference between a project model server and the shared model registry?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q052. What must be backed up outside the namespace?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q053. How do you prove project isolation?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q054. What evidence is required before deleting a project?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q055. How would you investigate a user who can see a project but cannot start a workbench?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q056. How would you diagnose a model deployment stuck in NotReady?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q057. How do you manage dormant workbenches and idle GPUs?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q058. How do you implement access recertification for hundreds of projects?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q059. How do you recover the project on a cluster with different StorageClass names?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q060. Which project controls should be policy-as-code?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q061. Why is an OpenShift AI data science project also an OpenShift namespace boundary?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q062. Which controls are enforced by the dashboard and which are enforced by Kubernetes/OpenShift?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q063. How do Admin, Contributor, custom roles, and service-account access differ?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q064. Why can granting `edit` be too broad in a high-security project?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q065. How do ResourceQuota and LimitRange complement each other?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q066. Why must GPU requests and limits be explicit?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q067. How would you select RWO versus RWX storage for workbenches?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q068. Why should pipeline artifacts be separated from general project data?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q069. How do you rotate a data connection without editing notebook code?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q070. How do you automate a version-sensitive dashboard-created resource safely?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q071. What is the difference between a project model server and the shared model registry?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q072. What must be backed up outside the namespace?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q073. How do you prove project isolation?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q074. What evidence is required before deleting a project?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q075. How would you investigate a user who can see a project but cannot start a workbench?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q076. How would you diagnose a model deployment stuck in NotReady?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q077. How do you manage dormant workbenches and idle GPUs?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q078. How do you implement access recertification for hundreds of projects?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q079. How do you recover the project on a cluster with different StorageClass names?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q080. Which project controls should be policy-as-code?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q081. Why is an OpenShift AI data science project also an OpenShift namespace boundary?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q082. Which controls are enforced by the dashboard and which are enforced by Kubernetes/OpenShift?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q083. How do Admin, Contributor, custom roles, and service-account access differ?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q084. Why can granting `edit` be too broad in a high-security project?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q085. How do ResourceQuota and LimitRange complement each other?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q086. Why must GPU requests and limits be explicit?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q087. How would you select RWO versus RWX storage for workbenches?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q088. Why should pipeline artifacts be separated from general project data?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q089. How do you rotate a data connection without editing notebook code?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q090. How do you automate a version-sensitive dashboard-created resource safely?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q091. What is the difference between a project model server and the shared model registry?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the artifact bucket workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q092. What must be backed up outside the namespace?
**Expected L3 answer:** explain the control objective, the `S3 bucket` enforcement point, the database workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q093. How do you prove project isolation?
**Expected L3 answer:** explain the control objective, the `Service` enforcement point, the connection secret workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q094. What evidence is required before deleting a project?
**Expected L3 answer:** explain the control objective, the `ServiceAccount` enforcement point, the server creation workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q095. How would you investigate a user who can see a project but cannot start a workbench?
**Expected L3 answer:** explain the control objective, the `DataSciencePipelinesApplication` enforcement point, the SDK access workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q096. How would you diagnose a model deployment stuck in NotReady?
**Expected L3 answer:** explain the control objective, the `MariaDB or external DB` enforcement point, the pipeline versions workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q097. How do you manage dormant workbenches and idle GPUs?
**Expected L3 answer:** explain the control objective, the `Secret` enforcement point, the scheduled runs workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q098. How do you implement access recertification for hundreds of projects?
**Expected L3 answer:** explain the control objective, the `Route` enforcement point, the resource sizing workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q099. How do you recover the project on a cluster with different StorageClass names?
**Expected L3 answer:** explain the control objective, the `Job` enforcement point, the backup workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

### Q100. Which project controls should be policy-as-code?
**Expected L3 answer:** explain the control objective, the `Pipeline server pods` enforcement point, the troubleshooting workflow, evidence commands, failure modes, security implications, and rollback. Include both dashboard and CLI perspectives.

## 20. Hands-on capstone project

### Capstone: Enterprise implementation of Project pipeline server

1. Create identity-provider groups for project admins, contributors, auditors, and automation identities.
2. Create a dev project with mandatory labels, annotations, quota, LimitRange, and default-deny networking.
3. Add project storage, a secure object-store connection, and one approved workbench.
4. Configure an AI pipeline server with a dedicated artifact bucket and run a simple pipeline.
5. Register a model version with provenance metadata and grant the project service account registry access.
6. Deploy the model with an approved runtime and validate authenticated prediction traffic.
7. Create monitoring queries, alerts, and a runbook for one realistic failure mode.
8. Back up the project and external dependencies, then restore into an alternate namespace.
9. Run negative authorization, quota, and network-isolation tests.
10. Generate an evidence pack and perform a peer review against the governance checklist.
11. Suspend or clean up idle resources while retaining required data and audit evidence.
12. Present the design and troubleshooting walkthrough focusing on **Project pipeline server** to a platform review board.

### Capstone acceptance criteria
- [ ] AC-01: Artifact Bucket for `DataSciencePipelinesApplication` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-02: Database for `Pipeline server pods` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-03: Connection Secret for `MariaDB or external DB` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-04: Server Creation for `S3 bucket` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-05: Sdk Access for `Secret` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-06: Pipeline Versions for `Service` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-07: Scheduled Runs for `Route` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-08: Resource Sizing for `ServiceAccount` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-09: Backup for `Job` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-10: Troubleshooting for `DataSciencePipelinesApplication` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-11: Artifact Bucket for `Pipeline server pods` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-12: Database for `MariaDB or external DB` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-13: Connection Secret for `S3 bucket` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-14: Server Creation for `Secret` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-15: Sdk Access for `Service` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-16: Pipeline Versions for `Route` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-17: Scheduled Runs for `ServiceAccount` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-18: Resource Sizing for `Job` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-19: Backup for `DataSciencePipelinesApplication` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-20: Troubleshooting for `Pipeline server pods` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-21: Artifact Bucket for `MariaDB or external DB` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-22: Database for `S3 bucket` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-23: Connection Secret for `Secret` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-24: Server Creation for `Service` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-25: Sdk Access for `Route` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-26: Pipeline Versions for `ServiceAccount` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-27: Scheduled Runs for `Job` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-28: Resource Sizing for `DataSciencePipelinesApplication` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-29: Backup for `Pipeline server pods` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.
- [ ] AC-30: Troubleshooting for `MariaDB or external DB` is implemented, tested positively and negatively, documented, monitored, recoverable, and traceable to an approved change.

## 21. Glossary

- **Project:** OpenShift AI organizational construct backed by an OpenShift project/namespace for namespaced resources.
- **Workbench:** Managed development environment, commonly represented by a Notebook custom resource and related pod, storage, service, and route resources.
- **Data connection:** Dashboard-visible configuration that injects credentials and endpoint information, commonly backed by a Secret.
- **Pipeline server:** Project-attached service that stores and runs AI pipeline definitions, runs, metadata, and artifacts.
- **Model server:** Serving environment that loads a model artifact and exposes an inference endpoint.
- **Model registry:** Shared lifecycle and metadata repository for registered models and versions; distinct from serving.
- **ResourceQuota:** Namespace-scoped aggregate resource and object limit.
- **LimitRange:** Namespace policy that provides or enforces per-object/container request and limit constraints.
- **RBAC:** Role-based access control using Roles/ClusterRoles and bindings to users, groups, and service accounts.
- **NetworkPolicy:** Namespaced policy controlling allowed pod ingress and egress when supported by the cluster network provider.
- **PVC:** PersistentVolumeClaim used by workloads to request durable storage.
- **S3-compatible storage:** Object storage accessed through an API compatible with common S3 operations.
- **RPO:** Maximum acceptable data-loss interval.
- **RTO:** Maximum acceptable service-recovery duration.
- **GitOps:** Declarative configuration management in which Git-reviewed desired state is reconciled to clusters.

## 22. Official references

Use Red Hat documentation matching your installed OpenShift AI and OpenShift versions. The links below were used as the primary product baseline:
- [Red Hat OpenShift AI 3.5 - Working on projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_on_projects/)
- [Red Hat OpenShift AI 3.5 - Managing access to projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_on_projects/managing-access-to-projects_projects)
- [Red Hat OpenShift AI 3.5 - Project workbenches](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_on_projects/using-project-workbenches_projects)
- [Red Hat OpenShift AI 3.5 - Cluster storage for projects](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_on_projects/configuring-cluster-storage_projects)
- [Red Hat OpenShift AI 3.5 - AI pipelines](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_with_ai_pipelines/)
- [Red Hat OpenShift AI 3.5 - Model registries](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/working_with_model_registries/)
- [Red Hat OpenShift AI 3.5 - Managing model registry permissions](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_model_registries/managing-model-registry-permissions_managing-model-registries)
- [Red Hat OpenShift AI 3.5 - Managing users and groups](https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.5/html/managing_openshift_ai/managing-users-and-groups)
- [OpenShift Container Platform 4.22 documentation](https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/)

## 23. Final L3 summary

An L3 engineer implementing **Project pipeline server** must be able to connect user intent to namespace-level enforcement, shared OpenShift AI services, external dependencies, operational evidence, and lifecycle controls. The implementation is complete only when access, capacity, storage, networking, credentials, observability, recovery, cleanup, and governance have all been validated—not merely when the dashboard shows a resource.

